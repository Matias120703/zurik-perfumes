# CLAUDE.md — Somapp Commerce Engine

> Este documento es la **Constitución del proyecto**. No es un resumen de chat ni una nota de memoria: es la fuente oficial de verdad. Cualquier conversación nueva de Claude sobre este repositorio debe leer este archivo completo antes de responder o modificar código. Si una instrucción nueva contradice lo que está escrito acá, hay que pedir aclaración antes de proceder, no asumir cuál gana.

---

## 1. Identidad del proyecto

**Nombre:** Somapp Commerce Engine

**Qué es:** un template premium de e-commerce, desarrollado por Somapp, pensado para ser reutilizado tal cual entre distintos clientes cambiando únicamente contenido, marca y configuración — nunca la arquitectura ni los componentes.

**Primer cliente:** RegiShop (Paraguay — moneda PYG/guaraníes, locale es-PY, ubicación de referencia Villarrica, Guairá). Todos los textos, productos, categorías y datos de contacto que existen hoy en `config/` son placeholders de este primer cliente, no datos reales de producción. La tienda arrancó modelada sobre Argentina (moneda ARS, locale es-AR, Buenos Aires) y se readaptó por completo a Paraguay en el Sprint 4.7 — ver sección 9.

**Qué problema resuelve:** hoy, armar una tienda online profesional para un negocio chico o mediano implica reconstruir de cero header, catálogo, carrito y checkout cada vez. Este proyecto existe para que Somapp tenga una base ya resuelta — visual y funcionalmente — que se pueda "vestir" para un nuevo rubro (perfumería, ropa, ferretería, farmacia, tecnología, etc.) en días, no en semanas.

**Qué NO pretende ser:**
- No es Shopify ni un clon de Shopify.
- No es un marketplace (no hay múltiples vendedores).
- No es un ERP.
- No es un CRM.
- No es un SaaS multi-tenant (ver Filosofía, sección 2).

**Estado actual (a la fecha de este documento):** en desarrollo activo. **El flujo completo de compra de la V1 está cerrado**: Home → catálogo (con búsqueda, ordenamiento y navegación por categorías) → producto → carrito → checkout (con selección de ubicación exacta en mapa para Delivery) → confirmación → pedido enviado por WhatsApp con un mensaje estructurado. La tienda también tiene ya sus **páginas institucionales** (Contacto, Políticas, FAQ). **La infraestructura de Supabase ya está aplicada al proyecto real** (Fase 8, `supabase/migrations/`, `supabase/seed.sql`): las 12 tablas del diseño aprobado (Sprint 4.0, `docs/database-architecture.md`) más dos columnas nuevas en `products` (Fase 10), sus índices, RLS y un bucket de Storage para fotos de producto. **El Panel Administrativo ya arrancó** (Fase 9: `/admin/login` con Supabase Auth real, rutas protegidas, layout con Sidebar/Header) **y tiene su primer módulo funcional completo** (Fase 10): **Productos**, con alta/edición/baja e imágenes conectadas de verdad a Supabase. **Y desde la Fase 11, la tienda pública también lee de Supabase de verdad**: Home, Productos Destacados, Catálogo, página de Producto, Productos Relacionados y Categorías ya no usan `config/products.ts`/`config/categories.ts` como fuente — leen `products`/`product_images`/`categories` en cada request, así que crear/editar/desactivar/eliminar un producto desde el panel se refleja automáticamente en la tienda, sin rebuild. `config/products.ts` y `config/categories.ts` **siguen existiendo**, sin tocar, como fallback/documentación — ver sección 9, Fase 11. **Corrección post-Fase 11 (Fase 12):** las fotos reales subidas desde el Panel llegaban bien a Supabase Storage y a `Product.images`, pero `ProductCard`/`ProductGallery` nunca las renderizaban — mostraban siempre el placeholder decorativo. Corregido mostrando la foto real cuando existe (con fallback automático al placeholder si no hay o falla), habilitando el dominio de Supabase Storage en `next.config.ts` — ver sección 9, Fase 12. **Y desde la Fase 13, el panel tiene un segundo módulo funcional completo: Categorías** (`/admin/categorias`), con el mismo patrón de capas que Productos (`services/` → `hooks/` → `components/admin/categories/`), color y símbolo por categoría (`accent_color` + `icon_name`, este último un identificador de texto resuelto a un ícono de lucide-react únicamente en la presentación — nunca un componente de React guardado en la base), y borrado seguro (una categoría con productos asociados no se puede eliminar, ni desde la UI ni a nivel de base — `on delete restrict`). **Con esto, `config/categories.ts` dejó de tener cualquier consumidor activo en la tienda pública**: `ProductCard`, `ProductGallery`, `ProductBuyBox`, `ProductGrid`, `FeaturedProducts` y `RelatedProducts` ya no importan el array de config — reciben las categorías ya resueltas desde Supabase por la página que los renderiza. Las únicas dos referencias activas que quedan en todo el proyecto son `CartItem.tsx` y `CheckoutSummary.tsx` (Carrito y Checkout), fuera de alcance explícito de ese sprint — ver sección 9, Fase 13. **Y desde la Fase 14, el panel tiene un tercer módulo funcional completo: Configuración General** (`/admin/configuracion`), que deja casi toda la identidad del negocio (nombre, eslogan, descripción, logo, favicon, WhatsApp, contacto, redes sociales, ubicación) editable desde una fila única de `business_settings`, sin tocar código. Logo y favicon se suben a un bucket de Storage propio (`branding`, separado de `product-images`). Header, Footer, Hero y la página de Contacto ya no leen `config/business.ts`/`config/footer.ts` para esos campos — los reciben resueltos desde Supabase. **Tres piezas quedaron deliberadamente fuera de este alcance, documentadas explícitamente**: el mensaje de WhatsApp del pedido (`lib/whatsapp.ts`), la moneda/locale de `formatPrice()`, y el centro del mapa de Delivery en el Checkout siguen leyendo `config/business.ts` estático — migrarlos habría requerido tocar Checkout/Carrito/WhatsApp, explícitamente prohibido en ese sprint. Ver sección 9, Fase 14. **Y desde la Fase 15, el checkout deja de terminar únicamente en un mensaje de WhatsApp: cada pedido queda registrado de verdad en `orders`/`order_items`/`customers` antes de abrirlo**, a través de una única función de Postgres (`create_order`, `security definer`) que garantiza que el pedido y todas sus líneas se crean de forma atómica — si algo falla, no queda ningún registro parcial. El panel ganó su cuarto módulo funcional: **Pedidos** (`/admin/pedidos` y `/admin/pedidos/[id]`), con tabla completa, filtro y orden, y cambio de estado (Pendiente → Confirmado → En preparación → Enviado → Entregado, o Cancelado) que se refleja al instante en Supabase. El formato del mensaje de WhatsApp no cambió — `lib/whatsapp.ts` sigue exactamente igual — solo se agregó el paso de guardado justo antes de abrirlo. Ver sección 9, Fase 15. **Y desde la Fase 16, el panel tiene un quinto módulo funcional: Clientes** (`/admin/clientes` y `/admin/clientes/[id]`), de solo consulta — no hay alta, edición ni baja manual, los clientes se crean solos por el mismo upsert de teléfono que ya hacía `create_order` desde la Fase 15. El detalle muestra el historial completo de pedidos de cada cliente y sus estadísticas (total gastado, cantidad de compras, ticket promedio, primera y última compra), **calculadas en el momento a partir de sus pedidos, sin guardar ningún valor derivado en la base**. **Y desde la Fase 17, `/admin` dejó de ser un placeholder: es un Dashboard real**, con 6 tarjetas de resumen (pedidos totales/pendientes/entregados, clientes, productos y categorías activos), ventas (total, ticket promedio, última venta), los últimos 10 pedidos (con navegación al detalle), los 5 productos más vendidos y un desglose por estado de pedido — **todo calculado en cada carga a partir de `orders`/`order_items`/`customers`/`products`/`categories`, sin ningún valor hardcodeado ni guardado**. **Y desde la Fase 18, el panel tiene su sexto y último módulo funcional planeado: Promociones** (`/admin/promociones`, `/nueva`, `/[id]/editar`), campañas de descuento con vigencia por producto específico o por categoría completa (nunca ambas, restricción ya diseñada en la base desde la Fase 8), con estado calculado (Activa/Programada/Vencida/Inactiva). **El banner promocional de la Home dejó de leer `config/promotion.ts`**: ahora consume la promoción vigente desde Supabase (RLS filtra automáticamente `is_active` y las fechas), mostrando el descuento real y un link al producto o categoría de la campaña. Una promoción **no** recalcula el precio mostrado de un producto/categoría puntual — esa conexión fue explícitamente dejada como decisión de implementación futura desde el diseño original (Fase 8), y este sprint no la resolvió (fuera de su alcance explícito). Con este módulo, **los 6 módulos planeados del panel administrativo quedan completos** (Productos, Categorías, Configuración, Pedidos, Clientes, Promociones), además del Dashboard (Fase 17). **Y con la Fase 19 — Gestión de Inventario, el último sprint planeado de la Versión 1.0 —, el stock deja de ser un número que solo se edita a mano desde Productos: el panel gana un séptimo módulo funcional, Inventario** (`/admin/inventario` y `/admin/inventario/[id]`), con historial completo de movimientos (`stock_movements`, tabla nueva) y **descuento/restitución automática de stock integrada con Pedidos sin tocar ese módulo**: una función de Postgres (`register_stock_movement`) para entradas/ajustes manuales, y un trigger (`apply_order_stock_movement`) que descuenta stock al confirmar/preparar un pedido y lo restituye al cancelarlo — enganchado al mismo `update orders set status = ...` que `services/orders.ts` (Pedidos) ya hacía desde la Fase 15, sin cambiar ese archivo una sola línea. El stock **nunca puede quedar negativo**: un ajuste manual que lo dejaría negativo se rechaza con un error explícito; una confirmación automática de pedido que pida más de lo disponible se recorta a 0 en vez de bloquear el pedido. La tienda pública muestra "Sin stock" y deshabilita "Agregar al carrito" y "Comprar por WhatsApp" cuando el stock de un producto llega a 0, **sin ocultar el producto** — la única excepción tocada en la tienda pública este sprint fue `ProductBuyBox.tsx` (el botón de WhatsApp no se deshabilitaba todavía). **Con esto, la Versión 1.0 de Somapp Commerce Engine queda completa**: los 7 módulos planeados del panel administrativo (Productos, Categorías, Inventario, Configuración, Pedidos, Clientes, Promociones) más el Dashboard. **Y con la Fase 20 (Sprint 6.0.1 — Auditoría Final y Corrección de Bugs), la Versión 1.0 queda además estabilizada como Release Candidate**: una auditoría funcional completa encontró y corrigió 6 bugs reales (tarjetas de producto sin `Link` a `/productos/[slug]`, banner de promoción de producto sin la imagen del producto, el mensaje de WhatsApp del checkout todavía leyendo `config/business.ts` en vez de `business_settings`, categorías inactivas visibles en la tienda cuando quien navega tiene una sesión de admin activa en el mismo navegador, el favicon/logo sin validación de tamaño antes de subir, y el Dashboard rediseñado visualmente con gráficos de Recharts) más una auditoría de código que encontró y corrigió miniaturas de producto rotas en `/admin/productos` (mismos placeholders de seed nunca reales, Fase 12, sin la misma guarda en el panel) y un nombre de categoría corrompido en la base real ("Tecnología parece" → "Tecnología"). Ver sección 9, Fase 20, para el detalle completo de cada bug. **Y con la Fase 21 (Sprint 6.1), el sistema de promociones queda definitivamente completo**: hasta este sprint, el panel permitía crear múltiples promociones vigentes a la vez pero la tienda pública solo mostraba una (la más reciente), y ninguna promoción modificaba el precio realmente mostrado de un producto — la conexión "promoción activa → precio de catálogo recalculado" había quedado explícitamente pendiente desde el diseño original (Fase 8) y así seguía tras la Fase 18. Las dos piezas quedan resueltas: (1) **el banner de la Home es ahora un carrusel real** cuando hay 2 o más promociones vigentes a la vez (con 1 sola, se comporta exactamente igual que antes de este sprint) — avanza solo cada ~2.5 segundos con una transición horizontal de Framer Motion, en loop infinito, con flechas y dots de navegación manual (que reinician el temporizador) y pausa al pasar el mouse por encima; (2) **un motor de precios nuevo, único y reutilizable** (`calculatePromotion()`/`getProductDisplayPrice()`, `lib/promotions.ts`) calcula el precio final de cualquier producto a partir de las promociones vigentes — porcentual o por monto fijo, con prioridad fija (promoción del producto puntual primero, de su categoría después, nunca las dos a la vez) y sin acumular nunca dos descuentos — y se muestra (precio tachado → precio final → badge del descuento real) en los mismos lugares donde ya se mostraba un precio: Home, Productos Destacados, Catálogo, página de Producto, Productos Relacionados y Categorías. Cuando ningún producto tiene una promoción vigente, el precio mostrado vuelve exactamente al comportamiento legacy de siempre (`oldPrice`/`onSale`/`badge` estáticos de Fases 2-3) — cero regresión para el catálogo sin promociones activas. Carrito, Checkout y WhatsApp no se tocaron. Ver sección 9, Fase 21, para el detalle completo. **Y con la Fase 22 (Sprint 6.2), el panel gana su octavo módulo funcional: Logística** (`/admin/logistica`), tarifas de envío por ciudad completamente administrables, cerrando el último costo que todavía estaba hardcodeado en la tienda: el envío. Cuatro tablas nuevas (`departments`/`cities`, seed completo de Paraguay -- 18 "departamentos" incluyendo Asunción, 228 ciudades -- y `shipping_rates`/`shipping_rate_cities`, la tarifa en sí y a qué ciudades cubre), con una interfaz de selección de ciudades por departamento (buscador + checkboxes + "seleccionar todas"/"deseleccionar todas") en vez de un `<select>` común. **El checkout dejó de mostrar un envío placeholder**: `ShippingInformation` reemplazó sus campos de texto libre de departamento/ciudad por dos `<Select>` conectados a Supabase -- al elegir una ciudad, busca automáticamente la tarifa que la cubre y actualiza el Total en tiempo real; si ninguna tarifa la cubre, muestra "A confirmar" (nunca un costo inventado) y el pedido igual se puede completar. El pedido guarda el departamento/ciudad/costo de envío reales (`orders.department`/`city`/`shipping_cost`, ya existían desde la Fase 8 sin usarse; `shipping_rate_id`/`shipping_rate_name` son las dos únicas columnas nuevas), con `shipping_cost = null` cuando no hay tarifa -- así el admin ve de inmediato en `/admin/pedidos/[id]` que tiene que informar el costo a mano. **Una excepción documentada, no un descuido**: `lib/whatsapp.ts` sigue sin sumar el envío al texto del mensaje ("NO modificar WhatsApp" fue instrucción explícita de este sprint) -- el total mostrado en pantalla y guardado en Supabase sí es el real, pero el mensaje que se manda por WhatsApp todavía no lo refleja. Ver sección 9, Fase 22, para el detalle completo. **Y con la Fase 23 (Sprint 6.3), una mejora puntual de UX**: la tarjeta "Branding" (Logo + Favicon) de `/admin/configuracion` se ocultó de la interfaz -- "no indispensable para el uso diario del panel", pedido explícito del usuario -- sin tocar Supabase Storage, el bucket `branding` ni `services/settings.ts`; la funcionalidad de subir/cambiar/eliminar logo y favicon sigue existiendo intacta en el código (`BrandingAssetField.tsx`), solo sin ningún punto de entrada visible en el panel hoy. Ver sección 9, Fase 23. **Y con la Fase 24 (Sprint 6.4), el espacio vacío/estático del panel derecho del Hero fue reemplazado por un Showcase dinámico de productos reales**: en vez de un marcador decorativo fijo, ahora rota automáticamente (cada 1.5 segundos, con una transición horizontal de Framer Motion) entre los productos destacados de la tienda (o, si no hay ninguno destacado, los más recientes -- nunca queda vacío), mostrando imagen real (con el mismo criterio de `isRealImageUrl` ya establecido desde la Fase 12), nombre, precio y un botón "Ver producto" que navega a su ficha. Se pausa al pasar el mouse por encima y se puede navegar manualmente con flechas o con indicadores inferiores, cualquiera de los cuales reinicia el temporizador -- mismo patrón ya probado por el carrusel de promociones (Fase 21). Es el primer custom hook propio de la tienda pública (`useProductShowcase`), pero acota exactamente qué rompe de la regla anterior: no fetchea nada, solo encapsula el estado de presentación (índice/dirección/pausa) de un array que ya le llega resuelto por props desde Home vía una consulta nueva (`getPublicHeroProducts()`). El resto del Hero (textos, botones, layout izquierdo, la columna de dos en desktop) no se tocó. Ver sección 9, Fase 24, para el detalle completo. **Y con la Fase 25 (Sprint 6.5), Configuración se reorganiza en dos pestañas**: "General" (las 4 tarjetas que ya existían: Información general, Contacto, Redes sociales, Ubicación) y "Apariencia" (nueva), que agrupa **Identidad Visual** -- Logo y Favicon, la tarjeta "Branding" oculta desde la Fase 23, reconectada acá con exactamente la misma lógica de siempre, sin tocar Supabase Storage ni el bucket `branding` -- más tres secciones nuevas puramente visuales y sin ninguna funcionalidad todavía: **Hero** (preview de las 3 opciones futuras: Imagen/Video/Productos destacados), **Colores del tema** (preview de Color principal/secundario/de botones) y **Tipografía** ("Disponible próximamente."). Sigue siendo un único formulario con un único submit -- las pestañas son navegación puramente visual, el guardado atómico de `business_settings` no cambió. Ver sección 9, Fase 25, para el detalle completo. **Y con la Fase 26 (Sprint 6.2.1.1), se corrigió un bug de RLS que rompía silenciosamente toda escritura administrativa del panel**: la tabla `public.admins` estaba vacía porque el usuario real (`felixmatiasarandarromero@gmail.com`) nunca tuvo su fila creada -- sin ella, `is_admin()` devolvía `false` y cualquier `insert`/`update` gateado por esa función (Storage, `business_settings`, Productos, Categorías, Pedidos, Promociones, Logística) fallaba con "new row violates row-level security policy". Se corrigió con un único `insert` de datos en `admins` (no una migración de esquema ni un cambio de policy -- las policies ya eran correctas), confirmado con el usuario real subiendo/guardando/reemplazando/eliminando logo y favicon sin error. Ver sección 9, Fase 26, para el detalle completo. **Y con la Fase 27 (Sprint 6.2.1.2), una auditoría completa de las ~65 policies RLS del esquema (las 17 tablas de `public` + Storage) confirmó que ninguna faltaba ni estaba mal escrita**: se probó cada combinación tabla+operación que usa `/admin` (Productos, Categorías, Pedidos, Logística, Configuración, Inventario, Promociones) simulando el JWT del admin real, y las 9 combinaciones funcionaron correctamente sin ningún cambio adicional -- la Fase 26 ya había sido la única causa real en todo el esquema. Se confirmó además que ningún usuario no autenticado puede escribir en ninguna tabla. Cero migraciones, cero policies modificadas. Ver sección 9, Fase 27, para el detalle completo. **Y con la Fase 28 (Sprint 6.6), la descripción completa de producto pasa de un `<textarea>` plano a un editor enriquecido profesional** (Tiptap, `RichTextEditor.tsx`): negrita/cursiva/subrayado/tachado, títulos H1-H6, listas con viñetas y numeradas, citas, separadores, enlaces, color de texto y de resaltado, imágenes (subida a un bucket nuevo, `product-content`, con resize por presets y alineación), GIFs, y videos (subidos como archivo o insertados por URL de YouTube). Ese contenido **se muestra únicamente en la página individual del producto** (`ProductRichDescription.tsx`, sección nueva "Descripción completa" entre la galería/buy box y "Productos relacionados") -- `ProductCard`, el Catálogo, la Home, los Productos Destacados y el Showcase del Hero no se tocaron, y ni siquiera reciben ese campo: `getPublicProducts()`/`getPublicHeroProducts()` (Home/Catálogo/Destacados/Showcase) no seleccionan `description`, solo `getPublicProductBySlug()` (la página de producto) lo hace. Sanitizado dos veces (al guardar y al mostrar, con `sanitize-html`) contra XSS. Ver sección 9, Fase 28, para el detalle completo, incluyendo varios bugs reales encontrados y corregidos durante la verificación (un problema de bundling de Turbopack, extensiones duplicadas de Tiptap, la descripción no cargaba al reabrir un producto para editar, y HTML sin sanitizar llegando al cliente por la hidratación de React aunque nunca se ejecutaba). Lo que **todavía no existe** es la persistencia del carrito/checkout, y el repositorio git sigue sin inicializarse. Ver sección 7 para el detalle exacto.

---

## 2. Filosofía

**Somapp Commerce Engine NO es un SaaS multi-tenant.** Esto es una decisión de arquitectura fundamental, no un detalle:

- Cada cliente tiene **su propio deploy**.
- Cada cliente tiene **su propio proyecto de Supabase**.
- Cada cliente tiene **su propia base de datos**.
- No existe (ni existirá) una tabla `tenants`, ni lógica de resolución de tenant en runtime, ni un solo despliegue sirviendo a múltiples negocios distintos.

Esto es intencional: elimina de raíz la complejidad de aislar datos entre clientes, de manejar planes/billing, y de lidiar con esquemas de base de datos compartidos. El costo de "un proyecto Supabase por cliente" es bajo comparado con la complejidad que evita.

**El objetivo central es la reutilización de código, no la configuración dinámica en runtime.** Para lanzar la tienda de un nuevo cliente, el trabajo es:
1. Fork/clonar este repositorio.
2. Editar los archivos de `src/config/` (nombre, colores, WhatsApp, redes, horarios).
3. Cargar sus productos y categorías (hoy en `config/products.ts` / `config/categories.ts`, mañana en su Supabase).
4. Reemplazar los placeholders de imagen por las fotos reales.
5. Ajustar la paleta de color en `globals.css` si la identidad de marca lo requiere.

Ese es todo el esfuerzo de "onboarding" de un cliente nuevo. Si alguna vez ese proceso empieza a requerir tocar componentes o lógica, es una señal de que algo se hardcodeó donde no debía.

---

## 3. Objetivos

### Corto plazo (en curso)
- Terminar la experiencia completa de la tienda pública funcionando sobre datos mock (`config/*.ts`), sin backend: Home → catálogo → producto → carrito → checkout. **Este objetivo está prácticamente cumplido** (ver sección 7).

### Mediano plazo
- Conectar Supabase como fuente real de datos: esquema de productos, categorías, y pedidos. **El diseño del esquema ya está aprobado (Sprint 4.0, `docs/database-architecture.md`)**; falta escribir las migraciones y conectarlo de verdad.
- Reemplazar los datos mock de `config/products.ts` y `config/categories.ts` por consultas reales, sin tocar los componentes que los consumen.
- ~~Construir el paso final del checkout: armar el pedido y enviarlo por WhatsApp (o guardarlo en Supabase, o ambos).~~ **Cumplido por completo**: el envío por WhatsApp desde el Sprint 3.8, y el guardado en Supabase (orders/order_items/customers, atómico vía `create_order`) desde la Fase 15.
- Persistencia real del carrito (hoy vive solo en memoria).
- Filtros de catálogo funcionales (hoy son solo interfaz). **La búsqueda y el ordenamiento ya están cumplidos (Sprints 4.2 y 4.3)**.

### Largo plazo
- Panel administrativo para que cada cliente gestione sus propios productos, categorías y pedidos sin tocar código.
- Documentar y afinar el proceso de "onboarding" de un cliente nuevo hasta que sea mecánico.
- Reutilizar esta misma base para lanzar la tienda de al menos un segundo cliente de un rubro distinto a RegiShop, validando que la reutilización funciona en la práctica y no solo en la teoría.

---

## 4. Stack tecnológico

| Tecnología | Rol en este proyecto |
|---|---|
| **Next.js 15** (App Router) | Enrutamiento, Server Components por defecto, `generateMetadata` para las páginas de producto y categoría. Turbopack como bundler (`next dev/build --turbopack`). Desde la Fase 11, `/productos/[slug]` y `/categorias/[slug]` ya **no** usan `generateStaticParams` — leen de Supabase en cada request (ver sección 5, "por qué"). Desde la Fase 12, `next/image` muestra las fotos reales de producto subidas desde el Panel; `next.config.ts` habilita el dominio de Supabase Storage vía `images.remotePatterns`, derivado en runtime de `NEXT_PUBLIC_SUPABASE_URL` (sin hardcodear el proyecto de un cliente). |
| **React 19** | Requisito de Next 15. |
| **TypeScript** | Tipado estricto en todo el proyecto. Cada dato de dominio (`Product`, `Category`, `CartLineItem`, `CheckoutFormValues`) tiene un `type` explícito exportado junto a su fuente. |
| **Tailwind CSS v4** | Estilos. Sintaxis CSS-first (`@theme`, `@import "tailwindcss"`), tokens de color en `oklch()` definidos en `globals.css`. No hay `tailwind.config.js` — la configuración vive en el propio CSS. |
| **shadcn/ui** | Primitivas de UI (`Button`, `Input`, `Sheet`, `Badge`, `Select`, `RadioGroup`, `Textarea`, `AlertDialog` y `Switch` desde la Fase 10, `Checkbox` desde la Fase 22, `Tabs` desde la Fase 25). Preset **`base-nova`**, que usa **Base UI** (no Radix) como librería headless subyacente — ver sección 11 para las implicancias concretas de esto. |
| **Framer Motion** | Animaciones: fade-in al hacer scroll (`whileInView`), hover sutil, cross-fade de imágenes. Los patrones compartidos viven en `lib/motion.ts`. Únicamente en la tienda pública — el panel administrativo no usa Framer Motion. |
| **Zustand** | Estado global de la tienda pública. Dos stores hoy: `store/cart-store.ts` (única fuente de verdad del carrito) y `store/checkout-store.ts` (única fuente de verdad del formulario de checkout, desde el Sprint 3.6). Sin middleware de persistencia todavía en ninguno de los dos. El panel administrativo no tiene (ni necesita) un store propio todavía — ver sección 5. |
| **Supabase** (`@supabase/supabase-js`, `@supabase/ssr`) | Clientes instalados desde el Sprint 1 (`lib/supabase/client.ts` y `server.ts`); esquema completo, RLS, Storage y seed ya aplicados al proyecto real desde la Fase 8 (`supabase/migrations/`). Supabase Auth conectado de verdad desde la Fase 9 para administradores. Desde la Fase 10, el módulo Productos del panel lee y escribe en `products`/`product_images`/`categories` y en el bucket `product-images`, vía `lib/supabase/client.ts` (browser). **Desde la Fase 11, la tienda pública también lee de verdad de Supabase** — Home, Productos Destacados, Catálogo, Producto, Relacionados y Categorías, vía `lib/supabase/server.ts` (server, ya usado por el panel para verificar sesión) — encapsulado en `src/services/storefront/` (ver sección 5). RLS es quien decide qué ve cada uno: el storefront (rol `anon`) solo ve filas `is_active = true`; el panel (autenticado) ve todo. Sin login para clientes de la tienda; el resto de las tablas de negocio (pedidos, clientes, promociones) todavía no las consume ningún componente. |
| **pnpm** | Package manager. Siempre usar `pnpm`, nunca `npm`/`yarn` (evita lockfiles mezclados). |
| **Leaflet + React Leaflet** (`leaflet`, `react-leaflet`) | Mapa de selección de ubicación exacta en el checkout (`LocationPicker`/`LeafletMap`, Sprint 3.7), sobre tiles de **OpenStreetMap**. Elegido explícitamente en vez de Google Maps: gratuito, sin API key, sin cuota, reutilizable tal cual para cualquier cliente. `react-leaflet@5` requiere React 19, ya cumplido por el proyecto. |
| **lucide-react** | Íconos. Nota importante: la versión instalada **ya no incluye íconos de marca** (Instagram, Facebook, etc. fueron removidos del paquete). Por eso existe `components/icons.tsx` con SVGs propios para esos casos — no es un error, es una decisión deliberada documentada en el Sprint 2.7. |
| **Recharts** | Gráficos del Dashboard administrativo (Sprint 6.0.1, bug 6): área de ventas (`SalesChart.tsx`) y barras de estados de pedido (`OrderStatusBreakdown.tsx`). Única librería de gráficos del proyecto — se agregó porque el sprint la pedía explícitamente ("si el proyecto ya utiliza Recharts... reutilizarla", implícitamente autorizándola si no estaba). Colores tomados de los tokens `oklch` ya existentes (`var(--foreground)`, `var(--chart-*)`, `var(--destructive)`), sin paleta nueva. Solo se usa en `components/admin/dashboard/` — el resto del panel sigue con CSS puro para barras/indicadores (`TopProductsList.tsx`, sin cambios de librería). |
| **tw-animate-css** | Ya instalada desde el proyecto base (usada por `Sheet`/`Dialog` de shadcn) — reutilizada en el Sprint 6.0.1 para las animaciones de entrada del Dashboard (`animate-in fade-in slide-in-from-bottom-2`, CSS puro). El panel administrativo no usa Framer Motion (ver más abajo) -- esta es la vía correcta para animaciones sutiles ahí. |
| **Tiptap** (`@tiptap/react`, `@tiptap/core`, `@tiptap/starter-kit`, `@tiptap/extension-text-style`, `@tiptap/extension-color`, `@tiptap/extension-highlight`, `@tiptap/extension-image`, `@tiptap/extension-youtube`) | Sprint 6.6: editor de texto enriquecido de la descripción completa de producto (`RichTextEditor.tsx`, `/admin/productos`). Headless sobre ProseMirror -- la toolbar es 100% propia (mismos `Button`/`Select`/`Popover` de shadcn que el resto del panel), elegido en vez de Quill/Lexical por traer extensiones oficiales listas para negrita/cursiva/subrayado/tachado/títulos/listas/citas/separador/enlaces/color/imágenes/YouTube sin reinventarlas -- ver CLAUDE.md sección 9, Fase 28. `StarterKit` (v3) ya incluye `Link`/`Underline` -- no se agregan como paquetes aparte (ver "por qué"). Único lugar del proyecto que usa Tiptap; el resto del panel sigue sin ningún editor de texto enriquecido. |
| **sanitize-html** | Sprint 6.6: sanitiza el HTML de la descripción enriquecida contra XSS, tanto al guardar (`ProductForm.tsx`) como al mostrar (`ProductRichDescription.tsx`) -- `lib/sanitize-html.ts`. Elegida en vez de `dompurify`/`isomorphic-dompurify` (primer intento) porque esta última depende de `jsdom` para correr en Node, y Turbopack no lograba resolver sus archivos internos al hacer `pnpm build` (ver sección 9, Fase 28, para el detalle del bug). `sanitize-html` no depende de ningún DOM emulado. |
| **@tailwindcss/typography** | Sprint 6.6: plugin oficial de Tailwind (`@plugin "@tailwindcss/typography"` en `globals.css`) para la clase `prose` -- estiliza automáticamente el HTML de la descripción enriquecida (títulos, listas, citas, enlaces) tanto en el editor del panel como en `ProductRichDescription.tsx`, sin tener que escribir CSS a mano para cada tag. Único consumidor de este plugin en el proyecto. |

---

## 5. Arquitectura

```
src/
├── middleware.ts                      # Protege /admin/* (Fase 9) -- invoca lib/supabase/middleware.ts
├── app/                              # Rutas (Next.js App Router)
│   ├── globals.css                    # Tailwind v4 + tokens de color (oklch) -- compartido por
│   │                                    # los dos root layouts de abajo
│   ├── (storefront)/                   # Route group -- root layout propio (Fase 9, ver "por qué")
│   │   ├── layout.tsx                    # Layout raíz de la tienda: monta Header y Footer,
│   │   │                                    # generateMetadata dinámico (Fase 14: nombre/
│   │   │                                    # descripción/favicon desde business_settings)
│   │   ├── page.tsx                      # Home
│   │   ├── productos/
│   │   │   ├── page.tsx                    # Catálogo completo
│   │   │   └── [slug]/page.tsx             # Página de producto individual
│   │   ├── categorias/                     # Navegación por categorías (Sprint 4.4)
│   │   │   ├── page.tsx                    # Índice: todas las categorías
│   │   │   └── [slug]/page.tsx             # Productos de una categoría (reutiliza CatalogResults)
│   │   ├── carrito/page.tsx                # Carrito
│   │   ├── checkout/
│   │   │   ├── page.tsx                    # Checkout (formulario)
│   │   │   └── confirmacion/page.tsx       # Confirmación de pedido, previa al envío (Sprint 3.6)
│   │   ├── contacto/page.tsx               # Página de contacto (Sprint 4.5)
│   │   ├── politicas/[slug]/page.tsx       # Envíos/Privacidad/Términos, ruta dinámica (Sprint 4.6)
│   │   └── preguntas-frecuentes/page.tsx   # FAQ (Sprint 4.6)
│   └── admin/                          # Panel administrativo (Fase 9)
│       ├── layout.tsx                    # Root layout propio (html/body/fuentes) -- SIN Header/Footer
│       ├── login/page.tsx                # Login con Supabase Auth, fuera del layout protegido
│       └── (panel)/                      # Route group protegido -- todo lo que requiere sesión
│           ├── layout.tsx                  # Verifica supabase.auth.getUser(); si no hay user, redirect
│           ├── page.tsx                    # Dashboard real, conectado a Supabase (Fase 17)
│           ├── productos/
│           │   ├── page.tsx                  # Tabla real, conectada a Supabase (Fase 10)
│           │   ├── nuevo/page.tsx            # Alta de producto (Fase 10)
│           │   └── [id]/editar/page.tsx      # Edición de producto (Fase 10)
│           ├── categorias/
│           │   ├── page.tsx                  # Tabla real, conectada a Supabase (Fase 13)
│           │   ├── nueva/page.tsx            # Alta de categoría (Fase 13)
│           │   └── [id]/editar/page.tsx      # Edición de categoría (Fase 13)
│           ├── inventario/
│           │   ├── page.tsx                  # Tabla real, conectada a Supabase (Fase 19)
│           │   └── [id]/page.tsx             # Historial de movimientos + ajustes (Fase 19)
│           ├── pedidos/
│           │   ├── page.tsx                  # Tabla real, conectada a Supabase (Fase 15)
│           │   └── [id]/page.tsx             # Detalle + cambio de estado (Fase 15)
│           ├── clientes/
│           │   ├── page.tsx                  # Tabla real, conectada a Supabase (Fase 16)
│           │   └── [id]/page.tsx             # Detalle + historial + estadísticas (Fase 16)
│           ├── promociones/
│           │   ├── page.tsx                  # Tabla real, conectada a Supabase (Fase 18)
│           │   ├── nueva/page.tsx            # Alta de promoción (Fase 18)
│           │   └── [id]/editar/page.tsx      # Edición de promoción (Fase 18)
│           └── configuracion/page.tsx      # Formulario real, conectado a Supabase (Fase 14)
│
├── components/
│   ├── ui/                            # Generado por shadcn. NUNCA se edita a mano.
│   ├── layout/                         # Header, Footer — "chrome" global de la tienda pública
│   ├── storefront/                     # Componentes de dominio comercio: secciones de Home,
│   │                                    # producto, carrito, checkout. La mayoría "use client".
│   │                                    # Incluye LocationPicker.tsx + LeafletMap.tsx (Sprint 3.7),
│   │                                    # CatalogResults.tsx (búsqueda + orden del catálogo,
│   │                                    # Sprints 4.2 y 4.3), CategoryCard.tsx (Sprint 4.4) y
│   │                                    # ProductShowcase.tsx (carrusel de productos del Hero,
│   │                                    # Fase 24) y ProductRichDescription.tsx (Fase 28,
│   │                                    # descripción enriquecida -- único lugar de la tienda
│   │                                    # que renderiza `product.description`)
│   ├── admin/                          # Componentes propios del panel (Fase 9) -- nunca importan
│   │                                    # nada de storefront/, y viceversa (ver "por qué")
│   │                                    # AdminShell.tsx, AdminSidebar.tsx, AdminHeader.tsx,
│   │                                    # LogoutButton.tsx (AdminPlaceholder.tsx se eliminó en
│   │                                    # el Sprint 6.0.1: sin consumidores desde que los 7
│   │                                    # módulos quedaron completos, Fase 19)
│   │   ├── products/                     # Módulo Productos (Fase 10): ProductsTable.tsx,
│   │   │                                  # ProductForm.tsx, ProductImagesField.tsx,
│   │   │                                  # DeleteProductDialog.tsx, RichTextEditor.tsx (Fase 28,
│   │   │                                  # editor Tiptap de la descripción completa) +
│   │   │                                  # rich-text/ (EditorToolbar.tsx, extensions.ts --
│   │   │                                  # ResizableImage y Video, los dos nodos custom)
│   │   ├── categories/                   # Módulo Categorías (Fase 13): CategoriesTable.tsx,
│   │   │                                  # CategoryForm.tsx, DeleteCategoryDialog.tsx,
│   │   │                                  # category-icons.ts (mapa icon_name -> LucideIcon)
│   │   ├── settings/                     # Módulo Configuración (Fase 14): SettingsForm.tsx
│   │   │                                  # (Fase 25: 2 pestañas -- General/Apariencia -- sobre
│   │   │                                  # un solo formulario y un solo submit, sin cambios),
│   │   │                                  # BrandingAssetField.tsx (logo/favicon, reconectado en
│   │   │                                  # la pestaña Apariencia desde la Fase 25)
│   │   ├── inventory/                    # Módulo Inventario (Fase 19): InventoryTable.tsx,
│   │   │                                  # InventoryDetailView.tsx (historial + ajustes +
│   │   │                                  # SKU/stock mínimo), StockMovementForm.tsx,
│   │   │                                  # inventory-status.ts (mapa estado/tipo -> Badge)
│   │   ├── orders/                       # Módulo Pedidos (Fase 15): OrdersTable.tsx,
│   │   │                                  # OrderDetailView.tsx, OrderStatusSelect.tsx
│   │   │                                  # (reutilizado en tabla y detalle), order-status.ts
│   │   │                                  # (mapa status -> variante de Badge)
│   │   ├── customers/                    # Módulo Clientes (Fase 16), solo lectura:
│   │   │                                  # CustomersTable.tsx, CustomerDetailView.tsx (historial
│   │   │                                  # + estadísticas). Reutiliza order-status.ts de orders/
│   │   │                                  # (import de solo lectura, "NO modificar Pedidos" respetado)
│   │   ├── dashboard/                    # Dashboard (Fase 17), solo lectura, agrega datos de
│   │   │                                  # todo el panel: SummaryCard.tsx (reutilizada 9 veces,
│   │   │                                  # con tono de color desde el Sprint 6.0.1),
│   │   │                                  # RecentOrdersTable.tsx, TopProductsList.tsx,
│   │   │                                  # SalesChart.tsx (Sprint 6.0.1: área de Recharts sobre
│   │   │                                  # recentOrders, sin consulta nueva), OrderStatusBreakdown.tsx
│   │   │                                  # (barras de Recharts desde el Sprint 6.0.1 -- antes CSS
│   │   │                                  # puro, Fase 17), Dashboard.tsx (orquestador de /admin)
│   │   └── promotions/                   # Módulo Promociones (Fase 18): PromotionsTable.tsx,
│   │                                      # PromotionForm.tsx (toggle producto/categoría, nunca
│   │                                      # ambos), DeletePromotionDialog.tsx, promotion-status.ts
│   │                                      # (mapa status calculado -> label + variante de Badge)
│   ├── shared/                         # Genéricos, sin conocimiento de e-commerce ni de un rubro
│   │                                    # particular (Breadcrumb, EmptyState, FormField) --
│   │                                    # reutilizados tanto por storefront/ como por admin/
│   └── icons.tsx                       # Íconos propios no cubiertos por lucide-react (+ TikTokIcon, Fase 14)
│
├── config/                            # ⭐ Toda la personalización por cliente vive acá.
│   ├── site.ts                         # Nombre*, descripción*, copy de cada sección/página
│   │                                    # (* `name`/`description` sin consumidores activos en la
│   │                                    # tienda pública desde la Fase 14 -- ver "por qué")
│   ├── business.ts                     # WhatsApp*, moneda, locale, centro del mapa de checkout
│   │                                    # (* solo `lib/whatsapp.ts`, `formatPrice()` y el mapa de
│   │                                    # Checkout lo siguen leyendo -- excepción documentada,
│   │                                    # Fase 14)
│   ├── navigation.ts                   # Ítems del menú (reutilizado por Header Y Footer)
│   ├── categories.ts                   # Categorías + helper de tipo Category
│   ├── products.ts                     # Productos + getProductBySlug + getRelatedProducts.
│   │                                    # `Product` ganó `description?: string` en la Fase 28
│   │                                    # (HTML enriquecido, opcional -- el array mock no lo usa)
│   ├── benefits.ts                     # Beneficios de la sección "Por qué comprar con nosotros"
│   ├── promotion.ts                    # Sin consumidores activos desde la Fase 18 -- fallback/
│   │                                    # documentación, mismo trato que products.ts/categories.ts
│   ├── testimonials.ts                 # Testimonios de clientes
│   ├── footer.ts                       # legalLinks/developedBy*, redes/contacto/descripción sin
│   │                                    # consumidores activos en la tienda pública desde la Fase 14
│   │                                    # (* excepción: Footer sigue leyendo legalLinks/developedBy,
│   │                                    # fuera de las 5 secciones del sprint -- ver "por qué")
│   ├── policies.ts                     # Políticas (envíos/privacidad/términos) + getPolicyBySlug (Sprint 4.6)
│   └── faq.ts                          # Preguntas frecuentes (Sprint 4.6)
│
├── lib/                                # Lógica y helpers compartidos, sin JSX de UI propio
│   ├── utils.ts                        # cn(), formatPrice(), formatDate() (Fase 15), getWhatsAppUrl(),
│   │                                    # getGoogleMapsUrl()
│   ├── motion.ts                       # Variants de Framer Motion compartidos entre secciones
│   ├── products.ts                     # getStockStatus(), getDiscountPercent(), isRealImageUrl()
│   │                                    # (compartida desde el Sprint 6.0.1 -- antes duplicada en
│   │                                    # ProductCard.tsx/ProductGallery.tsx, Fase 12; con un tercer
│   │                                    # consumidor real se cruzó el umbral de "3 o más lugares")
│   ├── checkout.ts                     # Tipos del formulario de checkout + valores iniciales
│   ├── whatsapp.ts                     # Arma el mensaje del pedido + su URL de wa.me (Sprint 3.8) --
│   │                                    # sin cambios en la Fase 15. Desde el Sprint 6.0.1 (bug 3),
│   │                                    # `getOrderWhatsAppUrl()` recibe `whatsappNumber` por
│   │                                    # parámetro en vez de importar `config/business.ts` -- quien
│   │                                    # llama (OrderConfirmationDetails.tsx) lo resuelve desde
│   │                                    # `business_settings`
│   ├── search.ts                       # searchProducts(): filtro de catálogo por nombre/categoría/descripción (Sprint 4.2)
│   ├── sort.ts                         # sortProducts(): orden por precio/nombre (Sprint 4.3)
│   ├── categories.ts                   # getProductsByCategory() (Sprint 4.4). getCategoryBySlug()
│   │                                    # se eliminó en la Fase 13: sin call sites desde la Fase 11
│   ├── promotions.ts                   # (Fase 18) formatDiscountLabel(), getPromotionStatus() --
│   │                                    # funciones puras, reutilizadas por el panel Y la tienda
│   │                                    # pública (única excepción: la mayoría de lib/ hasta ahora
│   │                                    # era o bien storefront o bien compartida vía tipo, no
│   │                                    # lógica ejecutada en ambos lados)
│   ├── sanitize-html.ts                # (Fase 28) sanitizeDescriptionHtml()/isEmptyDescriptionHtml()
│   │                                    # -- sanitiza el HTML de la descripción enriquecida contra
│   │                                    # XSS, llamada tanto desde ProductForm.tsx (al guardar,
│   │                                    # cliente de browser) como desde
│   │                                    # services/storefront/products.ts (al leer, servidor) --
│   │                                    # segundo archivo de lib/ con lógica real ejecutada en
│   │                                    # ambos lados, mismo criterio que lib/promotions.ts (Fase 18)
│   └── supabase/
│       ├── client.ts                    # Cliente de Supabase para browser -- usado desde Fase 9 en
│       │                                 # /admin/login (signInWithPassword, signOut)
│       ├── server.ts                    # Cliente de Supabase para Server Components -- usado desde
│       │                                 # Fase 9 en admin/(panel)/layout.tsx (getUser())
│       └── middleware.ts                # updateSession(): protege /admin/* a nivel de edge (Fase 9)
│
├── store/
│   ├── cart-store.ts                   # Zustand. Única fuente de verdad del carrito.
│   └── checkout-store.ts               # Zustand. Única fuente de verdad del form de checkout (Sprint 3.6).
│
├── services/                          # Única capa que le habla a Supabase, storefront y panel.
│   ├── products.ts                      # (Fase 10) CRUD admin de products + product_images -- cliente
│   │                                     # de browser (lib/supabase/client.ts)
│   ├── categories.ts                    # (Fase 10: solo lectura para el selector del form. Fase 13:
│   │                                     # CRUD completo -- create/update/delete con borrado seguro,
│   │                                     # isSlugTaken, conteo de productos por categoría)
│   ├── settings.ts                      # (Fase 14) Lectura/actualización de la fila única de
│   │                                     # business_settings + subida/borrado de logo y favicon
│   │                                     # (bucket "branding") -- sin alta ni baja, la fila es fija
│   ├── orders.ts                        # (Fase 15) Lectura admin de orders/order_items/customers
│   │                                     # (join) + updateOrderStatus -- sin alta: los pedidos los
│   │                                     # crea el checkout público, no el panel
│   ├── customers.ts                     # (Fase 16) Lectura admin de customers + sus orders --
│   │                                     # 100% solo lectura, sin alta/edición/baja. Reutiliza el
│   │                                     # tipo `OrderStatus` de services/orders.ts (import, sin
│   │                                     # tocar ese archivo). Calcula estadísticas en memoria a
│   │                                     # partir de los pedidos ya traídos -- nunca las guarda
│   ├── dashboard.ts                     # (Fase 17) Agrega orders/order_items/customers/products/
│   │                                     # categories para /admin -- 100% solo lectura, reutiliza
│   │                                     # OrderStatus/ORDER_STATUSES de services/orders.ts (import).
│   │                                     # Todas las cifras se recalculan en cada carga, ninguna se
│   │                                     # guarda (pedido explícito del sprint)
│   ├── promotions.ts                     # (Fase 18) CRUD admin de promotions -- sin restricción de
│   │                                     # borrado: nada referencia promotions.id como FK
│   ├── inventory.ts                      # (Fase 19) Lectura de products+stock_movements para el
│   │                                     # listado/detalle de Inventario, y las dos escrituras de
│   │                                     # stock -- ambas vía función de Postgres
│   │                                     # (register_stock_movement) o campos ajenos al stock en sí
│   │                                     # (sku/low_stock_threshold vía update directo). Nunca hace
│   │                                     # un `update products set stock = ...` directo: esa es la
│   │                                     # única forma de garantizar que el stock y su historial se
│   │                                     # muevan siempre juntos
│   ├── storage.ts                       # (Fase 10) Subida/borrado de archivos en product-images.
│   │                                     # + uploadDescriptionAsset() (Fase 28) para el bucket
│   │                                     # nuevo product-content (imágenes/GIFs/video embebidos
│   │                                     # en la descripción enriquecida)
│   └── storefront/                      # (Fase 11) Lectura pública -- cliente de servidor
│       │                                 # (lib/supabase/server.ts, el mismo que usa el panel para
│       │                                 # verificar sesión), nunca el de browser del panel.
│       ├── products.ts                    # getPublicProducts/getPublicProductBySlug/
│       │                                   # getPublicRelatedProducts -- devuelven el `Product` de
│       │                                   # siempre (config/products.ts), no un tipo nuevo. Desde
│       │                                   # el Sprint 6.0.1, ambas filtran `.eq("is_active", true)`
│       │                                   # explícitamente además de RLS (ver "por qué"). Desde la
│       │                                   # Fase 24, + getPublicHeroProducts() -- destacados primero,
│       │                                   # más recientes si no hay ninguno, para el Showcase del Hero.
│       │                                   # Desde la Fase 28, getPublicProductBySlug() también trae
│       │                                   # `description` (select propio, PUBLIC_PRODUCT_DETAIL_SELECT,
│       │                                   # sin tocar el select compartido que usan las otras dos) --
│       │                                   # sanitizada acá mismo con sanitizeDescriptionHtml() antes de
│       │                                   # devolverla, no solo al renderizarla (ver "por qué")
│       ├── categories.ts                  # getPublicCategories/getPublicCategoryBySlug -- devuelven
│       │                                   # el `Category` de siempre (config/categories.ts). Mismo
│       │                                   # filtro explícito de `is_active` desde el Sprint 6.0.1
│       │                                   # (bug 4)
│       ├── business.ts                    # (Fase 14) getPublicBusinessSettings -- devuelve un tipo
│       │                                   # nuevo, `BusinessSettings` (sin equivalente previo en
│       │                                   # config/business.ts, que nunca exportó un `type`). Cliente
│       │                                   # de **servidor** exclusivamente -- ver business-client.ts
│       ├── business-client.ts             # (Sprint 6.0.1, bug 3) getPublicBusinessSettingsClient()
│       │                                   # -- misma fila, mismo mapeo que business.ts, pero con el
│       │                                   # cliente de browser, en su propio archivo a propósito (ver
│       │                                   # "por qué": mezclarla en business.ts rompía el build en
│       │                                   # cuanto un Client Component la importaba)
│       ├── orders.ts                      # (Fase 15) createOrder -- única escritura pública hacia
│       │                                   # orders/order_items/customers, con el cliente de
│       │                                   # **browser** (no el de servidor): se invoca desde un click
│       │                                   # en Checkout, no durante el render de una página (ver
│       │                                   # "por qué"). Toda la escritura real vive en la función de
│       │                                   # Postgres `create_order`, no acá.
│       └── promotions.ts                  # (Fase 18) getActivePromotion() -- desde el Sprint 6.0.1
│                                           # también filtra `is_active`/`starts_at`/`ends_at`
│                                           # explícitamente, además de RLS (bug 4, mismo criterio que
│                                           # products.ts/categories.ts). Devuelve un tipo nuevo,
│                                           # `PublicPromotion`, con el label/href del producto o
│                                           # categoría ya resueltos, y (desde el Sprint 6.0.1, bug 2)
│                                           # `imageUrl`: la imagen principal de `product_images`
│                                           # cuando la promoción es de un producto, reutilizada sin
│                                           # duplicar ni agregar un campo nuevo
│
├── hooks/                              # (Fase 10) Puente entre components/admin/ y services/.
│   ├── useProducts.ts                    # Listado: fetch + búsqueda/orden/paginación en memoria + borrar
│   ├── useProduct.ts                     # Un producto por id (para el formulario de edición)
│   ├── useCategories.ts                  # (Fase 10: solo lectura. Fase 13: + búsqueda/orden/borrado,
│   │                                      # mismo criterio que useProducts.ts -- sin paginación real
│   │                                      # todavía, preparada pero no implementada)
│   ├── useCategory.ts                    # (Fase 13) Una categoría por id (formulario de edición)
│   ├── useSettings.ts                    # (Fase 14) Trae y guarda la fila única de business_settings
│   │                                      # -- sin búsqueda/orden/paginación (no aplica a una fila)
│   ├── useOrders.ts                      # (Fase 15) Listado: fetch + búsqueda/filtro por estado/orden
│   │                                      # + cambio de estado optimista -- sin paginación real todavía
│   ├── useOrder.ts                       # (Fase 15) Un pedido por id + cambio de estado (para el
│   │                                      # detalle) -- optimista, revierte si Supabase lo rechaza
│   ├── useCustomers.ts                   # (Fase 16) Listado: fetch + búsqueda/orden en memoria --
│   │                                      # sin paginación real todavía, sin borrado (solo lectura)
│   ├── useCustomer.ts                    # (Fase 16) Un cliente por id + su historial de pedidos y
│   │                                      # estadísticas ya calculadas (para el detalle)
│   ├── useDashboard.ts                   # (Fase 17) Trae todo /admin de una vez -- sin búsqueda,
│   │                                      # orden, filtro ni paginación (una sola pantalla de lectura)
│   ├── usePromotions.ts                  # (Fase 18) Listado: fetch + búsqueda/orden en memoria +
│   │                                      # borrar -- sin paginación real todavía
│   ├── usePromotion.ts                   # (Fase 18) Una promoción por id (formulario de edición)
│   ├── usePromotionTargets.ts            # (Fase 18) Lista completa de productos y categorías para
│   │                                      # los selectores de PromotionForm -- llama a
│   │                                      # listProducts()/listCategories() directamente (no a
│   │                                      # useProducts()/useCategories(), que paginan o traen de
│   │                                      # más para lo que necesita acá un combo)
│   ├── useInventory.ts                   # (Fase 19) Listado: fetch + búsqueda/orden en memoria --
│   │                                      # sin paginación real todavía, mismo criterio que el resto
│   ├── useInventoryItem.ts               # (Fase 19) Un producto + su historial completo de
│   │                                      # movimientos, para /admin/inventario/[id] -- registrar un
│   │                                      # movimiento o guardar SKU/stock mínimo actualiza el estado
│   │                                      # local de forma optimista con el resultado que ya devuelve
│   │                                      # la escritura, sin volver a pedir nada a Supabase (ver
│   │                                      # "por qué": un refetch completo remontaba el formulario)
│   └── useProductShowcase.ts             # (Fase 24) Primer hook propio de la tienda pública -- no
│                                          # fetchea nada (recibe `products` ya resuelto por Home),
│                                          # solo encapsula índice/dirección/pausa del carrusel del
│                                          # Hero (ver "por qué")
│
public/
├── branding/                           # Logo/assets del cliente. Vacío hoy, reservado (sin uso
│                                         # real todavía: el logo/favicon subidos desde el panel van
│                                         # al bucket de Storage "branding", no acá).
└── favicon-default.ico                 # (Fase 14) Antes vivía en app/favicon.ico -- se movió acá
                                          # para que sea el fallback estático cuando la tienda no
                                          # tiene favicon_url propio (ver "por qué").

docs/
└── database-architecture.md          # Diseño conceptual de la base de datos (Sprint 4.0, aprobado)

supabase/                              # Infraestructura de Supabase (Fase 8). El panel (Fase 10) y,
│                                        # desde la Fase 11, la tienda pública también leen de acá.
├── config.toml                        # Generado por `supabase init`; configuración del proyecto/CLI
├── seed.sql                            # Datos iniciales, tomados de config/*.ts (sin customers/
│                                        # orders/order_items/promotions/admins -- ver sección 9).
│                                        # Ya aplicado al proyecto real desde la Fase 10.
└── migrations/
    ├── ..._create_schema.sql           # Las 12 tablas de docs/database-architecture.md + triggers
    │                                    # de updated_at + is_admin()
    ├── ..._create_indexes.sql          # Todos los índices listados en el documento aprobado
    ├── ..._create_storage.sql          # Bucket "product-images" + sus policies de Storage
    ├── ..._create_rls_policies.sql     # RLS de las 12 tablas (lectura pública de catálogo/
    │                                    # contenido, escritura solo admins)
    ├── ..._products_admin_fields.sql   # (Fase 10) Agrega products.is_active y products.description,
    │                                    # + ajusta el SELECT público de products al mismo patrón
    │                                    # público-activos/admin-todos que ya usan categories/banners
    ├── ..._categories_admin_fields.sql # (Fase 13) Agrega categories.icon_name
    ├── ..._business_settings_admin_fields.sql # (Fase 14) Agrega tagline/logo_url/favicon_url/
    │                                    # instagram_url/facebook_url/tiktok_url/map_country/map_city
    ├── ..._create_order_function.sql   # (Fase 15) Función create_order (security definer) -- único
    │                                    # punto de escritura pública hacia customers/orders/order_items
    └── ..._create_inventory.sql        # (Fase 19) products.sku/low_stock_threshold,
                                         # orders.stock_adjusted, tabla stock_movements + RLS,
                                         # función register_stock_movement (entradas/ajustes
                                         # manuales) y trigger apply_order_stock_movement (descuenta/
                                         # restituye stock automáticamente según el estado del pedido)
```

### Por qué existe cada carpeta

- **`app/`** solo contiene rutas y layouts. Nunca lógica de negocio ni componentes reutilizables — eso vive en `components/`.
- **`components/ui/`** es tratado como código de terceros (vendored): si hace falta un componente nuevo, se agrega con `pnpm dlx shadcn@latest add <nombre>`, nunca se escribe a mano.
- **`components/layout/`** vs **`components/storefront/`** vs **`components/shared/`**: la pregunta que separa estas tres carpetas es "¿esto es específico de comercio, específico de layout global, o genérico para cualquier tipo de sitio?". `Breadcrumb` y `EmptyState`, por ejemplo, no saben qué es un producto — por eso son `shared/`, no `storefront/`.
- **`config/`** es la carpeta más importante del proyecto en términos de negocio: es literalmente lo único que debería cambiar al lanzar la tienda de un cliente nuevo (además de las imágenes reales).
- **`lib/`** es para lógica que **no es JSX** y que más de un componente necesita. Si dos componentes necesitan la misma fórmula de cálculo o el mismo helper, va acá — no se copia y pega.
- **`store/`** existe recién desde el Sprint 3.4. Antes de eso, el carrito vivía en `useState` local dentro de la página `/carrito`, a propósito, como paso intermedio. Desde el Sprint 3.6 tiene un segundo store, `checkout-store.ts`: existe porque `/checkout` y `/checkout/confirmacion` son rutas distintas y, sin persistencia todavía, un `useState` local en `CheckoutForm` no sobrevive esa navegación (y "Editar datos" volvería a un formulario vacío). Mismo patrón que `cart-store.ts`, aplicado a un problema real y presente, no anticipado.
- **`LocationPicker.tsx` / `LeafletMap.tsx`** (Sprint 3.7, dentro de `components/storefront/`): dos archivos, no uno, por una razón técnica puntual — `leaflet` toca `window` al evaluar el módulo (no solo al renderizar), así que el código que lo importa **no puede** llegar nunca a un import estático server-side. `LeafletMap.tsx` concentra toda la lógica real del mapa y solo se carga vía `next/dynamic(..., { ssr: false })` desde `LocationPicker.tsx`, que es el único archivo que el resto de la app conoce e importa normalmente.
- **`lib/whatsapp.ts`** (Sprint 3.8): arma el texto completo del pedido (cliente, productos, resumen, entrega, pago, notas) y lo devuelve como URL de wa.me. Existe separado de `lib/checkout.ts` y de `lib/utils.ts` porque es lógica de dominio propia (el formato del mensaje del pedido), no un helper genérico ni parte del contrato de tipos del formulario — y porque `OrderConfirmationDetails` no debe construir texto de mensaje por su cuenta.
- **`lib/search.ts` / `CatalogResults.tsx`** (Sprint 4.2): la búsqueda del catálogo se dividió en dos capas a propósito. `searchProducts()` en `lib/` es la función pura (normaliza, filtra por nombre/categoría/descripción) — sin JSX, sin hooks, testeable sola. `CatalogResults` es quien la conecta a la URL (`useSearchParams`) y decide qué renderizar (`ProductGrid` o `EmptyState`). `ProductGrid` no cambió: sigue sin saber que existe un buscador.
- **`lib/sort.ts`** (Sprint 4.3): mismo criterio que `lib/search.ts` — `sortProducts()` es una función pura, en su propio archivo, sin mezclarse con la búsqueda ("cada responsabilidad vive en su propio archivo"). `CatalogResults` aplica primero `searchProducts` y recién después `sortProducts` sobre el resultado — nunca al revés.
- **`lib/categories.ts` / `app/categorias/`** (Sprint 4.4): `getProductsByCategory()` es el "paso 0" del pipeline del catálogo — se resuelve en la página (`/categorias/[slug]`, Server Component), *antes* de pasarle el array ya acotado a `CatalogResults`, que sigue sin saber nada de categorías. Por eso `CatalogResults` no ganó ningún prop ni lógica nueva en este sprint: solo se le arregló un bug que le impedía ser reutilizado (tenía `/productos` hardcodeado al escribir `?sort=` en la URL — ahora usa `usePathname()`). `CategoryCard` se extrajo de `FeaturedCategories` (mismo JSX, cero cambios visuales) para que `/categorias` y la Home usen la misma tarjeta, corrigiendo de paso el link roto (`/categoria/[slug]`, que nunca existió, por `/categorias/[slug]`).
- **`docs/`** (Sprint 4.0): documentación técnica que no es código ni pertenece a ningún sprint de UI. Hoy contiene solo `database-architecture.md`, el diseño conceptual de las 12 tablas (`categories`, `products`, `product_images`, `customers`, `orders`, `order_items`, `business_settings`, `banners`, `promotions`, `testimonials`, `benefits`, `admins`) que va a usar Supabase.
- **`supabase/`** (Fase 8): estructura oficial generada con `supabase init` + 4 migraciones versionadas + `seed.sql`. Existe separada de `docs/` porque `docs/database-architecture.md` es el diseño (por qué existen las tablas), mientras que `supabase/migrations/` es la implementación literal de ese diseño en SQL — uno no reemplaza al otro, y el segundo debe poder auditarse contra el primero tabla por tabla. Dividida en 4 migraciones (schema/índices/storage/RLS) en vez de una sola, siguiendo el mismo principio de "una responsabilidad por archivo" que ya rige `lib/search.ts`/`lib/sort.ts`/`lib/categories.ts`: cada una se puede leer, revisar y (si hiciera falta) revertir por separado. El seed vive en `supabase/seed.sql` (la ruta que Supabase reconoce automáticamente en `db reset`), no como una quinta migración numerada: sembrar datos de ejemplo no es un cambio de esquema, y una migración normal se re-ejecuta contra producción con `db push` — un `seed.sql` aparte es el único de los cinco archivos que nunca se aplica remotamente sin pedirlo explícitamente (`db push --include-seed`), evitando pisar datos reales de un cliente futuro con el contenido placeholder de RegiShop.
- **`app/contacto/page.tsx`** (Sprint 4.5): página nueva, sin componente propio en `storefront/` ni en `shared/` — su contenido (info + dos botones) no se reutiliza en ningún otro lugar de la tienda, así que crear un archivo aparte solo para eso habría sido sobreingeniería. Reutiliza `footerConfig.contact` (email/horario/dirección) tal cual, sin duplicar esos datos en `business.ts`: la instrucción de este sprint pedía que la dirección "provenga de la configuración del negocio", pero como el Footer ya la tenía y el mismo sprint prohibía explícitamente duplicar información, se interpretó como "debe ser config, no texto hardcodeado" — no como una exigencia de mover el dato a `business.ts`. El botón "Ver ubicación" arma la URL de Google Maps con el helper nuevo `getGoogleMapsUrl()` (`lib/utils.ts`), a partir de esa misma dirección — sin mapa embebido, sin API key.
- **`app/politicas/[slug]/page.tsx`** / **`app/preguntas-frecuentes/page.tsx`** (Sprint 4.6): las tres políticas (`envios`, `privacidad`, `terminos`) comparten una única ruta dinámica — mismo patrón que `/categorias/[slug]` (Sprint 4.4): `generateStaticParams` desde `config/policies.ts`, `generateMetadata` por policy, `notFound()` si el slug no existe. Esa ruta dinámica *es* la reutilización pedida en este sprint: en vez de 3 archivos casi idénticos, hay uno solo. La FAQ vive en una ruta aparte (`/preguntas-frecuentes`) porque su contenido tiene una forma distinta (pares pregunta/respuesta, no secciones de texto) — no se forzó a compartir tipo con `Policy`. No se extrajo un componente `PolicyPage`/`InstitutionalPageLayout` para el bloque común de header (breadcrumb + título + descripción): con la ruta dinámica ya colapsando las 3 políticas en un archivo, ese bloque solo se repite en 2 lugares (la página de políticas y la de FAQ) — por debajo del umbral de "3 o más lugares" que este proyecto ya usa para decidir cuándo abstraer (sección 6, principio 6), el mismo criterio con el que `/categorias` y `/categorias/[slug]` tampoco comparten ese bloque hoy.
- **`app/(storefront)/`** (Fase 9): route group nuevo que agrupa las 8 rutas que antes vivían sueltas directo en `app/` (Home, `/productos`, `/categorias`, `/carrito`, `/checkout`, `/contacto`, `/politicas`, `/preguntas-frecuentes`), junto con el layout raíz que antes era `app/layout.tsx`. **No es un cambio de diseño ni de comportamiento** — cada URL de la tienda pública sigue exactamente igual (`(nombre)` no aparece en la URL), y ningún componente ni página cambió una línea de contenido, solo de ubicación en el disco. Existe porque Next.js exige que un único `app/layout.tsx` envuelva *todo* lo que cuelga de `app/`, sin excepciones — y el panel administrativo (`app/admin/`, ver bullet siguiente) necesita su propio `<html>`/`<body>` sin el `Header`/`Footer` de la tienda. La única forma soportada de tener dos "shells" (tienda pública vs. panel) en la misma app de Next.js es la técnica oficial de *multiple root layouts*: cada rama de primer nivel debajo de `app/` define su propio layout raíz completo. `globals.css` y `favicon.ico` quedaron en `app/` (fuera de ambos grupos) porque son compartidos por los dos.
- **`app/admin/`** (Fase 9): el panel administrativo, con dos capas de layout. `app/admin/layout.tsx` es la otra raíz del punto anterior (fuentes Geist + `globals.css`, sin Header/Footer). Adentro, `login/page.tsx` (público, Supabase Auth) queda **fuera** de `(panel)/`, y `(panel)/layout.tsx` es quien de verdad protege el resto: hace `supabase.auth.getUser()` del lado del servidor y redirige a `/admin/login` si no hay sesión, antes de renderizar `AdminShell` (Sidebar + Header propios) alrededor de cualquier página de `(panel)/`. Todas las páginas de `(panel)/` son placeholders (`AdminPlaceholder`, `components/admin/`) — sin CRUD, sin datos reales, a propósito (alcance de este sprint).
- **`components/admin/`** (Fase 9): espejo de `components/storefront/`, pero para el panel — mismo criterio de separación que ya distingue `storefront/` de `layout/`. `AdminShell.tsx` orquesta el estado de UI (drawer mobile abierto/cerrado) y compone `AdminSidebar` + `AdminHeader`; `AdminSidebar.tsx` resuelve las tres variantes responsive (fija/colapsada/drawer) reutilizando un mismo listado de nav interno, sin triplicar el JSX; `LogoutButton.tsx` es el único punto que llama `supabase.auth.signOut()` (reutilizado por Sidebar y Header); `AdminPlaceholder.tsx` reutiliza `EmptyState` (`components/shared/`) para el cuadro "Próximamente" en vez de reimplementar el borde punteado. Ningún componente de `admin/` importa nada de `storefront/`, y viceversa.
- **`middleware.ts`** / **`lib/supabase/middleware.ts`** (Fase 9): el middleware en sí (`src/middleware.ts`) es solo dos líneas — invoca `updateSession()`, que vive en `lib/` porque es lógica de Supabase, no configuración de Next.js (mismo criterio que separa `client.ts`/`server.ts`). Su `matcher` está limitado a `/admin/:path*`: la tienda pública no tiene login y no necesita que este middleware la toque en absoluto. La verificación real y no salteable sigue viviendo en `admin/(panel)/layout.tsx` (`getUser()` del lado del servidor) — el middleware es una redirección temprana (mejor UX, evita el parpadeo de una página protegida), no la única barrera.
- **`services/`** / **`hooks/`** (Fase 10, dos carpetas nuevas de primer nivel): separación explícita entre "cómo se habla con Supabase" y "cómo un componente consume ese dato". `services/*.ts` son funciones puras (sin JSX, sin hooks de React) que arman queries/mutations de Supabase y devuelven tipos propios del panel (`AdminProduct`, no el `Product` de `config/products.ts`) — ningún componente arma un `supabase.from(...)` por su cuenta, exactamente como el sprint lo pidió ("no colocar consultas SQL dentro de componentes"). `hooks/*.ts` son quienes de verdad usan React (`useState`/`useEffect`), llaman a `services/`, y exponen `{ data, isLoading, error, ... }` a los componentes — mismo rol que ya cumplía `CatalogResults` en el storefront (conectar datos a UI), pero como hook reutilizable en vez de un componente, porque acá hay tres consumidores distintos (tabla, formulario de alta, formulario de edición) que necesitan la misma lógica de fetching. Búsqueda/orden/paginación de la tabla de productos viven *dentro* de `useProducts.ts`, no en archivos propios tipo `lib/search.ts`/`lib/sort.ts`: con un solo listado (todavía sin un segundo módulo que las necesite), separarlas en más archivos hubiera sido sobreingeniería — si Categorías/Pedidos terminan necesitando el mismo patrón de búsqueda+orden+paginación, ahí se justifica extraerlo a algo compartido.
- **`components/admin/products/`** (Fase 10): el primer módulo funcional del panel. `ProductsTable.tsx` (tabla + buscador + orden + paginación + botón "Nuevo producto"), `ProductForm.tsx` (un solo componente para alta y edición — misma UI, misma validación, decide `create` vs `update` según si recibe `productId`), `ProductImagesField.tsx` (administra imágenes ya guardadas + nuevas todavía sin subir, con preview local vía `URL.createObjectURL`; no sube ni borra nada por su cuenta, eso lo hace `ProductForm` recién al guardar), `DeleteProductDialog.tsx` (usa el `AlertDialog` de shadcn, agregado en este sprint — controlado con `open`/`onOpenChange` porque `AlertDialogAction` no cierra el diálogo solo, a diferencia de `AlertDialogCancel`, y hace falta esperar a que el borrado real termine antes de cerrarlo).
- **`supabase/migrations/..._products_admin_fields.sql`** (Fase 10): agrega `is_active` (boolean, default `true`) y `description` (text, nullable) a `products` — ninguna de las dos estaba en el diseño aprobado de la Fase 8. Se agregaron porque este sprint pidió explícitamente una columna "Estado (Activo/Inactivo)" y un campo "Descripción completa" en el formulario, que no tienen ninguna otra forma de implementarse sin tocar el esquema. De paso, actualiza la policy de lectura pública de `products` al mismo patrón "público ve solo activos, admin ve todo" que ya usan `categories`/`banners`/`testimonials`/`benefits` — `products` no tenía ese split porque `is_active` no existía hasta ahora.
- **`services/storefront/`** (Fase 11): mismo criterio de encapsulamiento que `services/products.ts` del panel (Fase 10), pero para lectura pública — separado en su propia carpeta porque usa un cliente de Supabase distinto (`lib/supabase/server.ts`, no el de browser) y devuelve tipos distintos (`Product`/`Category` de `config/*.ts`, no `AdminProduct`/`AdminCategory`). **No hay `hooks/` para el storefront**: los hooks son para Client Components que fetchean sus propios datos (como hace el panel), pero en la tienda pública ninguno lo hace — todos los componentes (`ProductGrid`, `ProductCard`, `CategoryCard`, `FeaturedProducts`, `FeaturedCategories`) siguen recibiendo un array ya resuelto por props, exactamente como antes; lo único que cambió es que ahora lo resuelve un `await services/storefront/*` en una página (Server Component) en vez de un `import` de `config/*.ts`. Agregar un hook ahí hubiera sido una capa sin ningún componente que la necesitara.
- **`generateStaticParams` eliminado de `/productos/[slug]` y `/categorias/[slug]`** (Fase 11): con datos en `config/*.ts`, pre-generar la lista completa de slugs en build time tenía sentido (el catálogo era fijo). Con Supabase, un producto puede crearse desde el panel en cualquier momento — una lista de slugs fija en build time quedaría desactualizada de inmediato. Ambas rutas ya renderizan dinámicamente en cada request de todos modos, porque `lib/supabase/server.ts` usa `cookies()` (una "Dynamic API" de Next.js), lo mismo que ya obliga a `admin/(panel)/layout.tsx` a ser dinámico desde la Fase 9 — es el mismo mecanismo, aplicado ahora también del lado público.
- **`lib/search.ts` recibe `categories` por parámetro, ya no la importa** (Fase 11): antes `searchProducts()` importaba `categories` directo de `config/categories.ts` para poder buscar también por nombre de categoría. Como ahora `CatalogResults` recibe las categorías reales de Supabase (para poder pasarlas al formulario/tabla en el futuro y para que la búsqueda compare contra el nombre real, no uno potencialmente desactualizado), `searchProducts(products, query, categories)` pasó a recibir ese array como argumento en vez de tener un import fijo — la función sigue siendo pura, solo dejó de tener una dependencia oculta a `config/`.
- **`config/products.ts` y `config/categories.ts` no se tocaron ni se eliminaron** (Fase 11, instrucción explícita del sprint): siguen exportando exactamente lo mismo que antes (`products`, `categories`, `getProductBySlug`, `getRelatedProducts`) — hoy es código muerto desde la perspectiva de las páginas (nada los importa ya para renderizar la tienda), pero se dejaron como fallback/documentación, tal como se pidió, para un sprint futuro que decida su destino final.
- **`next.config.ts` (Fase 12)**: no aparece en el árbol de `src/` porque vive en la raíz del proyecto (configuración de Next.js, no código de dominio), pero se documenta acá porque ahora es parte del camino de datos de las fotos de producto — `images.remotePatterns` es lo que le permite a `next/image` servir imágenes alojadas en el bucket de Supabase Storage de cada cliente. Deriva el hostname permitido de `NEXT_PUBLIC_SUPABASE_URL` en vez de hardcodear el project ref de RegiShop, para no romper el flujo de onboarding de un cliente nuevo (sección 2: nuevo `.env.local`, sin tocar código).
- **`components/admin/categories/`** (Fase 13): segundo módulo funcional del panel, mismo patrón exacto que `products/` (Fase 10) -- `CategoriesTable.tsx`, `CategoryForm.tsx`, `DeleteCategoryDialog.tsx`. Se suma `category-icons.ts`, que no tiene equivalente en `products/`: un mapa `{ [iconName]: LucideIcon }` con un set curado de íconos de e-commerce (genérico, tecnología, hogar, belleza, deportes, ropa, alimentos, etc.), mismo criterio que ya usa `config/benefits.ts`/`icon_name` para la sección de Beneficios -- la base de datos guarda un identificador de texto (`categories.icon_name`), nunca un componente de React, y este archivo es el único lugar del proyecto que lo resuelve a un ícono real. Vive en `components/` (capa de presentación) a propósito: `services/` y `hooks/` no conocen lucide-react.
- **`services/categories.ts` extendido, no duplicado** (Fase 13): hasta la Fase 10 este archivo solo tenía `listCategories()` de solo lectura, para el selector del formulario de Productos. Se amplió en el mismo archivo (no se creó un `services/categories-admin.ts` aparte) porque sigue siendo "toda la comunicación con la tabla `categories`" -- mismo criterio que ya separa un archivo de servicio por tabla. `AdminCategory` ganó campos nuevos (`productCount`, `iconName`, `displayOrder`, etc.) de forma aditiva: `ProductForm.tsx` (Productos, sin tocar este sprint) sigue destructurando solo `id`/`name` de la misma función y sigue funcionando idéntico.
- **Borrado seguro de categorías, en dos capas** (Fase 13): `products.category_id` ya tenía `on delete restrict` desde la Fase 8 -- Postgres directamente rechaza borrar una categoría con productos asociados, la integridad referencial nunca dependió de este sprint. Sobre esa base, `deleteCategory()` (`services/categories.ts`) agrega un chequeo proactivo (cuenta productos antes de intentar el borrado) para dar un mensaje claro en vez de un error crudo de Postgres, y conserva el catch del código `23503` como red de seguridad ante una carrera (otro admin crea un producto justo entre el chequeo y el borrado). `DeleteCategoryDialog.tsx` decide en la UI, con el `productCount` que ya trae el listado, si mostrar la confirmación destructiva normal o directamente el bloqueo explicando que hay que reasignar/eliminar los productos primero -- nunca ofrece un botón "Eliminar" que solo va a fallar.
- **Eliminación de la dependencia activa a `config/categories.ts` en la tienda pública** (Fase 13): `ProductCard`, `ProductGallery`, `ProductBuyBox`, `ProductGrid`, `FeaturedProducts` y `RelatedProducts` dejaron de importar el array `categories` de config -- todos pasaron a recibir `categories: Category[]` por prop, resuelta por la página (mismo patrón que ya establecía `CatalogResults` desde la Fase 11 para la búsqueda). `CategoryCard` y `FeaturedCategories` no necesitaron ningún cambio: ya recibían la categoría/las categorías resueltas por prop desde la Fase 11, nunca hicieron un `.find()` propio. El tipo `Category` en sí (`config/categories.ts`) sigue importándose como *tipo* (`import type`) en varios archivos -- eso no es una dependencia activa (se borra en tiempo de compilación), es el mismo contrato que ya se preservó para `Product` en la Fase 11. Las únicas dos referencias activas (`import { categories }`, valor en tiempo de ejecución) que quedan en todo el proyecto son `CartItem.tsx` y `CheckoutSummary.tsx` -- Carrito y Checkout, explícitamente fuera de alcance de este sprint (ver sección 13).
- **`services/settings.ts` y `services/storefront/business.ts`** (Fase 14): mismo patrón admin/storefront que ya separa Productos y Categorías -- uno con el cliente de browser y tipos `AdminBusinessSettings`/`SettingsFormInput` (panel), otro con el cliente de servidor y un tipo propio `BusinessSettings` (tienda pública). A diferencia de `Product`/`Category`, `business_settings` nunca tuvo un `type` en `config/business.ts` para reutilizar (`businessConfig` es un objeto `as const` sin tipo exportado) -- `BusinessSettings` es un contrato nuevo, no una migración de uno existente.
- **Bucket de Storage `branding`, separado de `product-images`** (Fase 14): logo y favicon son archivos únicos por negocio (no una galería por fila como `product_images`), sin relación con ningún producto puntual y con un ciclo de vida propio (nunca se borran en cascada junto con nada). Mismas policies que `product-images` (lectura pública, escritura solo `is_admin()`), límite de 2 MB en vez de 5 (un logo/favicon no necesita tanto) y tipos MIME que incluyen `image/svg+xml`/`image/x-icon` (product-images no los permite, un logo vectorial o un `.ico` sí son válidos acá).
- **`app/favicon.ico` movido a `public/favicon-default.ico`** (Fase 14): Next.js genera **siempre** su propio `<link rel="icon">` para el archivo especial `app/favicon.ico`, sin importar lo que devuelva `generateMetadata`'s `icons` -- son dos mecanismos independientes que no se pisan, coexisten. Mientras existiera ese archivo, un favicon subido desde el panel (`business_settings.favicon_url`) convivía con el estático y el navegador elegía uno de los dos de forma no confiable. Sacarlo de la convención especial de Next.js y dejarlo como un estático más en `public/` resuelve esto: `generateMetadata` en `(storefront)/layout.tsx` pasa a ser la única fuente del ícono, con `favicon_url` si existe o `/favicon-default.ico` (el mismo archivo de siempre) si no. `admin/layout.tsx` referencia ese mismo archivo de forma estática en su propio `metadata.icons` -- el panel no necesita (ni debe) reflejar el branding de cada negocio.
- **Migración de la tienda pública fuera de `config/business.ts`/`config/footer.ts`/`config/site.ts` (parcial, documentada)** (Fase 14): `Header`, `Footer`, `Hero` (solo el link de WhatsApp) y `/contacto` pasaron a recibir `settings: BusinessSettings` por prop en vez de importar esos tres archivos directamente, para nombre/eslogan/descripción/WhatsApp/contacto/redes/ubicación. Tres cosas quedaron **deliberadamente afuera**, documentadas explícitamente en vez de romper la regla "NO modificar Checkout/Carrito/WhatsApp" de este sprint: (1) `formatPrice()` (`lib/utils.ts`) sigue leyendo `businessConfig.currency`/`locale` -- cambiar su firma habría obligado a tocar cada call site, incluidos `CartItem.tsx`/`CheckoutSummary.tsx`/`OrderSummary.tsx`; (2) `lib/whatsapp.ts` (mensaje del pedido) sigue leyendo `businessConfig.whatsapp.number` -- es exactamente "el envío por WhatsApp" que el sprint pidió no tocar; (3) el centro del mapa de Delivery en `ShippingInformation`/`LocationPicker` sigue leyendo `businessConfig.map.defaultCenter/defaultZoom` -- tocarlo requería editar la ruta `/checkout` y sus componentes de formulario, ambos protegidos. Consecuencia real y visible: cambiar el WhatsApp en `/admin/configuracion` actualiza todos los botones "Contactar por WhatsApp" de la tienda, pero el mensaje final de confirmación de pedido sigue yendo al número viejo hasta que un sprint futuro autorice tocar `lib/whatsapp.ts`. `footerConfig.legalLinks`/`developedBy` tampoco se migraron -- no están entre las 5 secciones que pidió el sprint.
- **`getGoogleMapsUrl()` con coordenadas opcionales** (Fase 14): `lib/utils.ts` ganó un segundo parámetro opcional (`coords?: {lat, lng}`) para que `/contacto` pueda armar un link de Google Maps más preciso con `business_settings.map_default_lat/lng` en vez de solo una búsqueda por texto de la dirección. Cambio aditivo y con valor por defecto (`address` sigue funcionando sola si no hay coords) -- no rompe el único otro comportamiento que ya tenía.
- **Función de Postgres `create_order`, no policies de insert para `anon`** (Fase 15): `customers`/`orders`/`order_items` siguen con RLS 100% cerrada al público (Fase 8, sin ningún cambio) -- en vez de agregar policies de "insert" para `anon` en las tres tablas (lo que dejaría insertar directamente cualquier cosa: un total inventado, un `status` distinto de `'pending'`, un `orders` sin sus `order_items`), se expone una única función `security definer` como el único punto de entrada público para crear un pedido. Corre con privilegios propios (no los de `anon`), así que puede escribir en las tres tablas sin policies nuevas, pero el público solo puede hacer exactamente lo que la función deja hacer. Vive en `supabase/migrations/..._create_order_function.sql` -- no aparece en el árbol de `src/` porque es SQL, no código de aplicación, pero es la pieza central de este sprint.
- **Atomicidad real, no "borrar a mano" si algo falla**: una función de Postgres se ejecuta dentro de una única transacción implícita -- si el `insert` de `order_items` falla (por ejemplo, `quantity <= 0`, que la tabla ya rechaza con un `CHECK`), Postgres revierte automáticamente **todo** lo que la función ya había insertado en esa misma llamada, incluido el `customers`/`orders` que se hubiera creado un instante antes. Verificado en vivo contra el proyecto real: un intento con una cantidad inválida no dejó ningún pedido ni ningún cliente nuevo en la base (sección 9, Fase 15, para el detalle de la prueba). Esta es la garantía real que pedía el sprint ("utilizar transacciones o la mejor estrategia disponible") -- no hizo falta ninguna lógica de rollback manual en JavaScript.
- **`services/storefront/orders.ts` usa el cliente de *browser*, no el de servidor** (Fase 15): única excepción al patrón que el resto de `services/storefront/` viene siguiendo desde la Fase 11 (siempre cliente de servidor, porque leen durante el render de una página). Acá se escribe, no se lee, y se dispara desde un click en un Client Component ya hidratado (`OrderConfirmationDetails`, dentro de Checkout) -- exactamente el mismo motivo por el que `services/products.ts`/`categories.ts`/`settings.ts` (panel) usan el cliente de browser. La regla real es "¿esto pasa durante el render de una página, o en respuesta a una interacción del usuario ya en el cliente?", no "¿es storefront o panel?".
- **`upsert` de `customers` por teléfono, no un `insert` nuevo por cada pedido**: `customers.phone` tiene un índice único desde la Fase 8, pensado exactamente para esto (`docs/database-architecture.md`, sección 4.3: "se referencia por upsert de phone al crear un pedido"). `create_order` hace `on conflict (phone) do update` -- un cliente que ya compró antes se reconoce por número, no se duplica en la tabla `customers` en cada pedido nuevo.
- **`lib/whatsapp.ts` no se tocó** (Fase 15): el sprint pidió explícitamente "no cambiar el formato del mensaje". `buildOrderWhatsAppMessage()` (el texto) y `getOrderWhatsAppUrl()` (la URL de wa.me) se siguen llamando exactamente igual desde `OrderConfirmationDetails.tsx` -- lo único nuevo es que, antes de abrir esa URL, se llama a `createOrder()` y el texto ya armado se guarda tal cual en `orders.whatsapp_message` (auditoría de qué se mandó realmente).
- **Pestaña de WhatsApp pre-abierta en blanco, redirigida después del guardado** (Fase 15): abrir una pestaña nueva con `window.open()` *después* de un `await` es un patrón que varios navegadores tratan como popup no solicitado y bloquean, porque ya no ocurre de forma síncrona dentro del gesto de click. La solución estándar (y la que usa `OrderConfirmationDetails.tsx`) es abrir la pestaña en blanco de forma síncrona, en el mismo click, y recién asignarle la URL real (`tab.location.href = whatsappUrl`) una vez que `createOrder()` termina -- preserva el comportamiento de siempre (WhatsApp se abre en una pestaña nueva, la confirmación queda visible detrás) sin depender de que el navegador sea permisivo.
- **Si falla el guardado, no se bloquea la compra**: si `createOrder()` lanza un error, `OrderConfirmationDetails` no abre WhatsApp automáticamente, pero tampoco deja al cliente sin salida -- muestra el error y un link explícito para "continuar de todas formas por WhatsApp, sin guardar el pedido". Decisión deliberada: "no romper el flujo existente" (instrucción del sprint) se interpretó como que una falla transitoria de Supabase nunca debe poder impedir que un cliente complete su compra por WhatsApp, aunque eso signifique que ese pedido puntual quede sin registrar en el panel.
- **Módulo Clientes 100% de solo lectura, sin migración nueva** (Fase 16): a diferencia de Productos/Categorías/Configuración/Pedidos, este módulo no agrega ni una columna ni una policy -- `customers` ya existía completa desde la Fase 8, y ya se puebla sola desde la Fase 15 (`create_order` hace upsert por teléfono). El sprint fue enteramente de aplicación: leer lo que ya está, no crear nada nuevo en Supabase.
- **Estadísticas calculadas en memoria, no en una vista de Postgres** (Fase 16): `total gastado`/`cantidad de compras`/`ticket promedio`/`primera y última compra` se computan en `services/customers.ts` a partir de los pedidos ya traídos, nunca se guardan (instrucción explícita del sprint). Se evaluó una vista SQL (`create view customer_stats as select ... sum(total) ... group by customer_id`) para hacerlo más escalable, pero se descartó: agregaría una migración nueva, una consideración de seguridad (`security_invoker` para que la vista respete RLS en vez de correr con los permisos del dueño) y un artefacto de esquema más para mantener, a cambio de una ganancia que esta tienda ("una tienda chica", CLAUDE.md sección 1) no necesita todavía -- mismo criterio de "no sobre-ingeniería para una escala que no existe" que ya aplican `useProducts`/`useCategories`/`useOrders` calculando búsqueda/orden en memoria. Si el volumen de pedidos crece lo suficiente como para que traer todos los pedidos de un cliente (o de todos los clientes, para el listado) deje de ser trivial, una vista o función agregada de Postgres es el paso natural siguiente -- documentado como pendiente (sección 13), no resuelto preventivamente.
- **Listado de clientes: `orders(total, created_at)` embebido, no una segunda consulta por fila**: `listCustomers()` trae, en una sola query, cada cliente con el array completo de sus pedidos (solo las dos columnas que hacen falta para las estadísticas de la tabla) vía el embed de PostgREST -- ni N+1 consultas ni una vista. Para el detalle de un cliente (`getCustomerById`), sí son dos consultas separadas (cliente, después sus pedidos completos) porque el detalle necesita más columnas de cada pedido (para el historial) que las que tiene sentido embeber en la consulta del listado.
- **Navegación Cliente → Pedido implementada; Pedido → Cliente, no** (Fase 16): cada fila del historial de un cliente linkea a `/admin/pedidos/[id]` (esto vive enteramente en el módulo nuevo de Clientes, no toca nada de Pedidos). La dirección inversa -- un link desde `OrderDetailView.tsx` de vuelta al cliente -- habría requerido modificar ese componente y `services/orders.ts` (agregar `customer_id` a lo que se trae y expone), ambos parte de "Pedidos", explícitamente prohibido este sprint. Se documenta como pendiente (sección 13) en vez de tocarlo en silencio.
- **`primaryAddress` derivada, no una columna de `customers`**: `customers` nunca guardó una dirección propia (decisión ya aprobada desde la Fase 8 -- la dirección es un dato del *pedido*, no del *cliente*, porque un mismo cliente puede pedir a direcciones distintas). "Dirección principal" en el detalle se deriva del pedido de Delivery más reciente de ese cliente (buscado sobre el array ya ordenado por fecha descendente); si el cliente solo hizo Retiro en tienda, queda en null y se muestra un texto explicando por qué.
- **`services/dashboard.ts` agrega datos de 5 tablas sin tocar ningún otro `services/*.ts`** (Fase 17): el Dashboard necesita cifras de `orders`, `order_items`, `customers`, `products` y `categories` a la vez, pero "NO modificar Productos/Categorías/Pedidos/Clientes" prohibía usar (o para colmo, extender) `services/products.ts`/`categories.ts`/`orders.ts`/`customers.ts` para esto. La solución fue un archivo nuevo, `services/dashboard.ts`, que consulta esas mismas tablas de forma completamente independiente (mismo criterio ya sentado en la Fase 16 para `services/customers.ts` leyendo `orders` sin pasar por `services/orders.ts`) -- reutiliza únicamente `OrderStatus`/`ORDER_STATUSES` de `services/orders.ts` vía `import` de solo lectura, sin tocar ese archivo.
- **Una sola consulta a `orders` alimenta tres secciones a la vez**: `getOrderAggregates()` trae `status`/`total`/`created_at` de **todos** los pedidos en un único `select`, y ese mismo array se reduce en memoria para el resumen (total/pendientes/entregados), Ventas (total vendido/ticket promedio/última venta) y el desglose por estado -- evita repetir la misma consulta tres veces. "Ventas totales" y "ticket promedio" suman **todos** los pedidos sin importar su estado, incluidos los cancelados: mismo criterio ya aplicado a "total gastado" en `services/customers.ts` (Fase 16), por consistencia en todo el panel -- ningún sprint pidió excluir un estado de las cifras de venta.
- **Productos más vendidos: agrupado en memoria sobre `order_items`, no una vista de Postgres** (Fase 17): mismo criterio ya evaluado y descartado en la Fase 16 para las estadísticas de clientes -- el volumen de `order_items` de una tienda chica no justifica todavía una vista o función agregada. Se agrupa por `product_id` cuando existe (más preciso que por nombre) y se cae a `product_name` si el producto ya fue borrado del catálogo (`product_id` null, `on delete set null`, Fase 8).
- **"Preparar la estructura para un gráfico futuro" sin implementar un gráfico** (Fase 17, Estados de pedidos): `getDashboardData()` devuelve un array `{ status, label, count }` con los 6 estados siempre presentes (incluso en 0), exactamente la forma que necesitaría un gráfico de barras/torta el día de mañana. `OrderStatusBreakdown.tsx` lo renderiza hoy como una lista de barras con **CSS puro** (ancho proporcional al máximo) -- sin agregar ninguna librería de gráficos, tal como pidió explícitamente el sprint ("no implementar gráficos todavía").
- **"Variación futura preparada, sin implementar lógica todavía"** (Fase 17, tarjetas de resumen): `SummaryCard.tsx` acepta un prop opcional `trend` (flecha + texto tipo "+12%") que hoy nadie le pasa -- ninguna de las 9 tarjetas del Dashboard (6 de resumen + 3 de ventas) calcula ninguna variación real todavía. La tarjeta ya sabe **mostrar** una variación si algún día se le calcula una, pero no se inventó ningún número para no violar "no hardcodear métricas".
- **(Fase 18) Una promoción no recalculaba el precio mostrado de un producto/categoría** -- decisión heredada del diseño original: `docs/database-architecture.md` (sección 4.7) ya dejaba explícito que "`promotions` es la única tabla sin equivalente 1:1 en el código... la forma en que una promoción activa se refleja en el storefront (actualizando `products.on_sale`/`old_price`, vía un job o al guardar) es una decisión de implementación fuera de [ese] sprint". El Sprint 5.9 tampoco la resolvió -- construía la gestión de campañas (CRUD + banner), no la conexión "promoción activa → precio de catálogo recalculado", que seguiría requiriendo tocar `products`/`ProductCard`/`ProductBuyBox` (Productos, explícitamente protegido en ese sprint). **Esta decisión quedó explícitamente revertida en el Sprint 6.1** (ver más abajo): ese sprint autorizó y pidió exactamente esta conexión -- ya no es una limitación vigente, ver la entrada de Fase 21 para el detalle de cómo se resolvió.
- **`lib/promotions.ts`, la primera lógica compartida entre panel y tienda pública en un mismo archivo**: `formatDiscountLabel()` arma el texto del descuento ("-15%"/"-Gs. 50.000") para la columna "Descuento" de `PromotionsTable` (admin) Y para el badge del banner público -- misma función pura, dos consumidores en capas distintas. `getPromotionStatus()` (Activa/Programada/Vencida/Inactiva) solo la usa el panel: la tienda pública nunca necesita calcularlo, porque RLS (`promotions_public_read_active`) ya decide qué fila es visible antes de que el código de la aplicación la vea.
- **RLS, no código de aplicación, es quien hace que "las promociones vencidas desaparezcan"**: `promotions_public_read_active` (Fase 8, sin cambios) filtra `is_active = true and now() between starts_at and ends_at` directamente en Postgres. `getActivePromotions()` (`services/storefront/promotions.ts`, plural desde el Sprint 6.1 -- ver Fase 21) repite el mismo filtro explícitamente por auditoría (mismo criterio que el bug 4 del Sprint 6.0.1), pero de todos modos no podría recibir una fila vencida o programada, la misma garantía ya verificada para productos/categorías inactivos desde la Fase 11.
- **`usePromotionTargets.ts`, no reutilizar `useProducts()`/`useCategories()` para los combos del formulario**: `useProducts()` (Fase 10) pagina de a 10 -- reutilizarlo tal cual para el selector de producto de una promoción dejaría fuera del combo cualquier producto más allá de la primera página. `useCategories()` sí devuelve la lista completa (sin paginación real, Fase 13), pero para mantener el mismo criterio en ambos combos se resolvieron los dos en un hook nuevo, propio del módulo de Promociones, que llama directamente a `listProducts()`/`listCategories()` (los servicios, no los hooks) -- import de solo lectura, sin tocar Productos ni Categorías.
- **`config/promotion.ts` sin consumidores activos, no eliminado** (Fase 18): mismo trato que `config/products.ts`/`config/categories.ts` desde la Fase 11 -- el archivo sigue existiendo tal cual, como fallback/documentación, aunque `PromotionalBanner.tsx` ya no lo importa.
- **`register_stock_movement`, una función de Postgres para las escrituras manuales de stock, sin `security definer`** (Fase 19): a diferencia de `create_order` (Fase 15, invocada por `anon`, sí `security definer`), acá quien invoca siempre es un admin ya autenticado que ya tiene permiso de RLS para actualizar `products` e insertar en `stock_movements` -- no hace falta bypasear RLS, solo garantizar que el `update` del stock y el `insert` del movimiento pasen juntos en una única transacción, y que el stock nunca pueda quedar negativo. El chequeo explícito de `is_admin()` de adentro de la función es solo para dar un mensaje de error más claro que el genérico de RLS -- la barrera real la sigue poniendo RLS. "Nunca permitir stock negativo" es acá un `raise exception` (rechaza el ajuste), no un recorte silencioso: el admin cargó la cantidad a mano y merece un error explícito.
- **`apply_order_stock_movement`, un trigger en `orders` -- no un cambio en `services/orders.ts`** (Fase 19, la pieza central del sprint): el sprint pedía que confirmar/preparar un pedido descuente stock y que cancelarlo lo restituya, pero "NO modificar Pedidos" prohibía tocar `services/orders.ts`/`updateOrderStatus()`. La solución, siguiendo el mismo espíritu que `create_order` (Fase 15) -- lógica de integridad en la base de datos, no en la aplicación --, fue un trigger `before update on orders` que se engancha al mismo `update orders set status = ...` que ese archivo ya hacía sin cambiar una sola línea. `orders.stock_adjusted` (columna nueva) evita descontar dos veces si el pedido pasa por `confirmed -> preparing -> ready_or_shipped -> delivered`, y solo restituye si `cancelled` llega habiendo descontado antes. No es `security definer`: corre con los mismos permisos del admin autenticado que ya cambia el estado desde `/admin/pedidos`, exactamente igual que `register_stock_movement`.
- **Asimetría deliberada entre el ajuste manual (rechaza) y la deducción automática (recorta a 0)**: un ajuste manual que dejaría el stock negativo se rechaza con un error explícito -- el admin lo cargó a mano y puede corregirlo. Una confirmación automática de pedido que pida más stock del disponible (dato inconsistente, ajuste manual de por medio, etc.) se recorta a 0 con `greatest(stock - cantidad, 0)` en vez de fallar -- bloquear la confirmación de un pedido por un desajuste de stock habría alterado el flujo de Pedidos, explícitamente protegido este sprint. Verificado en vivo: un pedido de 10 unidades de un producto con 3 en stock confirmó sin error y dejó el stock en 0, con el movimiento registrando el delta real aplicado (-3), no la cantidad pedida.
- **`sku`/`low_stock_threshold` viven en `products`, pero se editan únicamente desde Inventario** (Fase 19): ambas columnas se agregaron a `products` porque no hay otro lugar donde guardarlas, pero `ProductForm.tsx`/`services/products.ts` (Productos, protegido este sprint) no ganaron ningún campo nuevo -- `services/inventory.ts` es quien las lee y las escribe, con su propio `update` a la tabla `products` (mismo criterio ya sentado en la Fase 16 para `services/customers.ts` leyendo `orders` sin pasar por `services/orders.ts`, y en la Fase 18 para `usePromotionTargets` llamando a `listProducts()` directamente).
- **`useInventoryItem.ts`, un bug real encontrado y corregido durante la verificación de este mismo sprint**: la primera versión de `submitMovement`/`saveSettings` volvía a llamar a `fetchAll()` después de cada escritura exitosa -- pero `fetchAll` marca `isLoading = true` al empezar, y `InventoryDetailView` reemplaza toda la vista (incluido `StockMovementForm`) mientras `isLoading` es `true`. El resultado: cada movimiento registrado con éxito desmontaba y volvía a montar el formulario, reseteando el tipo de movimiento seleccionado al default ("Entrada de stock") sin ningún aviso -- un admin que encadenara dos ajustes (por ejemplo, dos "Ajuste negativo" seguidos) podía terminar registrando el segundo como una entrada positiva sin darse cuenta. Corregido actualizando el estado local de forma optimista con el resultado que ya devuelve la escritura (`register_stock_movement` devuelve el movimiento creado; `updateInventorySettings` no necesita volver a leer nada, ya se conocen los valores nuevos) -- mismo criterio que `useOrder.ts`/`useOrders.ts` (CLAUDE.md, sección 11), sin volver a pedir nada a Supabase tras un cambio de estado exitoso.
- **`ProductBuyBox.tsx`, excepción autorizada por el propio sprint** (Fase 19): el botón "Comprar por WhatsApp" no se deshabilitaba cuando el producto no tenía stock -- solo "Agregar al carrito" lo hacía, ya resuelto en un sprint anterior. Como la sección "TIENDA PÚBLICA" de este sprint pedía explícitamente deshabilitar ambos botones sin ocultar el producto, se ajustó únicamente esa condición: sin stock, el botón se renderiza como un `<button disabled>` nativo (mismas clases `disabled:opacity-50` que ya usa "Agregar al carrito") en vez de `render={<a href={whatsappHref} .../>}` -- con stock, es exactamente el mismo link de siempre. Ningún otro componente de Carrito/Checkout se tocó.
- **`ProductCard.tsx` gana un `<Link>` real, la causa exacta del bug 1** (Sprint 6.0.1): desde que existe, `ProductCard` nunca envolvió su contenido en un `Link` a `/productos/[slug]` -- solo el botón "Agregar al carrito" tenía interactividad (`onClick`), y clickear la imagen, el nombre o la descripción no hacía nada. `CategoryCard.tsx` (Sprint 4.4) sí lo tenía desde siempre (`<Link href={.../categorias/${slug}}>` envolviendo todo el contenido); `ProductCard` simplemente nunca replicó ese patrón. Corregido envolviendo la imagen + nombre + descripción + precio + stock en un `Link`, con el botón "Agregar al carrito" como hermano fuera de él (un `<button>` anidado dentro de un `<a>` es HTML inválido y capturaría el click de navegación) -- mismo criterio exacto que `CategoryCard`, aplicado por primera vez acá.
- **El banner de promoción de producto reutiliza `product_images`, sin campo ni tabla nueva** (bug 2, Sprint 6.0.1): `services/storefront/promotions.ts` amplía su `select` para embeber `product_images(url, display_order)` bajo `products` cuando la promoción es de un producto específico, y resuelve la misma imagen principal que ya usaría la ficha de ese producto (`isRealImageUrl` + menor `display_order`, `lib/products.ts`). Promociones de categoría siguen sin imagen (el placeholder decorativo de siempre) -- las categorías no tienen `product_images`, y agregarles una columna de imagen propia no se pidió.
- **`services/storefront/business-client.ts`, un archivo nuevo y no una segunda función en `business.ts` -- lección real de este sprint** (bug 3): la primera versión del fix agregó `getPublicBusinessSettingsClient()` directamente en `services/storefront/business.ts`, que ya importaba `lib/supabase/server.ts` (y, a través de él, `next/headers`, una Dynamic API exclusiva de Server Components). En cuanto `OrderConfirmationDetails.tsx` (Client Component) importó algo de ese archivo, Next.js intentó incluir `next/headers` en el bundle del navegador y el build se rompió ("You're importing a component that needs 'next/headers'..."). La lección, generalizable: **un archivo que importa `lib/supabase/server.ts` no puede tener también una función pensada para ser importada desde un Client Component, aunque esa función en particular use el cliente de browser** -- el análisis estático de imports de Next.js no distingue "qué función se usa", solo "qué archivos están en la cadena". La solución es un archivo hermano, sin ningún import de `lib/supabase/server.ts`, aunque eso signifique duplicar el tipo/mapeo de fila (mínimo, ~30 líneas) en vez de compartirlo.
- **`/checkout/confirmacion` (`OrderConfirmationDetails.tsx`) resuelve `business_settings` con el cliente de browser, no el de servidor** (bug 3): esa página es "use client" de punta a punta porque depende de `useCartStore`/`useCheckoutStore` (estado que solo existe en el navegador) -- no hay forma de resolverla como Server Component sin restructurar toda la página, fuera de alcance de un sprint de estabilización ("NO modificar la arquitectura"). Se lee `business_settings` con `getPublicBusinessSettingsClient()` en un `useEffect` al montar, y el botón "Confirmar pedido" queda deshabilitado hasta que esa lectura resuelve -- mismo criterio que `services/storefront/orders.ts` (Fase 15) usando el cliente de browser cuando no hay otra opción real.
- **`.eq("is_active", true)` explícito en `services/storefront/*`, además de RLS -- la causa exacta del bug 4**: `categories_public_read_active`/`products_public_read_active`/`promotions_public_read_active` (RLS) filtran correctamente para el rol `anon`, pero conviven con policies `..._admin_read_all`/`..._admin_update` para `authenticated` (`using (is_admin())`) que se combinan con OR. Si quien navega la tienda pública tiene, en el mismo navegador, una sesión de admin activa (probar el panel y la tienda en dos pestañas es exactamente el escenario que reportó este bug), la request corre como `authenticated` y ve filas inactivas/vencidas también -- RLS decide por rol, no por ruta. Se agregó el mismo filtro explícito en `getPublicCategories()`/`getPublicCategoryBySlug()` (categorías, lo que pedía el bug), y por el mismo motivo también en `getPublicProducts()`/`getPublicProductBySlug()` (productos) y `getActivePromotion()` (promociones -- función que el Sprint 6.1 renombró a `getActivePromotions()`, plural, ver Fase 21; el filtro explícito de fechas/`is_active` se conservó igual, con `starts_at`/`ends_at` además de `is_active`) -- mismo patrón, mismo riesgo, verificado y corregido de una vez en auditoría en vez de dejarlo latente en dos tablas más.
- **Validación de tamaño en `BrandingAssetField`/`SettingsForm`, no en `services/settings.ts` -- la causa exacta del bug 5**: el bucket `branding` tiene `file_size_limit = 2097152` (2 MB, Fase 14) a nivel de Supabase Storage, pero nada en el cliente lo validaba antes de intentar subir -- un logo/favicon más grande recién se enteraba al hacer clic en "Guardar cambios", cuando Storage lo rechazaba con `413 Payload too large` (`"The object exceeded the maximum allowed size"`, el mensaje textual que veía el usuario). Se agregó `MAX_BRANDING_FILE_SIZE_BYTES`/`_MB` (`services/settings.ts`, mismo valor que la migración, documentado como duplicado a propósito) y una validación en `onSelectFile` (`SettingsForm.tsx`) que rechaza el archivo *antes* de guardarlo como `pending` -- nunca llega a intentar la subida. El `File` en sí nunca se guardó en la base (`business_settings.favicon_url`/`logo_url` son siempre `text`, la URL después de subir) -- eso ya era correcto, confirmado con `pg_typeof()` contra el proyecto real.
- **Gráficos del Dashboard sobre datos ya existentes, sin ninguna consulta nueva** (bug 6): `SalesChart.tsx` (área) usa `recentOrders` (los mismos "últimos 10 pedidos" que ya alimentaba `RecentOrdersTable`, Fase 17) ordenados cronológicamente -- no es una serie histórica agregada por día (esa consulta no existe y este sprint no la agrega, "no modificar métricas, no modificar consultas"), y el título de la sección lo deja explícito ("Ventas de los últimos N pedidos") para no sugerir más precisión de la que hay. `OrderStatusBreakdown.tsx` pasó de barras de CSS puro a un `BarChart` de Recharts sobre el mismo `statusBreakdown` de siempre, con los colores traducidos 1:1 desde `getOrderStatusVariant()` (mismo import de solo lectura de `components/admin/orders/order-status.ts`, sin tocar Pedidos). Las animaciones de entrada (`Section`, tarjetas de resumen) usan `tw-animate-css` (ya instalado, usado por `Sheet`/`Dialog`) -- nunca Framer Motion, que el panel administrativo no usa a propósito (CLAUDE.md sección 4).
- **`isRealImageUrl` extraída a `lib/products.ts`, primer cruce del umbral de "3 o más lugares"**: hasta este sprint vivía duplicada, a propósito, en `ProductCard.tsx`/`ProductGallery.tsx` (Fase 12, con solo dos consumidores). El banner de promociones (bug 2) la necesitaba también en `services/storefront/promotions.ts` -- un tercer consumidor real, exactamente el umbral que el proyecto usa para decidir cuándo compartir código (CLAUDE.md, principio 6). Se comparte para el código nuevo sin tocar los dos duplicados ya aprobados en los componentes.
- **Auditoría: miniaturas rotas en `/admin/productos` y `ProductImagesField.tsx`, un bug no reportado pero encontrado en vivo**: la corrección de la Fase 12 (mostrar la foto real, con fallback al placeholder si la URL no es real) solo se había aplicado en la tienda pública (`ProductCard`/`ProductGallery`) -- el panel (`ProductsTable.tsx`, `ProductImagesField.tsx`) seguía renderizando `product.images[0]?.url`/`image.url` sin ninguna guarda, pidiendo directamente las 5 rutas de placeholder del seed original (`/products/iphone-14-1.jpg`, etc.) y rompiendo con un 404 silencioso. Detectado durante el barrido de regresión final (`page.on("response")` capturando 404s reales), no durante el desarrollo de ningún bug numerado -- corregido con la misma guarda `isRealImageUrl` ya compartida.
- **Auditoría: nombre de categoría corrompido en el proyecto real** ("Tecnología parece" en vez de "Tecnología"): detectado visualmente en una captura de `/admin/productos` durante la verificación del punto anterior -- no es un bug de código, es un dato mal guardado en algún momento anterior a este sprint (probablemente un guardado accidental a mitad de tipeo en `CategoryForm`). Corregido directamente en la base (`update categories set name = 'Tecnología' where slug = 'tecnologia'`) por ser una corrección de datos evidente y de bajo riesgo, no un cambio de esquema ni de código.
- **`getActivePromotion()` (singular) reemplazada por `getActivePromotions()` (plural)** (Fase 21, Sprint 6.1): tanto el banner rotativo como el motor de precios necesitan el conjunto **completo** de promociones vigentes, no solo la más reciente -- una sola consulta nueva (`services/storefront/promotions.ts`), reutilizada por ambos desde cada página que la llama, en vez de repetirla. `PublicPromotion` se extendió de forma aditiva con los campos crudos que necesita el cálculo de precio (`discountType`, `discountValue`, `productId`, `categorySlug`) -- los campos que ya tenía (`discountLabel`, `targetLabel`, `targetHref`, `imageUrl`) no cambiaron, así que el banner no necesitó ningún ajuste en cómo lee sus propios datos.
- **`calculatePromotion()`/`getProductDisplayPrice()` (`lib/promotions.ts`), el motor de precios único** (Fase 21): dos funciones puras, sin JSX, que el sprint pidió explícitamente como "una única función reutilizable" para que ningún componente calcule un descuento por su cuenta (principio 2, sección 6). `calculatePromotion(product, promotions)` decide qué promoción aplica (prioridad fija: producto puntual primero, categoría después, nunca las dos) y devuelve precio original/final/tipo/badge -- exactamente la forma que pedía el sprint. `getProductDisplayPrice()` es una segunda capa sobre esa, pensada para consumo directo de componentes: combina el resultado de `calculatePromotion()` con el comportamiento legacy ya aprobado (`product.oldPrice`/`onSale`/`badge`, Fases 2-3) cuando ninguna promoción de Supabase aplica, así `ProductCard`/`ProductBuyBox` siguen mostrando exactamente lo mismo que mostraban antes de este sprint para cualquier producto sin promoción vigente. Ambas funciones reciben un tipo mínimo (`PricingPromotion`) definido en el propio `lib/promotions.ts`, no el `PublicPromotion` de `services/storefront/promotions.ts` -- importarlo hubiera creado una dependencia circular (`services` ya importa `DiscountType`/`formatDiscountLabel` de `lib/promotions.ts`), y `PublicPromotion` ya cumple esa forma estructuralmente, sin necesitar ningún cast.
- **Prioridad producto > categoría, nunca acumular, resuelta con dos `.find()` encadenados**: `calculatePromotion()` busca primero una promoción cuyo `productId` coincida con el producto puntual; solo si no la encuentra, busca una cuya `categorySlug` coincida con la categoría del producto; aplica como máximo una. Verificado en vivo con el proyecto real: un producto (iPhone 14) con una promoción propia (15%) y perteneciente a una categoría (Tecnología) con su propia promoción (10%) mostró siempre el 15% -- nunca 10%, nunca ambos acumulados (lo que hubiera dado un precio distinto y verificablemente incorrecto).
- **`PromotionalBanner.tsx` se convierte en carrusel solo cuando hace falta, mismo componente, mismo comportamiento con 1 sola promoción** (Fase 21): la pieza compartida (`PromotionContent`) es la única que sabe dibujar una promoción -- ni el caso de una sola promoción (con el mismo stagger de entrada de Framer Motion que ya tenía desde el Sprint 5.9) ni cada slide del carrusel (`PromotionCarousel`, transición horizontal con `AnimatePresence`, flechas, dots, pausa en hover, loop infinito) duplican ese JSX. El temporizador de auto-avance (`useEffect` con `setInterval`) depende de `index`, así que se reinicia solo con cualquier cambio de slide -- auto-avance o navegación manual -- sin ningún código aparte para "reiniciar el temporizador".
- **`PromotionalBanner.tsx` gana el mismo `mounted`-gate que `Header.tsx` para `useReducedMotion()`** (Fase 21): al reescribir sustancialmente este archivo para el carrusel, se aplicó el mismo patrón ya documentado (sección 6/11) para evitar el mismatch de hidratación que produce `useReducedMotion()` leyendo `window.matchMedia` de forma sincrónica en el primer render del cliente -- este archivo ya tenía el mismo riesgo latente desde el Sprint 5.9 (nunca reportado como bug), y se corrigió de paso por tocar exactamente esas líneas como parte del trabajo central de este sprint, no como una mejora fuera de alcance. `Hero.tsx`, `FeaturedCategories.tsx`, `FeaturedProducts.tsx`, `FeaturedBenefits.tsx`, `TestimonialsSection.tsx` siguen con el mismo patrón sin corregir -- fuera de alcance de este sprint, documentado como pendiente (sección 13).
- **Precio promocional mostrado en todos los lugares donde ya se mostraba un precio, con una única excepción documentada**: `ProductCard`, `ProductBuyBox`, `ProductGrid`, `FeaturedProducts`, `RelatedProducts`, `CatalogResults` y las 4 páginas que resuelven productos (Home, Catálogo, Producto, Categoría) reciben `promotions: PublicPromotion[]` por prop, resuelta por la página vía `getActivePromotions()` -- mismo patrón de "resuelto por la página, recibido por prop" que ya usan `categories`/`settings` desde las Fases 13/14. `ProductGallery.tsx` **no** se tocó: es un componente 100% de imágenes (visor + miniaturas), nunca mostró un precio -- forzarle uno hubiera duplicado la responsabilidad que ya tiene `ProductBuyBox`, su hermano en la misma página. `CartItem.tsx`/`CheckoutSummary.tsx` (Carrito/Checkout) tampoco se tocaron, por la restricción explícita del sprint ("NO modificar Checkout/Carrito") -- siguen mostrando el precio ya cargado en el carrito al momento de agregar el producto, sin recalcular contra una promoción que pudiera activarse/desactivarse después. Documentado como pendiente (sección 13).
- **`getDiscountPercent()` (`lib/products.ts`) eliminada, no dejada como código muerto** (Fase 21): su único consumidor (`ProductBuyBox.tsx`) pasó a usar `getProductDisplayPrice()` -- confirmado por grep que no quedó ningún otro import antes de borrarla, mismo criterio que `getCategoryBySlug` (Fase 13) o `AdminPlaceholder.tsx` (Sprint 6.0.1).
- **`departments`/`cities`, catálogo geográfico de solo lectura desde toda la aplicación** (Fase 22): a diferencia de cualquier otra tabla de contenido del proyecto (`categories`, `promotions`, `benefits`...), ninguna de las dos tiene policy de `insert`/`update`/`delete` -- ni siquiera para `is_admin()`. Es una decisión deliberada, no un olvido: el sprint pidió explícitamente "nunca deberán escribirse manualmente desde el Panel", así que la única forma de modificarlas es `supabase/seed_logistics.sql` (o una migración de datos futura). Normalizadas por FK (`cities.department_id`), nunca repitiendo el nombre del departamento en cada fila.
- **`shipping_rates` sin columna de departamento, a propósito** (Fase 22): el formulario del panel pide elegir un departamento antes de tildar ciudades (así "una tarifa cubre ciudades de un único departamento" es la experiencia normal), pero esa restricción vive enteramente en `DepartmentCityPicker.tsx` -- la tabla en sí no tiene ninguna columna que la fuerce. `shipping_rate_cities` es una tabla puente muchos-a-muchos sin ninguna suposición sobre "un departamento por tarifa", así que una tarifa que cubra ciudades de varios departamentos (o cualquier otra regla de cobertura futura) no requeriría ninguna migración, solo un cambio de UI -- la base ya está "preparada para crecer" como pedía el sprint.
- **`orders.department`/`city`/`shipping_cost` ya existían desde la Fase 8, sin usarse -- Sprint 6.2 los puso a trabajar, no los creó**: el diseño original (`docs/database-architecture.md`) ya incluía estas tres columnas (`department`/`city` como texto libre, pensadas para lo que hoy escribía `ShippingInformation`; `shipping_cost` nullable, ya documentado en el Sprint 5.6 como "listo para cuando el cálculo exista"). Este sprint no agregó ninguna columna nueva para esos tres campos -- solo `orders.shipping_rate_id` (FK a `shipping_rates`, `on delete set null`) y `orders.shipping_rate_name` (snapshot de texto) son columnas realmente nuevas, con el mismo criterio dual ya establecido por `order_items.product_id`/`product_name` (Fase 8): la FK es trazabilidad opcional, el texto es lo que el panel muestra sin necesitar ningún join.
- **`create_order()` extendida con parámetros nuevos al final, con default `null` -- y por qué `create or replace` no alcanzó** (Fase 22): agregar `p_shipping_cost`/`p_shipping_rate_id`/`p_shipping_rate_name` al final de la firma con default `null` hizo que Postgres tratara la función como un *segundo overload* en vez de reemplazar el original (mismos nombres, firma distinta), dejando `public.create_order` ambiguo apenas se intentó comentar la función sin especificar tipos. Se resolvió con un `drop function if exists` explícito (firma vieja exacta) antes del `create or replace` -- lección generalizable: agregar parámetros a una función de Postgres ya existente, aunque sea al final y con default, no es un cambio "seguro" por sí solo si algo más adelante en la misma migración referencia la función por nombre sin tipos (un `comment on function`, un segundo `grant`, etc.).
- **`ShippingCitySelect.tsx`, mismo patrón que `LocationPicker`/`LeafletMap` con `useCheckoutStore`** (Fase 22): `department`/`city` (los nombres) se siguen escribiendo vía el `onChange` genérico de `CheckoutFieldChange`, igual que cualquier otro campo -- pero el resultado de buscar la tarifa (costo, tiempo estimado, id de la tarifa) no encaja en ese contrato, así que se escribe directo a `useCheckoutStore` (`setShippingResult`/`resetShipping`), mismo criterio ya establecido por `LocationPicker` con `latitude`/`longitude` desde el Sprint 3.7.
- **`services/storefront/logistics.ts` usa el cliente de *browser* en los tres casos, no solo en la escritura** (Fase 22): a diferencia del resto de `services/storefront/*` (cliente de servidor durante el render de una página), acá ni siquiera las lecturas (`getPublicDepartments`/`getPublicCitiesByDepartment`) tienen un consumidor de servidor -- `/checkout` es "use client" de punta a punta (depende de `useCheckoutStore`), así que nunca hay una página que necesite esta data por otra vía. Mismo criterio ya sentado por `services/storefront/orders.ts` (Fase 15); si un sprint futuro agrega un consumidor de servidor, la lección del Sprint 6.0.1 (bug 3) aplica: un archivo hermano nuevo, no una segunda función acá.
- **Bug real encontrado y corregido durante la verificación de este mismo sprint: el `<Select>` de departamento mostraba el UUID crudo (o el placeholder) en vez del nombre, en modo edición** (`ShippingRateForm.tsx`/`DepartmentCityPicker.tsx`): la causa era una carrera de fetches -- `useDepartments()` vivía *adentro* de `DepartmentCityPicker`, que en modo edición no se montaba hasta que se resolvía a qué departamento pertenecían las ciudades ya asignadas de la tarifa (un segundo round-trip, encadenado después del que trae la tarifa). Para cuando `departmentId` finalmente tenía un valor real, la lista de departamentos (que recién arrancaba su propio fetch en ese mismo instante) todavía no había llegado, así que el `<Select>` no encontraba ningún `item` para resolver la etiqueta. Corregido subiendo `useDepartments()` a `ShippingRateForm` (arranca en el primer render, en paralelo con la tarifa) y pasando `departments` por prop al picker -- mismo criterio que "pedir un fetch lo antes posible, no recién cuando el componente que lo necesita se monta" ya aplicado implícitamente en otros hooks del proyecto. Detectado con Playwright comparando una navegación SPA (client-side, la real) contra una recarga completa (`page.goto`, que ocultaba el problema por dar más tiempo de sobra) -- no se reprodujo nunca con datos de prueba insuficientes, hizo falta reproducir la interacción real del admin.
- **`ProductShowcase.tsx` + `useProductShowcase.ts`, el primer custom hook propio de la tienda pública** (Fase 24, Sprint 6.4): hasta este sprint, "la tienda pública sigue sin custom hooks propios" (sección 11) era una regla sin excepciones, porque ningún componente del storefront fetcheaba sus propios datos -- todos recibían props ya resueltas por su página (Server Component). Este hook no rompe esa razón de fondo: `useProductShowcase(products)` no le pide nada a Supabase -- recibe el array ya resuelto por `Home` (vía `getPublicHeroProducts()`, nuevo en `services/storefront/products.ts`) y encapsula únicamente el estado de *presentación* del carrusel (índice activo, dirección para la transición de Framer Motion, pausa en hover). Es exactamente el mismo tipo de lógica que ya vivía dentro de `PromotionCarousel` (Fase 21) como estado local de ese componente, no como hook -- acá se extrajo a uno propio para que `ProductShowcase.tsx` se mantenga puramente presentacional, mismo criterio que ya separa `lib/search.ts` de `CatalogResults.tsx`. `getPublicHeroProducts(limit = 6)` es una función nueva (no una reutilización de `getPublicProducts()`): resuelve productos destacados primero (`.eq("featured", true)`) y, si no hay ninguno, cae a los más recientes (`order by created_at desc`) -- una consulta propia a Postgres, porque el `Product` que ya devuelve `getPublicProducts()` no expone ningún campo de fecha con el que derivar "más recientes" sobre el array ya en memoria.
- **Flechas e indicadores del Showcase viven fuera del slide animado, no dentro de `AnimatePresence`** (Fase 24, bug real encontrado durante la verificación de este sprint): la primera versión de `ProductShowcase.tsx` renderizaba los indicadores inferiores dentro del mismo `motion.div` que `AnimatePresence` reemplaza en cada auto-avance -- cada ~1.5s, la fila completa de dots se desmontaba y volvía a montar, con el riesgo real de que un click quedara "pisado" justo cuando el carrusel avanzaba solo (reproducido con Playwright: clicks intermitentemente fallidos con "element is not stable" sobre un indicador puntual). Corregido moviendo flechas y dots a hermanos estables, fuera del `AnimatePresence` -- solo el contenido que de verdad cambia con cada producto (imagen, nombre, precio, botón "Ver producto") vive dentro del slide animado; un segundo bug menor encontrado en la misma verificación (un dot inactivo con `width: 0px`, por faltarle la clase base `w-2` junto a `h-2`) se corrigió de paso.
- **`AnimatePresence initial={false}`, en vez de un `mounted`-gate, para evitar el mismatch de hidratación de `useReducedMotion()`** (Fase 24): `Header.tsx`/`PromotionalBanner.tsx` (Fases previas) resuelven el mismo riesgo -- `useReducedMotion()` devuelve `false` en el servidor y podría devolver `true` en el cliente si el sistema operativo del visitante pide "reducir movimiento", produciendo un primer render distinto entre servidor y cliente -- con un estado `mounted` que retrasa la animación hasta después del primer efecto. `ProductShowcase.tsx` no necesitó ese patrón: con `initial={false}` en `AnimatePresence`, la primera imagen nunca anima su entrada (siempre estática, sea cual sea `shouldReduceMotion`) -- el valor de `useReducedMotion()` solo importa para acortar la *duración* de transiciones posteriores (0.15s en vez de 0.5s), nunca para decidir si el primer render anima o no. Mismo problema, una solución distinta y más simple cuando el componente lo permite (acá, el primer producto mostrado no depende de si hubo o no una transición previa).
- **Showcase reducido y centrado en tablet, resuelto enteramente dentro de `ProductShowcase.tsx`, sin tocar el grid del Hero** (Fase 24): el sprint pedía que tablet (768-1023px) mostrara el Showcase "más chico" que en desktop, pero el grid de dos columnas del Hero (`grid-cols-1 lg:grid-cols-2`) es de los componentes protegidos -- no se podía agregar un breakpoint `md:` ahí. Se resolvió con `mx-auto w-full md:max-w-md lg:max-w-none` en el contenedor raíz de `ProductShowcase.tsx`: sin límite en mobile (ocupa el ancho completo, `<768px`), acotado y centrado en tablet, y sin límite de nuevo en desktop (`lg:`, vuelve a llenar su columna del grid). Misma lección general que ya deja el proyecto en otros lados: un ajuste responsive de un componente hijo no siempre requiere tocar el layout del padre.
- **`Tabs` (shadcn, Fase 25) sobre un único `<form>`, no dos formularios independientes**: `SettingsForm.tsx` se organiza en dos pestañas ("General"/"Apariencia") con `Tabs`/`TabsList`/`TabsTrigger`/`TabsContent`, agregado con el CLI (`pnpm dlx shadcn@latest add tabs`) -- mismo Base UI que el resto de `components/ui/`, nunca Radix. Las pestañas son puramente navegación de UI (`Tabs` no controlado, `defaultValue="general"`): todo el estado del formulario (`form`, `pendingLogo`, `pendingFavicon`, etc.) sigue viviendo en el único componente `SettingsForm`, por encima de las pestañas, así que cambiar de pestaña nunca pierde nada ya cargado en la otra y el submit sigue siendo uno solo, exactamente como antes de este sprint (principio ya establecido: `business_settings` es una fila única, no amerita más de un round-trip). Patrón a repetir si otro módulo del panel necesita organizar un formulario largo en pestañas visuales, sin fragmentar su guardado.
- **Identidad Visual (Logo/Favicon) reconectada con la lógica exacta de antes de ocultarse, no reescrita** (Fase 25): la validación de tamaño (`logoSizeError`/`faviconSizeError`, comparados contra `MAX_BRANDING_FILE_SIZE_BYTES` de `services/settings.ts`) y los handlers `onSelectFile`/`onRemove` de `BrandingAssetField` que se habían quitado en la Fase 23 (por quedar sin ninguna UI que los mostrara) se restauraron línea por línea desde el commit previo a esa Fase -- confirmado por diff contra `git show` de la versión anterior a que la tarjeta "Branding" se ocultara. `BrandingAssetField.tsx` y `services/settings.ts` (`uploadBrandingAsset`/`deleteBrandingAsset`/`MAX_BRANDING_FILE_SIZE_BYTES`) no se tocaron ni una línea -- solo cambió qué `SettingsSection` (ahora "Identidad Visual", dentro de la pestaña Apariencia) los renderiza.
- **Hero/Colores del tema/Tipografía: solo estructura visual, cero estado y cero columnas nuevas** (Fase 25): las tres secciones nuevas de la pestaña Apariencia no tienen `useState`, no leen ni escriben ningún campo de `SettingsFormInput`/`business_settings`, y no aceptan ninguna interacción (son `<div>`s decorativos, no `<button>`/`<input>` reales) -- exactamente lo que pedía el sprint ("solo dejar preparada la interfaz"). `HERO_PREVIEW_OPTIONS`/`THEME_COLOR_PREVIEW_OPTIONS` son arrays estáticos a nivel de módulo en `SettingsForm.tsx`, sin ningún tipo nuevo en `services/settings.ts`. Los swatches de "Colores del tema" reutilizan los tokens `oklch` ya existentes (`var(--primary)`/`var(--secondary)`/`var(--accent)` de `globals.css`) en vez de inventar colores de ejemplo sueltos, para que la previsualización ya sea coherente con la paleta real de la tienda el día que esta sección se implemente de verdad.
- **Tiptap elegido sobre Quill/Lexical para el editor enriquecido** (Fase 28): headless de verdad -- la toolbar (`EditorToolbar.tsx`) es 100% propia, con los mismos `Button`/`Select`/`Popover` de shadcn que ya usa el resto del panel, en vez de forzar el tema visual de una librería ajena a parecerse al proyecto (Quill trae su propia UI difícil de re-estilizar; Lexical es más bajo nivel y hubiera requerido escribir a mano varias de las extensiones que Tiptap ya trae oficiales). `@tiptap/starter-kit` cubre negrita/cursiva/tachado/títulos/listas/citas/separador de una sola vez; `@tiptap/extension-image`/`@tiptap/extension-youtube` se extienden (no se reescriben) para agregar resize/alineación/upload -- ver el bullet siguiente.
- **`ResizableImage`/`Video` (`rich-text/extensions.ts`): extender el `Image` oficial de Tiptap en vez de escribir un nodo desde cero, y un nodo `Video` propio porque Tiptap no trae uno oficial** (Fase 28): ambos comparten el mismo patrón de atributos (`width` con presets de 25/50/75/100%, `align` izquierda/centro/derecha) para que la toolbar aplique los mismos comandos (`updateAttributes("image", ...)`/`updateAttributes("video", ...)`) sobre cualquiera de los dos sin lógica condicional. "Redimensionar" se resolvió con presets de ancho aplicados sobre la imagen/video ya seleccionado (clic la selecciona como nodo, ProseMirror ya lo hace solo) en vez de handles de arrastre libre -- cubre el requisito del sprint sin la complejidad de un NodeView custom con eventos de puntero.
- **`sanitize-html`, no `dompurify`/`isomorphic-dompurify` -- bug real de bundling encontrado durante la verificación** (Fase 28): el primer intento con `isomorphic-dompurify` compilaba bien pero rompía `pnpm build` -- depende de `jsdom` para sanitizar en Node, y Turbopack, al intentar tratarlo como paquete externo del servidor, no lograba resolver los archivos internos de `jsdom` (`default-stylesheet.css`) y tiraba `ENOENT` con un path inválido (`C:\ROOT\...`) al "Collecting page data". `sanitize-html` no emula ningún DOM (parsea el string directamente), así que no depende de `jsdom` y no tiene ese problema -- de paso, `allowedIframeHostnames` restringe los `<iframe>` a dominios de YouTube explícitamente, más preciso que una lista blanca de tags/atributos sola.
- **Sanitizar en el límite donde el dato sale de la base, no solo al renderizar -- bug real de exposición de datos encontrado durante la verificación** (Fase 28): la primera versión sanitizaba únicamente en `ProductRichDescription.tsx` (al renderizar). Aunque eso alcanzaba para que el HTML peligroso nunca se ejecutara ni se insertara en el DOM, el `Product` completo (con su `description` todavía sin sanitizar en ese punto de la cadena) se pasaba igual como prop a `ProductGallery`/`ProductBuyBox` (Client Components de la misma página) -- y React serializa cada prop en el payload de RSC para la hidratación, sin importar si algún componente la renderiza visualmente. Resultado: el HTML crudo (incluido un payload de prueba con `<script>`/`onerror=`) terminaba inerte pero visible en el código fuente de la página. Se corrigió sanitizando dentro de `getPublicProductBySlug()` (`services/storefront/products.ts`), no solo en el componente que lo muestra -- así el HTML sin filtrar nunca sale del servidor bajo ninguna forma, ni renderizado ni serializado para hidratación. `ProductRichDescription.tsx` conserva su propia sanitización (defensa en profundidad, doblemente barata sobre HTML ya limpio).
- **`useEditor({ content: value })` solo usa `value` para el contenido inicial -- bug real de round-trip encontrado durante la verificación** (Fase 28): en modo edición, `ProductForm.tsx` arranca con `description = ""` y recién lo actualiza cuando `useProduct()` termina de traer el producto de forma asíncrona -- para ese momento, Tiptap ya había creado el editor con el `value` inicial (vacío). Sin un efecto que sincronice manualmente (`editor.commands.setContent(value, { emitUpdate: false })` cuando `value` cambia desde afuera), abrir "Editar producto" mostraba el editor vacío aunque el producto sí tuviera descripción guardada -- guardar en ese estado la hubiera borrado. `emitUpdate: false` evita que ese `setContent` dispare `onUpdate` (que llamaría a `onChange`, re-setando el mismo `value` -- un round-trip de más, no un loop infinito, pero innecesario).
- **`@tiptap/extension-youtube` exige su wrapper `<div data-youtube-video>`, no un `<iframe>` suelto -- bug real de sanitización encontrado durante la verificación** (Fase 28): su `parseHTML()` busca literalmente `div[data-youtube-video] iframe` para reconocer un embed de YouTube al releer HTML guardado. La primera versión de la lista blanca de `sanitize-html` no incluía `div`, así que ese wrapper se eliminaba al sanitizar (dejando un `<iframe>` sin padre) -- el HTML seguía siendo válido y el iframe se veía bien en la tienda pública, pero al reabrir el producto para editarlo, Tiptap ya no reconocía ese iframe suelto como un nodo de YouTube y lo perdía. Se agregó `div` a la lista blanca, con un único atributo permitido (`data-youtube-video`, sin `style`/`class`/manejadores de eventos) -- lo mínimo necesario para que el wrapper sobreviva, sin abrir la puerta a un `<div>` genérico con contenido peligroso.
- **`StarterKit` (Tiptap v3) ya incluye `Link`/`Underline` -- registrarlos de nuevo como paquetes aparte genera extensiones duplicadas** (Fase 28): a diferencia de Tiptap v2 (donde `@tiptap/extension-link`/`@tiptap/extension-underline` eran obligatorios aparte), v3 los agrupó dentro de `@tiptap/starter-kit`, configurables vía `StarterKit.configure({ link: {...}, underline: {...} })`. Un primer intento agregó igual los paquetes sueltos "por costumbre" de v2, y la consola tiraba `Duplicate extension names found: ['link', 'underline']` -- inofensivo en apariencia, pero con comportamiento de edición inconsistente real observado durante la verificación. Corregido quitando los dos paquetes de `package.json` y configurando ambos a través de `StarterKit` directamente.

---

## 6. Principios arquitectónicos

1. **Nunca duplicar lógica.** Ejemplos concretos ya resueltos así: `sectionContainerVariants`/`sectionItemVariants` en `lib/motion.ts` (evita que cada sección de la Home reimplemente su propia animación de entrada); `getStockStatus`/`getDiscountPercent` en `lib/products.ts`.
2. **Separar presentación de negocio.** Los componentes de `components/` presentan datos que reciben; no calculan totales, no deciden de dónde vienen los datos, no saben si esos datos vinieron de un array local o de una base de datos.
3. **Los componentes no conocen Supabase.** Ni `ProductCard`, ni `CartItem`, ni ningún componente de `storefront/` importa Supabase. Hoy leen de `config/products.ts`; el día de mañana esa misma función (`getProductBySlug`, por ejemplo) puede volverse `async` y consultar Supabase adentro, y **ningún componente debería tener que cambiar**.
4. **La configuración vive en `config/`.** Ningún componente debería tener un string hardcodeado que sea specific de RegiShop (nombre, WhatsApp, textos de marketing). Los textos genéricos de UI ("Agregar al carrito", "Cerrar") sí pueden vivir en el componente — la distinción es "¿esto cambia si cambia el cliente?".
5. **Toda lógica compartida vive en `lib/`.**
6. **Cada Store (Zustand) es la única fuente de verdad de su dominio.** Ningún componente mantiene su propia copia de la cantidad de ítems o el subtotal — todos leen `useCartStore`. Desde el Sprint 3.6, lo mismo aplica al formulario de checkout: `CheckoutForm` y la pantalla de confirmación leen y escriben `useCheckoutStore`, nunca un `useState` propio.
7. **No romper componentes aprobados.** Cada sprint de este proyecto arrancó declarando explícitamente qué no se debía tocar. Esa disciplina se mantiene: antes de editar un componente que no es el foco de la tarea actual, confirmar que está realmente dentro del alcance.
8. **Construir primero experiencias, después persistencia.** Este proyecto deliberadamente construyó toda la UI/UX sobre datos mock antes de conectar Supabase. Es más barato iterar sobre diseño con datos falsos que rehacer diseño después de atarlo a un schema real.
9. **Reutilizar antes que crear.** Antes de escribir un componente nuevo, revisar si uno existente (`ProductCard`, `ProductGrid`, `Breadcrumb`, `FormField`) ya resuelve el problema con una prop nueva.
10. **Base UI, no Radix — y `render`, no `asChild`.** Los componentes de `shadcn/ui` en este proyecto usan **Base UI**, no Radix. Esto importa en la práctica: para renderizar un `Button` como link, el patrón es `render={<Link href="..." />}` junto con `nativeButton={false}` (no `asChild`, que es de Radix y no existe acá). Usar el patrón equivocado compila pero rompe accesibilidad/semántica en runtime.

---

## 7. Estado actual del desarrollo

### ✅ Terminado
- **Proyecto base**: Next.js 15 + TypeScript + Tailwind v4 + shadcn/ui + Framer Motion + Supabase (solo infraestructura, sin conectar) + pnpm.
- **Home completa**: Header (sticky, buscador funcional, WhatsApp, carrito), Hero, Categorías Destacadas, Productos Destacados, Banner Promocional, Beneficios, Testimonios, Footer.
- **Auditoría de calidad de la Home** (consistencia visual, accesibilidad, performance, duplicación de código) ya realizada y con mejoras aplicadas.
- **Catálogo** (`/productos`): breadcrumb, `CatalogToolbar` (cantidad + selector de orden), `ProductGrid`, estado vacío preparado.
- **Buscador funcional** (Sprint 4.2): el input de "Buscar productos..." del Header (el único que existe en la tienda) filtra en tiempo real por nombre, categoría y descripción, sin distinguir mayúsculas/minúsculas ni espacios extra, y soporta palabras parciales y múltiples términos. Al tipear navega a `/productos?q=...`; si ya se está ahí, actualiza la URL en el lugar (sin apilar historial por letra). Toda la lógica vive en `lib/search.ts` (`searchProducts`) y en `CatalogResults` (`components/storefront/`) — `ProductGrid` sigue sin saber que existe un buscador. Sin resultados, reutiliza el mismo `EmptyState` con un texto distinto (`searchEmptyState` en `config/site.ts`). El filtrado sigue siendo en memoria (sin re-consultar Supabase por cada letra) — desde la Fase 11, el array sobre el que filtra ya viene de `products`/`categories` de Supabase, no de `config/products.ts`.
- **Ordenamiento funcional** (Sprint 4.3): el selector de orden de `CatalogToolbar` (antes solo visual) ahora reordena de verdad — Precio: menor a mayor / mayor a menor, Nombre A-Z / Z-A, y "Más recientes" (default, sin reordenar: es el orden que ya existía en `config/products.ts`, sin campo de fecha real para ordenar por "más nuevo"). Vive en `lib/sort.ts` (`sortProducts`), aplicado en `CatalogResults` **después** de `searchProducts` — buscar y después ordenar, nunca al revés. Sincronizado con `?sort=` en la URL, combinable con `?q=` (deep-linkable, sobrevive un F5). `CatalogToolbar` pasó a ser un componente controlado (`sort`/`onSortChange` por props), igual que `RadioGroup` en `ShippingInformation`/`PaymentMethod` — no decide el orden, solo lo muestra.
- **Navegación por categorías** (Sprint 4.4): `/categorias` (índice, todas las categorías con `CategoryCard`) y `/categorias/[slug]` (productos de esa categoría, reutilizando `Breadcrumb`, `CatalogToolbar`, `CatalogResults`, `ProductGrid` y `EmptyState` tal cual — ningún catálogo nuevo). Las tarjetas de "Categorías Destacadas" de la Home y el link "Categorías" del Header, ambos rotos antes de este sprint, ahora navegan correctamente. Combinable con búsqueda y orden en la misma URL (`/categorias/tecnologia?q=iphone&sort=precio-desc`), en ese orden exacto: categoría (resuelta en la página, antes de llegar a `CatalogResults`) → búsqueda → orden.
- **Página de producto** (`/productos/[slug]`): galería con miniaturas (modelo ya preparado para múltiples imágenes), buy box sticky en desktop, botón "Agregar al carrito" funcional, botón "Comprar por WhatsApp" funcional, productos relacionados por categoría.
- **`ProductCard` — "Agregar al carrito" funcional en toda la tienda** (Sprint 4.1): una auditoría funcional detectó que `ProductCard` (usado en Home → Productos Destacados, Catálogo y Productos Relacionados) renderizaba el botón "Agregar al carrito" sin ningún `onClick` — no hacía nada al hacer clic, sin error visible. Corregido reutilizando exactamente `useCartStore((state) => state.addProduct)`, la misma acción que ya usaba `ProductBuyBox`. Verificado en navegador: badge del Header, cantidades y subtotal del carrito, y el mensaje de WhatsApp reflejan correctamente productos agregados desde cualquiera de los tres puntos de entrada, incluyendo agregar el mismo producto varias veces (suma cantidad, no duplica línea).
- **Carrito** (`/carrito`): conectado al store global de Zustand (`store/cart-store.ts`). Agregar, quitar, incrementar y decrementar cantidad funcionan de verdad (en memoria).
- **Checkout** (`/checkout`): formulario completo (datos de contacto, entrega con toggle Delivery/Retiro en tienda, método de pago, notas), validación nativa básica (HTML5), resumen conectado al Cart Store. El estado del formulario vive en `store/checkout-store.ts` (Sprint 3.6) en vez de un `useState` local. El botón "Continuar" navega a `/checkout/confirmacion`.
- **Selección de ubicación en el mapa** (`LocationPicker`, Sprint 3.7): solo cuando el método de entrega es Delivery, debajo de los campos de dirección — mapa de Leaflet/OpenStreetMap con marcador arrastrable. Lat/lng se guardan en `store/checkout-store.ts` (`setLocation`), no en estado local: sobreviven la navegación a `/checkout/confirmacion` y de vuelta. Con "Retiro en tienda" el mapa se desmonta por completo (no solo se oculta). Sin geocodificación, sin autocompletado, sin cálculo de distancia — deliberadamente fuera de alcance.
- **Arquitectura de base de datos** (`docs/database-architecture.md`, Sprint 4.0): diseño conceptual completo y **aprobado** de las 12 tablas que va a usar Supabase (productos + imágenes, categorías, clientes, pedidos + detalle, configuración del negocio, banners, promociones, testimonios, beneficios, administradores), con propósito/columnas/tipos/claves/índices/restricciones de cada una, relaciones explicadas, estrategia de RLS y mapeo 1:1 contra los `type` de TypeScript ya existentes.
- **Infraestructura de Supabase preparada** (Fase 8, `supabase/`): las 12 tablas quedaron implementadas como 4 migraciones versionadas (`supabase/migrations/`) — esquema con triggers de `updated_at` y el helper `is_admin()`, todos los índices del documento aprobado, un bucket de Storage (`product-images`, público de lectura) con sus policies, y RLS completo (lectura pública de catálogo/contenido, escritura solo para administradores autenticados). `supabase/seed.sql` carga datos iniciales tomados tal cual de `config/products.ts`, `config/categories.ts`, `config/business.ts`, `config/testimonials.ts`, `config/benefits.ts`, `config/promotion.ts` y `config/footer.ts` — sin inventar contenido. Estructura generada con `supabase init`, sin conexión real todavía: la tienda pública sigue sin tocar (ver sección 9, Fase 8).
- **Confirmación de pedido y envío por WhatsApp** (`/checkout/confirmacion`, Sprint 3.6 + 3.8): revisión final de solo lectura antes de confirmar — datos de contacto (con botón "Editar datos" que vuelve a `/checkout` sin perder lo ya cargado), método de entrega (con dirección completa si es Delivery), método de pago, notas (sección oculta si no hay), y el mismo `CheckoutSummary` del checkout reutilizado tal cual. Botón "Volver" y botón "Confirmar pedido" — este último es un link real (`render={<a href={whatsappUrl} target="_blank" />}`, mismo patrón que el resto de los CTAs de WhatsApp del proyecto) que abre WhatsApp con el pedido completo armado por `lib/whatsapp.ts`: título, datos del cliente, productos con cantidad/precio unitario/subtotal, resumen (cantidad total/subtotal/envío placeholder/total), entrega (dirección + coordenadas + link a Google Maps si hay Delivery con ubicación elegida), método de pago y notas (solo si existen). Redirige a `/carrito` si el carrito está vacío, o a `/checkout` si se entra directo a la URL sin datos de contacto cargados. El carrito **no se limpia** al hacer clic — decisión deliberada, ver sección 8.
- **Página de contacto** (`/contacto`, Sprint 4.5): título, descripción, WhatsApp, email, dirección y horario de atención, todo desde `config/` — sin ningún string de negocio hardcodeado. Reutiliza exactamente los mismos `footerConfig.contact.email/hours/address` que ya muestra el Footer (sin duplicar el dato en otro archivo de config) y `businessConfig.whatsapp` para el botón "Contactar por WhatsApp" (mismo `getWhatsAppUrl()` que usan Header/Footer/Hero/ProductBuyBox). Botón "Ver ubicación" abre Google Maps con la dirección configurada, vía el helper nuevo `getGoogleMapsUrl()` en `lib/utils.ts` — sin mapa embebido. El link "Contacto" del Header y del Footer (ambos ya apuntaban a `/contacto` desde `mainNav`, pero 404eaban) ahora navegan correctamente sin haber tocado ni `Header.tsx` ni `Footer.tsx`.
- **Páginas institucionales** (Sprint 4.6): `/politicas/envios`, `/politicas/privacidad`, `/politicas/terminos` (una única ruta dinámica, `app/politicas/[slug]/page.tsx`) y `/preguntas-frecuentes`. Título, descripción y contenido de cada política vienen de `config/policies.ts` (`policies`, `getPolicyBySlug`); las preguntas y respuestas de la FAQ vienen de `config/faq.ts` (`faqItems`) — nada de texto hardcodeado en JSX. Los cuatro links del Footer (`footerConfig.legalLinks`), que ya apuntaban a estas rutas exactas pero 404eaban, ahora funcionan sin tocar `Footer.tsx`. Un slug de política inexistente devuelve un 404 real (`notFound()`), igual que categorías (Sprint 4.4) y producto.
- **Localización para Paraguay** (Sprint 4.7): toda la tienda (moneda, WhatsApp, dirección, mapa del checkout, testimonios, políticas, FAQ) reconfigurada para un negocio paraguayo — moneda `PYG`, locale `es-PY`, ubicación de referencia Villarrica, Guairá. Cero cambios de arquitectura o de componentes: fue enteramente una actualización de `config/*.ts` (ver sección 9). Sin referencias a Argentina/ARS/es-AR remanentes, verificado en toda la tienda.
- **Estructura y autenticación del Panel Administrativo** (Fase 9): `/admin/login` con Supabase Auth real (`signInWithPassword`), conectado al proyecto ya configurado en `.env.local`. Todo lo que cuelga de `/admin` (Dashboard + 6 secciones) queda protegido en dos capas — `middleware.ts` a nivel de edge y `admin/(panel)/layout.tsx` del lado del servidor (`supabase.auth.getUser()`) — con redirect automático a `/admin/login` si no hay sesión. Layout completo tipo SaaS: Sidebar con 3 variantes responsive (fija en desktop, colapsada a íconos en tablet, drawer en mobile reutilizando el `Sheet` del storefront) y Header (nombre del negocio desde `siteConfig`, avatar con inicial + email de la sesión, logout). Las 7 páginas (`/admin`, `/admin/productos`, `/admin/categorias`, `/admin/pedidos`, `/admin/clientes`, `/admin/promociones`, `/admin/configuracion`) son placeholders (`AdminPlaceholder`) — sin CRUD, sin Dashboard con datos reales, sin conexión a las tablas de negocio, a propósito. Requirió mover las 8 rutas de la tienda pública a un route group `(storefront)` (ver sección 5) — cero cambio visual o de comportamiento, pura reubicación de archivos, necesaria porque Next.js exige que cada "root layout" distinto (tienda vs. panel) sea su propia raíz.
- **Módulo Productos del Panel Administrativo** (Fase 10, el primer módulo funcional): `/admin/productos` reemplaza su placeholder por una tabla real (imagen principal, nombre, categoría, precio, stock, Estado, Destacado), con buscador, tres criterios de orden (nombre/precio/stock) y paginación real (10 por página) — todo aplicado en memoria dentro de `useProducts.ts` sobre los datos ya traídos de Supabase, mismo criterio que el pipeline de búsqueda/orden del catálogo público pero sin extraer archivos propios todavía (un solo listado no lo justifica). Botón "Nuevo producto" → `/admin/productos/nuevo`; ícono de editar en cada fila → `/admin/productos/[id]/editar`; ambas rutas comparten `ProductForm.tsx` (mismo componente decide alta o edición según si recibe un `productId`). El formulario cubre los 10 campos pedidos (nombre, slug —autogenerado del nombre hasta que se edita a mano—, descripción corta, descripción completa, categoría, precio, precio anterior, stock, Activo, Destacado) más un administrador de imágenes (`ProductImagesField.tsx`: imágenes ya guardadas + nuevas por subir, con preview local, principal = primera de la lista) y borrado con confirmación (`DeleteProductDialog.tsx`, con `AlertDialog` nuevo de shadcn) que limpia también los archivos del bucket antes de borrar la fila. Toda la comunicación con Supabase vive en `services/` (`products.ts`, `categories.ts`, `storage.ts`), consumida por `hooks/` (`useProducts`, `useProduct`, `useCategories`) — ningún componente arma una query. Requirió una migración nueva (`products_admin_fields.sql`, Fase 10) para agregar `is_active` y `description` a `products`, no contempladas en el diseño original de la Fase 8. Verificado en vivo contra el proyecto real: alta, edición, baja (con verificación de que también borra las imágenes del bucket), subida de imágenes, y RLS confirmado con requests anónimas (`SELECT` funciona, `INSERT`/`UPDATE`/`DELETE` no afectan ninguna fila).
- **Tienda pública conectada a Supabase** (Fase 11): Home, Productos Destacados, Catálogo, página de Producto, Productos Relacionados y Categorías dejaron de leer `config/products.ts`/`config/categories.ts` — ahora leen `products`/`product_images`/`categories` en cada request, vía `services/storefront/` (cliente de servidor, el mismo que ya usa el panel para verificar sesión). RLS hace todo el trabajo de visibilidad: el storefront (rol `anon`) solo ve filas `is_active = true`, así que crear/editar/desactivar/eliminar un producto desde el panel se refleja automáticamente en la tienda — sin rebuild, sin caché manual que limpiar. Verificado en vivo, con dos sesiones de navegador en paralelo (una admin, una anónima): producto nuevo aparece en Home/Catálogo/su propia página/su categoría; editar precio y stock se refleja al instante; desactivar lo esconde de la tienda pública (404 en su URL directa) pero lo sigue mostrando en el panel con badge "Inactivo"; eliminar lo hace desaparecer de ambos lados. Búsqueda, orden, categorías, carrito, checkout y WhatsApp verificados sin regresiones funcionales. `config/products.ts` y `config/categories.ts` **no se tocaron ni se eliminaron** — quedan como fallback/documentación, tal como pidió el sprint.
- **Fotos reales de producto visibles en la tienda pública** (Fase 12, corrección de bug): las fotos subidas desde el Panel llegaban bien a Storage y a `Product.images`, pero `ProductCard`/`ProductGallery` nunca las renderizaban — mostraban siempre el placeholder decorativo, incluso con una foto real cargada. Corregido para mostrar la foto real cuando `product.images` tiene una URL absoluta (`isRealImageUrl`), con `next/image` y fallback automático al placeholder si no hay foto o si falla la carga; `next.config.ts` habilita el dominio de Supabase Storage (derivado de `NEXT_PUBLIC_SUPABASE_URL`, no hardcodeado). Verificado en vivo con el producto real "Tacos": misma foto en Home, Catálogo, página de producto y su categoría, en desktop/tablet/mobile, sin regresiones en carrito/checkout/WhatsApp ni en productos sin foto real todavía (siguen mostrando el placeholder de siempre).
- **Módulo Categorías del Panel Administrativo** (Fase 13, segundo módulo funcional): `/admin/categorias` reemplaza su placeholder por una tabla real (color, ícono, nombre, slug, cantidad de productos, Estado, orden de visualización), con buscador y tres criterios de orden (nombre/cantidad de productos/orden de visualización, cada uno asc/desc) — paginación preparada pero deliberadamente no implementada (alcance explícito del sprint). Botón "Nueva categoría" → `/admin/categorias/nueva`; ícono de editar → `/admin/categorias/[id]/editar`; ambas comparten `CategoryForm.tsx` (nombre, slug —autogenerado hasta editarlo a mano—, descripción, color, ícono, orden de visualización, Activa/Inactiva). Slug único validado en el formulario (`isSlugTaken`, chequeo proactivo) y protegido además por el índice único de la Fase 8. Borrado seguro: una categoría con productos asociados no puede eliminarse — bloqueado en la UI con un mensaje explicando que hay que reasignar/eliminar esos productos primero, y protegido a nivel de base por `on delete restrict` (Fase 8) como red de seguridad final. **Con este módulo, la tienda pública terminó de eliminar su dependencia activa de `config/categories.ts`**: `ProductCard`, `ProductGallery`, `ProductBuyBox`, `ProductGrid`, `FeaturedProducts` y `RelatedProducts` ya no importan ese array — reciben las categorías resueltas desde Supabase por la página que los renderiza. Verificado en vivo contra el proyecto real: alta, edición, activar/desactivar (confirmado con RLS y dos navegadores en paralelo que una categoría inactiva desaparece de la tienda pública), borrado de una categoría vacía, bloqueo real al intentar borrar una con productos, y una categoría totalmente nueva ("Ferretería", que nunca existió en `config/categories.ts`) creada y verificada visible en Home, Catálogo, su propia página de producto, Productos Relacionados y su página de categoría — la prueba más directa de que ya no hay ningún camino que dependa del archivo de config. Búsqueda, orden y navegación por categorías verificados sin regresiones. `config/products.ts`/`config/categories.ts` no se eliminaron (siguen como fallback/documentación), pero `config/categories.ts` ya no tiene ningún consumidor activo fuera de Carrito/Checkout (`CartItem.tsx`, `CheckoutSummary.tsx`), explícitamente fuera de alcance de este sprint.
- **Módulo Configuración General del Panel Administrativo** (Fase 14, tercer módulo funcional): `/admin/configuracion` reemplaza su placeholder por un formulario real, organizado en 5 tarjetas visuales (Información general, Branding, Contacto, Redes sociales, Ubicación) con un único submit — toda la fila de `business_settings` se guarda de una vez, no tarjeta por tarjeta. Logo y favicon se suben a un bucket de Storage nuevo (`branding`), con preview local antes de guardar y reemplazo/borrado del archivo anterior al guardar (mismo patrón que las imágenes de producto, Fase 10, adaptado a un único archivo en vez de una galería). Slug/color/ícono no aplican acá (no es un catálogo) — validación mínima: nombre y WhatsApp obligatorios. **Con este módulo, Header, Footer, Hero (el link de WhatsApp) y la página de Contacto dejaron de leer `config/business.ts`/`config/footer.ts` para esos campos** — reciben `settings: BusinessSettings` resuelto desde Supabase por su página/layout. El `<title>`/descripción/favicon globales (root layout de la tienda) también se volvieron dinámicos vía `generateMetadata`. **Tres excepciones documentadas, no un descuido**: el mensaje de WhatsApp del pedido (`lib/whatsapp.ts`), la moneda/locale de `formatPrice()`, y el centro del mapa de Delivery en el Checkout siguen leyendo `config/business.ts` estático — las tres requerían tocar Checkout/Carrito/WhatsApp, explícitamente prohibido este sprint. Verificado en vivo contra el proyecto real: cambiar nombre, WhatsApp, dirección, horario y subir un logo/favicon de prueba se reflejó de inmediato en Home, Header (logo reemplazando el texto), Footer, Contacto y el botón de WhatsApp de la página de producto — sin tocar código. Confirmado que el mensaje de WhatsApp de un pedido de Checkout siguió usando el número viejo (comportamiento esperado, no un bug). Responsive verificado en desktop/tablet/mobile. Datos y archivos de prueba limpiados del proyecto real al finalizar.
- **Checkout registrado en Supabase + módulo Pedidos del Panel Administrativo** (Fase 15, cuarto módulo funcional): el checkout dejó de terminar únicamente en un mensaje de WhatsApp — antes de abrirlo, `OrderConfirmationDetails.tsx` llama a una función nueva de Postgres, `create_order`, que crea (atómicamente) el cliente por upsert de teléfono, el pedido y todas sus líneas. `/admin/pedidos` reemplaza su placeholder por una tabla real (N° de pedido, cliente, fecha, total, cantidad de productos, entrega, pago, estado editable en línea), con buscador, filtro por estado, tres criterios de orden (fecha/total/estado) y paginación preparada pero no implementada (mismo criterio que Categorías, Fase 13). `/admin/pedidos/[id]` muestra el detalle completo: cliente, productos con cantidades/precios/subtotales, total, entrega y dirección, ubicación (link a Google Maps si hay coordenadas), notas, método de pago, fecha y número de pedido — con el mismo selector de estado que la tabla. Los 6 estados (Pendiente, Confirmado, En preparación, Enviado, Entregado, Cancelado) ya existían en el `CHECK` de `orders.status` desde la Fase 8, sin necesitar ninguna migración. El formato del mensaje de WhatsApp **no cambió** — `lib/whatsapp.ts` sigue exactamente igual, tal como pidió el sprint. Verificado en vivo contra el proyecto real: pedido completo desde la tienda (agregar "Tacos", checkout, confirmar) creó correctamente el cliente/pedido/línea en Supabase, abrió WhatsApp con el mensaje esperado (mismo formato de siempre), y apareció de inmediato en `/admin/pedidos`; el detalle mostró toda la información correctamente; cambio de estado a "Confirmado" persistió después de refrescar la página. Atomicidad probada de forma directa: un intento de crear un pedido con una cantidad inválida no dejó ningún registro (ni cliente, ni pedido) en la base. Responsive verificado en desktop/tablet/mobile. Datos de prueba limpiados del proyecto real al finalizar.
- **Módulo Clientes del Panel Administrativo** (Fase 16, quinto módulo funcional, 100% de solo lectura): `/admin/clientes` reemplaza su placeholder por una tabla real (nombre, teléfono, email, cantidad de pedidos, total gastado, última compra, fecha de alta), con buscador y 4 criterios de orden (nombre/total gastado/cantidad de pedidos/última compra, cada uno asc/desc) y paginación preparada pero no implementada (mismo criterio que Categorías y Pedidos). `/admin/clientes/[id]` muestra información general (nombre, apellido, teléfono, email, dirección principal derivada del pedido de Delivery más reciente, fecha de registro), estadísticas (total gastado, cantidad de compras, ticket promedio, primera y última compra) y el historial completo de pedidos (número, fecha, estado, total, entrega, pago), cada uno linkeando a su detalle en `/admin/pedidos/[id]`. **Los clientes no se crean manualmente**: se crean solos por el mismo `upsert` de teléfono que ya hacía `create_order` desde la Fase 15 — este sprint no agregó ninguna migración ni policy nueva, fue enteramente trabajo de aplicación sobre datos que ya existían. Las estadísticas se calculan en memoria a partir de los pedidos ya traídos, nunca se guardan. Verificado en vivo contra el proyecto real: 3 pedidos con el mismo teléfono no duplicaron el cliente (una sola fila, 3 pedidos, total y ticket promedio correctos); un cliente distinto apareció aparte; el historial mostró los 3 pedidos correctamente; la navegación Cliente → Pedido llevó al pedido correcto con sus datos completos. La dirección inversa (Pedido → Cliente) no se implementó -- hubiera requerido tocar `OrderDetailView.tsx`/`services/orders.ts`, explícitamente prohibido este sprint ("NO modificar Pedidos"); queda documentada como pendiente. Responsive verificado en desktop/tablet/mobile. Datos de prueba limpiados del proyecto real al finalizar.
- **Dashboard del Panel Administrativo** (Fase 17): `/admin` reemplaza su placeholder por un Dashboard real, con 6 tarjetas de resumen (total de pedidos, pendientes, entregados, total de clientes, productos activos, categorías activas), una sección de Ventas (ventas totales, ticket promedio, última venta), los últimos 10 pedidos (con navegación a `/admin/pedidos/[id]`), los 5 productos más vendidos (calculados desde `order_items`, con una barra proporcional al máximo) y un desglose por los 6 estados de pedido (estructura lista para un gráfico futuro, hoy renderizada como barras de CSS puro, sin ninguna librería de gráficos). **Cada cifra se recalcula en cada carga a partir de `orders`/`order_items`/`customers`/`products`/`categories` — nada hardcodeado, nada guardado**, tal como pidió explícitamente el sprint. Las tarjetas de resumen ya aceptan una "variación futura" (`trend`, flecha + texto) que hoy ninguna calcula, dejando la estructura preparada sin inventar ningún número. Verificado en vivo contra el proyecto real: se crearon 4 pedidos de prueba (4 clientes, 3 productos, 4 estados distintos) y las 9 cifras del Dashboard coincidieron exactamente con lo calculado a mano (total de pedidos, pendientes, entregados, ventas totales, ticket promedio, productos más vendidos con su cantidad y su empate correcto entre dos productos); la navegación a un pedido desde "Últimos pedidos" llevó al detalle correcto; no se detectaron regresiones en Productos/Categorías/Pedidos/Clientes/Configuración. Responsive verificado en desktop/tablet/mobile. Datos de prueba limpiados del proyecto real al finalizar.
- **Módulo Promociones del Panel Administrativo** (Fase 18, sexto y último módulo funcional planeado): `/admin/promociones` reemplaza su placeholder por una tabla real (título, tipo de descuento, producto o categoría asociada, descuento, fecha inicio, fecha fin, estado calculado), con buscador y 3 criterios de orden (fecha de inicio/descuento/estado) y paginación preparada pero no implementada. `/admin/promociones/nueva` y `/[id]/editar` comparten `PromotionForm.tsx`, con un selector explícito "¿A qué aplica esta promoción?" (una categoría completa o un producto específico, nunca ambas) que refleja en la UI la restricción ya diseñada en la base desde la Fase 8 (`promotions_target_check`). El "Estado" de la tabla no es solo `is_active`: se calcula combinando esa bandera con las fechas de vigencia (Activa/Programada/Vencida/Inactiva) para que el admin entienda de un vistazo qué campañas están corriendo de verdad. **La tienda pública dejó de depender de `config/promotion.ts`**: el banner promocional de la Home ahora consume la promoción vigente desde Supabase (`getActivePromotion()`), mostrando el descuento real como badge y linkeando al producto o la categoría de la campaña — sin promoción vigente, el banner simplemente no se renderiza. **Una promoción no recalcula el precio mostrado de un producto o categoría**: esa conexión ya había quedado explícitamente pendiente desde el diseño original (Fase 8, `docs/database-architecture.md`) y este sprint no la resolvió, por estar fuera de su alcance explícito ("NO modificar Productos") -- **resuelta en el Sprint 6.1** (Fase 21, ver más abajo), que también convirtió el banner en un carrusel para más de una promoción vigente. Verificado en vivo contra el proyecto real: promoción por categoría (Tecnología, 15%) y por producto (iPhone 14, Gs. 50.000) creadas correctamente; edición del valor de descuento reflejada; desactivar/reactivar cambia el estado correctamente; eliminar la quita de la tabla; el banner de la Home mostró siempre la promoción vigente más reciente con el link correcto a `/productos/iphone-14`; una promoción vencida (creada directamente por SQL, con fecha de fin en el pasado) se mostró como "Vencida" en el panel pero **nunca apareció en el banner público** — confirmando que RLS filtra automáticamente lo vencido, sin necesitar ningún chequeo de fechas en el código de la aplicación. Sin regresiones en el resto del panel ni de la tienda pública. Responsive verificado en desktop/tablet/mobile. Datos de prueba limpiados del proyecto real al finalizar.
- **Módulo Inventario del Panel Administrativo** (Fase 19, séptimo módulo funcional — cierre de la Versión 1.0): `/admin/inventario` reemplaza su placeholder por una tabla real (producto, SKU, stock actual, stock mínimo, estado calculado, último movimiento), con buscador (nombre/SKU) y 3 criterios de orden (nombre/stock/último movimiento, cada uno asc/desc) y paginación preparada pero no implementada. `/admin/inventario/[id]` muestra el stock actual, un formulario para registrar movimientos (Entrada de stock / Ajuste positivo / Ajuste negativo, con motivo obligatorio), el historial completo (fecha, tipo, cantidad con signo, stock anterior/posterior, motivo) y un formulario aparte para SKU/stock mínimo. **El stock nunca puede quedar negativo**: un ajuste manual que lo dejaría negativo se rechaza con un error explícito (verificado directamente contra Supabase, simulando la sesión de un admin autenticado, con cero efecto en la base tras el rechazo). **La integración con Pedidos no tocó `services/orders.ts`**: una función de Postgres (`register_stock_movement`) centraliza las escrituras manuales, y un trigger (`apply_order_stock_movement`) descuenta stock automáticamente al confirmar/preparar un pedido y lo restituye al cancelarlo, enganchado al mismo `update` que el módulo de Pedidos ya hacía desde la Fase 15 sin cambiar una línea de ese archivo — con una bandera (`orders.stock_adjusted`) que evita descontar dos veces si el pedido avanza por varios estados "activos" seguidos. Una confirmación automática que pida más stock del disponible se recorta a 0 en vez de bloquear el pedido (asimetría deliberada frente al rechazo del ajuste manual — ver sección 6). **La tienda pública muestra "Sin stock" y deshabilita "Agregar al carrito" y "Comprar por WhatsApp" sin ocultar el producto** — la primera ya estaba resuelta de un sprint anterior; la segunda (el botón de WhatsApp en `ProductBuyBox.tsx`) se corrigió en este sprint, única modificación a un componente protegido de la tienda pública. Verificado en vivo contra el proyecto real, de punta a punta: entrada manual (+10) y ajuste negativo manual (-3) sobre iPhone 14, reflejados correctamente en el stock y en el historial; un pedido real de 2 "Tacos" creado desde el checkout público (sin tocar Checkout/Carrito/WhatsApp) permaneció sin afectar el stock mientras estuvo "Pendiente", descontó 2 unidades al pasar a "Confirmado", no volvió a descontar al pasar a "En preparación", y restituyó las 2 unidades al "Cancelar" — confirmado tanto en la UI del panel como por SQL directo contra `stock_movements`; un pedido sintético de 10 unidades de un producto con 3 en stock (Smart Watch) confirmó sin error y dejó el stock en 0, con el movimiento registrando el delta real aplicado (-3); "Aspiradora" (stock 0, ya sembrado así) mostró ambos botones deshabilitados y el producto visible tanto en su página como en el Catálogo. Un bug real (el formulario de movimiento reseteaba su tipo seleccionado tras cada envío exitoso, por un refetch que remontaba el componente) se encontró y corrigió durante esta misma verificación — ver sección 6. Sin regresiones en el resto del panel ni de la tienda pública (barrido completo de las 6 rutas públicas y las 8 rutas del panel). Responsive verificado en desktop/tablet/mobile. Todos los datos de prueba (movimientos, pedidos, clientes) se limpiaron del proyecto real al finalizar, devolviendo cada producto a su stock original. **Con este módulo, la Versión 1.0 de Somapp Commerce Engine queda completa.**
- **Auditoría Final y Corrección de Bugs — Release Candidate V1.0** (Fase 20, Sprint 6.0.1): sprint exclusivamente de estabilización, sin funcionalidades nuevas. Se encontraron y corrigieron 6 bugs: (1) `ProductCard.tsx` nunca tuvo un `Link` a `/productos/[slug]` — clickear la tarjeta (imagen/nombre/descripción) no navegaba a ningún lado, en Home/Catálogo/Categorías/Relacionados por igual, ya que las cuatro áreas renderizan el mismo componente; (2) el banner de una promoción de producto no mostraba la imagen del producto — ahora reutiliza `product_images` (la misma foto principal de la ficha), sin campo nuevo; (3) el mensaje de WhatsApp del checkout seguía leyendo `config/business.ts` en vez de `business_settings` — única pieza de las tres excepciones documentadas en la Fase 14 que este sprint sí resolvió; (4) una categoría marcada Inactiva seguía visible en la tienda si quien navegaba tenía una sesión de admin activa en el mismo navegador (RLS decide por rol, no por ruta) — corregido con un filtro explícito de `is_active` en `services/storefront/categories.ts`, extendido por auditoría también a productos y promociones; (5) seleccionar un logo/favicon mayor a 2 MB recién mostraba el error de Supabase Storage al guardar — ahora se valida al seleccionar el archivo, antes de subir nada; (6) el Dashboard ganó dos gráficos de Recharts (ventas, estados de pedido), tarjetas con indicadores de color e animaciones de entrada sutiles (CSS, no Framer Motion), sin tocar ninguna métrica ni consulta existente. La auditoría de código, además, encontró y corrigió miniaturas de producto rotas en `/admin/productos`/`ProductImagesField.tsx` (mismos placeholders de seed nunca reales, sin la guarda que ya tenía la tienda pública desde la Fase 12) y un nombre de categoría corrompido en la base real ("Tecnología parece" → "Tecnología"), y eliminó `AdminPlaceholder.tsx` (código muerto desde que los 7 módulos quedaron completos). Verificado en vivo contra el proyecto real, bug por bug (incluyendo la prueba clave del bug 4: la misma sesión de admin, antes y después del fix, contra una categoría creada y desactivada para la prueba) más un barrido de checklist completo (CRUD de productos/categorías, pedido real de punta a punta, clientes, inventario) y un barrido final de las 12 rutas públicas y 8 del panel sin errores de consola. Responsive verificado en desktop/tablet/mobile. Todos los datos de prueba se limpiaron del proyecto real al finalizar. `pnpm lint` y `pnpm exec tsc --noEmit` limpios. **Con este sprint, Somapp Commerce Engine Versión 1.0 queda estable, consistente y lista como Release Candidate.**
- **Sistema de Promociones completo: banner rotativo + precios dinámicos** (Fase 21, Sprint 6.1): hasta este sprint, el panel permitía crear varias promociones vigentes a la vez pero el banner de la Home solo mostraba la más reciente, y ninguna promoción modificaba el precio realmente mostrado de un producto. **Banner**: `PromotionalBanner.tsx` recibe ahora el array completo de promociones vigentes (`getActivePromotions()`, plural); con exactamente una, se comporta idéntico a como lo hacía desde la Fase 18 (mismo JSX, mismo stagger de entrada, sin controles); con dos o más, se convierte en un carrusel -- avanza solo cada ~2.5s con una transición horizontal de Framer Motion, en loop infinito, con flechas izquierda/derecha y dots de navegación manual (ambos reinician el temporizador) y pausa la rotación al pasar el mouse por encima, reanudando al salir. **Precios**: un motor nuevo y único (`calculatePromotion()`/`getProductDisplayPrice()`, `lib/promotions.ts`) calcula el precio final de cualquier producto a partir de las promociones vigentes -- porcentual o por monto fijo, con prioridad fija (promoción del producto puntual primero, de su categoría después, nunca las dos a la vez, nunca acumulando dos descuentos) -- y se muestra (precio tachado, precio final, badge del descuento real) en `ProductCard`, `ProductBuyBox`, y por lo tanto en Home, Productos Destacados, Catálogo, página de Producto, Productos Relacionados y Categorías; sin promoción vigente para un producto, el precio vuelve exactamente al comportamiento legacy de siempre (`oldPrice`/`onSale`/`badge` estáticos, Fases 2-3) -- cero regresión. `ProductGallery.tsx` no se tocó (nunca mostró un precio). Carrito, Checkout y WhatsApp explícitamente fuera de alcance, no se tocaron. Verificado en vivo contra el proyecto real: se insertaron 5 promociones de prueba (monto fijo, categoría, prioridad producto-sobre-categoría, vencida, inactiva) sumadas a las 2 promociones reales ya existentes del usuario (`OTRO`, `DESCUENTAZO`) -- confirmado el carrusel con 5 dots, auto-avance, pausa en hover, reanudación, flechas, navegación directa por dot, loop infinito; confirmados los 6 precios esperados exactamente (percentual, monto fijo, prioridad producto vs. categoría, producto sin promoción propia heredando la de su categoría, y un producto con promociones vencida/inactiva mostrando su precio normal sin ningún badge); confirmada la ausencia total del banner con 0 promociones vigentes y el comportamiento idéntico al anterior con exactamente 1. Todos los datos de prueba se limpiaron al finalizar, restaurando el estado exacto de las 2 promociones reales del usuario. Responsive verificado en desktop/tablet/mobile (capturas de pantalla, sin overflow horizontal en ningún breakpoint). `pnpm lint` y `pnpm exec tsc --noEmit` limpios. **Con este sprint, el sistema de promociones de Somapp Commerce Engine queda definitivamente terminado.**
- **Módulo Logística del Panel Administrativo: tarifas de envío completamente administrables** (Fase 22, Sprint 6.2): octavo módulo funcional del panel, y el primero en tocar tanto Supabase (4 tablas nuevas) como el Panel (`/admin/logistica`) como el Checkout (solo la parte de cálculo de envío) en un mismo sprint. `departments`/`cities` -- 18 "departamentos" (los 17 oficiales de Paraguay + Asunción como Distrito Capital) y 228 ciudades, sembrados una única vez con `supabase/seed_logistics.sql`, de solo lectura desde toda la aplicación (sin ninguna policy de escritura, ni para admins) -- y `shipping_rates`/`shipping_rate_cities` (la tarifa -- nombre, precio, tiempo estimado, estado -- y qué ciudades cubre, muchos a muchos). `/admin/logistica` reemplaza su placeholder por una tabla real (nombre, precio, cantidad de ciudades, tiempo estimado, estado, acciones), con buscador y 3 criterios de orden (nombre/precio/cantidad de ciudades) y paginación preparada pero no implementada, mismo patrón que el resto del panel. `/admin/logistica/nueva` y `/[id]/editar` comparten `ShippingRateForm.tsx`, con `DepartmentCityPicker.tsx` como la "interfaz cómoda" que pidió el sprint en vez de un `<select>` común: elegir un departamento muestra automáticamente sus ciudades con checkbox, más buscador de ciudades y "seleccionar todas"/"deseleccionar todas" sobre lo que esté filtrado -- pensado para tildar decenas de ciudades rápido. **El checkout dejó de mostrar un envío placeholder de siempre "se calcula en el próximo paso"**: `ShippingInformation.tsx` reemplazó sus dos `<Input>` de texto libre (departamento/ciudad) por `ShippingCitySelect.tsx`, dos `<Select>` conectados a Supabase -- al elegir una ciudad, busca automáticamente la tarifa que la cubre (si hay más de una activa cubriendo la misma ciudad, gana la más reciente) y actualiza `useCheckoutStore` con el resultado; `CheckoutSummary.tsx` (mostrado tanto en `/checkout` como en `/checkout/confirmacion`) refleja ese costo real y suma el Total automáticamente. **Sin tarifa que cubra la ciudad elegida, se muestra "A confirmar" -- nunca un costo inventado --, con el mensaje "El vendedor confirmará el costo del envío una vez recibido el pedido", y el cliente puede terminar la compra igual.** El pedido guarda el resultado real: `orders.department`/`city`/`shipping_cost` (columnas que ya existían desde la Fase 8 sin usarse) más `shipping_rate_id`/`shipping_rate_name` (las dos únicas columnas nuevas, mismo criterio dual que `order_items.product_id`/`product_name`) -- `shipping_cost` queda `null` exactamente cuando no había tarifa, y `/admin/pedidos/[id]` (Pedidos, no protegido este sprint) muestra "A confirmar" en ámbar en vez de ocultar la fila, para que el administrador vea de inmediato que tiene que informar el costo a mano. **Una excepción documentada, no un descuido**: `lib/whatsapp.ts` no se tocó ("NO modificar WhatsApp" fue instrucción explícita) -- el mensaje que se manda por WhatsApp sigue sin sumar el envío al total, aunque el total mostrado en pantalla y guardado en Supabase sí sea el real; ver sección 13. Un bug real (el `<select>` de departamento mostraba el UUID crudo en vez del nombre al editar una tarifa, por una carrera de fetches) se encontró y corrigió durante esta misma verificación -- ver sección 6. Verificado en vivo contra el proyecto real: alta de una tarifa (Central, 2 ciudades vía buscador + selección individual) y edición (precio actualizado, departamento/ciudades correctamente precargados) confirmadas con Playwright; checkout con Villarrica (ciudad cubierta) mostró el costo/tiempo estimado correctos y el Total sumado (Gs. 80.000 = Gs. 65.000 + Gs. 15.000); con Borja (misma provincia, sin tarifa) mostró "A confirmar" sin sumar nada al Total; dos pedidos reales completados de punta a punta (uno cubierto, uno no) confirmaron por SQL directo que `shipping_cost`/`shipping_rate_id`/`shipping_rate_name` quedaron exactamente como correspondía en cada caso, y `/admin/pedidos/[id]` los mostró correctamente. Sin regresiones: barrido de las 6 rutas públicas y las 9 del panel (incluida la nueva) sin errores de consola ni 500; Productos, Categorías, Promociones e Inventario verificados sin cambios de comportamiento. Responsive verificado en desktop/tablet/mobile, sin overflow horizontal. Todos los datos de prueba (tarifa, pedidos, clientes) se limpiaron del proyecto real al finalizar. `pnpm lint` y `pnpm exec tsc --noEmit` limpios.
- **Configuración: la tarjeta "Branding" se ocultó de la interfaz** (Fase 23, Sprint 6.3, mejora puntual de UX): `/admin/configuracion` pasó de 5 tarjetas a 4 (Información general, Contacto, Redes sociales, Ubicación) -- ni el esquema, ni `services/settings.ts`, ni el bucket `branding`, ni `BrandingAssetField.tsx` se tocaron. Logo y favicon siguen almacenados y sirviéndose exactamente igual (Header, favicon del `<head>`); simplemente no hay ningún punto de la interfaz hoy desde el que cambiarlos. Ver sección 9, Fase 23.
- **Showcase dinámico de productos en el Hero** (Fase 24, Sprint 6.4): el panel derecho del Hero, hasta ahora un marcador decorativo estático (blobs + grilla de puntos, "reservado para cuando haya una imagen real de campaña"), fue reemplazado por `ProductShowcase.tsx` -- un carrusel premium que rota automáticamente cada 1.5 segundos entre productos reales de Supabase (destacados primero; si no hay ninguno destacado, los más recientes -- nunca queda vacío), con transición horizontal de Framer Motion, mostrando imagen real (mismo criterio `isRealImageUrl` de la Fase 12, con el mismo placeholder decorativo de siempre si el producto no tiene foto real), nombre, precio (y precio anterior tachado si lo tiene) y un botón "Ver producto" a `/productos/[slug]`. Se pausa al pasar el mouse por encima y reanuda al salir; flechas izquierda/derecha e indicadores inferiores permiten navegar manualmente (loop infinito), reiniciando el temporizador -- mismo patrón ya probado por `PromotionCarousel` (Fase 21). El resto del Hero (textos, botones, layout de dos columnas en desktop) no se tocó; en tablet el Showcase se muestra más chico y centrado, y en mobile ocupa el ancho completo debajo del texto. Los productos se piden una única vez por `Home` (`getPublicHeroProducts()`, `services/storefront/products.ts`) y se pasan por prop a `Hero`/`ProductShowcase` -- la animación solo cambia qué producto ya cargado se muestra, sin ningún fetch adicional. Ver sección 5 ("por qué") para el detalle de arquitectura y el bug real encontrado y corregido durante la verificación. Verificado en vivo con Playwright: 6 productos destacados reales del proyecto mostrados con sus 6 indicadores; auto-avance cada ~1.5s; pausa en hover y reanudación al salir; flechas y click directo en un indicador; loop infinito (del último producto vuelve al primero); botón "Ver producto" navegando a la ficha correcta; fallback a "más recientes" confirmado desactivando temporalmente los destacados del proyecto real (restaurado al finalizar); responsive en desktop/tablet/mobile sin overflow horizontal; barrido de regresión de las 7 rutas públicas y las 9 del panel sin errores de consola. `pnpm lint` y `pnpm exec tsc --noEmit` limpios.
- **Configuración reorganizada en pestañas: General + Apariencia** (Fase 25, Sprint 6.5): `/admin/configuracion` gana `Tabs` (shadcn, agregado con el CLI) -- "General" agrupa las 4 tarjetas de siempre (Información general, Contacto, Redes sociales, Ubicación), sin cambios; "Apariencia" es nueva, con 4 secciones: **Identidad Visual** (Logo + Favicon, la tarjeta "Branding" oculta desde la Fase 23, reconectada acá con la lógica exacta de antes -- validación de tamaño, subida/reemplazo/borrado contra el bucket `branding` -- sin tocar `BrandingAssetField.tsx` ni `services/settings.ts`), **Hero** (preview de 3 opciones futuras: Imagen/Video/Productos destacados, con badge "Próximamente"), **Colores del tema** (preview de Color principal/secundario/de botones, mismo badge) y **Tipografía** ("Disponible próximamente."). Sigue siendo un único `<form>` con un único submit -- las pestañas son navegación de UI (`Tabs` no controlado), el estado sigue viviendo en `SettingsForm` por encima de ambas, así que cambiar de pestaña nunca pierde datos ya cargados en la otra. Las tres secciones nuevas son 100% visuales: sin `useState`, sin leer/escribir ningún campo de `business_settings`, sin ninguna columna ni migración nueva. Verificado en vivo con Playwright: "Branding" ya no existe en ningún lado de la pantalla; ambas pestañas muestran su contenido correcto; ciclo completo de Logo (subir un archivo real, guardarlo, refrescar la página y confirmar que la URL persistida es la de Supabase Storage, después quitarlo y guardar de nuevo para no dejar datos de prueba) verificado de punta a punta contra el proyecto real; responsive en desktop/tablet/mobile sin overflow horizontal; barrido de regresión de las 7 rutas públicas y las 9 del panel sin errores de consola. `pnpm lint` y `pnpm exec tsc --noEmit` limpios.
- **Descripción enriquecida de producto** (Fase 28, Sprint 6.6): el `<textarea>` plano de "Descripción completa" en `/admin/productos` se reemplaza por `RichTextEditor.tsx`, un editor Tiptap con toolbar propia -- negrita, cursiva, subrayado, tachado, títulos H1-H6, listas con viñetas y numeradas, citas, separadores, enlaces, color de texto y de resaltado, imágenes (con resize por presets de 25/50/75/100% y alineación izquierda/centro/derecha), GIFs (son solo imágenes con otro MIME type, sin código extra) y videos (subidos como archivo o insertados por URL de YouTube). Imágenes/GIFs/videos se suben a un bucket nuevo, `product-content` (separado de `product-images`, la galería estructurada, por el mismo criterio que ya separó `branding` en la Fase 14), en el momento de insertarlos en el editor -- no recién al guardar el formulario completo, mismo patrón que cualquier editor real (WordPress, Notion). Ese contenido **se muestra únicamente en la página individual del producto** (`ProductRichDescription.tsx`, nueva sección "Descripción completa" entre la galería/buy box y "Productos relacionados") -- `ProductCard`, el Catálogo, la Home, los Productos Destacados y el Showcase del Hero no se tocaron: `getPublicProducts()`/`getPublicHeroProducts()` ni siquiera seleccionan la columna `description`, solo `getPublicProductBySlug()` lo hace, con su propio select (`PUBLIC_PRODUCT_DETAIL_SELECT`). Sanitizado dos veces con `sanitize-html` (al guardar y al leer desde la base) contra XSS -- ver sección 5 ("por qué") para el detalle de los bugs reales encontrados y corregidos durante la verificación (un problema de bundling de Turbopack con la librería de sanitización original, extensiones duplicadas de Tiptap, la descripción no cargaba al reabrir un producto para editar, HTML sin sanitizar llegando al cliente por la hidratación de React, y el wrapper de YouTube perdiéndose al sanitizar). Verificado en vivo con Playwright, aislando cada capacidad individualmente para evitar falsos negativos de automatización (ver sección 9): las 15 capacidades pedidas por el sprint confirmadas al nivel del HTML guardado, ciclo completo de alta → edición (con el contenido real cargando de vuelta en el editor) → visualización en la tienda, prueba de XSS con un payload malicioso (`<script>`, `onerror=`, `iframe` a un dominio ajeno) confirmando que nunca se ejecuta ni queda expuesto en el código fuente de la página, y barrido de regresión de 18 rutas (públicas + panel) sin errores de consola. `pnpm lint` y `pnpm exec tsc --noEmit` limpios.
- **Rediseño Premium del Catálogo** (Fase 29, Sprint 6.7): rediseño 100% visual de `ProductCard.tsx` -- único archivo modificado -- sin agregar ninguna funcionalidad nueva. Tarjetas más bajas (se quitó la descripción corta y la fila de "estado de stock", ninguna de las dos pedida en la información a mostrar), imagen más protagonista, "Últimas unidades" migrado de fila de texto a badge (junto al de descuento), botón más compacto, hover/sombra/foco modernizados (`focus-within:ring-2` en toda la tarjeta), y `sizes` de `next/image` corregido para reflejar la grilla real. Al ser un componente compartido por Home/Catálogo/Categorías/Productos Relacionados, el rediseño se refleja en las cuatro áreas por igual -- decisión confirmada explícitamente con el usuario en vez de duplicar el componente. `ProductGrid.tsx` (usado también en la página de producto, protegida) no cambió su cantidad de columnas: "más productos por pantalla en mobile" se logró enteramente reduciendo la altura de cada tarjeta. Panel, checkout, carrito y la página individual de producto no se tocaron. Ver sección 9, Fase 29, para el detalle completo, incluyendo la limpieza de una duplicación de código ya documentada como deuda técnica (`isRealImageUrl`/`getStockStatus`, ahora importadas de `lib/products.ts`).

### ❌ Todavía no implementado
- Persistencia del carrito y del formulario de checkout (localStorage o backend) — ambos se reinician al recargar la página, a propósito.
- Limpieza del carrito tras enviar el pedido por WhatsApp — no se implementó a propósito en el Sprint 3.8: sin backend que confirme que el mensaje se mandó de verdad, vaciar el carrito en el click podría perder el pedido si el cliente cierra la pestaña de WhatsApp sin enviarlo.
- Geocodificación inversa (mostrar la dirección en texto a partir de lat/lng), autocompletado de direcciones y cálculo de distancia/costo de envío sobre el mapa — deliberadamente fuera de alcance del Sprint 3.7, que solo resuelve la selección del punto exacto.
- Mostrar la ubicación elegida dentro de la UI de `/checkout/confirmacion` — hoy vive en el checkout-store, se usa en el mapa y se incluye en el mensaje de WhatsApp, pero la pantalla de confirmación no la muestra en pantalla todavía (no se pidió, ver observación en el Sprint 3.7).
- Ninguno: los 7 módulos planeados originalmente para la Versión 1.0 (Productos, Categorías, Inventario, Configuración, Pedidos, Clientes, Promociones) y el Dashboard ya están resueltos (Fases 10, 13, 19, 14, 15, 16, 18 y 17 respectivamente), y desde la Fase 22 se sumó un octavo módulo no planeado originalmente: Logística (tarifas de envío). Ver sección 13 para el detalle de lo que queda pendiente en el resto del proyecto.
- Autenticación para clientes de la tienda (no aplica hoy: el checkout no requiere cuenta). La autenticación de administradores sí está resuelta (Fase 9).
- Filtros de catálogo (por categoría/precio) — todavía no hay ninguna UI para esto; ni la búsqueda (Sprint 4.2) ni el ordenamiento (Sprint 4.3) lo resuelven, son responsabilidades distintas.
- Paginación funcional en la tienda pública (`/productos`, `/categorias/[slug]`) — el panel sí la tiene, en su tabla de Productos (Fase 10).
- Página institucional "Nosotros" — no estaba en el alcance de ningún sprint hasta la fecha; Contacto (Sprint 4.5), Políticas de envío/Privacidad/Términos y Preguntas frecuentes (Sprint 4.6) ya están resueltas.
- Repositorio git inicializado.

---

## 8. Roadmap oficial

1. ~~Escribir las migraciones SQL y las políticas RLS reales.~~ **Hecho (Fase 8)** — `supabase/migrations/`, 4 archivos (esquema, índices, storage, RLS) + `supabase/seed.sql`, ya aplicadas al proyecto real.
2. ~~Reemplazar `config/products.ts` y `config/categories.ts` por consultas reales a Supabase.~~ **Hecho (Fase 11)** — Home, Destacados, Catálogo, Producto, Relacionados y Categorías ya leen de Supabase, manteniendo el mismo `type Product`/`type Category` como contrato (`services/storefront/`). Los archivos de `config/` siguen existiendo como fallback/documentación, sin usarse.
3. Persistencia del carrito y del checkout (Zustand `persist` middleware con `localStorage`, o asociarlos a una sesión en Supabase).
4. Limpiar el carrito tras un envío de pedido confirmado — hoy deliberadamente no ocurre (ver sección 7); requiere decidir una señal confiable de "el pedido se envió" (probablemente ligada a Supabase, no solo al click del link de WhatsApp).
5. Filtros de catálogo por precio/stock — búsqueda, ordenamiento y navegación por categoría ya resueltos (Sprints 4.2, 4.3 y 4.4, y ya conectados a Supabase desde la Fase 11); `searchProducts` (`lib/search.ts`) y `sortProducts` (`lib/sort.ts`) siguen filtrando/ordenando en memoria (catálogo chico) — pasar a un `WHERE`/`ORDER BY` real en Supabase es una optimización a futuro, no un requisito de correctitud hoy.
6. Paginación real en `/productos` y en `/categorias/[slug]` (la del panel, en Productos, ya es real desde la Fase 10).
7. ~~Autenticación (Supabase Auth) para el panel.~~ **Hecho (Fase 9)** — `/admin/login`, middleware + verificación server-side. ~~Primer usuario administrador real en producción.~~ **Hecho (Fase 26)** — la cuenta real ya existía en `auth.users` desde antes; le faltaba su fila en `admins`, corregida en ese sprint.
8. ~~CRUD de Productos (con subida de imágenes a `product-images`).~~ **Hecho (Fase 10)** — `/admin/productos`, alta/edición/baja/imágenes, conectado en vivo a Supabase. ~~CRUD de Categorías.~~ **Hecho (Fase 13)** — `/admin/categorias`, alta/edición/baja segura (bloqueada si hay productos asociados), color/ícono/orden de visualización, conectado en vivo a Supabase; la tienda pública terminó de dejar de depender de `config/categories.ts`. ~~Configuración conectada a `business_settings`.~~ **Hecho (Fase 14)** — `/admin/configuracion`, identidad del negocio (nombre, eslogan, descripción, logo, favicon, WhatsApp, contacto, redes, ubicación) editable sin tocar código; Header/Footer/Hero/Contacto ya no dependen de `config/business.ts`/`config/footer.ts` para esos campos (con tres excepciones documentadas — sección 9, Fase 14). ~~Gestión de Pedidos.~~ **Hecho (Fase 15)** — `/admin/pedidos` y `/admin/pedidos/[id]`, con el checkout guardando cada pedido de verdad en `orders`/`order_items`/`customers` (atómico, vía `create_order`) antes de abrir WhatsApp. ~~Gestión de Clientes.~~ **Hecho (Fase 16)** — `/admin/clientes` y `/admin/clientes/[id]`, de solo lectura (los clientes se crean solos, Fase 15), con historial de pedidos y estadísticas calculadas en memoria a partir de esos pedidos. ~~Dashboard con datos reales.~~ **Hecho (Fase 17)** — `/admin`, con resumen, ventas, últimos pedidos, productos más vendidos y desglose por estado, todo calculado en cada carga a partir de Supabase. ~~CRUD de Promociones.~~ **Hecho (Fase 18)** — `/admin/promociones`, campañas de descuento por producto o por categoría (nunca ambos), con estado calculado (Activa/Programada/Vencida/Inactiva); el banner promocional de la Home ya no lee `config/promotion.ts`. ~~Gestión de Inventario.~~ **Hecho (Fase 19)** — `/admin/inventario` y `/admin/inventario/[id]`, con historial de movimientos y descuento/restitución automática de stock integrada con Pedidos vía trigger, sin modificar ese módulo. **Con esto, los 7 módulos planeados del panel administrativo quedan completos.** La estructura, el layout y las rutas quedaron listas desde la Fase 9 — cada módulo reemplazó su `AdminPlaceholder` por contenido real, siguiendo el mismo patrón `services/`+`hooks/`+`components/admin/<módulo>/` que estableció Productos y repitieron Categorías, Configuración, Pedidos, Clientes, Dashboard, Promociones e Inventario. ~~Gestión de Logística (tarifas de envío).~~ **Hecho (Fase 22), octavo módulo no planeado originalmente** — `/admin/logistica`, tarifas de envío por ciudad con cobertura geográfica real de Paraguay (`departments`/`cities`, seed propio), conectadas al checkout público (cálculo automático del costo de envío por ciudad, con "A confirmar" cuando no hay tarifa) y a Pedidos (costo/tarifa usada guardados en el pedido).
9. Documentar el proceso de onboarding de un segundo cliente y ejecutarlo como validación real de la reutilización.

---

## 9. Historial de Sprints

### Fase 1 — Fundación
- **Sprint de arquitectura** (sin código): definición de la estructura de carpetas del proyecto y su justificación. Aprobada como base de todo lo siguiente.
- **Sprint 1**: creación del proyecto base — Next.js 15, TypeScript, Tailwind v4, shadcn/ui, Framer Motion instalados y configurados; Supabase agregado solo como infraestructura (`lib/supabase/client.ts`/`server.ts`, sin auth ni tablas); página temporal en `/` para verificar que todo compilaba.

### Fase 2 — Home
- **Sprint de UX** (sin código): diseño de la experiencia completa de la tienda — listado de páginas de la V1, objetivo/contenido/componentes/interacción de cada una, y el flujo completo del usuario hasta el cierre por WhatsApp.
- **Sprint 2.1**: Header premium (sticky, buscador visual, WhatsApp, carrito, menú mobile) + Hero Section, con `config/site.ts`, `config/navigation.ts`, `config/business.ts` como primeras piezas de configuración.
- **Sprint 2.2**: sección Categorías Destacadas (`FeaturedCategories`), `config/categories.ts`.
- **Sprint 2.3**: sección Productos Destacados (`FeaturedProducts` + `ProductCard` interno en ese momento), `config/products.ts`.
- **Sprint 2.4**: corrección de `formatPrice()` para leer `currency`/`locale` desde `business.ts` en vez de tenerlos hardcodeados; sección Beneficios (`FeaturedBenefits`), `config/benefits.ts`.
- **Sprint 2.5**: Banner Promocional (`PromotionalBanner`), `config/promotion.ts`, con `enabled` controlando si se renderiza sin condicionales en la página.
- **Sprint 2.6**: sección Testimonios (`TestimonialsSection`), `config/testimonials.ts`.
- **Sprint 2.7**: Footer premium (`Footer`), `config/footer.ts`, reutilizando `mainNav` y `businessConfig.whatsapp` en vez de duplicarlos.
- **Sprint 2.8**: auditoría técnica y visual completa de la Home. Se extrajo `lib/motion.ts` para eliminar variants de Framer Motion duplicados, se corrigieron inconsistencias de sombra/timing, y se mejoraron detalles de accesibilidad (aria-labels, aria-hidden en íconos decorativos).

### Fase 3 — E-commerce funcional
- **Sprint 3.1**: motor del catálogo. `ProductCard` extraído a su propio archivo reutilizable; `ProductGrid` (grilla genérica); `Breadcrumb` y `EmptyState` (`components/shared/`); `CatalogToolbar`; página `/productos`.
- **Sprint 3.2**: página de producto (`/productos/[slug]`). `ProductGallery` (múltiples imágenes, cross-fade), `ProductBuyBox`, `RelatedProducts`; `lib/products.ts` con helpers compartidos; `Product.image` pasó a `Product.images: string[]`.
- **Sprint 3.3**: experiencia visual del carrito (`/carrito`). `CartItem`, `OrderSummary`, `EmptyCart`; estado **local temporal** (`useState` en la página) a propósito, como paso previo a Zustand.
- **Sprint 3.4**: estado global del carrito. Instalación de Zustand, creación de `store/cart-store.ts` como única fuente de verdad; conexión de `ProductBuyBox` (agregar), `Header` (badge + link a `/carrito`), `OrderSummary` y la página `/carrito` (ya no usan `useState` local).
- **Sprint 3.5**: Checkout completo (`/checkout`). `CheckoutForm` (orquestador con estado local del formulario), `CustomerInformation`, `ShippingInformation` (con toggle Delivery/Retiro en tienda), `PaymentMethod`, `OrderNotes`, `CheckoutSummary` (conectado al Cart Store); `lib/checkout.ts`; `components/shared/FormField.tsx`.
- **Sprint 3.6**: pantalla de confirmación de pedido (`/checkout/confirmacion`). `store/checkout-store.ts` (segundo store de Zustand, única fuente de verdad del formulario de checkout); `CheckoutForm` migrado de `useState` local a ese store (cambio explícito a un componente aprobado, justificado por la necesidad de que los datos sobrevivan la navegación entre rutas); `OrderConfirmationDetails` (revisión de solo lectura: datos de contacto con botón "Editar datos", entrega, pago, notas condicionales, botones "Volver"/"Confirmar pedido"); `CheckoutSummary` reutilizado sin cambios. El botón "Confirmar pedido" deja preparado —pero sin conectar— el punto de integración con WhatsApp y Supabase.
- **Sprint 3.7**: selección de ubicación exacta para Delivery. `leaflet` + `react-leaflet` instalados (gratuitos, sin API key, sobre tiles de OpenStreetMap — elegidos explícitamente en vez de Google Maps); `LocationPicker.tsx` (componente público que consume `ShippingInformation`) + `LeafletMap.tsx` (implementación real del mapa, separada porque `leaflet` no puede evaluarse en el servidor — se carga con `next/dynamic(..., { ssr: false })`); marcador arrastrable que guarda `latitude`/`longitude` directamente en `store/checkout-store.ts` (acción nueva `setLocation`, sin variables locales); `CheckoutFormValues` ganó los campos `latitude`/`longitude` (`lib/checkout.ts`); `businessConfig.map` (`defaultCenter`, `defaultZoom`) nuevo en `config/business.ts` para que el centro inicial del mapa sea configurable por cliente. El mapa se desmonta por completo (no solo se oculta) con "Retiro en tienda".
- **Sprint 3.8**: cierre del flujo de compra de la V1. `lib/whatsapp.ts` nuevo — `buildOrderWhatsAppMessage()` arma el texto completo del pedido (título, cliente, productos, resumen, entrega con link a Google Maps si hay coordenadas, pago, notas condicionales) y `getOrderWhatsAppUrl()` lo combina con el `getWhatsAppUrl()` ya existente en `lib/utils.ts` (reutilizado tal cual, sin crear un segundo helper de URL). `OrderConfirmationDetails` se simplificó: se fue el aviso placeholder y el `useState` que lo controlaba; el botón "Confirmar pedido" ahora es un link real al mismo patrón `render={<a href={...} target="_blank" rel="noopener noreferrer" />}` que ya usan Header/Footer/Hero/ProductBuyBox para WhatsApp. El carrito no se limpia al confirmar (decisión deliberada, ver sección 7).

### Fase 4 — Documentación
- **Sprint de documentación**: creación de `CLAUDE.md` como fuente oficial de verdad del proyecto.

### Fase 5 — Diseño de datos
- **Sprint 4.0**: diseño conceptual completo de la base de datos (`docs/database-architecture.md`), sin código ni SQL. Se analizó Home/Catálogo/Producto/Carrito/Checkout/Confirmación/WhatsApp para derivar 12 tablas directamente de los `type` y `config/*.ts` ya existentes: `categories`, `products` + `product_images`, `customers`, `orders` + `order_items`, `business_settings`, `banners`, `promotions`, `testimonials`, `benefits`, `admins`. Cada una documentada con propósito, columnas, tipos, PK, FK, índices, restricciones y campos opcionales; relaciones explicadas; estrategia de RLS y encaje con Supabase Auth/Storage; uso previsto desde el futuro panel administrativo; y una sección explícita de decisiones descartadas (sin tabla `tenants`, sin categorías jerárquicas, sin variantes de producto, sin cupones) para no sobre-diseñar. **Diseño aprobado**, sin migraciones todavía.

### Fase 6 — Correcciones funcionales
- **Auditoría funcional** (sin código): recorrida completa de la tienda pública como usuario final — Home, Catálogo, Producto, Carrito, Checkout, Confirmación, WhatsApp, buscador, orden, filtros, categorías, links de Header/Footer, banner, relacionados. Verificado en navegador (no solo en código). Detectó, entre otras cosas ya conocidas (buscador visual, orden/filtros sin lógica, páginas institucionales/`/categorias` inexistentes), un bug no documentado: `ProductCard` renderizaba "Agregar al carrito" sin ningún `onClick`.
- **Sprint 4.1**: corrección puntual de ese bug. `ProductCard.tsx` ahora importa `useCartStore` y llama a `addProduct(product)` en el `onClick` del botón — exactamente la misma acción que ya usaba `ProductBuyBox`, sin crear un segundo método ni tocar `store/cart-store.ts`. Corrige automáticamente los tres puntos donde se usa `ProductCard`: Home → Productos Destacados, Catálogo y Productos Relacionados. Verificado en navegador: badge del Header, cantidades/subtotal del carrito y mensaje de WhatsApp correctos; agregar el mismo producto varias veces suma cantidad en una sola línea, no duplica.
- **Sprint 4.2**: buscador funcional del catálogo, cerrando otro punto de la auditoría. `lib/search.ts` (`searchProducts`) filtra localmente por nombre/categoría/descripción, normalizando mayúsculas y espacios, con soporte de palabras parciales y múltiples términos. `CatalogResults.tsx` (nuevo, `components/storefront/`) conecta ese helper a `?q=` de la URL y decide entre `ProductGrid` o `EmptyState` (con un texto nuevo, `searchEmptyState`, para diferenciar "sin resultados de búsqueda" de "catálogo vacío"). El input del Header (único buscador de la tienda, hasta ahora solo visual) se volvió controlado y navega a `/productos?q=...`, sin usar `useSearchParams` ahí para no forzar renderizado dinámico en toda la app — ese hook solo se usa dentro de `CatalogResults`, envuelto en `Suspense` en `app/productos/page.tsx`. `ProductGrid` no cambió.
- **Sprint 4.3**: ordenamiento funcional del catálogo. `lib/sort.ts` (`sortProducts`, `PRODUCT_SORT_OPTIONS` como única fuente de verdad de valores/etiquetas) ordena por precio asc/desc y nombre A-Z/Z-A; "recientes" es un no-op deliberado (el orden que ya existía, sin campo de fecha en `Product`). `CatalogToolbar` pasó de manejar su propio `Select` sin estado a ser un componente controlado (`sort`/`onSortChange` por props, `onSortChange` con default no-op para que el fallback de `Suspense` en `page.tsx` — un Server Component — no intente pasarle una función, algo que React Server Components no permite). `CatalogResults` aplica `sortProducts` **después** de `searchProducts`, sincronizado con `?sort=` en la URL (combinable con `?q=`). Fix incidental encontrado en la verificación: el `Select` de Base UI necesita el prop `items` (con la forma `{value, label}[]`, que `PRODUCT_SORT_OPTIONS` ya tiene) para que `SelectValue` muestre la etiqueta en vez del valor crudo — sin eso, el combo mostraba literalmente "precio-asc" en vez de "Precio: menor a mayor".
- **Sprint 4.4**: navegación completa por categorías. `app/categorias/page.tsx` (índice) y `app/categorias/[slug]/page.tsx` (productos de esa categoría) nuevos; `lib/categories.ts` (`getCategoryBySlug`, `getProductsByCategory`) resuelve la categoría *antes* de entregarle el array ya acotado a `CatalogResults` — mismo componente que `/productos`, cero catálogo nuevo. `CategoryCard` se extrajo de `FeaturedCategories` a su propio archivo (mismo JSX, sin cambios visuales) para reutilizarla en `/categorias`, corrigiendo de paso el link roto de la Home (`/categoria/[slug]`, ruta que nunca existió, por `/categorias/[slug]`). Dos ajustes necesarios para que la reutilización funcionara de verdad: `CatalogResults` tenía `/productos` hardcodeado al escribir `?sort=` en la URL — se cambió a `usePathname()` para que funcione igual en `/categorias/[slug]`; y el buscador del Header, que solo mantenía el filtro en `/productos`, se generalizó para quedarse también dentro de `/categorias/[slug]` en vez de sacar al usuario de la categoría. Orden del pipeline verificado explícitamente: categoría → búsqueda → orden (`/categorias/tecnologia?q=iphone&sort=precio-desc`).
- **Sprint 4.5**: página oficial de contacto (`app/contacto/page.tsx`), Server Component simple sin `"use client"` ni componente propio nuevo en `storefront/`/`shared/` (su contenido no se reutiliza en ningún otro lado). Título/descripción/etiquetas nuevos en `siteConfig.contactPage` (`config/site.ts`); email, horario y dirección reutilizados tal cual desde `footerConfig.contact` (mismos datos que el Footer, sin duplicarlos); WhatsApp desde `businessConfig.whatsapp` + el mismo `getWhatsAppUrl()` de siempre. Botón "Ver ubicación" nuevo: abre Google Maps con la dirección configurada vía un helper nuevo, `getGoogleMapsUrl()` (`lib/utils.ts`, mismo criterio que `getWhatsAppUrl` — un builder de URL genérico, sin mapa embebido ni API key). El link "Contacto" de `mainNav` (Header y Footer) ya apuntaba a `/contacto` desde antes pero daba 404; con la ruta creada, ambos empezaron a funcionar sin tocar `Header.tsx` ni `Footer.tsx`. Verificado el flujo completo Home → Contacto → Volver → Catálogo → Producto → Carrito → Checkout → Confirmación → WhatsApp, y responsive en desktop/tablet/mobile.
- **Sprint 4.6**: páginas institucionales restantes. `config/policies.ts` (`policies`, `getPolicyBySlug`) — cada política con `title`/`description`/`content` (secciones con `heading` + `body`); `config/faq.ts` (`faqItems`) — array de `{ id, question, answer }`. `app/politicas/[slug]/page.tsx` (nueva, única ruta dinámica para `envios`/`privacidad`/`terminos`, mismo patrón que `/categorias/[slug]`: `generateStaticParams`, `generateMetadata`, `notFound()` para slugs inválidos) y `app/preguntas-frecuentes/page.tsx` (nueva, lista estática `<dl>` de preguntas/respuestas — sin acordeón interactivo ni "use client": no se pidió interactividad y hubiera sido una dependencia nueva sin necesidad real). No se creó un componente `PolicyPage`/`InstitutionalPageLayout` compartido: la ruta dinámica ya resuelve la reutilización entre las 3 políticas, y el bloque de header (breadcrumb + título + descripción) solo se repite en 2 archivos (políticas y FAQ) — por debajo del umbral de 3+ que el proyecto usa para decidir cuándo abstraer, mismo criterio que ya separa `/categorias` de `/categorias/[slug]`. `siteConfig.faqPage` (`config/site.ts`) para el título/descripción de la FAQ. Los 4 links del Footer (`footerConfig.legalLinks`) ya apuntaban a estas rutas exactas — no se tocó `Footer.tsx`, los 404 se resolvieron solo con crear las páginas. Verificado el flujo completo Home → Contacto → Políticas → FAQ → Catálogo → Producto → Carrito → Checkout → Confirmación → WhatsApp, un slug de política inexistente (404 real) y responsive en desktop/tablet/mobile.

### Fase 7 — Localización
- **Sprint 4.7** (pedido al usuario como "Sprint 4.6", renumerado acá porque ese número ya correspondía a las páginas institucionales — ver nota más abajo): adaptación completa de la tienda a Paraguay, puramente de configuración/contenido — cero cambios de arquitectura ni de componentes. `config/business.ts`: `currency: "PYG"`, `locale: "es-PY"`, `whatsapp.number` con código de país `595` (antes `54`, Argentina), `map.defaultCenter`/`defaultZoom` recentrados en Villarrica, Guairá (`{ lat: -25.75, lng: -56.4333 }`, zoom 14). `formatPrice()` (`lib/utils.ts`) no cambió una línea: al leer `currency`/`locale` desde `businessConfig` (decisión ya tomada en el Sprint 2.4), automáticamente empezó a mostrar `Gs. 850.000` en vez de `$ 850.000` en toda la tienda — Home, catálogo, producto, carrito, checkout, confirmación y mensaje de WhatsApp. `config/footer.ts`: `contact.address` → `"Villarrica, Guairá, Paraguay"` (antes Buenos Aires), reflejado automáticamente en el Footer y en `/contacto` (que ya reutilizaba ese mismo dato desde el Sprint 4.5, sin tocar ningún componente). `config/testimonials.ts`: las 6 ciudades de los testimonios, todas argentinas, pasaron a ciudades paraguayas (Villarrica, Asunción, Ciudad del Este, Encarnación, Coronel Oviedo, Caaguazú). `config/policies.ts` y `config/faq.ts`: reemplazadas las menciones a CABA/GBA (zonas de envío rápido) por Villarrica/Asunción, y "pesos argentinos (ARS)" por "guaraníes (PYG)" en Términos y condiciones. `docs/database-architecture.md`: actualizados únicamente los valores de ejemplo de `currency`/`locale` en la tabla `business_settings` (de `ARS`/`es-AR` a `PYG`/`es-PY`) — sin tocar columnas, tipos ni relaciones, no es un cambio de arquitectura. `lib/whatsapp.ts` no se tocó: no tenía ninguna referencia argentina hardcodeada, arma el mensaje enteramente a partir de `businessConfig`/`siteConfig`/los datos del formulario, así que heredó automáticamente moneda y ubicación correctas. **Decisión que quedó fuera de este sprint, marcada como observación**: los montos numéricos de `config/products.ts` (por ejemplo, iPhone 14 a `850000`) no se reescalaron — como guaraníes, esos montos son bajos para un precio real de mercado en Paraguay (equivalen a poco más de US$100), pero el sprint pedido listaba explícitamente qué archivos tocar (`business.ts`, Footer, Contacto, mapa, WhatsApp) y no mencionaba `config/products.ts`; cambiar los precios habría sido una decisión de contenido/negocio no pedida, no una adaptación de configuración. Verificado con Playwright que no queda ninguna referencia a Argentina/ARS/es-AR/Buenos Aires/CABA/GBA en Home, Categorías, Catálogo, Producto, Carrito, Checkout, Confirmación, Contacto, Políticas, FAQ ni en el mensaje de WhatsApp; mapa de Leaflet en el checkout centrado en Villarrica; responsive verificado en desktop/tablet/mobile.

### Fase 8 — Infraestructura de Supabase
- **Sprint de infraestructura** (pedido al usuario como "Fase 5", renumerado acá porque ese número ya correspondía al diseño de datos, Sprint 4.0 — ver nota más abajo): preparación completa de Supabase para el futuro panel administrativo, sin tocar la tienda pública, componentes, Checkout, Carrito, WhatsApp ni empezar el panel en sí. `supabase/` inicializado con `supabase init` (CLI oficial), no armado a mano. 4 migraciones versionadas en `supabase/migrations/`, con timestamps reales del CLI (`supabase migration new <nombre>`), en vez de la numeración `001_`/`002_` sugerida — se usó la convención nativa de Supabase (única que el propio CLI reconoce y ordena) por ser la opción "más profesional" que el sprint dejaba habilitada explícitamente:
  1. `create_schema.sql` — las 12 tablas de `docs/database-architecture.md`, con UUIDs (`gen_random_uuid()`), FKs, `CHECK` constraints, defaults, y un trigger + función compartida (`set_updated_at()`) para mantener `updated_at` en cada `UPDATE` (el documento aprobado no especifica cómo se mantiene esa columna; un trigger es el patrón estándar, no una decisión de negocio). También define `is_admin()`, el helper de RLS reutilizado por las migraciones 3 y 4.
  2. `create_indexes.sql` — todos los índices listados en el documento (únicos, parciales, compuestos), separados del esquema para que cada migración se pueda auditar por separado.
  3. `create_storage.sql` — bucket `product-images` (público de lectura, 5 MB por archivo, solo imágenes) + sus policies de Storage (lectura pública, escritura solo admins). Sin subida de imágenes todavía — eso es del panel.
  4. `create_rls_policies.sql` — RLS de las 12 tablas: lectura pública en `categories`/`products`/`product_images`/`banners` activos/`promotions` activas y vigentes/`testimonials` activos/`benefits` activos/`business_settings`; sin lectura ni escritura pública en `customers`/`orders`/`order_items` (la policy de "el checkout puede insertar un pedido" queda para cuando se conecte de verdad, fuera de alcance de este sprint); escritura de catálogo/contenido solo si `is_admin()`; `admins` con self-select/self-update únicamente.
  
  **Tres adiciones puntuales sobre el documento aprobado, documentadas explícitamente (no un rediseño):** se agregó `created_at`/`updated_at` a `product_images` y a `benefits`, y `updated_at` a `admins` — el documento no las listaba en esas tres tablas, pero este mismo sprint pide "cada tabla debe incluir... timestamps" y son columnas de auditoría, no una columna de negocio inventada. `order_items` es la única tabla sin timestamps a propósito: es un snapshot inmutable de una línea de pedido, nunca se actualiza.

  `supabase/seed.sql` (no una quinta migración numerada — ver justificación en sección 5) carga: `business_settings` desde `config/business.ts` + `config/site.ts` + `config/footer.ts`; `categories` desde `config/categories.ts`; `products` + `product_images` desde `config/products.ts` (`category_id` resuelto por slug de categoría, nunca hardcodeado); `benefits` desde `config/benefits.ts`; `banners` desde `config/promotion.ts`; `testimonials` desde `config/testimonials.ts` (ya con las ciudades paraguayas del Sprint 4.7). Deliberadamente sin sembrar `customers`/`orders`/`order_items` (no existe ningún pedido real hoy, el carrito vive solo en memoria), `promotions` (sin equivalente 1:1 en el código actual, ya aclarado en el documento aprobado) ni `admins` (su `id` debe ser un `auth.users.id` real, que no existe sin login).

  **Verificación — limitación real del entorno, no omitida:** el sandbox donde se escribió este sprint no tiene Docker (`supabase start` falla explícitamente pidiéndolo) ni las credenciales para hacer `supabase link` contra el proyecto real ya configurado en `.env.local` (solo hay `NEXT_PUBLIC_SUPABASE_ANON_KEY`, que no alcanza para correr migraciones). Se hizo en su lugar una revisión manual exhaustiva de las 4 migraciones y el seed (FKs, tipos, constraints, orden de dependencias) y se confirmó con `supabase db lint` el mensaje de error esperado por falta de conexión, no un error de sintaxis. **Actualización (Fase 9):** las migraciones ya fueron aplicadas al proyecto real (fuera de esta conversación) — la Fase 9 arrancó con esa premisa y `/admin/login` ya autentica de verdad contra ese mismo proyecto.

  Confirmado explícitamente: la tienda pública, sus componentes, Checkout, Carrito y WhatsApp no se tocaron — todo el trabajo de este sprint vive exclusivamente dentro de `supabase/`.

### Fase 9 — Panel Administrativo: estructura y autenticación
- **Sprint de inicio del panel**: arrancó el Panel Administrativo, solo autenticación y estructura, sin CRUD, sin Dashboard con datos, sin conexión a las tablas de negocio. `/admin/login` con Supabase Auth real (`supabase.auth.signInWithPassword`), contra el proyecto ya configurado en `.env.local` — verificado con un intento de login fallido real: la request llega a `https://<proyecto>.supabase.co/auth/v1/token`, responde `400`, y el formulario muestra "Email o contraseña incorrectos." Protección de `/admin/*` en dos capas: `src/middleware.ts` (invoca `lib/supabase/middleware.ts` → `updateSession()`, matcher limitado a `/admin/:path*` para no tocar la tienda pública) redirige en el edge, y `app/admin/(panel)/layout.tsx` repite la verificación con `supabase.auth.getUser()` del lado del servidor antes de renderizar nada — la única de las dos que Supabase considera no salteable. Si ya hay sesión y se visita `/admin/login`, el middleware redirige a `/admin` en vez de mostrar el formulario de nuevo.

  **Bug encontrado y corregido durante la verificación, no en el pedido original:** al levantar `/admin/login` por primera vez, la página se renderizaba envuelta en el `Header`/`Footer` de la tienda pública — porque `app/layout.tsx` es un único root layout que Next.js aplica a *todo* lo que cuelga de `app/`, sin excepción. La única solución soportada por Next.js es la técnica oficial de *multiple root layouts*: se movieron las 8 rutas de la tienda pública (Home, `/productos`, `/categorias`, `/carrito`, `/checkout`, `/contacto`, `/politicas`, `/preguntas-frecuentes`) a un route group nuevo, `app/(storefront)/`, con su propio `layout.tsx` (el mismo contenido que antes tenía `app/layout.tsx`, reubicado — Header, Footer, fuentes); `app/admin/layout.tsx` pasó a ser la otra raíz, con las mismas fuentes y el mismo `globals.css`, pero sin Header ni Footer. Cero cambio visual o funcional en la tienda pública — mismas URLs, mismos componentes, mismo contenido; se verificó explícitamente con Playwright que Home/Catálogo/Producto/Carrito/Checkout siguen funcionando idénticos después del movimiento (agregar al carrito → carrito → checkout, sin errores de consola).

  Layout del panel (`components/admin/`): `AdminShell.tsx` (estado de UI del drawer mobile, sin store de Zustand — es un booleano local de un solo layout, no estado global) compone `AdminSidebar.tsx` (tres variantes responsive con un único listado de nav interno: fija con labels en desktop `lg:`, colapsada a solo íconos en tablet `md:`, y un `Sheet` — el mismo componente que ya usa el Header del storefront para su menú mobile — por debajo de `md:`) y `AdminHeader.tsx` (nombre del negocio desde `siteConfig.name`, botón de menú mobile, avatar con la inicial del email + el email de la sesión + logout). `LogoutButton.tsx` es el único punto que llama `supabase.auth.signOut()`, reutilizado por Sidebar y Header. Las 7 páginas (`/admin`, `/admin/productos`, `/admin/categorias`, `/admin/pedidos`, `/admin/clientes`, `/admin/promociones`, `/admin/configuracion`) usan `AdminPlaceholder.tsx`, que reutiliza `EmptyState` (`components/shared/`) para el cuadro "Próximamente" en vez de reimplementar el borde punteado.

  **Decisión explícita sobre "NO conexión con tablas":** el Header muestra el email de la sesión de Auth (no requiere ninguna consulta a una tabla), no el `full_name`/`role` de la tabla `admins` — aunque esa tabla ya existe (Fase 8) y tiene una policy propia (`admins_self_read`) pensada exactamente para este caso, se optó por la lectura más conservadora de "NO conexión con tablas" en este sprint, dejando esa consulta (una sola fila, la propia) para cuando el panel empiece a necesitar `full_name`/`role` de verdad.

  Verificado con Playwright: las 7 rutas protegidas devuelven 307 a `/admin/login` sin sesión; `/admin/login` responde 200; intento de login fallido muestra el error real; layout/Sidebar/Header revisados visualmente en desktop/tablet/mobile (con la verificación de auth momentáneamente desactivada solo para la captura de pantalla, revertida antes de terminar y re-confirmada); las 10 rutas de la tienda pública responden 200 después del movimiento a `(storefront)`; `pnpm lint` y `pnpm exec tsc --noEmit` limpios. Sin crear la primera cuenta de administrador real todavía (`admins` sigue vacía) — es un paso manual del próximo sprint, no de este.

### Fase 10 — Panel Administrativo: módulo de Productos
- **Sprint 5.2** (nombre pedido por el usuario; en el historial de `CLAUDE.md` es la Fase 10): primer módulo funcional del panel, conectado en vivo a Supabase — nada de datos hardcodeados. `/admin/productos` pasó de `AdminPlaceholder` a `ProductsTable.tsx`: imagen principal, nombre, categoría, precio (`formatPrice()`, reutilizado tal cual de `lib/utils.ts`), stock, Estado y Destacado, con buscador (nombre/categoría), tres criterios de orden y paginación real de 10 en 10 — toda esa lógica vive dentro de `hooks/useProducts.ts`, no en archivos propios tipo `lib/search.ts`/`lib/sort.ts` (un solo listado no justifica separarlos todavía). Botón "Nuevo producto" → `/admin/productos/nuevo`; ícono de editar → `/admin/productos/[id]/editar`; ambas rutas renderizan el mismo `ProductForm.tsx` (decide alta o edición según si recibe `productId`), con los 10 campos pedidos y un administrador de imágenes propio (`ProductImagesField.tsx`).

  **Arquitectura en capas, tal como se pidió:** `services/products.ts` + `services/categories.ts` + `services/storage.ts` son las únicas funciones que le hablan a Supabase (ningún componente arma un `supabase.from(...)` por su cuenta); `hooks/useProducts.ts` + `hooks/useProduct.ts` + `hooks/useCategories.ts` conectan esos servicios a React (`useState`/`useEffect`, loading/error); `components/admin/products/` es la UI que solo conoce los hooks. Dos componentes nuevos de shadcn, agregados con `pnpm dlx shadcn@latest add` (nunca a mano): `alert-dialog` (`DeleteProductDialog.tsx` — confirmación antes de borrar, controlada con `open`/`onOpenChange` porque `AlertDialogAction` no cierra el diálogo solo) y `switch` (toggles de Activo/Destacado en el formulario).

  **Migración nueva, fuera del diseño original de la Fase 8:** `products` no tenía columna para "Estado (Activo/Inactivo)" ni para una "Descripción completa" distinta de `short_description` — ambas pedidas explícitamente por este sprint. Se agregó `supabase/migrations/..._products_admin_fields.sql` (`is_active boolean default true`, `description text` nullable) y se actualizó la policy pública de `products` al mismo patrón "público ve solo activos, admin ve todo" que ya usan `categories`/`banners`/`testimonials`/`benefits`.

  **A diferencia de la Fase 8, esta vez se pudo aplicar y verificar todo en vivo:** el CLI de Supabase en este entorno ya estaba logueado y linkeado al proyecto real (`supabase migration list` confirmó que las 4 migraciones de la Fase 8 ya estaban aplicadas), así que la migración nueva se aplicó de verdad con `supabase db push`. Se descubrió además que el seed de la Fase 8 nunca se había cargado al proyecto real (todas las tablas en cero) — se corrió `supabase db query --linked -f supabase/seed.sql`, quedando 6 categorías/6 productos/18 imágenes/4 beneficios/1 banner/6 testimonios, tal como se documentó como pendiente en la Fase 8. Se creó además una cuenta de administrador de prueba (`admin-test@regishop.local`, vía la API de administración de Supabase Auth con el usuario ya pre-confirmado, más su fila correspondiente en `admins` con `role = 'owner'`) para poder probar el flujo real de punta a punta — la tabla `admins` seguía vacía desde la Fase 9.

  **Dos bugs reales encontrados y corregidos durante la verificación, no presentes en el pedido original:** (1) `useProduct("")` (llamado incondicionalmente desde `ProductForm` en modo creación, para no romper las reglas de hooks) mandaba una consulta a Supabase con un id vacío, que Postgres rechazaba (`invalid input syntax for type uuid`) — el hook ahora se salta la consulta por completo si `id` es `""`. (2) El `Select` de categoría pasaba `value={categoryId || undefined}`, lo que hacía que Base UI lo tratara como no controlado en el primer render y controlado después (warning de React); se corrigió a `value={categoryId}` (string vacío desde el inicio, siempre controlado).

  **Verificado en vivo contra el proyecto real, con Playwright y la cuenta de prueba:** login real; listado de los 6 productos sembrados; búsqueda y orden; alta de un producto con imagen subida a Storage; edición reflejada en la tabla; eliminación con diálogo de confirmación, verificada además por SQL directo (fila y archivo de Storage efectivamente borrados, sin huérfanos); RLS confirmado con requests anónimas reales (`SELECT` ve los productos activos, `INSERT` responde 401, `UPDATE`/`DELETE` no afectan ninguna fila); responsive en desktop/tablet/mobile; `pnpm lint` y `pnpm exec tsc --noEmit` limpios; la tienda pública (10 rutas) y el flujo de compra completo siguen funcionando sin cambios. La cuenta de prueba (`admin-test@regishop.local` / `TestAdmin123!`) quedó creada en el proyecto real — a borrar o reutilizar según se prefiera.

### Fase 11 — Tienda pública conectada a Supabase
- **Sprint 5.3**: la tienda pública dejó de leer `config/products.ts`/`config/categories.ts` para Home, Productos Destacados, Catálogo, página de Producto, Productos Relacionados y Categorías — las seis áreas pedidas explícitamente. Nuevo `services/storefront/` (`products.ts`, `categories.ts`), separado de `services/products.ts`/`categories.ts` del panel (Fase 10) porque usa el cliente de **servidor** (`lib/supabase/server.ts`, no el de browser) y devuelve los mismos `Product`/`Category` de siempre — ningún componente de `storefront/` cambió su forma de recibir datos, solo dejaron de venir de un `import` fijo para venir de un `await` resuelto por la página. `FeaturedProducts`/`FeaturedCategories` (antes importaban `config/*.ts` directo, siendo Client Components) pasaron a recibir `products`/`categories` por prop, resueltos por la Home (Server Component). `RelatedProducts` (ya era Server Component) solo cambió su línea de datos por una versión `async`. Sin `hooks/` para el storefront: ningún componente de la tienda fetchea sus propios datos (a diferencia del panel), así que esa capa no aplica acá — ver sección 5.

  **`generateStaticParams` eliminado** de `/productos/[slug]` y `/categorias/[slug]`: con productos que pueden crearse en cualquier momento desde el panel, pre-generar una lista fija de slugs en build time dejó de tener sentido. Ambas rutas ya renderizan dinámicamente en cada request de todos modos, porque `lib/supabase/server.ts` usa `cookies()` (Dynamic API de Next.js) — mismo mecanismo que ya fuerza a `admin/(panel)/layout.tsx` a ser dinámico desde la Fase 9.

  **Un ajuste real a `lib/search.ts`, no solo cosmético:** `searchProducts()` importaba `categories` directo de `config/categories.ts` para poder buscar por nombre de categoría. Se cambió a recibirlas por parámetro (`searchProducts(products, query, categories)`) — sigue siendo una función pura, pero ya no tiene una dependencia oculta a `config/`; `CatalogResults` ahora recibe `categories` como prop (resuelta por la página) y se la pasa.

  **`config/products.ts` y `config/categories.ts` no se tocaron, tal como pidió el sprint** — siguen ahí, sin usarse, como fallback/documentación para un sprint futuro que decida su destino.

  **RLS hace todo el trabajo de visibilidad**, sin lógica adicional en el código: el storefront lee con el rol `anon` (sin sesión), que la policy `products_public_read_active`/`categories_public_read_active` (Fase 10) ya limita a `is_active = true`. Esto significa que desactivar o eliminar un producto desde el panel lo esconde automáticamente de la tienda pública — no hizo falta escribir ningún filtro extra del lado de la aplicación.

  **Verificado en vivo contra el proyecto real, con dos navegadores en paralelo (uno con sesión de admin, otro anónimo, sin cookies):** producto nuevo creado desde el panel → aparece de inmediato en Home (si es Destacado), Catálogo, su propia página (`/productos/[slug]`, con precio correcto) y su categoría; edición de precio y de stock reflejada al instante en la tienda; desactivación → desaparece de Catálogo/Home para el visitante anónimo y su URL directa da 404, mientras el panel lo sigue mostrando con badge "Inactivo" (confirma que RLS, no una condición de UI, es quien decide); eliminación → desaparece por completo, para ambos. Búsqueda (por nombre y por nombre de categoría), orden, `/categorias` y `/categorias/[slug]`, agregar al carrito, checkout completo y mensaje de WhatsApp verificados sin regresiones funcionales — mismo diseño, mismos estilos, cero cambios de UX. `pnpm lint` y `pnpm exec tsc --noEmit` limpios.

  **Efecto secundario real, no un bug, documentado explícitamente:** como `/productos` y `/categorias/[slug]` ahora son dinámicas (por el `cookies()` de arriba), cada cambio de `?q=`/`?sort=` (que antes era una re-renderización puramente en el cliente, instantánea) ahora dispara un round-trip real al servidor para traer la página de nuevo — se observó un delay de unos cientos de milisegundos donde antes era inmediato. Es el costo esperado de que los datos sean reales y estén siempre al día (exactamente lo que pedía la verificación de este sprint); optimizarlo con caché/ISR queda como posible trabajo futuro, no resuelto acá para no arriesgar mostrar datos desactualizados.

  **Decisión deliberada, fuera de alcance de este sprint:** `ProductCard.tsx` (protegido, "no tocar" desde varios sprints) y `ProductBuyBox.tsx` siguen resolviendo el nombre/color de acento de la categoría con un `categories.find()` contra `config/categories.ts`, no contra Supabase. No genera ningún bug hoy porque no existe todavía un CRUD de Categorías (Fase 8/10 lo dejaron pendiente) — las dos fuentes tienen exactamente los mismos valores. El día que se conecte la gestión de categorías del panel, esto pasa a ser una inconsistencia real y hay que resolverlo (lo más simple: que la página resuelva la categoría del producto y se la pase por prop, en vez de que el componente la busque él mismo).

### Fase 12 — Corrección: las fotos de producto subidas desde el Panel no se mostraban en la tienda pública
- **Bug reportado por el usuario**, detectado después de la Fase 11: una foto subida a un producto desde `/admin/productos` aparecía correctamente en Supabase Storage, pero nunca se veía en la tienda pública — ni en Home, ni en el Catálogo, ni en la página del producto.

  **Auditoría completa realizada antes de tocar código**, punto por punto: (1) el upload (`ProductImagesField.tsx` + `services/storage.ts`) sube el archivo a `product-images/<productId>/<uuid>-<nombre>` y guarda en `product_images.url` el resultado de `getPublicUrl()` — una URL pública completa, no una ruta relativa; (2)/(3)/(4) confirmado con una consulta directa al proyecto real (`supabase db query --linked`) que la tabla `product_images` efectivamente contiene esa URL completa (`https://<proyecto>.supabase.co/storage/v1/object/public/product-images/...`) para un producto subido de verdad desde el panel; (5) `services/storefront/products.ts` (Fase 11) lee `product_images(url, display_order)`, ordena y mapea a `Product.images: string[]` sin transformar ni truncar la URL — esta capa **nunca tuvo el bug**; (6)/(7)/(8) acá apareció la causa real: `ProductCard.tsx` y `ProductGallery.tsx` (y también `CategoryCard.tsx`, sin relación con este bug) tenían, desde que se escribieron en los Sprints 3.1/3.2/4.4, un comentario explícito diciendo *"product.image(s) queda reservado para cuando haya foto real del cliente — hasta entonces se muestra un placeholder"*, pero ese momento nunca se conectó: los tres componentes renderizaban **incondicionalmente** el bloque decorativo (gradiente + inicial), sin ningún `<img>`/`<Image>` en su JSX, sin importar qué hubiera en `product.images`. `ProductBuyBox.tsx` nunca tuvo lógica de imagen (ni real ni placeholder) — la foto del producto la resuelve enteramente `ProductGallery`, que vive al lado en la misma página; (9) la URL pública se construye correctamente (`supabase.storage.from(BUCKET).getPublicUrl(path)`, `services/storage.ts`) y coincide byte a byte con lo guardado en la base; (10)/(11) como no existía ningún `<Image>`/`<img>` en el camino, este punto era inaplicable — pero se detectó además que `next.config.ts` no tenía **ningún** `images.remotePatterns`, así que aun arreglando el punto anterior, `next/image` habría rechazado la URL de Supabase por dominio no permitido.

  **Por qué el upload funcionaba pero la tienda no mostraba la foto:** el pipeline de datos (Storage → `product_images` → `services/storefront/products.ts` → `Product.images`) estuvo siempre correcto — de hecho, ya lo verificó la Fase 11 sin encontrar problemas ahí. El bug estaba enteramente en la capa de **renderizado**: los componentes de la tienda pública fueron escritos en un momento en que no existía ninguna foto real todavía, y mostrar el placeholder era una decisión correcta en ese momento — simplemente nunca se completó la otra mitad ("y mostrar la foto real cuando exista"), porque hasta la Fase 10 no había forma de subir una.

  **Archivos modificados:**
  - `next.config.ts`: agrega `images.remotePatterns` con el hostname de Supabase Storage, derivado en runtime de `NEXT_PUBLIC_SUPABASE_URL` (`new URL(...).hostname`) — **no hardcodea** el project ref de RegiShop, para no romper el flujo de "un cliente nuevo = nuevo `.env.local`, sin tocar código" (sección 2).
  - `ProductCard.tsx`: agrega `primaryImage = product.images.find(isRealImageUrl)` y renderiza `next/image` cuando existe una URL real; si no, cae al mismo placeholder decorativo de siempre (mismas clases, mismo timing de hover). `onError` en la imagen hace fallback automático al placeholder si la URL guardada dejara de ser válida (archivo borrado en Storage por fuera del panel, etc.).
  - `ProductGallery.tsx`: mismo criterio aplicado al visor principal (con cross-fade de Framer Motion ya existente, ahora envolviendo la foto real en vez de siempre el placeholder) y a cada miniatura — cada índice decide por separado si tiene foto real o placeholder, para tolerar productos con una mezcla de fotos reales y legado.
  - `isRealImageUrl(url)` (una función idéntica y deliberadamente duplicada en ambos archivos, no extraída a `lib/`): `url?.startsWith("http")`. Es la señal que distingue una foto real de Storage (siempre URL absoluta) de las 5 rutas de placeholder que trajo el seed original de la Fase 8 (`/products/iphone-14-1.jpg`, etc. — rutas relativas que nunca existieron como archivo en `public/`, confirmado por un `Glob` vacío). No es un hardcodeo de un producto ni de una URL puntual: es una regla general que funciona igual para cualquier producto de cualquier cliente futuro. No se extrajo a `lib/` porque solo dos archivos la usan hoy — mismo criterio que ya aplica el proyecto para no sobre-abstraer con dos casos (sección 6, principio 6).
  - `CategoryCard.tsx`, `ProductBuyBox.tsx`, Carrito, Checkout y WhatsApp **no se tocaron**: el bug reportado era específicamente sobre fotos de producto; no existe todavía ninguna forma de subir una foto de categoría desde el panel (CRUD de Categorías sigue pendiente, sección 13), y Carrito/Checkout/WhatsApp nunca mostraron imágenes (confirmado en la auditoría) — no había nada que corregir ahí.

  **Por qué esta solución es la correcta arquitectónicamente:** mantiene una única transformación correcta de punta a punta (Supabase → `services/storefront/products.ts` → `Product.images` → componente), sin agregar una segunda ruta de datos ni un tipo nuevo. No hardcodea ninguna URL ni ningún producto: `isRealImageUrl` es una regla estructural (¿es una URL absoluta o no?), no un caso especial de "Tacos" ni de este proyecto. Preserva el 100% del comportamiento visual ya aprobado para productos sin foto real todavía (mismo placeholder, mismas transiciones) — la foto real es un *agregado*, no un reemplazo condicionado a nada más que "¿existe una URL real?". Requiere tocar `ProductCard.tsx` y `ProductGallery.tsx`, ambos protegidos en la sección 12: se documenta acá explícitamente, como exige la sección 10, regla 2 — no había forma de que el bug ("las fotos no se ven") se resolviera sin tocar el componente responsable de mostrar la foto.

  **Verificado en vivo contra el proyecto real, con Playwright:** se usó "Tacos" (producto real cargado por el usuario entre sprints, con una foto subida de verdad al bucket) como caso de prueba. Confirmado que la foto real se muestra en Home (Productos Destacados), Catálogo, la página del producto (`/productos/tacos`, galería principal + miniaturas) y su categoría (`/categorias/hogar`) — la misma imagen en los cinco lugares. Confirmado en paralelo que un producto sin foto real (`iPhone 14`, todavía con las rutas de seed) sigue mostrando el placeholder decorativo sin ningún cambio visual, en las mismas cinco áreas — cero regresión para el resto del catálogo. Responsive verificado en desktop (1440px), tablet (768px) y mobile (390px): la foto se adapta correctamente al contenedor en los tres tamaños. Carrito, checkout y el mensaje de WhatsApp probados agregando "Tacos" (navegación client-side, no `page.goto`, porque ninguno de los dos stores tiene persistencia — un `page.goto` directo a `/carrito` los reinicia por diseño, no es un bug) — sin errores de consola ni regresiones. `pnpm lint` y `pnpm exec tsc --noEmit` limpios.

### Fase 13 — Panel Administrativo: módulo de Categorías + fin de la dependencia de `config/categories.ts`
- **Sprint 5.4**: segundo módulo funcional del panel (después de Productos, Fase 10), con el mandato explícito de dejar el catálogo *completo* (productos y categorías) administrable desde Supabase, sin ninguna dependencia activa restante de archivos de configuración.

  **Esquema:** `categories` no tenía columna para el ícono pedido por el formulario. Se agregó `supabase/migrations/..._categories_admin_fields.sql` (`icon_name text not null default 'package'`) — mismo patrón exacto que `benefits.icon_name` (Fase 8): un identificador de texto, nunca un componente de React, resuelto a un ícono real únicamente en `components/admin/categories/category-icons.ts` (un mapa curado de ~14 íconos de lucide-react relevantes para comercio — genérico, tecnología, hogar, belleza, deportes, ropa, alimentos, regalos, automotor, mascotas, librería, arte). `default 'package'` (mismo criterio que `products.is_active` en la Fase 10) backfilleó automáticamente las 6 categorías ya sembradas sin dejar ninguna en null. El resto del esquema de `categories` (`accent_color`, `display_order`, `is_active`, y el índice único `categories_slug_key`) ya existía desde la Fase 8 — no hizo falta ninguna migración de RLS: `categories_admin_read_all`/`_insert`/`_update`/`_delete` (gateadas por `is_admin()`) ya estaban completas desde la Fase 8, a diferencia de `products` que necesitó su propia migración de RLS recién en la Fase 10.

  **`services/categories.ts` pasó de solo-lectura a CRUD completo**, en el mismo archivo (no uno nuevo): `AdminCategory` ganó `productCount` (calculado con un embed de PostgREST, `products ( count )`, sobre la misma query que ya trae el resto de columnas — no una segunda consulta por fila), `createCategory`/`updateCategory`/`deleteCategory`, `isSlugTaken` (chequeo proactivo de unicidad para el formulario) y `getCategoryProductCount`. `listCategories()` sigue siendo la misma función que ya consumía `ProductForm.tsx` para su selector de categoría — se extendió de forma aditiva, sin cambiar su comportamiento por defecto, así que Productos (fuera de alcance de este sprint) no se tocó ni se rompió.

  **Borrado seguro, la parte más delicada del sprint:** `products.category_id` ya tenía `on delete restrict` desde el diseño original de la Fase 8 — Postgres iba a rechazar el borrado de una categoría con productos incluso sin ningún código nuevo. Sobre esa garantía ya existente, `deleteCategory()` agrega dos capas más: (1) un chequeo proactivo de `getCategoryProductCount()` antes de siquiera intentar el `delete`, para poder mostrar "tiene N productos asociados, reasignalos o eliminalos primero" en vez de un error crudo de Postgres; (2) un catch del código `23503` (foreign_key_violation) como red de seguridad final, por si otro admin crea un producto justo entre el chequeo y el borrado (carrera real en un panel con más de un usuario). `DeleteCategoryDialog.tsx` usa el `productCount` que ya trae el listado para decidir, sin otra consulta, si mostrar el diálogo destructivo normal o el bloqueo directo (sin ofrecer nunca un botón "Eliminar" condenado a fallar).

  **`components/admin/categories/`** (`CategoriesTable.tsx`, `CategoryForm.tsx`, `DeleteCategoryDialog.tsx`) replica el mismo patrón de capas que dejó Productos en la Fase 10 (`components → hooks → services → Supabase`), con las mismas convenciones: slug autogenerado del nombre hasta que se edita a mano, `Switch` para Activa, `AlertDialog` para confirmar el borrado. Sin campo de imagen en el formulario -- el sprint no lo pidió (`image_url` sigue existiendo en la tabla, sin gestión desde el panel todavía). Color: `<input type="color">` nativo emparejado con un `<Input>` de texto para el hex, sin agregar ninguna librería nueva. Paginación: **preparada, no implementada** (instrucción explícita del sprint) -- `useCategories.ts` sí tiene búsqueda y las 6 variantes de orden (nombre/cantidad de productos/orden de visualización, cada una asc/desc), pero no recorta la lista por página; el catálogo de categorías de una tienda chica no lo justifica todavía, mismo criterio que ya usó `/productos` (storefront) para reservar su propio contenedor de paginación futura sin implementarla.

  **Eliminación de la dependencia activa de `config/categories.ts` en la tienda pública**, el segundo objetivo del sprint: `ProductCard.tsx`, `ProductGallery.tsx`, `ProductBuyBox.tsx`, `ProductGrid.tsx`, `FeaturedProducts.tsx` y `RelatedProducts.tsx` tenían un `import { categories } from "@/config/categories"` y hacían su propio `categories.find(...)` — los seis pasaron a recibir `categories: Category[]` por prop, resuelta por la página (Home, `/productos`, `/categorias/[slug]`, `/productos/[slug]`) exactamente igual que ya hacía `CatalogResults` desde la Fase 11 para la búsqueda. `CategoryCard.tsx` y `FeaturedCategories.tsx` no necesitaron tocarse: ya recibían la categoría/las categorías resueltas por prop desde la Fase 11, nunca hacían un `.find()` propio. La página de producto (`/productos/[slug]/page.tsx`) ganó un `Promise.all` nuevo (`getPublicProductBySlug` + `getPublicCategories`, en paralelo) para poder pasar `categories` a `ProductGallery`/`ProductBuyBox`/`RelatedProducts`. `lib/categories.ts` perdió `getCategoryBySlug` (buscaba en el array de config): confirmado por grep que no tenía ningún call site desde que `getPublicCategoryBySlug` (Fase 11) la reemplazó — código muerto, eliminado; `getProductsByCategory` (función pura, sin ninguna dependencia a config) se mantuvo intacta.

  **Lo que se preservó a propósito:** el tipo `Category` (`config/categories.ts`) se sigue importando como *tipo* (`import type { Category }`) en varios archivos (`ProductCard`, `ProductGallery`, `ProductBuyBox`, `ProductGrid`, `FeaturedProducts`, `RelatedProducts`, `CatalogResults`, `lib/search.ts`, `services/storefront/categories.ts`) — eso no es una dependencia activa, se borra por completo en tiempo de compilación, y es exactamente el mismo tratamiento que ya recibió `Product` en la Fase 11 (el tipo se queda en `config/`, es el contrato; los datos en tiempo de ejecución vienen de Supabase). `config/products.ts`/`config/categories.ts` en sí no se tocaron ni se eliminaron, tal como se viene sosteniendo desde la Fase 11.

  **Excepción explícita, fuera de alcance:** `CartItem.tsx` (Carrito) y `CheckoutSummary.tsx` (Checkout) siguen con `import { categories } from "@/config/categories"` y su propio `.find()` — son las únicas dos referencias activas que quedan en todo el proyecto. El sprint pidió explícitamente "NO modificar Checkout" y "NO modificar Carrito" como restricción de más alta prioridad que el objetivo de "eliminar definitivamente cualquier dependencia restante"; ante ese conflicto directo, se priorizó la instrucción explícita y se documenta acá la excepción en vez de tocarlos en silencio. Queda como pendiente explícito (sección 13) para el día que se autorice tocar esos dos componentes.

  **Verificado en vivo contra el proyecto real, con Playwright y dos navegadores en paralelo (admin autenticado + anónimo):** categoría creada ("Ferretería", con nombre/slug/descripción/orden) aparece en la tabla del panel y en `/categorias` para el visitante anónimo; edición de la descripción reflejada; desactivación confirmada con RLS -- el anónimo deja de verla, el panel la sigue mostrando; reactivada para continuar la prueba; producto nuevo creado usando esa categoría, verificado visible en Home (por ser Destacado), Catálogo, su propia página de producto (con "FERRETERÍA ..." como categoría, resuelto 100% desde Supabase -- esta categoría **nunca existió** en `config/categories.ts`, la prueba más directa posible de que ya no hay ningún camino que dependa del archivo de config) y su página de categoría; segundo producto en la misma categoría creado para poblar "Productos relacionados", confirmado visible ahí. Borrado: una categoría vacía se eliminó sin fricción (diálogo destructivo normal); intentar borrar la categoría con 2 productos asociados mostró el bloqueo explícito ("tiene 2 productos asociados... reasigná o eliminá esos productos primero"), sin ningún botón "Eliminar" disponible. Validación de slug duplicado probada contra un slug ya existente (`tecnologia`): error inline, sin redirigir. Búsqueda, orden y navegación por categorías de la tienda pública verificados sin regresiones (mismos resultados que antes del sprint para los datos ya existentes). Todos los datos de prueba (categoría, productos) se limpiaron del proyecto real al finalizar. `pnpm lint` y `pnpm exec tsc --noEmit` limpios en todo el proyecto.

### Fase 14 — Panel Administrativo: módulo de Configuración General
- **Sprint 5.5**: tercer módulo funcional del panel, con el objetivo de que un cliente pueda personalizar la identidad completa de su tienda (nombre, eslogan, descripción, logo, favicon, WhatsApp, contacto, redes sociales, ubicación) sin tocar código, todo almacenado en la fila única de `business_settings`.

  **Esquema:** `business_settings` (fila única, id=1, Fase 8) no tenía columnas para eslogan, logo, favicon, redes sociales nombradas ni país/ciudad. Se agregó `supabase/migrations/..._business_settings_admin_fields.sql`: `tagline`, `logo_url`, `favicon_url`, `instagram_url`, `facebook_url`, `tiktok_url`, `map_country`, `map_city` -- las 8, `text` nullable (a diferencia de `icon_name`/`is_active` en sprints anteriores, no hay un "default razonable" para un logo o un usuario de TikTok). Se decidió agregar `instagram_url`/`facebook_url`/`tiktok_url` como columnas nombradas en vez de generalizar el `social_links jsonb` que ya existía desde la Fase 8 (nunca tuvo un consumidor real: `Footer.tsx` siempre leyó `footerConfig.social`, un array estático con íconos-componente, no ese jsonb) -- el sprint pidió explícitamente 3 redes fijas, no una lista dinámica, así que 3 campos de texto son más simples y más "profesionales" en el formulario que reconstruir un editor de lista genérico que nadie pidió. RLS no necesitó ninguna migración nueva: `business_settings_public_read` (sin filtro) y `business_settings_admin_update` (gateada por `is_admin()`) ya cubrían exactamente lo necesario desde la Fase 8 -- y como es una fila única creada por el seed, no hay (ni debe haber) policies de insert/delete.

  **Storage: bucket nuevo, `branding`, en vez de reutilizar `product-images`.** Documentado explícitamente porque el sprint pedía decidir y justificar: logo y favicon son archivos singleton por negocio (no una galería por fila, como si fueran fotos de un producto), sin relación con ningún producto puntual, y con un ciclo de vida propio (nunca deberían borrarse en cascada junto con nada). Mismas policies que `product-images` (lectura pública, escritura solo `is_admin()`), pero límite de 2 MB (no 5) y tipos MIME que incluyen SVG e ICO -- casos reales para un logo o un favicon que `product-images` nunca necesitó permitir.

  **`services/settings.ts`** (nuevo): `getBusinessSettings()`/`updateBusinessSettings()` (solo lectura/actualización, nunca insert/delete -- la fila es fija) más `uploadBrandingAsset()`/`deleteBrandingAsset()` para el bucket `branding`. A diferencia de Productos (que separa `services/storage.ts` de `services/products.ts`), acá la lógica de Storage vive en el mismo archivo que la lógica de la fila: un módulo con un único formulario y dos archivos (logo, favicon) no justificaba un segundo archivo de servicio solo para eso -- separar habría sido sobreingeniería para este tamaño de necesidad.

  **`hooks/useSettings.ts`** (nuevo): mucho más simple que `useProducts`/`useCategories` -- sin búsqueda, sin orden, sin paginación, sin borrado (una fila no tiene esas operaciones). Solo `fetch` + `save`.

  **`components/admin/settings/`**: `SettingsForm.tsx` (un solo `<form>`, un solo submit, organizado en 5 `SettingsSection` visuales -- "tarjetas" es una decisión de presentación, no 5 formularios ni 5 llamadas a Supabase) y `BrandingAssetField.tsx` (subida de un único archivo con preview local antes de guardar, reutilizado dos veces -- logo y favicon -- para no duplicar esa lógica). Mismo patrón que `ProductImagesField` (Fase 10): nada se sube a Storage hasta que se confirma el guardado; si se reemplaza un logo/favicon existente, el archivo anterior se borra recién después de subir el nuevo.

  **Íconos de red social como componentes propios, no de lucide-react**: `TikTokIcon` se sumó a `components/icons.tsx`, mismo criterio que `InstagramIcon`/`FacebookIcon` (Sprint 2.7) -- lucide-react no tiene íconos de marca. `Footer.tsx` arma la lista de redes a mostrar filtrando cuáles de las 3 URLs vinieron con valor, en vez de una lista fija de 2 (Instagram/Facebook) como antes.

  **Favicon dinámico, un problema real encontrado durante la verificación:** `app/favicon.ico` (el archivo especial de Next.js) genera **siempre** su propio `<link rel="icon">`, sin que `generateMetadata`'s `icons` lo reemplace -- son dos mecanismos independientes. Con el archivo estático todavía en `app/`, un favicon subido desde el panel convivía con el de siempre y el navegador no elegía de forma confiable cuál mostrar (confirmado inspeccionando el `<head>`: aparecían **dos** `<link rel="icon">`). Se resolvió moviendo `app/favicon.ico` a `public/favicon-default.ico` -- ya no es un archivo especial de Next.js, es un estático más, y `generateMetadata` pasa a ser la única fuente del ícono (`favicon_url` si existe, `/favicon-default.ico` si no). `admin/layout.tsx` (que no tenía ninguna referencia propia a favicon, dependía del archivo global) ganó su propio `metadata.icons` estático apuntando al mismo archivo -- el panel no necesita reflejar el branding de cada negocio.

  **Migración parcial y documentada de la tienda pública**, el núcleo del sprint: `Header.tsx`, `Footer.tsx`, `Hero.tsx` (solo el link de WhatsApp) y `/contacto/page.tsx` dejaron de importar `config/business.ts`/`config/footer.ts` para nombre, eslogan, descripción, WhatsApp, contacto y redes sociales -- reciben `settings: BusinessSettings` resuelto desde Supabase (`(storefront)/layout.tsx` para Header/Footer, la Home para Hero, la propia página para Contacto). El logo se muestra con `next/image` cuando `logo_url` existe, con el mismo criterio placeholder-vs-real ya establecido en la Fase 12 para fotos de producto (fallback al nombre en texto si no hay logo o si falla la carga). `ProductBuyBox.tsx` (ya tocado en Fases 12/13) también pasó a recibir `settings` por prop para su botón de WhatsApp.

  **Tres exclusiones deliberadas, no un descuido -- documentadas explícitamente porque el sprint las pedía verificar ("Moneda", "Locale", "Checkout", "Mensajes de WhatsApp") sin poder tocar los archivos que las implementan:**
  1. `formatPrice()` (`lib/utils.ts`) sigue leyendo `businessConfig.currency`/`locale`. Cambiar su firma para recibirlos por parámetro habría obligado a tocar cada call site, incluidos `CartItem.tsx`, `CheckoutSummary.tsx` y `OrderSummary.tsx` -- Carrito y Checkout, explícitamente protegidos.
  2. `lib/whatsapp.ts` (`buildOrderWhatsAppMessage`/`getOrderWhatsAppUrl`) sigue leyendo `businessConfig.whatsapp.number` y `siteConfig.checkoutPage.*`. Es, literalmente, "el envío por WhatsApp" que el sprint pidió no tocar -- ni una línea de este archivo se modificó.
  3. El centro del mapa de Delivery (`businessConfig.map.defaultCenter`/`defaultZoom`, consumido por `ShippingInformation`/`LocationPicker` dentro de `/checkout`) sigue siendo estático. Migrarlo requería editar la ruta `/checkout` y sus componentes de formulario, ambos nombrados explícitamente como protegidos en la sección 12.

  Consecuencia real, verificada y documentada, no oculta: cambiar el número de WhatsApp en `/admin/configuracion` actualiza de inmediato los botones "Contactar por WhatsApp" del Header, el Hero, el Footer, la página de Producto y Contacto -- pero el mensaje final de WhatsApp que se manda al confirmar un pedido en Checkout sigue yendo al número viejo, hasta que un sprint futuro autorice explícitamente tocar `lib/whatsapp.ts`. Se documenta como pendiente en la sección 13.

  **`getGoogleMapsUrl()` (`lib/utils.ts`) ganó un segundo parámetro opcional**, `coords?: {lat, lng}`: `/contacto` arma ahora un link de Google Maps más preciso con `business_settings.map_default_lat/lng` en vez de solo buscar la dirección como texto. Cambio aditivo (el único otro comportamiento que tenía, buscar por `address`, sigue funcionando si no se pasan coordenadas) -- no rompe nada.

  **Verificado en vivo contra el proyecto real, con Playwright:** cambio de nombre del negocio, número de WhatsApp, dirección y horario, más subida de un logo y un favicon de prueba (PNG de 1x1) -- guardado confirmado con mensaje de éxito en el formulario. Del lado del visitante anónimo: nombre nuevo visible en Header (como logo, reemplazando el texto) y Footer; dirección y horario nuevos visibles en Footer y en `/contacto`; botón de WhatsApp del Header y de la página de producto (`/productos/tacos`) usando el número nuevo; favicon nuevo confirmado como el único `<link rel="icon">` en el `<head>` (antes de la corrección, aparecían dos); redes sociales (Instagram/Facebook ya cargadas, TikTok agregado en la prueba) visibles en el Footer; link de "Ver ubicación" en Contacto usando las coordenadas de `business_settings`. Quitar el logo confirmado con fallback correcto al nombre en texto. Regresión explícita en Carrito/Checkout: precio sigue mostrando `Gs.` correctamente (moneda sin tocar), y el mensaje de WhatsApp de un pedido completo (agregar "Tacos", ir a Checkout, confirmar) siguió usando el número **viejo** -- comportamiento esperado, documentado arriba, no un bug. Responsive verificado en desktop/tablet/mobile sobre `/admin/configuracion`. Todos los datos y archivos de prueba (nombre, WhatsApp, dirección, horario, logo, favicon, TikTok) se restauraron/limpiaron en el proyecto real al finalizar, incluyendo el archivo huérfano en el bucket `branding`. `pnpm lint` y `pnpm exec tsc --noEmit` limpios en todo el proyecto.

### Fase 15 — Gestión de Pedidos: checkout registrado en Supabase + panel
- **Sprint 5.6**: a diferencia de los tres sprints anteriores (Categorías, Configuración), este sí requería tocar Checkout — el objetivo era, literalmente, que el checkout dejara de terminar únicamente en un mensaje de WhatsApp. `orders`/`order_items`/`customers` ya estaban diseñadas desde la Fase 8 (`docs/database-architecture.md`, sección 4.3/4.4), con RLS 100% cerrada a `is_admin()` — este sprint las puso a trabajar de verdad por primera vez.

  **La pregunta central del sprint era de integridad: ¿cómo garantizar que nunca quede un `orders` sin sus `order_items`, si el cliente (browser) que dispara la creación no puede abrir una transacción SQL de verdad contra PostgREST?** La respuesta: una función de Postgres, `create_order` (`supabase/migrations/..._create_order_function.sql`), invocada vía `supabase.rpc()`. Una función de Postgres se ejecuta dentro de una única transacción implícita — si cualquier `insert` de adentro falla, Postgres revierte automáticamente todo lo que la función ya había hecho en esa misma llamada. Es `security definer` (corre con privilegios propios, no los de quien la invoca), lo que permite dos cosas a la vez: (1) que el rol `anon` pueda crear un pedido sin necesitar policies de `insert` directas en las tres tablas (que hubieran permitido insertar cualquier cosa: un total inventado, un `status` que no sea `'pending'`, filas sueltas), y (2) que RLS siga 100% cerrada al público para lectura/escritura directa — la función es el único punto de entrada, y controla exactamente qué se puede crear (`status` no es ni siquiera un parámetro: todo pedido nuevo entra en `'pending'`, el default de la tabla). `customers` se resuelve con `on conflict (phone) do update` (upsert por teléfono, tal como ya preveía el diseño de la Fase 8) — un cliente que ya compró antes no se duplica.

  **Verificación real de la atomicidad, no solo teórica:** se probó primero con una llamada SQL directa a `create_order` con datos válidos (creó cliente + pedido + línea correctamente) y después con `quantity: -1` (viola el `CHECK` de `order_items`) — el intento falló como se esperaba, y una consulta posterior confirmó que **no quedó ningún registro**, ni el pedido ni el cliente que se había insertado un instante antes de que fallara la línea. Esa es la garantía real que pedía el sprint, no una promesa de diseño sin comprobar.

  **`services/orders.ts` (admin, nuevo)**: `listOrders()`/`getOrderById()` (con un `join` a `customers` y a `order_items` vía embeds de PostgREST) y `updateOrderStatus()` — sin alta ni baja, los pedidos los crea únicamente el checkout público. `services/storefront/orders.ts` (nuevo, distinto): `createOrder()`, la única escritura pública hacia las tres tablas, con el cliente de **browser** (no el de servidor que usa el resto de `services/storefront/`) porque se dispara desde un click en `OrderConfirmationDetails.tsx`, ya en el cliente — mismo motivo por el que el panel siempre usó el cliente de browser para sus escrituras.

  **`hooks/useOrders.ts`/`useOrder.ts` (nuevos)**: mismo patrón que Productos/Categorías — búsqueda + filtro por estado + tres criterios de orden (fecha/total/estado) en memoria, paginación preparada pero no implementada (mismo criterio que Categorías, Fase 13). El cambio de estado es optimista en ambos (se refleja al toque en la UI y se revierte si Supabase lo rechaza), tanto desde la tabla (`useOrders.changeOrderStatus`) como desde el detalle (`useOrder.changeStatus`) — un solo componente, `OrderStatusSelect.tsx`, se reutiliza en los dos lugares.

  **`components/admin/orders/`**: `OrdersTable.tsx` (buscador, filtro por estado, tres criterios de orden, estado editable en línea sin abrir el detalle), `OrderDetailView.tsx` (cliente, productos con cantidades/precios/subtotales, total, entrega + dirección, ubicación con link a Google Maps si hay coordenadas — reutilizando `getGoogleMapsUrl()`, Fase 14 —, notas, pago, fecha, número de pedido, y el mismo selector de estado), `order-status.ts` (mapa `status` → variante de `Badge`, capa de presentación, mismo criterio que `category-icons.ts`). Los 6 estados (Pendiente, Confirmado, En preparación, Enviado, Entregado, Cancelado) ya existían en el `CHECK` de `orders.status` desde la Fase 8 (`ready_or_shipped` internamente, pensado para cubrir tanto "listo para retirar" como "enviado" según el método de entrega) — no hizo falta ninguna migración de esquema para los estados en sí, solo para la función `create_order`.

  **Integración con Checkout, el archivo más delicado del sprint**: se tocó **un solo componente**, `OrderConfirmationDetails.tsx` — ni `CheckoutForm.tsx`, ni `ShippingInformation.tsx`, ni `PaymentMethod.tsx`, ni ningún otro paso del formulario. El botón "Confirmar pedido" pasó de ser un link estático (`<a href={whatsappUrl} target="_blank">`) a un `onClick` que: 1) previene la navegación por defecto, 2) abre una pestaña en blanco **de forma síncrona**, dentro del mismo gesto de click (evita que el navegador bloquee un `window.open()` disparado después de un `await`, que varios tratan como popup no solicitado), 3) llama a `createOrder()`, y 4) si tiene éxito, redirige esa pestaña ya abierta a la URL real de WhatsApp (`tab.location.href = whatsappUrl`) — construida exactamente igual que siempre, sin tocar `lib/whatsapp.ts`. Un booleano local (`orderSaved`) evita crear un segundo pedido si el usuario vuelve a clickear "Confirmar pedido" después de un envío exitoso (por ejemplo, si cerró la pestaña de WhatsApp sin mandar el mensaje) — en ese caso, el click reabre WhatsApp sin volver a llamar a `createOrder()`.

  **Si falla el guardado, no se bloquea la compra**: si `createOrder()` lanza un error (por ejemplo, Supabase caído un instante), no se abre WhatsApp automáticamente, pero se muestra el error junto con un link explícito para "continuar de todas formas por WhatsApp, sin guardar el pedido" — decisión deliberada: "no romper el flujo existente" (instrucción explícita del sprint) se interpretó como que una falla transitoria de backend nunca debe poder impedirle a un cliente completar su compra por WhatsApp, aunque ese pedido puntual quede sin registrar. El carrito **sigue sin limpiarse** tras confirmar (sin cambios respecto a como ya funcionaba desde el Sprint 3.8) — el sprint no lo pidió, y limpiarlo automáticamente ahora que sí hay confirmación real de guardado es una decisión que queda para un sprint futuro (sección 13).

  **`lib/utils.ts` ganó `formatDate()`** (nuevo, para las fechas de `/admin/pedidos`), mismo criterio que `formatPrice()` (lee `businessConfig.locale`, la misma excepción ya documentada en la Fase 14) — sin crear un archivo aparte para una sola función.

  **Verificado en vivo contra el proyecto real, con Playwright, de punta a punta:** (1) a nivel SQL directo: `create_order` con datos válidos creó cliente + pedido + línea correctamente; con una cantidad inválida, falló y no dejó ningún registro parcial (atomicidad confirmada explícitamente, no asumida). (2) A través de la tienda pública real: agregar "Tacos" al carrito, completar el checkout (retiro en tienda, transferencia, con notas), confirmar el pedido — se abrió exactamente una pestaña nueva, con la URL de WhatsApp correcta (mismo número, mismo formato de mensaje de siempre, incluyendo las notas cargadas), sin quedar el botón trabado en "Guardando..." ni mostrar ningún error. Confirmado en Supabase que el cliente, el pedido (`status: 'pending'`, `total`/`subtotal` correctos) y la línea de producto quedaron creados con los datos exactos del formulario. (3) Del lado del panel: el pedido apareció de inmediato en `/admin/pedidos` (buscándolo por nombre), con todos los datos de la tabla correctos; el detalle (`/admin/pedidos/[id]`) mostró cliente, producto, notas y método de entrega correctamente; se cambió el estado a "Confirmado" desde el detalle, y **tras refrescar la página** el estado siguió mostrando "Confirmado" — confirma que el cambio se persistió de verdad en Supabase, no solo en el estado local de React. (4) Regresión: las rutas core de la tienda (`/`, `/productos`, `/categorias`, `/carrito`, `/checkout`, `/contacto`) siguen respondiendo 200 sin errores de consola. Todos los datos de prueba (cliente, pedido, línea) se limpiaron del proyecto real al finalizar. `pnpm lint` y `pnpm exec tsc --noEmit` limpios en todo el proyecto.

### Fase 16 — Gestión de Clientes: consulta y estadísticas sobre pedidos ya registrados
- **Sprint 5.7**: el sprint más contenido desde que el panel administrativo arrancó (Fase 9) — cero migraciones, cero cambios de esquema, cero policies nuevas. `customers` ya existía completa desde la Fase 8 y ya se poblaba sola desde la Fase 15 (`create_order` hace `upsert` por teléfono); el objetivo era enteramente construir una capa de consulta sobre datos que ya existían.

  **"Los clientes NO se crearán manualmente" no requirió ninguna decisión de diseño nueva**: ya estaba resuelto. `create_order` (Fase 15) inserta en `customers` con `on conflict (phone) do update` cada vez que se confirma un pedido -- este sprint simplemente confirmó (con una prueba real, no solo con lectura de código) que ese mecanismo ya crea automáticamente al primer pedido y reconoce al mismo cliente en los siguientes, sin duplicarlo.

  **`services/customers.ts` (nuevo, 100% solo lectura)**: `listCustomers()` (para la tabla) y `getCustomerById()` (para el detalle) -- sin `create`/`update`/`delete`, a propósito: este módulo no gestiona clientes, los consulta. Reutiliza el tipo `OrderStatus` de `services/orders.ts` con un `import` de solo lectura -- no se tocó ningún archivo de Pedidos.

  **Cómo se calculan las estadísticas, sin guardarlas**: para el listado, `listCustomers()` trae cada cliente con sus pedidos embebidos (`orders(total, created_at)`, vía PostgREST) en una sola consulta, y calcula `orderCount`/`totalSpent`/`lastOrderAt` en JavaScript sobre ese array. Para el detalle, `getCustomerById()` trae el cliente y **todos** sus pedidos completos (para el historial) y calcula sobre ese mismo array: `totalSpent` (suma de `total`), `orderCount` (longitud del array), `averageTicket` (`totalSpent / orderCount`, con guarda para no dividir por cero), `firstOrderAt`/`lastOrderAt` (extremos del array, ya ordenado por fecha descendente desde la consulta). Se evaluó explícitamente una vista de Postgres (`customer_stats`) para hacer esto más escalable, y se descartó por ahora -- ver sección 6 para la justificación completa. Ninguna de estas cifras se guarda en ninguna tabla ni columna: se recalculan en cada carga, tal como pidió el sprint.

  **`primaryAddress` ("Dirección principal"), un campo que no existe en `customers`**: el diseño aprobado de la Fase 8 decidió explícitamente que la dirección es un dato del *pedido*, no del *cliente* (un mismo cliente puede pedir a direcciones distintas). Para poder mostrar "Dirección principal" sin contradecir esa decisión ya aprobada, se deriva del pedido de Delivery más reciente del cliente (buscado sobre el array de pedidos, ya ordenado por fecha descendente) -- si todos sus pedidos fueron Retiro en tienda, queda en `null` y la UI lo explica en vez de mostrar un campo vacío.

  **`components/admin/customers/`**: `CustomersTable.tsx` (buscador, 4 criterios de orden con ambas direcciones cada uno, paginación preparada pero no implementada -- mismo criterio que Categorías y Pedidos) y `CustomerDetailView.tsx` (información general, tarjetas de estadísticas, historial de pedidos con `Badge` de estado reutilizando `getOrderStatusVariant()` de `components/admin/orders/order-status.ts` -- import de solo lectura, ver "por qué"). Sin `CustomerForm.tsx`: no hay nada que crear ni editar.

  **Navegación Cliente → Pedido implementada; Pedido → Cliente, deliberadamente no**: cada fila del historial de un cliente linkea a `/admin/pedidos/[id]` (código nuevo, dentro del módulo de Clientes, no toca Pedidos). La dirección inversa hubiera requerido agregar `customer_id` a lo que expone `services/orders.ts` y un link nuevo en `OrderDetailView.tsx` -- ambos archivos de "Pedidos", con instrucción explícita de "NO modificar Pedidos" este sprint. Se prioriza la instrucción explícita, exactamente el mismo criterio ya aplicado en sprints anteriores ante tensiones equivalentes (Fase 13, Fase 14). Documentado como pendiente explícito en la sección 13.

  **Verificado en vivo contra el proyecto real:** se usó `create_order` (la misma función RPC de la Fase 15) para simular pedidos reales sin pasar por toda la UI de checkout cada vez -- 3 pedidos con el mismo teléfono (montos distintos, un Delivery y dos Retiro en tienda) y 1 pedido con un teléfono distinto. Confirmado por SQL directo que el cliente del primer grupo no se duplicó (una sola fila en `customers`, 3 pedidos asociados, total sumado correcto). En la UI: la tabla de `/admin/clientes` mostró ambos clientes sin duplicados, con pedidos/total/última compra correctos; el detalle del primer cliente mostró las 5 estadísticas correctas (total gastado, cantidad de compras, ticket promedio, primera y última compra), la dirección principal derivada correctamente del único pedido de Delivery (a pesar de no ser el pedido más reciente en general), y el historial con los 3 pedidos; la navegación Cliente → Pedido llevó al pedido correcto, con los datos del cliente visibles en su detalle. Responsive verificado en desktop/tablet/mobile. Todos los datos de prueba se limpiaron del proyecto real al finalizar. `pnpm lint` y `pnpm exec tsc --noEmit` limpios en todo el proyecto.

### Fase 17 — Dashboard Administrativo: resumen del negocio calculado en cada carga
- **Sprint 5.8**: el sprint con más restricciones de "NO modificar" hasta la fecha (Productos, Categorías, Configuración, Pedidos, Clientes, Checkout, Carrito y WhatsApp, los ocho a la vez) — coherente con que el Dashboard es, por naturaleza, una capa de agregación por encima de todo lo demás, no un módulo con datos propios. Cero migraciones: todo lo que el Dashboard necesita ya existía en `orders`/`order_items`/`customers`/`products`/`categories`.

  **`services/dashboard.ts` (nuevo, 100% solo lectura, sin tocar ningún otro `services/*.ts`)**: agrega datos de 5 tablas de forma completamente independiente, con el mismo criterio ya sentado en la Fase 16 (`services/customers.ts` leyendo `orders` directamente en vez de pasar por `services/orders.ts`). Reutiliza únicamente `OrderStatus`/`ORDER_STATUSES` de `services/orders.ts`, vía `import` de solo lectura. Una sola función pública, `getDashboardData()`, que hace `Promise.all` de todas las consultas independientes y devuelve un único objeto con las 5 secciones que pidió el sprint.

  **Cómo se calcularon las métricas, sin guardar ninguna:**
  - **Resumen superior** (total de pedidos, pendientes, entregados, total de clientes, productos activos, categorías activas): los primeros tres salen de una única consulta a `orders` (`select("status, total, created_at")`, sin filtrar, todas las filas) reducida en memoria; clientes/productos activos/categorías activas son tres `count` independientes (`{ count: "exact", head: true }`, con `.eq("is_active", true)` para las dos últimas).
  - **Ventas** (total, ticket promedio, última venta): calculadas sobre la **misma** consulta a `orders` de arriba -- no se repite la consulta tres veces. Ventas totales y ticket promedio suman **todos** los pedidos sin importar su estado (incluidos los cancelados), por consistencia con el mismo criterio ya aplicado a "total gastado" en `services/customers.ts` (Fase 16) -- ver sección 6 para la justificación completa.
  - **Últimos 10 pedidos**: una consulta aparte (`orders` + `customers` embebido, `order by created_at desc limit 10`) -- distinta de la de arriba porque necesita el nombre del cliente, que no vale la pena traer para los pedidos que no se van a mostrar.
  - **Productos más vendidos**: se trae `order_items` completo (`product_id, product_name, quantity, subtotal`) y se agrupa en memoria por `product_id` (o por `product_name` si el producto ya fue borrado y `product_id` quedó `null`, `on delete set null`, Fase 8), sumando cantidad e ingreso por grupo, y se toman los 5 primeros por cantidad vendida. Mismo criterio ya evaluado y descartado en la Fase 16 (vista de Postgres vs. agregación en memoria) -- se optó de nuevo por memoria, por la misma razón de escala.
  - **Estados de pedidos**: se cuenta cada uno de los 6 estados sobre la misma consulta de `orders` ya traída, incluyendo los que tienen 0 pedidos (para que la estructura del array esté siempre completa, lista para alimentar un gráfico futuro sin que ese componente tenga que rellenar los estados faltantes).

  **`components/admin/dashboard/`**: `SummaryCard.tsx` (una tarjeta reutilizada 9 veces -- las 6 del resumen y las 3 de ventas -- con un ícono, un valor, una descripción y un slot opcional `trend` para la "variación futura preparada" que pidió el sprint, sin que nadie le pase un valor real todavía); `RecentOrdersTable.tsx` (reutiliza `getOrderStatusVariant` de `components/admin/orders/order-status.ts`, import de solo lectura, con link a `/admin/pedidos/[id]`); `TopProductsList.tsx` (lista con barra de progreso vía CSS, sin navegación al producto -- no la pidió el sprint para esta sección, a diferencia de "Últimos pedidos"); `OrderStatusBreakdown.tsx` (barras de CSS puro, "preparada para un gráfico futuro" sin serlo todavía -- sin ninguna librería de gráficos); `Dashboard.tsx` (orquestador de `/admin`, compone las 5 secciones a partir de un solo `useDashboard()`).

  **Verificado en vivo contra el proyecto real:** se crearon 4 pedidos de prueba vía `create_order` (4 clientes distintos, 3 productos -- dos de ellos empatados en cantidad vendida, uno único -- y 4 estados de pedido distintos: pendiente, confirmado, entregado, cancelado) sobre una base que partía en 0 pedidos/clientes y 6 productos/categorías activos (sin cambios). Las 9 cifras del Dashboard coincidieron exactamente con lo calculado a mano: total de pedidos (4), pendientes (1), entregados (1), total de clientes (4), productos activos (6), categorías activas (6), ventas totales (suma de los 4, incluido el cancelado), ticket promedio (ventas totales / 4), última venta (fecha del pedido más reciente). "Productos más vendidos" mostró correctamente el empate entre los dos productos con la misma cantidad vendida. "Estados de pedidos" mostró los 4 estados con pedidos y los 2 sin pedidos en 0. La navegación desde "Últimos pedidos" llevó al pedido correcto en `/admin/pedidos/[id]`. Se verificó explícitamente que `/admin/productos`, `/admin/categorias`, `/admin/pedidos`, `/admin/clientes` y `/admin/configuracion` siguen respondiendo 200 sin errores de consola -- sin regresiones. Responsive verificado en desktop/tablet/mobile. Todos los datos de prueba se limpiaron del proyecto real al finalizar. `pnpm lint` y `pnpm exec tsc --noEmit` limpios en todo el proyecto.

### Fase 18 — Gestión de Promociones: sexto módulo del panel + banner dinámico
- **Sprint 5.9**: el sprint con más restricciones de "NO modificar" de todo el proyecto hasta la fecha (Productos, Categorías, Configuración, Pedidos, Clientes, Dashboard, Checkout, Carrito y WhatsApp, los nueve a la vez) — y, a la vez, el único sprint de módulo nuevo que también rediseña una pieza de la tienda pública (`PromotionalBanner.tsx`), no solo el panel. Cero migraciones: `promotions` (con su restricción `promotions_target_check`, su índice compuesto `(starts_at, ends_at)` y su RLS `promotions_public_read_active`) ya existía completa desde la Fase 8, sin un solo consumidor real hasta ahora.

  **Una pregunta de diseño resuelta antes de escribir código, no sobre la marcha**: `docs/database-architecture.md` (sección 4.7) ya advertía, desde que se aprobó el esquema, que `promotions` es "la única tabla del diseño que no tiene un equivalente 1:1 en el código" y que conectar una promoción activa con el precio mostrado de un producto ("actualizando `products.on_sale`/`old_price`, vía un job o al guardar") quedaba **explícitamente** como una decisión de implementación futura, fuera de aquel sprint. El Sprint 5.9 tampoco la resuelve: construye la gestión de campañas (alta/edición/baja + banner), no la conexión "promoción → precio recalculado de catálogo" -- eso seguiría requiriendo tocar `products`, `ProductCard` y `ProductBuyBox`, y "NO modificar Productos" es explícito este sprint. Se prioriza la instrucción explícita y se documenta la decisión (ver sección 6), en vez de inventar una conexión no pedida y arriesgar romper el catálogo.

  **`lib/promotions.ts` (nuevo)**: `formatDiscountLabel()` (texto del descuento, "-15%"/"-Gs. 50.000") y `getPromotionStatus()` (Activa/Programada/Vencida/Inactiva, combinando `is_active` con las fechas de vigencia). Es la primera vez que una función de `lib/` se reutiliza tanto en el panel (columna "Descuento" de la tabla) como en la tienda pública (badge del banner) dentro del mismo archivo -- hasta ahora `lib/` o era exclusivamente storefront, o exclusivamente compartía un *tipo*, no lógica ejecutada de los dos lados.

  **`services/promotions.ts` (admin) y `services/storefront/promotions.ts` (público)**: mismo patrón admin/storefront que ya separa Productos, Categorías, Configuración. Sin restricción de borrado (a diferencia de Categorías): nada referencia `promotions.id` como clave foránea, así que `deletePromotion()` es un `delete` simple, sin chequeo previo. El lado público, `getActivePromotion()`, no repite el filtro de vigencia -- RLS (`promotions_public_read_active`, sin cambios desde la Fase 8) ya garantiza que una fila vencida o programada nunca llegue a la respuesta que ve `anon`.

  **`hooks/usePromotionTargets.ts` (nuevo), no reutilizar `useProducts()`/`useCategories()` para los combos de `PromotionForm`**: `useProducts()` (Fase 10) pagina de a 10 -- usarlo tal cual dejaría fuera del selector de producto cualquiera más allá de la primera página. `useCategories()` sí devuelve la lista completa, pero para tratar ambos combos igual se resolvieron los dos en un hook nuevo que llama directamente a `listProducts()`/`listCategories()` (los servicios, no los hooks) -- import de solo lectura, cero cambios a Productos ni Categorías.

  **`components/admin/promotions/`**: `PromotionsTable.tsx` (buscador, 3 criterios de orden -- fecha de inicio/descuento/estado --, paginación preparada pero no implementada, mismo criterio que Categorías/Pedidos/Clientes), `PromotionForm.tsx` (un selector explícito "¿A qué aplica esta promoción?" con dos radios -- categoría completa o producto específico -- que limpia el otro campo al cambiar, reflejando en la UI la restricción `promotions_target_check` ya existente en la base desde la Fase 8, más una validación de fechas -- `ends_at > starts_at` -- que espeja `promotions_period_check`), `DeletePromotionDialog.tsx` (mismo patrón que Productos/Categorías, sin guard de integridad referencial porque no aplica acá), `promotion-status.ts` (mapa de estado calculado a label + variante de `Badge`, capa de presentación, mismo criterio que `order-status.ts`/`category-icons.ts`).

  **`PromotionalBanner.tsx` rediseñado, no solo reconectado**: a diferencia de Header/Footer/Hero (Fase 14, que solo cambiaron de dónde leían los mismos campos), acá varios campos de `config/promotion.ts` (`subtitle`, `buttonText`, `backgroundVariant`, `badge`) no tienen ninguna columna equivalente en `promotions` -- se resolvieron así: `badge` ahora es el descuento real (`formatDiscountLabel()`) en vez de un texto fijo ("PROMOCIÓN"); `subtitle` usa `description` si la promoción la tiene, o cae a una oración genérica armada con el nombre del producto/categoría si no; `buttonText` ("Ver oferta") y `backgroundVariant` (siempre oscuro) quedaron hardcodeados a propósito -- son copy genérico de UI y una decisión de diseño fija, no datos de negocio, así que no aplica la regla de "no hardcodear métricas". El placeholder decorativo (blobs + ícono Sparkles) no cambió: `promotions` tampoco tiene una columna de imagen todavía. Sin promoción vigente, el banner no se renderiza -- mismo comportamiento que `promotionConfig.enabled = false` antes de este sprint. `config/promotion.ts` no se tocó ni se eliminó -- mismo trato que `config/products.ts`/`categories.ts`, fallback/documentación sin consumidores activos.

  **Verificado en vivo contra el proyecto real, con Playwright:** creación de una promoción por categoría (Tecnología, 15%, vigente 7 días) y una por producto (iPhone 14, Gs. 50.000, vigente 7 días) -- ambas aparecieron correctamente en la tabla con tipo/descuento/fechas/estado "Activa". Edición del valor de descuento (15% → 25%) reflejada de inmediato. Desactivar → estado "Inactiva"; reactivar → estado "Activa" de nuevo. Eliminar la promoción de producto la quitó de la tabla. En la tienda pública: el banner de la Home mostró siempre la promoción vigente más reciente, con el badge de descuento real y el botón "Ver oferta" apuntando a `/productos/iphone-14` (mientras existió esa promoción) y después a `/categorias/tecnologia` (tras eliminarla). Prueba específica de vigencia: se insertó una promoción ya vencida directamente por SQL (`ends_at` en el pasado) -- el panel la mostró correctamente como "Vencida", pero **nunca apareció en el banner público**, confirmando que RLS filtra automáticamente lo vencido sin ningún chequeo de fechas en el código de la aplicación. Regresión: `/admin` (Dashboard), `/admin/productos`, `/admin/categorias`, `/admin/pedidos`, `/admin/clientes`, `/admin/configuracion`, y las rutas core de la tienda (`/`, `/productos`, `/categorias`, `/carrito`, `/checkout`, `/contacto`) siguen respondiendo 200 sin errores de consola. Responsive verificado en desktop/tablet/mobile. Todos los datos de prueba se limpiaron del proyecto real al finalizar. `pnpm lint` y `pnpm exec tsc --noEmit` limpios en todo el proyecto.

---

### Fase 19 — Gestión de Inventario: séptimo módulo del panel, cierre de la Versión 1.0
- **Sprint 6.0**: el sprint con más restricciones de "NO modificar" hasta la fecha (Productos, Categorías, Configuración, Pedidos, Clientes, Dashboard, Promociones, Checkout, Carrito y WhatsApp, los diez a la vez) -- coherente con que Inventario necesita leer/escribir sobre datos de Productos y reaccionar a cambios de Pedidos sin tocar ninguno de los dos módulos. Explícitamente el último sprint planeado de la Versión 1.0.

  **Esquema, una sola migración nueva** (`supabase/migrations/..._create_inventory.sql`): `products` gana `sku` (`text`, único cuando tiene valor -- `create unique index ... where sku is not null`) y `low_stock_threshold` (`integer not null default 5`, mismo umbral que ya usaba `getStockStatus()` en la tienda pública desde el Sprint 3.2); `orders` gana `stock_adjusted` (`boolean not null default false`, la bandera que evita descontar/restituir stock dos veces); y una tabla nueva, `stock_movements` (historial inmutable -- snapshot, sin `updated_at`, mismo criterio que `order_items` -- con un `check` que obliga `new_stock = previous_stock + quantity`, para que el historial nunca pueda mentir sobre su propia aritmética). RLS de `stock_movements`: solo `is_admin()` puede leer o insertar -- **sin policy de `update`/`delete`**, a propósito: un movimiento equivocado se corrige con un movimiento nuevo, nunca editando o borrando el anterior (mismo espíritu que "el historial de auditoría no se toca" ya aplicado a `order_items`).

  **Dos piezas de lógica en la base de datos, no en la aplicación, el núcleo del sprint:**
  1. `register_stock_movement(product_id, type, quantity, reason)`, invocada vía `supabase.rpc()` desde `services/inventory.ts` -- único punto de escritura manual de stock. Actualiza `products.stock` e inserta el movimiento en la misma transacción implícita de la función; rechaza con un error explícito cualquier ajuste que dejaría el stock negativo. No es `security definer` (a diferencia de `create_order`, Fase 15): quien la invoca siempre es un admin ya autenticado con permisos de RLS propios sobre `products`/`stock_movements`, así que no hace falta bypasear nada -- el chequeo de `is_admin()` de adentro es solo para un mensaje de error más claro.
  2. `apply_order_stock_movement()`, un trigger `before update on orders` -- la pieza que permite integrar Inventario con Pedidos sin abrir `services/orders.ts`. Se engancha al mismo `update orders set status = ...` que `updateOrderStatus()` ya hacía desde la Fase 15: al entrar en un estado "activo" (`confirmed`/`preparing`/`ready_or_shipped`/`delivered`) sin haber descontado stock todavía, descuenta una única vez y marca `stock_adjusted = true`; al pasar a `cancelled` habiendo descontado antes, restituye una única vez y marca `stock_adjusted = false`. Un pedido `pending` no toca el stock. La deducción automática nunca puede fallar por un desajuste de datos: se recorta a `greatest(stock - cantidad, 0)` en vez de bloquear la confirmación -- asimetría deliberada frente al rechazo explícito del ajuste manual (ver sección 6 para la justificación completa de ambas decisiones).

  **`services/inventory.ts`**: `listInventory()`/`getInventoryItemById()` (con el último movimiento embebido en la misma consulta, mismo criterio que `listCustomers()` desde la Fase 16 -- una sola query, no una segunda por fila) y `listMovements()` para el historial completo; `registerStockMovement()` y `updateInventorySettings()` (esta última sí un `update` directo a `products`, pero solo de `sku`/`low_stock_threshold` -- nunca de `stock`) como las dos únicas escrituras. Ninguna de las dos toca `services/products.ts` ni `ProductForm.tsx` -- SKU y stock mínimo se editan exclusivamente desde Inventario, mismo criterio que ya aplicó `usePromotionTargets.ts` (Fase 18) llamando a `listProducts()` sin pasar por el módulo de Productos.

  **`components/admin/inventory/`**: `InventoryTable.tsx` (buscador, 3 criterios de orden, sin alta ni baja -- los productos se crean/eliminan desde Productos, Inventario administra el stock de los que ya existen), `InventoryDetailView.tsx` (stock actual, `StockMovementForm.tsx` para registrar movimientos, historial completo, y un formulario chico inline para SKU/stock mínimo), `inventory-status.ts` (mapa estado calculado/tipo de movimiento -> variante de `Badge`, mismo criterio que `order-status.ts`/`promotion-status.ts`). Las 3 acciones que pidió el sprint ("Entrada de stock"/"Ajuste positivo"/"Ajuste negativo") se traducen en el formulario a los 2 tipos que acepta la función de Postgres (`manual_in`/`manual_adjustment`) más el signo de la cantidad -- el backend nunca ve "entrada"/"ajuste" como tal.

  **Bug real encontrado y corregido durante la propia verificación de este sprint**: la primera versión de `useInventoryItem.ts` volvía a llamar a `fetchAll()` después de cada movimiento/guardado exitoso -- pero `fetchAll` marca `isLoading = true` al empezar, y `InventoryDetailView` reemplaza toda la vista (incluido `StockMovementForm`) mientras `isLoading` es `true`. Cada envío exitoso desmontaba y remontaba el formulario, reseteando el tipo de movimiento seleccionado al default ("Entrada de stock") sin ningún aviso -- confirmado en vivo: un ajuste negativo de -3 seguido de un intento de ajuste negativo de -9999 (que debía bloquearse) en realidad se registró como una **entrada positiva de +9999**, porque el selector ya había vuelto a "Entrada de stock" sin que el test (ni un admin real) lo notara. Corregido actualizando el estado local de forma optimista con el resultado que ya devuelve cada escritura (`register_stock_movement` retorna el movimiento creado; `updateInventorySettings` no necesita releer nada) -- mismo criterio que `useOrder.ts`/`useOrders.ts` (CLAUDE.md, sección 11), sin volver a pedir nada a Supabase tras un cambio exitoso. El movimiento corrupto generado durante la detección del bug se limpió del proyecto real junto con el resto de los datos de prueba.

  **Tienda pública: una sola modificación, autorizada explícitamente por el propio sprint.** `ProductCard.tsx` ya deshabilitaba "Agregar al carrito" y mostraba "Sin stock" desde un sprint anterior; `ProductBuyBox.tsx` hacía lo mismo para "Agregar al carrito" pero **no** para "Comprar por WhatsApp", que seguía siendo un link activo incluso sin stock. Corregido: sin stock, ese botón se renderiza como un `<button disabled>` nativo (mismas clases de opacidad que ya usa el otro botón) en vez de `render={<a href={whatsappHref} .../>}` -- con stock, es exactamente el mismo link de siempre. Ningún otro archivo de Carrito/Checkout/WhatsApp se tocó. `AdminSidebar.tsx` ganó una entrada nueva ("Inventario", entre Categorías y Pedidos) -- capa de layout del panel, no uno de los módulos protegidos.

  **Verificado en vivo contra el proyecto real:** (1) Entrada manual (+10) y ajuste negativo manual (-3) sobre iPhone 14 (8 → 18 → 15), reflejados correctamente en el stock y en el historial (fecha, tipo, cantidad con signo, stock anterior/posterior, motivo); un ajuste negativo excesivo (9999 sobre un stock de 15) fue bloqueado por la UI sin llegar a Supabase. (2) Guardado de SKU/stock mínimo persistido y confirmado tras refrescar la página. (3) Prueba directa contra la base, simulando la sesión de un admin autenticado (`set_config('request.jwt.claim.sub', ...)`): un intento de `register_stock_movement` que dejaría el stock negativo (Smart Watch, stock 3, ajuste -10) fue rechazado con el mensaje de error esperado, y una consulta posterior confirmó que el stock quedó en 3 y no se insertó ningún movimiento -- cero escritura parcial. (4) Ciclo completo de pedido real, sin tocar Checkout/Carrito/WhatsApp: 2 unidades de "Tacos" agregadas y confirmadas por el checkout público de verdad (navegación 100% client-side, sin `page.goto` entre pasos -- el cart-store/checkout-store no tienen persistencia); el pedido quedó "Pendiente" sin afectar el stock (20 sin cambios); al pasar a "Confirmado" desde `/admin/pedidos` (componente sin modificar), el stock de Tacos bajó a 18 con un movimiento `order_confirmed` ("Pedido #12 confirmado"); al pasar a "En preparación", el stock **no** volvió a bajar (sin movimiento nuevo, `stock_adjusted` ya en `true`); al "Cancelar", el stock volvió a 20 con un movimiento `order_cancelled` -- confirmado tanto en la UI de Inventario como por SQL directo contra `stock_movements`. (5) Prueba del recorte a 0: un pedido sintético de 10 unidades de Smart Watch (stock 3) confirmado por SQL dejó el stock en 0 (no negativo, no bloqueó el pedido), con el movimiento registrando el delta real aplicado (-3, no -10). (6) "Aspiradora" (stock 0, ya sembrado así) mostró "Sin stock" y ambos botones deshabilitados tanto en su página de producto como en su `ProductCard` del Catálogo, con el producto siempre visible. (7) Regresión: las 6 rutas core de la tienda y las 8 rutas del panel administrativo respondieron 200 sin errores de consola. Responsive verificado en desktop/tablet/mobile. Todos los datos de prueba (movimientos, pedidos, clientes sintéticos) se limpiaron del proyecto real al finalizar, devolviendo cada producto afectado a su stock/SKU/umbral original. `pnpm lint` y `pnpm exec tsc --noEmit` limpios en todo el proyecto.

  **Con este sprint, la Versión 1.0 de Somapp Commerce Engine queda completa**: los 7 módulos planeados del panel administrativo (Productos, Categorías, Inventario, Configuración, Pedidos, Clientes, Promociones) más el Dashboard, sobre una tienda pública 100% conectada a Supabase.

---

### Fase 20 — Sprint 6.0.1: Auditoría Final y Corrección de Bugs (Release Candidate V1.0)
- **Sprint de estabilización, no de funcionalidad nueva**: primer sprint del proyecto explícitamente dedicado a auditar y corregir, sin agregar características ni cambiar arquitectura o estructura de carpetas. A diferencia de todos los sprints anteriores, esta vez Checkout/Carrito no estaban blanket-protegidos (el bug 3 pedía explícitamente tocar el mensaje de WhatsApp del checkout) -- cada bug definió su propio alcance, no una lista fija de "NO modificar" al estilo de los sprints de módulo.

  **Bug 1 -- tarjetas de producto sin navegación.** Causa exacta: `ProductCard.tsx` nunca tuvo un `<Link>` a `/productos/[slug]` desde que se escribió (Sprint 3.1) -- únicamente el botón "Agregar al carrito" tenía `onClick`; clickear la imagen, el nombre o la descripción no hacía nada, en las cuatro áreas que renderizan este componente (Home, Catálogo, Categorías, Productos Relacionados) por igual, porque las cuatro comparten el mismo `ProductCard`. `CategoryCard.tsx` (Sprint 4.4) sí envolvía todo su contenido en un `Link` desde siempre -- `ProductCard` simplemente nunca replicó ese patrón ya establecido. Corregido envolviendo imagen + nombre + descripción + precio + stock en `<Link href={`/productos/${product.slug}`}>`, con "Agregar al carrito" como hermano fuera del Link (un `<button>` no puede anidarse dentro de un `<a>`). Verificado en vivo: click en la tarjeta navega correctamente en Home, `/productos`, `/categorias/[slug]` y "Productos relacionados"; "Agregar al carrito" sigue sumando al carrito sin navegar.

  **Bug 2 -- banner de promoción de producto sin imagen.** Causa exacta: `services/storefront/promotions.ts` nunca pedía `product_images` en su `select`, así que `PromotionalBanner.tsx` no tenía ninguna URL de foto para mostrar y siempre caía al placeholder decorativo (Sparkles + blobs), sin importar si la promoción era de un producto con foto real. Corregido: el `select` de `getActivePromotion()` embebe `product_images(url, display_order)` bajo `products`; se resuelve la misma imagen principal que usaría la ficha del producto (`isRealImageUrl` + menor `display_order`) y se agrega `imageUrl: string | null` al tipo `PublicPromotion` -- sin campo nuevo en la tabla `promotions`, sin duplicar ninguna imagen. `PromotionalBanner.tsx` muestra esa foto con `next/image` cuando existe; sin ella (promoción de categoría, o producto sin foto real todavía), sigue el mismo placeholder de siempre. Verificado en vivo: promoción de "Tacos" (con foto real) mostró la foto real en el banner de la Home.

  **Bug 3 -- WhatsApp del checkout con número estático.** Causa exacta: de las tres excepciones documentadas en la Fase 14 (`formatPrice()`, `lib/whatsapp.ts`, mapa de Checkout, todas leyendo `config/business.ts` porque tocarlas requería modificar Checkout/Carrito/WhatsApp, prohibido en ese sprint), este sprint autorizaba resolver la del mensaje de WhatsApp. `getOrderWhatsAppUrl()` pasó a recibir `whatsappNumber` por parámetro en vez de importar `businessConfig.whatsapp.number`. Como `/checkout/confirmacion` es "use client" de punta a punta (depende de `useCartStore`/`useCheckoutStore`, estado que no existe en el servidor), no hay forma de resolver `business_settings` con el cliente de servidor que usa el resto del storefront -- `OrderConfirmationDetails.tsx` lo lee con el cliente de browser en un `useEffect` al montar, y el botón "Confirmar pedido" queda deshabilitado hasta que esa lectura resuelve.

  **Error real cometido y corregido durante este mismo bug**: la primera versión agregó la función cliente-de-browser directamente en `services/storefront/business.ts`, que ya importaba `lib/supabase/server.ts` (y, con él, `next/headers`). En cuanto `OrderConfirmationDetails.tsx` importó algo de ese archivo, Next.js intentó incluir `next/headers` en el bundle del navegador y el build se rompió en pleno desarrollo -- detectado con Playwright al ver que un click quedaba bloqueado por el overlay de error de Next.js, no por ningún linter ni por `tsc` (ninguno de los dos detecta esta clase de conflicto de bundling). Se resolvió moviendo la función a un archivo nuevo, `services/storefront/business-client.ts`, sin ningún import de `lib/supabase/server.ts` -- la lección (documentada en la sección 6) es que un archivo con ese import no puede tener también una función pensada para un Client Component, sin importar qué cliente de Supabase use esa función en particular.

  **Bug 4 -- categorías inactivas visibles con sesión de admin activa.** Causa exacta: `categories_public_read_active` (RLS) filtra correctamente `is_active = true` para el rol `anon`, pero convive con `categories_admin_read_all` (`to authenticated using (is_admin())`), combinadas con OR. Si la tienda pública se navega en el mismo navegador donde hay una sesión de admin abierta, la request corre como `authenticated` y ve categorías inactivas también -- RLS decide por rol, no por ruta. Corregido agregando `.eq("is_active", true)` explícito en `getPublicCategories()`/`getPublicCategoryBySlug()`, como segunda capa además de RLS. Por auditoría (mismo patrón, mismo riesgo latente), se aplicó el mismo criterio a `getPublicProducts()`/`getPublicProductBySlug()` y a `getActivePromotion()` (agregando también el filtro de vigencia `starts_at`/`ends_at`), sin que ningún bug reportado lo pidiera para esas dos tablas. Verificado en vivo, en el **mismo contexto de navegador** que la sesión de admin (la prueba que de verdad reproduce el bug): se creó una categoría de prueba, se verificó visible activa, se desactivó desde el panel, y en el mismo navegador dejó de aparecer en `/categorias`, en Home, y su URL directa devolvió 404 -- exactamente lo que fallaba antes del fix.

  **Bug 5 -- favicon/logo sin validación de tamaño.** Causa exacta (ya diagnosticada en una consulta previa a este sprint): el bucket `branding` tiene `file_size_limit = 2097152` (2 MB, Fase 14) a nivel de Supabase Storage, pero ningún código del cliente lo validaba antes de subir -- un archivo más grande recién se enteraba al hacer clic en "Guardar cambios", cuando Storage lo rechazaba con `413 Payload too large` (mensaje literal: "The object exceeded the maximum allowed size"). Corregido con `MAX_BRANDING_FILE_SIZE_BYTES`/`_MB` (`services/settings.ts`, mismo valor que la migración, documentado como intencionalmente duplicado) y una validación en `onSelectFile` (`SettingsForm.tsx`) que rechaza el archivo *antes* de guardarlo como `pending` -- nunca llega a intentar la subida. Confirmado que `business_settings.favicon_url`/`logo_url` son siempre `text` (URLs), nunca un objeto `File` -- eso ya era correcto desde la Fase 14, verificado con `pg_typeof()` contra el proyecto real. Verificado en vivo: un favicon de 3 MB mostró el error inmediatamente al seleccionarlo, sin ninguna request al bucket; uno válido subió y guardó correctamente.

  **Bug 6 -- Dashboard, rediseño visual (sin tocar métricas ni consultas).** Se instaló Recharts (única librería de gráficos del proyecto, nombrada explícitamente por el propio sprint) para dos gráficos nuevos: `SalesChart.tsx` (área, sobre `recentOrders` -- los mismos "últimos 10 pedidos" que ya existían, ordenados cronológicamente; no es una serie histórica agregada por día, esa consulta no existe y este sprint no la agrega) y `OrderStatusBreakdown.tsx` (barras, reemplazando las barras de CSS puro de la Fase 17 sobre el mismo `statusBreakdown` de siempre, con los colores traducidos 1:1 desde `getOrderStatusVariant()`). `SummaryCard.tsx` ganó un prop `tone` (`neutral`/`positive`/`warning`) que reutiliza colores semánticos ya existentes en el proyecto (`text-emerald-600`/`bg-amber-500`, del estado de stock desde el Sprint 3.2) en vez de una paleta nueva. Las animaciones de entrada usan `tw-animate-css` (ya instalado, usado por `Sheet`/`Dialog`) -- nunca Framer Motion, que el panel administrativo no usa a propósito. Verificado en vivo: los dos gráficos renderizan con datos reales, cero errores de consola, responsive en desktop/tablet/mobile.

  **Auditoría de código, más allá de los 6 bugs numerados:**
  - `isRealImageUrl` (antes duplicada a propósito en `ProductCard.tsx`/`ProductGallery.tsx`, Fase 12, con solo dos consumidores) se extrajo a `lib/products.ts` porque el bug 2 la necesitaba en un tercer archivo (`services/storefront/promotions.ts`) -- cruzando el umbral de "3 o más lugares" que el proyecto usa para decidir cuándo compartir código (principio 6). Los dos duplicados ya aprobados en los componentes no se tocaron.
  - **Miniaturas rotas en `/admin/productos` y `ProductImagesField.tsx`**, encontradas durante el barrido de regresión final (capturando responses 404 reales con Playwright, no adivinadas): la corrección de la Fase 12 (mostrar la foto real con fallback al placeholder) solo se había aplicado en la tienda pública -- el panel seguía pidiendo directamente `product.images[0]?.url`/`image.url` sin ninguna guarda, incluidas las 5 rutas de placeholder del seed original que nunca existieron como archivo. Corregido con la misma guarda `isRealImageUrl` ya compartida, en ambos archivos.
  - **Nombre de categoría corrompido en el proyecto real**: "Tecnología parece" en vez de "Tecnología" (visible en la captura de `/admin/productos` del punto anterior). No es un bug de código -- es un dato mal guardado en algún momento anterior a este sprint, probablemente un guardado accidental a mitad de tipeo en `CategoryForm`. Corregido directamente en la base con una `update` puntual, por ser una corrección de datos evidente y de bajo riesgo.
  - `AdminPlaceholder.tsx` eliminado -- código muerto desde que los 7 módulos del panel quedaron completos (Fase 19); confirmado por grep que no tenía ningún import activo antes de borrarlo.
  - Sin `TODO`/`FIXME` reales en el código (el único match de un grep amplio fue "MÉTODO", falso positivo). Sin `console.log`/`console.warn` de debug olvidados. Sin rutas rotas (`/categoria/` singular, el bug histórico de la Fase 4.4, no reapareció). `CartItem.tsx`/`CheckoutSummary.tsx` siguen importando `config/categories.ts` directamente (pendiente #9, sección 13) -- se evaluó migrarlos en este sprint (Checkout no estaba blanket-protegido) pero se decidió no tocarlos: ambos son "use client" sin ningún Server Component que les resuelva `categories` por prop, y agregar un tercer fetch de browser-client a la tienda pública (además del ya agregado para WhatsApp) empezaba a acumular un patrón que un sprint de estabilización no debía introducir sin más justificación que "ya que estamos". Documentado como decisión consciente, no un olvido.

  **Verificación en vivo contra el proyecto real, bug por bug más el checklist completo del sprint:** los 6 bugs (arriba); además, CRUD completo de un producto de prueba (crear/editar/eliminar) y una categoría de prueba (crear/eliminar); un pedido real de punta a punta desde el checkout público (con el número de WhatsApp ya corregido); el cliente y el pedido aparecieron correctamente en `/admin/clientes`/`/admin/pedidos`; Inventario reflejó correctamente que un pedido "Pendiente" todavía no descuenta stock (sin regresión del propio Sprint 6.0). Barrido final de las 12 rutas públicas y las 8 del panel, sin errores de consola. Responsive verificado en desktop/tablet/mobile (capturas de Dashboard en los tres tamaños). Todos los datos de prueba (productos, categorías, pedidos, clientes, promoción, favicon) se limpiaron del proyecto real al finalizar. `pnpm lint` y `pnpm exec tsc --noEmit` limpios en todo el proyecto.

  **Con este sprint, Somapp Commerce Engine Versión 1.0 queda estable, consistente y lista como Release Candidate para producción.**

### Fase 21 — Sprint 6.1: Sistema de Promociones completo (banner rotativo + precios dinámicos)
- **Sprint 6.1**: primer sprint posterior al Release Candidate que agrega funcionalidad nueva (no un bugfix ni una estabilización) -- con el mandato explícito de terminar de una vez el sistema de promociones que la Fase 18 había dejado con dos límites conocidos y documentados: el panel permitía crear varias promociones vigentes a la vez, pero el banner de la Home solo mostraba la más reciente; y ninguna promoción modificaba el precio realmente mostrado de un producto (la conexión que el diseño original, Fase 8, ya dejaba explícitamente como "decisión de implementación futura"). Diez restricciones explícitas de "NO modificar" (Checkout, Carrito, WhatsApp, y no romper ningún módulo existente) -- el sprint más restrictivo hasta la fecha en cuanto a superficie protegida, a pesar de ser el primero en agregar una funcionalidad nueva desde la Fase 19.

  **`services/storefront/promotions.ts`: `getActivePromotion()` (singular) reemplazada por `getActivePromotions()` (plural).** Ambas cosas que necesita este sprint -- el carrusel del banner y el motor de precios en cada página de productos -- necesitan el conjunto **completo** de promociones vigentes ahora mismo, no solo la más reciente. Una sola consulta nueva reemplaza la anterior; `PublicPromotion` se extendió de forma aditiva con los campos crudos que el motor de precios necesita (`discountType`, `discountValue`, `productId`, `categorySlug` -- este último resuelto del embed `categories(slug)` que ya traía la consulta, sin una columna nueva) sin tocar los campos que el banner ya usaba (`discountLabel`, `targetLabel`, `targetHref`, `imageUrl`). El filtro explícito de `is_active`/`starts_at`/`ends_at` (Sprint 6.0.1, bug 4) se mantuvo intacto.

  **`lib/promotions.ts`: `calculatePromotion()` y `getProductDisplayPrice()`, el motor de precios único que pedía el sprint.** `calculatePromotion(product, promotions)` es la función que el sprint pidió explícitamente ("una única función reutilizable... debe devolver: precio original, precio final, promoción aplicada, tipo, badge"): resuelve qué promoción aplica con prioridad fija (producto puntual primero; solo si no tiene, la de su categoría; nunca las dos a la vez, nunca acumulando), calcula el precio final (porcentual o monto fijo, recortado a un mínimo de 0) y devuelve exactamente esa forma. `getProductDisplayPrice()` es una segunda función, no pedida literalmente por el sprint pero necesaria para cumplir "no duplicar lógica": combina el resultado de `calculatePromotion()` con el comportamiento legacy ya aprobado (`product.oldPrice`/`onSale`/`badge`, Fases 2-3) para los casos -- la mayoría del catálogo, hoy -- donde ninguna promoción de Supabase aplica a un producto puntual. Sin esta segunda capa, `ProductCard`/`ProductBuyBox` hubieran tenido que repetir la misma lógica de "si hay promoción, mostrar eso; si no, mostrar lo de siempre" cada uno por su cuenta -- exactamente la duplicación que el sprint prohibía.

  **Evitar una dependencia circular, una decisión de diseño explícita, no accidental**: `services/storefront/promotions.ts` ya importaba `DiscountType`/`formatDiscountLabel` de `lib/promotions.ts` desde la Fase 18 -- si `calculatePromotion()` hubiera importado el tipo `PublicPromotion` de `services/storefront/promotions.ts` para tipar su parámetro, se habría creado un ciclo `lib` → `services` → `lib`. En cambio, `lib/promotions.ts` define su propio tipo mínimo (`PricingPromotion`, con exactamente los campos que el cálculo necesita) sin importar nada de `services/`; cualquier `PublicPromotion` ya cumple esa forma de manera estructural (TypeScript), así que se le puede pasar tal cual a `calculatePromotion()`/`getProductDisplayPrice()` sin ningún cast ni adaptador.

  **`PromotionalBanner.tsx`: de un solo componente estático a un carrusel condicional, sin duplicar el JSX de una promoción.** Se extrajo `PromotionContent` (todo el bloque de badge + título + subtítulo + botón + imagen/placeholder) como la única pieza que sabe dibujar una promoción -- reutilizada tanto por el caso de una sola promoción (con el mismo stagger de entrada de Framer Motion que ya tenía desde la Fase 18) como por cada slide del carrusel nuevo (`PromotionCarousel`, sin ese stagger por bloque: la transición horizontal del slide ya es la animación de entrada, repetir el stagger interno se hubiera visto redundante). El carrusel usa `AnimatePresence` (`mode="wait"`) con una transición de `x`/`opacity` por dirección (`custom={direction}`), avanza cada 2.5 segundos vía `setInterval` dentro de un `useEffect` que depende de `index` -- por lo que se reinicia solo con cualquier cambio de slide, sea automático o manual (flechas/dots), sin ningún código aparte para "reiniciar el temporizador" -- y se pausa con `onMouseEnter`/reanuda con `onMouseLeave` sobre el contenedor completo. Con exactamente 1 promoción vigente, `promotions.length > 1` es `false` y se renderiza `PromotionContent` directo, sin ningún control de navegación -- mismo comportamiento pixel a pixel que antes de este sprint.

  **`PromotionalBanner.tsx` gana el mismo `mounted`-gate que ya tiene `Header.tsx` para `useReducedMotion()`**, aplicado de paso al reescribir sustancialmente este archivo: `useReducedMotion()` (Framer Motion) lee `window.matchMedia` de forma sincrónica en el primer render del cliente, lo que puede no coincidir con el servidor (que no tiene `window`) para un visitante con "reducir movimiento" activado en su sistema operativo -- el mismo mismatch de hidratación ya diagnosticado y corregido en `Header.tsx` en una consulta anterior a este sprint. Este archivo ya tenía el mismo riesgo latente desde la Fase 18 (nunca reportado como bug); se corrigió acá porque el sprint ya obligaba a tocar exactamente esas líneas para el carrusel, no como una mejora fuera de alcance. `Hero.tsx`/`FeaturedCategories.tsx`/`FeaturedProducts.tsx`/`FeaturedBenefits.tsx`/`TestimonialsSection.tsx` siguen con el mismo patrón sin corregir -- documentado como pendiente (sección 13), fuera de alcance de este sprint.

  **Precio promocional threading por prop, mismo patrón que `categories`/`settings` desde las Fases 13/14**: `ProductCard.tsx` y `ProductBuyBox.tsx` ganaron un prop `promotions: PublicPromotion[]`, resuelto por `getProductDisplayPrice()` para decidir precio/tachado/badge -- ninguno de los dos calcula un descuento por su cuenta. `ProductGrid.tsx`, `FeaturedProducts.tsx`, `RelatedProducts.tsx` y `CatalogResults.tsx` solo reenvían ese mismo array hacia `ProductCard`, sin lógica propia. Las 4 páginas que resuelven productos (Home, `/productos`, `/productos/[slug]`, `/categorias/[slug]`) suman `getActivePromotions()` a su `Promise.all` existente y pasan el resultado hacia abajo. `ProductGallery.tsx` **no se tocó**: es un componente 100% de imágenes (visor + miniaturas), nunca mostró un precio -- la responsabilidad de precio siempre fue de `ProductBuyBox`, su hermano en la misma página; forzarle un precio a la Galería hubiera duplicado esa responsabilidad sin ningún beneficio.

  **Cuando una promoción aplica, el precio tachado es el `price` actual del producto, no su `oldPrice` legacy** -- una decisión de diseño explícita, no un descuido: `calculatePromotion()` calcula el descuento sobre `product.price` (el precio vigente hoy), así que el precio tachado que se muestra es ese mismo `price`, sin relación con el `oldPrice` estático que pudiera tener el producto de antes. Es la interpretación correcta: la promoción descuenta el precio *actual* de venta, no un precio histórico ya desactualizado. Cuando ninguna promoción aplica, `getProductDisplayPrice()` cae exactamente al comportamiento legacy (`oldPrice`/`onSale`/`badge`), sin ningún cambio.

  **`getDiscountPercent()` (`lib/products.ts`) eliminada, no dejada como código muerto**: su único consumidor, `ProductBuyBox.tsx`, pasó a usar `getProductDisplayPrice()` -- confirmado por grep que no quedaba ningún otro import antes de borrarla, mismo criterio ya aplicado a `getCategoryBySlug` (Fase 13) y `AdminPlaceholder.tsx` (Sprint 6.0.1).

  **Verificado en vivo contra el proyecto real, con datos reales del usuario más datos de prueba propios, todos limpiados al finalizar**: el proyecto real ya tenía 2 promociones vigentes creadas por el propio usuario (`OTRO`, 50% sobre "Tacos"; `DESCUENTAZO`, 5% sobre "Licuadora") -- ninguna de las dos se tocó ni se borró. Se sumaron 5 promociones de prueba propias (tituladas `SPRINT61 TEST - ...`, para poder identificarlas y borrarlas sin ambigüedad): una de monto fijo (Gs. 20.000 sobre "Aspiradora"), una de categoría (10% sobre "Tecnología"), una de producto en la misma categoría que la anterior para probar prioridad (15% sobre "iPhone 14" -- debía ganarle a la de categoría), una vencida (99% sobre "Auriculares Bluetooth", `ends_at` en el pasado) y una inactiva (99% sobre el mismo producto, `is_active = false`). Con las 7 sumadas (2 reales + 5 de prueba, de las cuales 5 quedaban vigentes -- la vencida y la inactiva correctamente invisibles), se confirmó: el carrusel con exactamente 5 dots (no 7); auto-avance recién después de ~2.5 segundos; pausa completa de la rotación con el mouse sobre el banner y reanudación al salir; flecha derecha/izquierda avanzando y retrocediendo un slide exacto; click directo en un dot saltando a ese índice; loop infinito (del último slide, la flecha derecha vuelve al primero); precio de "iPhone 14" con exactamente -15% (Gs. 722.500, tachado Gs. 850.000) y nunca con el 10% de su categoría ni con ambos acumulados; precio de "Smart Watch" (misma categoría, sin promoción propia) heredando correctamente el 10% de categoría (Gs. 162.000); precio de "Aspiradora" con el descuento de monto fijo exacto (Gs. 100.000, tachado Gs. 120.000, badge "-Gs. 20.000"); precios de "Tacos" y "Licuadora" reflejando correctamente las dos promociones reales ya existentes del usuario (Gs. 250.000 y Gs. 61.750 respectivamente); "Auriculares Bluetooth" mostrando su precio normal (Gs. 45.000) sin ningún tachado ni badge, confirmando que una promoción vencida y una inactiva -- ambas apuntando a este mismo producto -- no se cuelan por ningún lado. Los mismos precios se confirmaron consistentes entre la página de producto (`ProductBuyBox`), el Catálogo y la página de categoría (`ProductCard`). Se verificó además, con el mismo mecanismo de desactivar/reactivar promociones (reversible, sin borrar nada del usuario), que con exactamente 1 promoción vigente el banner vuelve a comportarse sin ningún control de carrusel (sin dots, sin flechas, mismo badge/título de siempre) y que con 0 promociones vigentes el banner no se renderiza en absoluto -- ambos, el comportamiento exacto de antes de este sprint. Responsive verificado con capturas en desktop (1440px), tablet (768px) y mobile (390px): sin overflow horizontal en ningún breakpoint, flechas y dots visibles y sin superposición con el texto en ninguno de los tres tamaños. Al finalizar, se borraron las 5 promociones de prueba y se confirmó que las 2 promociones reales del usuario quedaron exactamente como se encontraron (mismos valores, mismo `is_active`). `pnpm lint` y `pnpm exec tsc --noEmit` limpios en todo el proyecto.

  **Con este sprint, el sistema de promociones de Somapp Commerce Engine queda definitivamente completo**: crear campañas (Fase 18), banner dinámico con soporte para múltiples promociones simultáneas y precio de catálogo recalculado en tiempo real (Fase 21) -- las dos piezas que el diseño original (Fase 8) había dejado deliberadamente para más adelante.

### Fase 22 — Sprint 6.2: módulo de Logística (tarifas de envío)
- **Sprint 6.2**: primer sprint que agrega un módulo enteramente nuevo (no planeado en el diseño original de la Fase 8) desde que la Versión 1.0 quedó completa -- con el objetivo explícito de que "no exista ningún costo de envío hardcodeado" y "toda la lógica provenga de Supabase". Diez restricciones de "NO modificar" (Productos, Categorías, Inventario, Promociones, Clientes, Dashboard, Hero, Carrito, WhatsApp) más una autorización explícita y acotada para tocar Checkout ("modificar únicamente la parte correspondiente al cálculo del envío") y ninguna restricción sobre Pedidos -- el primer sprint desde la Fase 19 que toca Supabase, el Panel y el Checkout a la vez.

  **Esquema, cuatro tablas nuevas más dos columnas en `orders`** (`supabase/migrations/..._create_logistics.sql`): `departments`/`cities` (catálogo geográfico normalizado -- `cities.department_id` como FK, nunca se repite el nombre del departamento -- de solo lectura desde toda la aplicación: ninguna policy de `insert`/`update`/`delete`, ni siquiera para `is_admin()`, porque el sprint pidió explícitamente que "nunca deberán escribirse manualmente desde el Panel") y `shipping_rates`/`shipping_rate_cities` (la tarifa -- nombre, precio, `estimated_days` de texto libre para admitir "24 hs"/"3-5 días" tal como pedía el sprint, estado -- y la relación muchos-a-muchos con las ciudades que cubre). `shipping_rates` no tiene ninguna columna de departamento a propósito -- ver sección 5/6 para el razonamiento completo de por qué esto es lo que deja la base "preparada para crecer" sin rediseñarla. `orders.department`/`city`/`shipping_cost` ya existían desde la Fase 8 sin ningún consumidor real -- este sprint no agregó columnas para eso, solo `shipping_rate_id` (FK, `on delete set null`) y `shipping_rate_name` (snapshot de texto), mismo criterio dual que `order_items.product_id`/`product_name`. `create_order()` se extendió con `p_shipping_cost`/`p_shipping_rate_id`/`p_shipping_rate_name` (default `null`) -- requirió un `drop function` explícito de la firma vieja antes del `create or replace`, ver sección 6 para la lección completa.

  **Seed de Paraguay** (`supabase/seed_logistics.sql`, corrido a mano contra el proyecto real, no parte de `db reset`/`db push --include-seed`, mismo criterio que `seed.sql`): 18 filas en `departments` (los 17 departamentos oficiales + Asunción, modelada como su propio "departamento" de una sola ciudad -- criterio práctico estándar para direcciones paraguayas) y 228 filas en `cities` -- la capital departamental más una selección extensa de distritos reales y reconocibles por departamento. Documentado explícitamente en el propio archivo como un conjunto extenso y verificado con cuidado, pero no una garantía de exhaustividad oficial contra el nomenclador completo de la DGEEC (~260 distritos) -- ampliarlo después es una migración de datos chica (más filas en `cities`), nunca un cambio de esquema.

  **`/admin/logistica` (tercer módulo del panel que sigue exactamente el patrón `components/admin/logistics/` → `hooks/` → `services/logistics.ts` → Supabase)**: tabla real (nombre, precio, cantidad de ciudades, tiempo estimado, estado, acciones) con buscador y 3 criterios de orden (nombre/precio/cantidad de ciudades), paginación preparada pero no implementada -- mismo criterio que Categorías/Pedidos/Promociones/Inventario. `/admin/logistica/nueva` y `/[id]/editar` comparten `ShippingRateForm.tsx`. La pieza central del sprint es `DepartmentCityPicker.tsx`: la "interfaz cómoda" pedida explícitamente en vez de un `<select>` común -- elegir un departamento muestra automáticamente sus ciudades, cada una con checkbox, más un buscador de ciudades y "seleccionar todas"/"deseleccionar todas" sobre lo que esté filtrado, pensado para tildar decenas de ciudades rápido. La restricción "una tarifa cubre ciudades de un único departamento" vive enteramente en este componente (cambiar de departamento vacía la selección), no en la base de datos -- ver sección 6.

  **Checkout: solo la parte de cálculo de envío, tal como autorizó el sprint.** `ShippingInformation.tsx` reemplazó sus dos `<Input>` de texto libre (departamento/ciudad) por `ShippingCitySelect.tsx`, dos `<Select>` conectados a `services/storefront/logistics.ts` (cliente de browser en los tres casos -- ver sección 6 para el porqué). Elegir una ciudad dispara automáticamente la búsqueda de su tarifa (`getShippingRateForCity`, un inner join `shipping_rate_cities → shipping_rates` filtrando `is_active` explícitamente además de RLS, mismo criterio que el bug 4 del Sprint 6.0.1) y escribe el resultado directo en `useCheckoutStore` (`setShippingResult`/`resetShipping`, campos nuevos: `shippingChecked`/`shippingRateId`/`shippingRateName`/`shippingCost`/`shippingEstimatedDays`) -- mismo patrón que `LocationPicker` ya usaba para `latitude`/`longitude` desde el Sprint 3.7, porque este dato tampoco encaja en el `onChange` genérico del formulario. `CheckoutSummary.tsx` (compartido por `/checkout` y `/checkout/confirmacion`) lee ese estado para reemplazar el placeholder "Se calcula en el próximo paso" por el costo real y sumarlo al Total -- sin ciudad elegida todavía, o en "Retiro en tienda", se comporta exactamente igual que antes de este sprint (cero regresión).

  **"A confirmar", nunca un costo inventado**: si `getShippingRateForCity` no encuentra ninguna tarifa activa para la ciudad elegida, `shippingCost` queda `null` con `shippingChecked = true` -- `ShippingCitySelect` muestra "Costo de envío: A confirmar" más el texto "El vendedor confirmará el costo del envío una vez recibido el pedido", `CheckoutSummary` no suma nada al Total, y el cliente puede completar la compra igual (el botón "Continuar" nunca se bloquea por esto). `services/storefront/orders.ts` (`createOrder()`) ahora recibe un cuarto parámetro, `shipping: ShippingSelection | null` (`null` en "Retiro en tienda"), calcula `total = subtotal + (shipping?.cost ?? 0)` y pasa `shipping_cost`/`shipping_rate_id`/`shipping_rate_name` tal cual a `create_order()` -- `null` se guarda como `null`, nunca `0`, para que el panel distinga "sin envío" (Retiro) de "envío a confirmar" (Delivery sin tarifa) con total==subtotal en ambos casos pero con la fila de Pedidos mostrando algo distinto en cada uno.

  **Pedidos (no protegido este sprint): la única modificación fuera de Logística/Checkout, y la que hace visible el "A confirmar" al administrador.** `services/orders.ts` (admin) suma `shippingRateName` a `AdminOrder`/`ORDER_SELECT`. `OrderDetailView.tsx`: con Delivery, la fila "Envío" ahora siempre se muestra (antes desaparecía por completo si `shippingCost` era `null`) -- con costo real, el monto; sin tarifa, "A confirmar" en ámbar, para que el administrador vea de inmediato que tiene que informar el costo a mano, tal como pedía el sprint explícitamente. Con Retiro en tienda, la fila sigue sin mostrarse (no aplica). Una fila nueva, "Tarifa usada", aparece solo cuando `shippingRateName` no es null.

  **Una excepción documentada, no un descuido**: `lib/whatsapp.ts` no se tocó ("NO modificar WhatsApp", instrucción explícita) -- el mensaje que se manda por WhatsApp sigue mostrando `summary.shippingPlaceholder` y un total sin sumar el envío, aunque el total mostrado en pantalla (`CheckoutSummary`) y el guardado en Supabase (`orders.total`) sí sean los reales desde este sprint. Es la misma categoría de tensión ya documentada varias veces en este proyecto (Fase 14, tres exclusiones similares) -- se prioriza la instrucción explícita y se deja constancia en vez de romperla o resolverla en silencio. Documentado como pendiente explícito (sección 13).

  **Bug real encontrado y corregido durante la verificación de este mismo sprint**: el `<Select>` de departamento del formulario de edición mostraba el UUID crudo (o el placeholder "Elegí un departamento") en vez del nombre, de forma intermitente. La causa: `useDepartments()` vivía dentro de `DepartmentCityPicker`, que en modo edición no se montaba hasta resolver a qué departamento pertenecían las ciudades ya asignadas (un segundo round-trip encadenado después del que trae la tarifa) -- para cuando `departmentId` tenía un valor real, la lista de departamentos recién empezaba a pedirse, y el `<Select>` no encontraba ningún `item` para resolver la etiqueta. Corregido subiendo `useDepartments()` a `ShippingRateForm` (arranca en el primer render, en paralelo con la tarifa) y pasando `departments`/`isLoadingDepartments` por prop al picker, más un estado `isLoadingDepartment` que mantiene el formulario en "Cargando tarifa de envío..." hasta que ambos fetches resuelven. Detectado comparando una navegación SPA real (client-side, la que usa un admin de verdad) contra una recarga completa con Playwright -- la segunda ocultaba el bug por darle de sobra tiempo a los fetches.

  **Verificado en vivo contra el proyecto real, con Playwright de punta a punta**: (1) admin -- alta de una tarifa (Central, 25.000 Gs., "48 hs", 2 ciudades combinando "seleccionar todas" filtrado por búsqueda + un checkbox individual) confirmada en la tabla con el conteo de ciudades correcto; edición (precio actualizado a 30.000, departamento y ciudades correctamente precargados tras el fix del bug de arriba); buscador y eliminación confirmados. (2) Checkout -- Villarrica (Guairá, con una tarifa de prueba de Gs. 15.000/"24-48 hs" cubriéndola) mostró el costo y tiempo estimado correctos, con el Total sumado (Gs. 80.000 = Gs. 65.000 de "Licuadora" + Gs. 15.000 de envío); Borja (mismo departamento, sin tarifa) mostró "A confirmar" con el Total quedando en Gs. 65.000, sin sumar nada. (3) Dos pedidos reales completados de punta a punta (uno por cada caso) confirmados por SQL directo: el de Villarrica guardó `shipping_cost=15000`, `shipping_rate_id`/`shipping_rate_name` correctos y `total=80000`; el de Borja guardó `shipping_cost=null`, `shipping_rate_id=null` y `total=65000` (igual al subtotal). (4) `/admin/pedidos/[id]` de ambos pedidos mostró exactamente lo esperado -- el costo y la tarifa usada en uno, "A confirmar" en ámbar en el otro. (5) Regresión: barrido de las 6 rutas públicas y las 9 del panel (incluida `/admin/logistica`) sin errores de consola ni respuestas 500; Productos, Categorías, Promociones e Inventario navegados sin cambios de comportamiento; el stock de "Licuadora" confirmado sin alteraciones (ambos pedidos de prueba quedaron en estado "Pendiente", sin disparar el trigger de stock de la Fase 19). Responsive verificado en desktop/tablet/mobile (capturas de pantalla), sin overflow horizontal en ningún breakpoint. Todos los datos de prueba (tarifa, pedidos, clientes) se limpiaron del proyecto real al finalizar, confirmado por conteo posterior. `pnpm lint` y `pnpm exec tsc --noEmit` limpios en todo el proyecto.

  **Con este sprint, Somapp Commerce Engine deja de tener ningún costo de envío hardcodeado**: el módulo de Logística queda completo y administrable de punta a punta -- tarifas, cobertura geográfica y cálculo automático en el checkout -- con la base de datos explícitamente preparada para envío gratis, transportadoras, peso, volumen, distancia, múltiples sucursales, retiro en tienda vinculado a tarifas y delivery express en versiones futuras, sin necesitar ningún rediseño de esquema para empezar (ver sección 13).

### Fase 23 — Sprint 6.3: ocultar la tarjeta "Branding" de Configuración
- **Sprint 6.3**: mejora puntual de UX, explícitamente acotada por el propio usuario a mitad de su pedido original -- un primer pedido ("reorganizar completamente la pantalla de Configuración en 3 tarjetas") empezó a implementarse, pero un segundo mensaje lo reemplazó por uno mucho más chico ("NO quiero reorganizar toda la pantalla... quiero eliminar únicamente la sección visual llamada Branding") antes de terminar. Se descartó el trabajo de reorganización completa ya en curso (relabeling de campos, fusión de Ubicación/Redes sociales dentro de Contacto, rediseño de `BrandingAssetField.tsx` con botones "Cambiar"/"Eliminar") y se aplicó en su lugar el cambio mínimo que pedía el mensaje corregido -- documentado acá por trazabilidad, no porque haya quedado nada de ese trabajo descartado en el código.

  **El cambio real**: `SettingsForm.tsx` dejó de renderizar la tarjeta "Branding" (título + los dos `BrandingAssetField` de Logo/Favicon). Las otras cuatro tarjetas (Información general, Contacto, Redes sociales, Ubicación) quedaron exactamente como estaban -- mismos títulos, mismos campos, mismo orden. `BrandingAssetField.tsx` no se tocó (sigue con su diseño original, simplemente sin ningún componente que lo importe). `services/settings.ts` (`uploadBrandingAsset`/`deleteBrandingAsset`/`MAX_BRANDING_FILE_SIZE_BYTES`), el bucket de Storage `branding` y la migración de la Fase 14 quedaron sin ningún cambio -- "no eliminar código de backend, servicios, buckets, uploads" fue instrucción explícita.

  **Qué se quitó de `SettingsForm.tsx`, y por qué era inevitable**: sin la tarjeta que las mostraba, `logoSizeError`/`faviconSizeError` (estado que solo existía para el mensaje de "archivo demasiado grande" de esa UI) quedaron sin ningún lugar donde leerse ni escribirse -- se eliminaron para no dejar variables muertas que `pnpm lint` (`@typescript-eslint/no-unused-vars`) iba a marcar como error, incumpliendo la verificación que el propio sprint pedía. `pendingLogo`/`pendingFavicon`/`removeLogo`/`removeFavicon` **sí** se dejaron intactos: siguen leyéndose y escribiéndose dentro de `handleSubmit` (el reset a `null`/`false` tras guardar), así que no están muertos -- simplemente ya no hay ninguna interacción de UI que los cambie de su valor por defecto, con lo cual esas ramas de `handleSubmit` (la subida/borrado real a Storage) no se ejecutan hoy, pero siguen ahí, sin tocar, listas para functionar si algún sprint futuro vuelve a exponer esa UI.

  **Verificado en vivo contra el proyecto real**: `/admin/configuracion` muestra exactamente 4 tarjetas (confirmado por Playwright leyendo los `<h2>` de la página), sin ningún input de archivo (`#logo`/`#favicon`) presente en el DOM; guardar un cambio en un campo normal (Eslogan) sigue funcionando y muestra "Configuración guardada."; el Header de la tienda pública sigue resolviendo el logo/nombre desde `business_settings` sin cambios, y el favicon del `<head>` sigue sirviéndose igual (`/favicon-default.ico`, al no haber ninguno personalizado cargado en el proyecto real). Barrido de las 6 rutas públicas y las 9 del panel sin errores de consola ni 500 -- sin regresiones en ningún otro módulo. `pnpm lint` y `pnpm exec tsc --noEmit` limpios.

---

### Fase 24 — Sprint 6.4: Showcase dinámico de productos en el Hero
- **Sprint pedido al usuario como "Sprint 6.3", renumerado acá como Fase 24/Sprint 6.4** porque ese número ya correspondía a la Fase 23 (ocultar la tarjeta "Branding", ver arriba) -- mismo criterio ya aplicado en este documento ante una colisión de numeración idéntica (Fase 8, pedida como "Fase 5"; Sprint 4.7, pedido como "Sprint 4.6"): se sigue la cronología real de sprints ya ejecutados sobre este repositorio, no el número que cada pedido trae de por sí. Diez restricciones explícitas de "NO modificar" (Productos, Categorías, Inventario, Logística, Checkout, Pedidos, Clientes, Promociones) más el mandato de mantener exactamente el diseño visual del Hero -- textos, botones y layout izquierdo intactos, únicamente el panel derecho podía cambiar.

  **El objetivo**: el panel derecho del Hero era, desde el Sprint 2.1, un marcador visual decorativo y estático (blobs difuminados + una grilla de puntos), pensado en su momento como reserva para una futura imagen de campaña que nunca se implementó. Este sprint lo reemplaza por un Showcase real: productos verdaderos de la tienda, rotando solos, con imagen/nombre/precio/botón "Ver producto".

  **`services/storefront/products.ts`: `getPublicHeroProducts(limit = 6)`, una consulta nueva, no una reutilización de `getPublicProducts()`.** El sprint pedía "productos destacados primero; si no hay ninguno, los más recientes; nunca debe quedar vacío". `getPublicProducts()` ya trae todo el catálogo activo, pero el `Product` que devuelve (el mismo tipo de `config/products.ts` de siempre) no tiene ningún campo de fecha -- no había forma de derivar "más recientes" filtrando en memoria sobre ese array. `getPublicHeroProducts()` hace una consulta a `.eq("featured", true)` con el mismo `PUBLIC_PRODUCT_SELECT`/`mapPublicProduct` que ya usa el resto del archivo (reutilizados, no duplicados); si esa consulta devuelve un array vacío, cae a una segunda consulta `order by created_at desc limit N` sobre los mismos productos activos. Mismo filtro explícito de `is_active` que el resto de `services/storefront/products.ts` desde el Sprint 6.0.1.

  **`hooks/useProductShowcase.ts`, el primer custom hook propio de la tienda pública.** Hasta este sprint, la convención de la sección 11 era categórica: "la tienda pública sigue sin custom hooks propios" -- porque ningún componente fetcheaba sus propios datos, todos recibían props ya resueltas por su página. Este hook no contradice la razón de fondo de esa regla: no le pide nada a Supabase, recibe `products: Product[]` ya resuelto por `Home` y solo encapsula el estado de *presentación* del carrusel -- índice activo, dirección (para que Framer Motion sepa si el slide entra desde la izquierda o la derecha) y si está en pausa. Es exactamente el mismo tipo de estado que ya vivía, sin ser un hook, dentro de `PromotionCarousel` (Fase 21) -- acá se extrajo a uno propio para que `ProductShowcase.tsx` se mantenga puramente presentacional, mismo criterio que ya separa `lib/search.ts` de `CatalogResults.tsx`. El temporizador de auto-avance (`setInterval` cada 1500 ms, dentro de un `useEffect` que depende de `paused`/`products.length`/`index`) se reinicia solo con cualquier cambio de índice -- automático o manual -- sin ningún código aparte para "reiniciar el temporizador", mismo patrón que `PromotionCarousel`.

  **`components/storefront/ProductShowcase.tsx`**: recibe `products: Product[]` por prop (nunca consulta Supabase) y arma el carrusel en el mismo contenedor visual que ya tenía el Hero (`aspect-[4/5]`, `rounded-[2rem]`, `border`) -- mismo tamaño/posición de siempre, para no alterar el layout ya aprobado. Cada slide muestra la imagen real del producto (mismo criterio `isRealImageUrl`/fallback al placeholder decorativo ya establecido desde la Fase 12 para `ProductCard`/`ProductGallery`), nombre, precio (+ precio anterior tachado si `oldPrice` existe) y un botón "Ver producto" (`render={<Link href={...} />}`, patrón Base UI de siempre) hacia `/productos/[slug]`. `AnimatePresence` con `initial={false}` evita que la primera imagen anime su entrada -- la primera pintada (servidor y cliente) es siempre estática, sin necesitar el `mounted`-gate que sí usan `Header.tsx`/`PromotionalBanner.tsx` para el mismo riesgo de mismatch de hidratación de `useReducedMotion()` (ver sección 5, "por qué", para la comparación completa). Pausa al entrar el mouse (`onMouseEnter`/`onMouseLeave` sobre el contenedor completo), flechas izquierda/derecha y dots inferiores para navegación manual con loop infinito.

  **Dos bugs reales encontrados y corregidos durante la verificación de este mismo sprint** (ver sección 5, "por qué", para el detalle técnico completo): (1) los indicadores inferiores vivían originalmente dentro del `motion.div` que `AnimatePresence` reemplaza en cada auto-avance, así que se desmontaban y remontaban cada ~1.5s -- con el riesgo real de que un click sobre un dot quedara "pisado" justo cuando el carrusel avanzaba solo (reproducido con Playwright: clicks intermitentemente fallidos). Corregido moviendo flechas y dots a hermanos estables, fuera del área animada. (2) Un dot inactivo tenía `width: 0px` -- la clase base le daba altura (`h-2`) pero ninguna clase de ancho, solo el dot activo ganaba `w-5`; sin contenido, un `<button>` sin ancho explícito colapsa a 0px. Corregido agregando `w-2` a la clase base.

  **Responsive, resuelto enteramente dentro de `ProductShowcase.tsx`, sin tocar el grid del Hero** (`grid-cols-1 lg:grid-cols-2`, protegido): `mx-auto w-full md:max-w-md lg:max-w-none` en el contenedor raíz -- ancho completo en mobile (debajo del texto, ya era el comportamiento del grid de una columna), acotado a `28rem` y centrado específicamente en tablet (768-1023px, el pedido explícito del sprint de "reducir el tamaño"), y sin límite en desktop (vuelve a llenar su columna del grid de dos columnas, sin cambios).

  **`Hero.tsx`**: único cambio, un prop nuevo `showcaseProducts: Product[]` y el reemplazo del `<div>` decorativo estático por `<ProductShowcase products={showcaseProducts} />`, dentro del mismo `motion.div variants={item}` de siempre (mismo stagger de entrada del Hero, sin cambios). Textos, botones, columna izquierda y las variantes `container`/`item` de Framer Motion no se tocaron.

  **`app/(storefront)/page.tsx` (Home)**: se sumó `getPublicHeroProducts()` como una quinta llamada en paralelo dentro del `Promise.all` ya existente (junto a productos destacados/categorías/settings/promociones), y su resultado se pasa a `Hero` como `showcaseProducts` -- los productos se piden una única vez por request, nunca dentro del componente ni en cada tick del carrusel.

  **Verificado en vivo contra el proyecto real, con Playwright**: 6 productos destacados reales del proyecto (`iPhone 14`, `Tacos`, etc.) mostrados con sus 6 indicadores correspondientes; auto-avance confirmado después de ~1.5-2s con el nombre del producto cambiando; pausa confirmada mientras el mouse permanece sobre el Showcase (sin avanzar durante 3s de espera) y reanudación confirmada al retirar el mouse; flecha derecha avanza un slide exacto, flecha izquierda retrocede, click directo en un dot navega a ese índice exacto; loop infinito confirmado (del último producto, la flecha derecha vuelve al primero); botón "Ver producto" navega a `/productos/[slug]` y el `<h1>` de la página de destino coincide exactamente con el nombre mostrado en el Showcase al momento del click. Fallback a "más recientes" confirmado desmarcando temporalmente `featured` de los 6 productos reales por SQL directo (restaurado exactamente igual al finalizar): el Showcase mostró productos igual, nunca vacío. Responsive confirmado con capturas en desktop (1440px, Showcase a ancho completo de su columna), tablet (768px, visiblemente más chico y centrado) y mobile (390px, ancho completo debajo del texto) -- sin overflow horizontal en ningún breakpoint. Barrido de regresión de las 7 rutas públicas (incluyendo `/productos/tacos`) y las 9 rutas del panel: todas 200, sin errores de consola ni de página. `pnpm lint` y `pnpm exec tsc --noEmit` limpios en todo el proyecto.

---

### Fase 25 — Sprint 6.5: Configuración reorganizada en pestañas (General + Apariencia)
- **Sprint 6.5**: mejora de UX/UI sobre el módulo Configuración, sin numeración pedida por el usuario que colisionara esta vez -- se continuó la secuencia desde la Fase 24/Sprint 6.4. Diez restricciones explícitas de "NO modificar" (Productos, Categorías, Inventario, Promociones, Logística, Checkout, la tienda pública, Supabase, Storage) más "NO modificar ninguna funcionalidad existente" -- este sprint es 100% de reorganización visual sobre un módulo ya existente, sin tocar ninguna otra parte del proyecto.

  **El objetivo**: desde la Fase 23 (Sprint 6.3), `/admin/configuracion` tenía 4 tarjetas visibles (Información general, Contacto, Redes sociales, Ubicación) y una quinta -- "Branding" (Logo/Favicon) -- oculta mediante instrucción explícita de un sprint anterior, con su funcionalidad intacta pero sin ningún punto de entrada en la interfaz. Este sprint pidió eliminar definitivamente la idea de una tarjeta "Branding" y en su lugar crear una segunda pestaña, "Apariencia", que además de traer de vuelta Logo/Favicon (renombrados "Identidad Visual"), deja preparada -- solo visualmente, sin funcionalidad -- la estructura para tres personalizaciones futuras: Hero, Colores del tema y Tipografía.

  **`Tabs` de shadcn, agregado con el CLI (`pnpm dlx shadcn@latest add tabs`), primera vez que el panel usa pestañas.** Genera `components/ui/tabs.tsx` sobre `@base-ui/react/tabs` -- mismo Base UI que el resto del proyecto, nunca Radix (principio 10, sección 6). `SettingsForm.tsx` envuelve todo su contenido en un único `<Tabs defaultValue="general">` (no controlado -- no hace falta levantar el tab activo a ningún estado, es pura navegación) con dos `TabsContent`: `"general"` (las 4 `SettingsSection` que ya existían, sin ningún cambio de campos, validación ni copy) y `"apariencia"` (Identidad Visual + Hero + Colores del tema + Tipografía, las cuatro nuevas para este sprint). El `<form onSubmit={handleSubmit}>` sigue envolviendo el `Tabs` completo -- un solo submit, exactamente como antes: cambiar de pestaña no dispara ningún guardado ni pierde ningún campo ya tocado en la otra, porque todo el estado (`form`, `pendingLogo`, `pendingFavicon`, `removeLogo`, `removeFavicon`, etc.) sigue viviendo en `SettingsForm`, por encima de las pestañas.

  **Identidad Visual: Logo y Favicon reconectados con la lógica exacta de antes de ocultarse en la Fase 23, no reescrita.** La Fase 23 había quitado `logoSizeError`/`faviconSizeError` (sin ninguna UI que los mostrara, quedaban sin uso) pero había dejado adrede intactos `pendingLogo`/`pendingFavicon`/`removeLogo`/`removeFavicon` y toda la lógica de subida/reemplazo/borrado dentro de `handleSubmit`. Este sprint restauró `logoSizeError`/`faviconSizeError` y los handlers `onSelectFile`/`onRemove` de `BrandingAssetField` **línea por línea** desde la versión anterior a que "Branding" se ocultara (confirmado por diff contra `git show` del commit previo) -- la única diferencia real es el título de la tarjeta ("Identidad Visual" en vez de "Branding") y su nueva ubicación (pestaña Apariencia en vez de en medio de las tarjetas generales). `BrandingAssetField.tsx` y `services/settings.ts` (`uploadBrandingAsset`/`deleteBrandingAsset`/`MAX_BRANDING_FILE_SIZE_BYTES`/`MAX_BRANDING_FILE_SIZE_MB`) no se tocaron ni una línea.

  **Hero, Colores del tema y Tipografía: estructura visual, cero funcionalidad -- tal como pidió explícitamente el sprint.** Las tres son `SettingsSection` nuevas dentro de la pestaña Apariencia, cada una con un badge "Próximamente" (`ComingSoonBadge`, un wrapper chico sobre `Badge` de shadcn) junto al título:
  - **Hero**: tres tarjetas decorativas (`<div>`, no controles reales) para "Imagen"/"Video"/"Productos destacados", cada una con un ícono de lucide-react (`Image`, `Video`, `Sparkles`) y su etiqueta -- sin ningún `onClick`, sin ningún estado de selección. `HERO_PREVIEW_OPTIONS` es un array estático a nivel de módulo.
  - **Colores del tema**: tres tarjetas con un swatch circular + etiqueta ("Color principal"/"Color secundario"/"Color de botones") -- los swatches usan `var(--primary)`/`var(--secondary)`/`var(--accent)`, los tokens `oklch` que ya existen en `globals.css`, para que la previsualización ya sea coherente con la paleta real de la tienda en vez de mostrar colores de ejemplo inventados. `THEME_COLOR_PREVIEW_OPTIONS`, mismo criterio que `HERO_PREVIEW_OPTIONS`.
  - **Tipografía**: un único párrafo, "Disponible próximamente." -- tal como pedía el sprint literalmente, sin ningún elemento adicional.

  Ninguna de las tres agrega un `useState`, ni lee ni escribe ningún campo de `SettingsFormInput`/`business_settings`, ni requiere ninguna migración: son exactamente lo que pedía el sprint, "dejar preparada la interfaz para futuros sprints" sin implementar nada detrás.

  **`SettingsSection` gana un prop opcional `badge`** (`ReactNode`, no usado por ninguna de las 4 secciones de "General", que simplemente no lo pasan) para que Hero/Colores del tema/Tipografía puedan mostrar el badge "Próximamente" junto al título sin que las secciones existentes cambien su forma.

  **Verificado en vivo contra el proyecto real, con Playwright**: confirmado que el `<h2>` "Branding" ya no existe en ningún lado de la pantalla; ambas pestañas presentes y navegables, con "General" activa por defecto mostrando las 4 tarjetas de siempre (y ninguna traza de "Identidad Visual" ahí) y "Apariencia" mostrando sus 4 secciones nuevas con los 3 badges "Próximamente" exactos (Hero, Colores del tema, Tipografía) más el mensaje literal en Tipografía. Ciclo completo de Logo verificado de punta a punta contra Supabase real: seleccionar un archivo real dispara el file chooser nativo y muestra el preview local; "Guardar cambios" sube el archivo al bucket `branding` y persiste la URL en `business_settings` (confirmado con "Configuración guardada." y, tras un reload completo de la página, la imagen mostrada con una URL real de `supabase.co/storage/...`); "Quitar" + guardar de nuevo lo borra correctamente, dejando el proyecto real limpio de datos de prueba. Responsive verificado en desktop (1440px), tablet (768px) y mobile (390px) en ambas pestañas, sin overflow horizontal en ningún breakpoint -- en mobile, las tarjetas de Hero y Colores del tema pasan a una sola columna. Barrido de regresión de las 7 rutas públicas y las 9 rutas del panel: todas 200, sin errores de consola ni de página. `pnpm lint` y `pnpm exec tsc --noEmit` limpios en todo el proyecto.

---

### Fase 26 — Sprint 6.2.1.1: corrección de RLS al guardar Configuración → Apariencia
- **Bug reportado por el usuario**: en producción y en local, subir un logo/favicon y presionar "Guardar cambios" en `/admin/configuracion` devolvía `new row violates row-level security policy`. Sprint exclusivamente de diagnóstico y corrección -- sin nuevas funcionalidades, sin cambios de arquitectura, con la restricción explícita de no desactivar RLS ni usar la Service Role Key desde el frontend.

  **Causa raíz, no una policy mal escrita**: `is_admin()` (`select exists (select 1 from public.admins where id = auth.uid())`) es la función que gatea *todas* las policies de escritura del proyecto -- `branding_admin_insert`/`_update` (Storage), `business_settings_admin_update`, y las de `products`/`categories`/`orders`/`promotions`/`shipping_rates`/etc. (50+ policies en total). La tabla `public.admins` estaba **completamente vacía**. La cuenta con la que el usuario opera hoy (`felixmatiasarandarromero@gmail.com`, un usuario real de `auth.users`) es distinta de la cuenta de prueba `admin-test@regishop.local` creada en la Fase 10 (que ya no existe en el proyecto) -- nunca se le había creado su fila correspondiente en `admins`. Sin esa fila, `is_admin()` devolvía `false` para el usuario logueado, y **cualquier** escritura administrativa del panel (no solo Configuración) estaba silenciosamente rota -- el bug reportado era solo el primer síntoma que el usuario notó. Coincide exactamente con el pendiente #1 de la sección 13 ("crear el primer usuario administrador real").

  **Diagnóstico verificado, no hipotético**: se reprodujo el error exacto simulando el JWT del usuario real (`set_config('request.jwt.claims', ...)` + `set local role authenticated`) dentro de una transacción con `rollback` -- el mismo `insert` que hace la subida a `storage.objects` (bucket `branding`) devolvió textualmente `ERROR: 42501: new row violates row-level security policy for table "objects"`, idéntico al error reportado por el usuario.

  **Corrección: un `insert` de datos, no una migración de esquema ni un cambio de policy** -- mismo criterio ya establecido en la Fase 20 (corrección de datos vs. cambio de esquema). Las policies existentes (`branding_admin_insert`, `business_settings_admin_update`, etc.) ya seguían la práctica correcta (exigir `is_admin()`, ningún acceso público de escritura) -- no había nada que corregir ahí. Se ejecutó una única sentencia contra el proyecto real:
  ```sql
  insert into public.admins (id, full_name, role)
  values ('8b8a2d89-9feb-4296-ad92-b57e7e0a7cce', 'Matias Aranda', 'owner');
  ```
  `admins` no tiene (ni debe tener) policy de `insert` para `authenticated` -- permitir que un usuario se auto-inserte como admin sería una escalación de privilegios -- así que la única forma correcta de dar de alta un admin real sigue siendo un insert directo contra la base, hecho una vez, exactamente el mismo mecanismo ya usado en la Fase 10 para la cuenta de prueba.

  **Verificado sin necesitar la contraseña real del usuario**: se repitió la misma simulación de JWT después del insert, en una transacción con `rollback` (sin dejar residuos) -- el mismo `insert` a `storage.objects` y un `update` a `business_settings` devolvieron éxito. Se confirmó además, listando todas las policies que dependen de `is_admin()` en el esquema completo, que la fila nueva en `admins` restaura *todas* las escrituras administrativas a la vez (Productos, Categorías, Pedidos, Promociones, Logística, ambos buckets de Storage) -- no una corrección puntual para Configuración, sino la causa real detrás de cualquier "RLS violation" que el panel pudiera dar hoy. El usuario confirmó en vivo, con su cuenta real, que subir/guardar/reemplazar/eliminar logo y favicon ya funciona.

  **Cero archivos de código modificados** (`git status` limpio antes y después) -- el bug nunca estuvo en `SettingsForm.tsx`, `services/settings.ts`, `BrandingAssetField.tsx` ni en ninguna policy: vivía enteramente en un dato faltante en Supabase.

---

### Fase 27 — Sprint 6.2.1.2: auditoría completa de políticas RLS del Panel Administrativo
- **Motivo**: tras la Fase 26, apareció el mismo tipo de error (`new row violates row-level security policy`) pero para la tabla `promotions` -- el usuario pidió, en vez de seguir corrigiendo tabla por tabla, una auditoría completa de RLS sobre todas las tablas que usa `/admin`, sin desactivar RLS ni usar la Service Role Key desde el frontend.

  **Alcance de la auditoría**: las 17 tablas de `public` (`admins`, `banners`, `benefits`, `business_settings`, `categories`, `cities`, `customers`, `departments`, `order_items`, `orders`, `product_images`, `products`, `promotions`, `shipping_rate_cities`, `shipping_rates`, `stock_movements`, `testimonials`) más `storage.objects` (buckets `product-images`/`branding`) -- confirmado con `pg_tables` que **todas** tienen `rowsecurity = true`, ninguna con RLS desactivado.

  **Metodología, no hipotética**: se listaron las ~65 policies del esquema completo (`pg_policies`), se mapeó cada operación que el panel realmente ejecuta (`grep` sobre `services/*.ts` para `.insert(`/`.update(`/`.delete(`/`.upsert(`/`.rpc(`), y se probó **cada** combinación tabla+operación simulando el JWT real del usuario administrador (`set_config('request.jwt.claims', ...)` + `set local role authenticated`, dentro de una transacción con `rollback`, sin dejar ningún dato de prueba): `products`+`product_images` (insert/update/delete), `categories` (insert/update/delete), `orders` (update de estado), `shipping_rates`+`shipping_rate_cities` (insert/update/delete), `business_settings` (update), inventario (`register_stock_movement` vía RPC + update directo de `sku`/`low_stock_threshold` en `products`), y `promotions` (insert/update/delete) -- **las 9 combinaciones devolvieron éxito**.

  **La causa del error de `promotions` era exactamente la misma de la Fase 26** (la fila faltante en `admins`, ya corregida en ese sprint) -- no una policy nueva ni distinta. La policy `promotions_admin_insert` (`with_check: is_admin()`) ya seguía el mismo patrón correcto que las demás 49+ policies de escritura del esquema; una vez insertada la fila del admin real, el mismo `insert` que fallaba antes de la Fase 26 pasó a funcionar sin ningún cambio adicional.

  **Confirmado también en sentido inverso**: simulando el rol `anon` (sin sesión), un `insert` en `products`/`promotions` fue rechazado con el mismo error de RLS (`42501`); un `update` en `business_settings` y un `delete` en `products` fueron bloqueados silenciosamente (0 filas afectadas -- comportamiento esperado de Postgres para `USING`, a diferencia del error explícito que da `WITH CHECK` en un `insert`, pero igual de seguro: se confirmó que el valor real de la fila no cambió). Ningún usuario no autenticado puede escribir en ninguna tabla del panel.

  **Resultado de la auditoría: ninguna policy faltaba ni estaba mal escrita.** Las ~65 policies del esquema siguen, sin excepción, el mismo patrón ya documentado desde la Fase 8: lectura pública acotada (`is_active = true`, y para `promotions` además el rango de fechas) donde corresponde, escritura únicamente para `{authenticated}` gateada por `is_admin()`, sin ninguna policy de escritura para `public`/`anon` en ningún lado. `banners`/`testimonials`/`benefits` tienen su RLS ya correctamente preparada (lectura pública activa + CRUD admin) pero **sin ningún módulo del panel que las use hoy** -- no es un gap de este sprint, son tablas del diseño original (Fase 8) sin módulo construido todavía. `departments`/`cities` siguen deliberadamente sin ninguna policy de escritura (ni para admins), y `admins` sigue sin policy de `insert` para `authenticated`, ambas decisiones de diseño ya documentadas, no bugs.

  **Cero migraciones creadas, cero policies modificadas, cero archivos de código tocados** -- la Fase 26 ya había resuelto la única causa real (el dato faltante en `admins`), y esta auditoría confirma exhaustivamente que esa fue la única causa en todo el esquema, no una entre varias. El panel completo (Productos, Categorías, Inventario, Pedidos, Promociones, Logística, Configuración) puede crear, editar y eliminar registros sin ningún error de RLS.

---

### Fase 28 — Sprint 6.6: descripción enriquecida para la página del producto
- **Sprint 6.6** (pedido al usuario como "Sprint 6.3", renumerado acá por la misma razón ya aplicada varias veces en este documento -- ese número ya correspondía a la Fase 23; se sigue la cronología real de sprints ejecutados, no el número que trae cada pedido). Objetivo: reemplazar el `<textarea>` plano de "Descripción completa" en `/admin/productos` por un editor enriquecido profesional, mostrado únicamente en la página individual del producto -- con restricciones explícitas de no tocar el catálogo, `ProductCard`, `/productos`, los Productos Destacados ni el carrusel del Home.

  **Diagnóstico inicial, antes de escribir código**: `product.description` (agregada a la tabla en la Fase 10) nunca se leía en ningún lado de la tienda pública -- `PUBLIC_PRODUCT_SELECT` (`services/storefront/products.ts`) ni siquiera la seleccionaba. Esto simplificó el alcance real de la restricción "no tocar el catálogo/Home/destacados": esos componentes nunca conocieron ese campo, así que no hacía falta ninguna guarda especial para que siguieran sin mostrarlo -- alcanzaba con no agregarlo a las consultas que ellos usan.

  **Editor elegido: Tiptap** (`@tiptap/react` + `@tiptap/starter-kit` + extensiones oficiales de color/highlight/imagen/YouTube) -- ver sección 5 ("por qué") para la comparación completa contra Quill/Lexical. Headless de verdad: la toolbar (`EditorToolbar.tsx`) usa los mismos `Button`/`Select`/`Popover` de shadcn que el resto del panel, sin ningún tema visual ajeno.

  **`RichTextEditor.tsx`** (`components/admin/products/`): orquesta `useEditor()` con `StarterKit` (negrita, cursiva, tachado, títulos H1-H6, listas, citas, separador, `Link`/`Underline` incluidos desde v3), `TextStyle`+`Color` (color de texto), `Highlight` (resaltado), `ResizableImage`/`Video` (nodos custom, `rich-text/extensions.ts`) y `Youtube` (oficial). `immediatelyRender: false` -- la solución que el propio Tiptap documenta para evitar un error de hidratación en frameworks SSR como Next.js, mismo problema de fondo que `leaflet` (Sprint 3.7) pero resuelto con la opción que Tiptap expone para su propio caso, no con `next/dynamic(ssr:false)`.

  **`ResizableImage`/`Video`** (`rich-text/extensions.ts`): `ResizableImage` extiende `@tiptap/extension-image` agregando `width` (presets 25/50/75/100%) y `align` (izquierda/centro/derecha) como atributos que se renderizan como `style` inline (`mergeAttributes` de `@tiptap/core` concatena los fragmentos de `style` de ambos atributos correctamente). `Video` es un Node propio (Tiptap no trae uno oficial) con el mismo patrón de atributos, renderizado como `<video controls>`. Ninguno de los dos necesitó un NodeView custom con handles de arrastre: clickear la imagen/video ya la selecciona como nodo (comportamiento nativo de ProseMirror para nodos atómicos), y los botones de la toolbar aplican `updateAttributes("image"|"video", {...})` sobre lo que esté seleccionado.

  **Subida de imágenes/GIFs/video: bucket nuevo, `product-content`**, separado de `product-images` (la galería estructurada de fotos, Fase 8/10) por el mismo criterio que ya separó `branding` de `product-images` en la Fase 14 -- migración `20260717142856_create_product_content_bucket.sql` (30 MB de límite, admite `image/png`/`jpeg`/`webp`/`gif` y `video/mp4`/`webm`; políticas RLS idénticas al patrón ya establecido: lectura pública, escritura solo `is_admin()`). `uploadDescriptionAsset()` (`services/storage.ts`) sube el archivo **al insertarlo en el editor**, no al guardar el formulario completo -- mismo comportamiento que cualquier editor real (WordPress, Notion): el `scopeId` (carpeta del bucket) es el `productId` real en edición, o un UUID generado una única vez por `ProductForm.tsx` mientras se crea un producto nuevo que todavía no tiene id.

  **Sanitización contra XSS, con `sanitize-html` (no `dompurify`/`isomorphic-dompurify`)** -- `lib/sanitize-html.ts` (`sanitizeDescriptionHtml()`, lista blanca explícita de tags/atributos/estilos; `isEmptyDescriptionHtml()`, para no guardar un `<p></p>` vacío como si fuera contenido real). El cambio de librería fue un bug real encontrado durante la verificación -- ver más abajo.

  **`getPublicProductBySlug()` gana su propio select** (`PUBLIC_PRODUCT_DETAIL_SELECT = PUBLIC_PRODUCT_SELECT + ", description"`), sin tocar el select compartido que usan `getPublicProducts()`/`getPublicHeroProducts()` (Catálogo, Home, Destacados, Showcase) -- esos tres siguen sin traer `description` en absoluto, ni con el campo vacío: la instrucción de "no modificar el catálogo/destacados/carrusel" se cumple por diseño de la consulta, no por una condición en el componente.

  **`ProductRichDescription.tsx`** (`components/storefront/`, nuevo): única pieza de la tienda pública que renderiza `product.description` -- una sección "Descripción completa" entre la galería/buy box y "Productos relacionados" en `/productos/[slug]/page.tsx` (el único archivo de página tocado). Devuelve `null` si no hay descripción o si `isEmptyDescriptionHtml()` la considera vacía. Estilizada con la clase `prose` de `@tailwindcss/typography` (plugin nuevo, `@plugin "@tailwindcss/typography"` en `globals.css`) -- único consumidor de ese plugin en el proyecto.

  **Cinco bugs reales encontrados y corregidos durante la verificación de este mismo sprint** (ver sección 5, "por qué", para el detalle técnico completo de cada uno):
  1. **Bundling de Turbopack con `isomorphic-dompurify`**: `pnpm build` fallaba con un `ENOENT` al intentar resolver archivos internos de `jsdom` (`default-stylesheet.css`) con un path inválido (`C:\ROOT\...`). Resuelto migrando a `sanitize-html`, que no depende de ningún DOM emulado.
  2. **Extensiones duplicadas de Tiptap**: `StarterKit` (v3) ya incluye `Link`/`Underline` -- agregar `@tiptap/extension-link`/`@tiptap/extension-underline` como paquetes aparte (hábito de v2) generaba `Duplicate extension names` y comportamiento de edición inconsistente. Resuelto configurándolos vía `StarterKit.configure({ link: {...} })` y quitando los dos paquetes.
  3. **Round-trip roto**: `useEditor({ content: value })` solo usa `value` para el contenido *inicial* -- en modo edición, el editor ya se había creado con contenido vacío para cuando `useProduct()` terminaba de traer la descripción real de forma asíncrona, así que reabrir un producto para editarlo mostraba el editor vacío (guardar así lo hubiera borrado). Resuelto con un efecto que llama a `editor.commands.setContent(value, { emitUpdate: false })` cuando `value` cambia desde afuera.
  4. **HTML sin sanitizar llegando al cliente**: aunque `ProductRichDescription.tsx` sanitizaba antes de renderizar, el `Product` completo (con la `description` todavía cruda en ese punto de la cadena) se pasaba como prop a `ProductGallery`/`ProductBuyBox` (Client Components) -- React serializa cada prop para la hidratación sin importar si algún componente la muestra, así que el HTML peligroso (probado con un payload real: `<script>`, `onerror=`, un `<iframe>` a un dominio ajeno) terminaba inerte pero visible en el código fuente de la página. Nunca se ejecutó (confirmado), pero tampoco debía salir del servidor en su forma cruda. Resuelto sanitizando dentro de `getPublicProductBySlug()`, no solo al renderizar.
  5. **El wrapper de YouTube se perdía al sanitizar**: `@tiptap/extension-youtube` exige `div[data-youtube-video] iframe` para reconocer un embed al releer HTML guardado -- la lista blanca de `sanitize-html` no incluía `div`, así que ese wrapper se eliminaba al guardar (dejando un `<iframe>` suelto, que se veía bien en la tienda pero ya no se reconocía como YouTube al reabrir el producto para editar). Resuelto agregando `div` a la lista blanca, con el único atributo `data-youtube-video` permitido.

  Un sexto detalle menor, no un bug de comportamiento visible: el navegador normaliza `margin-left: 0` a `"0px"` en el estilo inline, y el regex original de `allowedStyles` solo aceptaba `"0"` literal -- se corrigió para aceptar ambas formas (no cambiaba el resultado visual, porque el margen inicial de un bloque ya es 0 por default, pero no correspondía depender de esa coincidencia).

  **Verificado en vivo con Playwright, aislando cada capacidad individualmente** (una batería inicial de pruebas combinadas -- muchas acciones en secuencia rápida sobre el mismo editor -- producía pérdida de contenido intermitente; aislar cada capacidad por separado, con esperas generosas y reposicionando el cursor explícitamente entre inserciones, confirmó que el editor en sí funciona correctamente y que la inconsistencia era de la automatización, no del producto): negrita, cursiva, subrayado y tachado combinados; título H1; listas con viñetas y numeradas; cita; separador; enlace; color de texto; resaltado; imagen (con resize a 50% y alineación izquierda); GIF; video subido como archivo -- las 15 capacidades pedidas por el sprint, confirmadas al nivel del HTML efectivamente guardado en Supabase. Ciclo completo alta → edición (reabrir el producto y confirmar que el editor carga con la imagen/lista/separador/YouTube ya guardados) → visualización en `/productos/[slug]` (sección "Descripción completa" visible, imagen/lista/iframe renderizados). Prueba de XSS: un payload malicioso inyectado directo en la base (`<script>`, `onerror=`, `<iframe>` a `evil.example.com`, un link `javascript:`) confirmado sin ejecutarse y sin aparecer en ninguna forma en el código fuente de la página, con el texto legítimo del mismo payload mostrándose con normalidad. Confirmado que Home y el Catálogo (con el producto de prueba visible por nombre) no muestran ni la imagen de la descripción ni ningún `<iframe>` en ningún lado. Barrido de regresión de 18 rutas (7 públicas incluyendo `/productos/[slug]`, 10 del panel incluyendo `/admin/productos/nuevo`) sin errores de consola. Todos los datos y archivos de prueba (producto, 9 archivos subidos al bucket `product-content`, la cuenta de administrador temporal creada para poder loguearse en Playwright ya que la cuenta real no tiene contraseña conocida) se limpiaron del proyecto real al finalizar. `pnpm lint` y `pnpm exec tsc --noEmit` limpios en todo el proyecto.

---

### Fase 29 — Sprint 6.7: Rediseño Premium del Catálogo
- **Sprint 6.7** (pedido al usuario como "Sprint 6.4", renumerado acá por la misma razón ya aplicada varias veces en este documento -- ese número ya correspondía a la Fase 24, el Showcase del Hero; se sigue la cronología real de sprints ejecutados, no el número que trae cada pedido). Objetivo explícito: rediseñar la experiencia **visual** del catálogo para una imagen premium/moderna, sin agregar ninguna funcionalidad nueva -- sin selector de columnas, sin tocar el panel, la página individual de producto, el checkout ni el carrito.

  **Tensión real detectada antes de escribir código, resuelta con el usuario, no asumida**: el pedido decía a la vez "rediseñar completamente las tarjetas" y "NO modificar Categorías" -- pero `ProductCard.tsx` es un único componente compartido por Home, Catálogo, Categorías y Productos Relacionados (principio 9, reutilizar antes que crear); no existe forma de rediseñarlo sin que Categorías (y Home, y la sección de relacionados de la página de producto) hereden el mismo cambio visual. Se preguntó explícitamente: mantener una única tarjeta compartida (la interpretación de "NO modificar Categorías" pasa a ser "no tocar su lógica de navegación/filtrado", no "que su tarjeta se vea distinta a las demás") vs. duplicar en un `CatalogProductCard.tsx` propio de `/productos` que dejara el resto de la tienda pixel-a-pixel intacto. El usuario confirmó la primera opción, con el objetivo explícito de que el rediseño priorice **la experiencia mobile**: tarjetas más bajas, mejor aprovechamiento del espacio, más productos por pantalla, diseño limpio y profesional, sin crear un componente duplicado.

  **De ahí se derivó el límite real de qué se podía tocar, sin que el pedido lo dijera explícitamente campo por campo**: `ProductGrid.tsx` (`grid-cols-1 md:grid-cols-2 lg:grid-cols-4`) es renderizado directamente por `RelatedProducts.tsx` en `/productos/[slug]` -- una página explícitamente protegida este sprint. Cambiar la cantidad de columnas de `ProductGrid` (por ejemplo, a 2 columnas en mobile, que hubiera sido la forma más directa de lograr "que entren más productos por pantalla") habría alterado también la sección "Productos relacionados" de la página de producto -- se descartó esa vía. En su lugar, "más productos por pantalla en mobile" se resuelve enteramente **reduciendo la altura de cada tarjeta** dentro de `ProductCard.tsx` (mismo número de columnas, cada fila ocupa menos alto) -- ni `ProductGrid.tsx`, ni `CatalogToolbar.tsx`, ni `CatalogResults.tsx` (los tres compartidos con Categorías) se tocaron. `src/app/(storefront)/productos/page.tsx`, aunque no compartido con Categorías (cada ruta tiene su propio archivo de página), tampoco se modificó -- ya era un shell mínimo (breadcrumb + título + descripción), y tocarlo solo para "hacer algo" sin una razón concreta hubiera introducido una inconsistencia visual gratuita frente a `/categorias/[slug]/page.tsx`, que usa el mismo bloque de header pixel a pixel.

  **`ProductCard.tsx`, el único archivo modificado, rediseñado de punta a punta**:
  - **Menos altura, imagen protagonista**: se eliminó la descripción corta (`shortDescription`, un párrafo de 2 líneas) y la fila separada de "estado de stock" (punto de color + texto "En stock"/"Sin stock"/"¡Últimas unidades!") que ocupaban buena parte de la tarjeta -- ninguna de las dos estaba en la lista de información que pedía el sprint (Nombre / Precio / Precio anterior / Descuento / Botones), y "Últimas unidades" está listada ahí mismo como *ejemplo de badge*, no de texto plano. Se migró exactamente a eso: un badge ámbar sobre la imagen (junto al de descuento, cuando aplica), no una fila de texto aparte. El padding pasó de `p-5` a `p-3 sm:p-4`, el título a una sola línea (`line-clamp-1`, con `title` nativo para el texto completo) y el botón a `size="sm"` -- la imagen (`aspect-square`, sin cambios de proporción) pasa a ser una porción mayor del alto total de la tarjeta.
  - **Botones**: se mantiene "Agregar al carrito" (sin agregar un segundo botón de compra -- ver más abajo, "confirmación explícita de que no se agregó funcionalidad"), con `size="sm"` para acompañar la tarjeta más compacta. Los estados hover/focus/disabled/microanimación de presión (`active:translate-y-px`) ya vivían en el componente compartido `Button` (`components/ui/button.tsx`, sin tocar) -- reutilizados tal cual, sin reinventarlos.
  - **Badges**: unificados en un solo contenedor (`top-2.5 left-2.5`, `flex flex-col gap-1.5`) que apila el badge de descuento/info (ya existente, vía `getProductDisplayPrice()`, sin cambios de lógica) y el nuevo badge de "Últimas unidades" cuando `stock > 0 && stock <= 5` (mismo umbral que ya definía `getStockStatus()`, reutilizado -- ver más abajo). Sin stock, la imagen se atenúa (`opacity-70 grayscale-[0.35]`) y el botón ya decía "Sin stock" -- se reforzó visualmente una señal que antes solo estaba en el botón.
  - **Hover/focus modernos**: elevación sutil (`whileHover={{ y: -4 }}`, antes `-6`, más "premium/discreto"), sombra elegante en hover (`hover:shadow-xl`) más un leve realce del borde (`hover:border-foreground/15`), y **`focus-within:ring-2` en el contenedor de la tarjeta completa** -- nuevo: antes, navegar con teclado hasta el `Link` interno no dejaba ningún indicador visible claro alrededor de toda la tarjeta (el foco por defecto del navegador queda parcialmente recortado por el `overflow-hidden` de la imagen); ahora el anillo de foco envuelve la tarjeta entera, con `outline-none` en el `Link` interno para no duplicar el indicador.
  - **Imagen protagonista, preparada para un futuro hover-swap sin implementarlo**: `sizes` del `next/image` se corrigió para reflejar la grilla real (`grid-cols-1` en mobile, `md:grid-cols-2`, `lg:grid-cols-4` -- antes decía `50vw` para mobile cuando en mobile la tarjeta ocupa prácticamente el 100% del ancho, causando que Next sirviera una imagen más chica de la que realmente se necesitaba); `object-cover` y lazy loading (default de `next/image` sin `priority`) sin cambios. Se dejó un comentario explícito, sin ningún estado ni handler nuevo, marcando dónde un futuro `onMouseEnter` podría intercambiar a `product.images[1]` -- el dato (`product.images: string[]`) ya soporta esto desde siempre, no hizo falta ningún cambio de arquitectura para dejarlo preparado.
  - **Limpieza de deuda técnica documentada, habilitada por este sprint**: `ProductCard.tsx` mantenía copias locales de `isRealImageUrl()`/`getStockStatus()` en vez de importarlas de `lib/products.ts` -- deuda ya señalada explícitamente en la sección 11 desde hace varios sprints ("unificarla es un cambio de una línea cuando se autorice tocar ese componente"). Con este sprint autorizando tocar el archivo por completo, se unificó: ambas funciones se importan ahora de `lib/products.ts`, sin ningún cambio de comportamiento.

  **Confirmación explícita de que no se agregó ninguna funcionalidad nueva** (pedida por el propio sprint): sin selector de columnas; sin un segundo botón "Comprar" (la sección INFORMACIÓN del pedido lista "Botón Comprar" junto a "Botón Agregar al carrito", pero la tarjeta nunca tuvo un botón de compra directa/WhatsApp -- agregar uno hubiera sido una funcionalidad nueva, explícitamente prohibida; se interpretó como la jerarquía de información ya existente, no como un pedido de agregar un botón); sin cambio de comportamiento en "Agregar al carrito" (mismo `addProduct` de `useCartStore`, sin tocar); sin ningún nuevo campo de datos ni columna de Supabase; sin cambiar cuántas columnas muestra la grilla en ningún breakpoint; sin implementar el cambio de imagen al pasar el mouse (solo se dejó preparada la arquitectura, sin wiring). "Loading" (uno de los 4 estados de botón pedidos) no se modeló con un spinner: "Agregar al carrito" es una actualización síncrona de Zustand, sin ninguna operación asíncrona real detrás -- fabricar un estado de carga falso para algo instantáneo habría sido una UI engañosa, no un estado real.

  **Performance**: sin `useMemo`/`useCallback` nuevos -- los cálculos por render (`categories.find`, `getProductDisplayPrice`, `getStockStatus`) ya eran igual de baratos antes de este sprint, agregar memoización acá hubiera sido sobreingeniería sin un problema real que resolver (principio 6). La corrección del prop `sizes` de `next/image` (mobile ahora pide `100vw` en vez de `50vw`) es una mejora real, no cosmética: antes la tarjeta en mobile (que ocupa casi todo el ancho de la pantalla) podía recibir una imagen más pequeña de la necesaria, con posible pérdida de nitidez; ahora `next/image` sirve el tamaño que realmente corresponde a cada breakpoint.

  **Verificado**: `pnpm exec tsc --noEmit` y `pnpm lint` limpios. Sin browser/Playwright disponible en este entorno para una inspección visual directa -- se verificó en su lugar que `/`, `/productos`, `/categorias`, `/categorias/hogar`, `/productos/iphone-14` y `/productos/aspiradora` (el único producto sin stock del seed, para confirmar el estado "Sin stock") responden 200 sin errores de servidor ni de hidratación en el log del dev server, y se inspeccionó el HTML servido por `/productos` para confirmar que el nuevo marcado está presente en producción real (14 títulos con `line-clamp-1`, 12 tarjetas con `focus-within:ring-2`, 2 badges de "Últimas unidades", 0 apariciones del antiguo párrafo de descripción `line-clamp-2`). Verificación visual completa en navegador (desktop/tablet/mobile) queda pendiente del propio usuario antes de dar el sprint por cerrado -- ver informe entregado al usuario al finalizar este sprint para el detalle completo.

---

### Fase 30 — Sprint 6.3.1: Auditoría y Corrección Completa del Panel Administrativo
- **Sprint de estabilización**, disparado por un reporte del usuario: editar/actualizar/eliminar no funcionaban en Productos, Categorías y Promociones. Alcance explícito: auditar todo el flujo frontend → Supabase (RLS, Auth, roles, permisos, INSERT/UPDATE/DELETE/SELECT) en **todos** los módulos del panel (Productos, Categorías, Promociones, Inventario, Branding, Configuración, Pedidos, Clientes, Logística), sin deshabilitar RLS, agregando manejo/mensajes de error donde faltaran, y sin desarrollar funcionalidad nueva hasta dejar el panel completamente operativo.

  **Dos causas reales, no una -- y la razón por la que el síntoma era "no pasa nada" en vez de un error visible.** La investigación arrancó reproduciendo el problema a nivel de base, simulando el JWT del admin real dentro de una transacción con `rollback` (mismo método ya usado en la Fase 26/27): `public.is_admin()` devolvía `false`. `select * from public.admins` confirmó la tabla **vacía** -- exactamente la misma clase de bug que la Fase 26 ya había encontrado y corregido una vez, pero reaparecida: `auth.users` hoy solo tiene una cuenta real (`regi@gmail.com`, creada el mismo día de este sprint, con `last_sign_in_at` reciente -- evidencia de que es la cuenta con la que el usuario venía probando el panel), y esa cuenta nunca tuvo su fila en `admins`. La cuenta documentada en la Fase 26 (`felixmatiasarandarromero@gmail.com`) ya no existe en el proyecto -- consistente con que el proyecto de Supabase se recreó o se reseteó en algún punto posterior a esa fase, sin que `admins` se volviera a poblar.

  **Pero la tabla vacía no explica por completo el síntoma reportado.** Un `insert` bloqueado por RLS (`WITH CHECK`) devuelve un error explícito de Postgres (código `42501`) -- por eso "Crear producto" nunca fue mencionado como roto. Un `update`/`delete` bloqueado por RLS (`USING`) es distinto: la cláusula simplemente filtra qué filas son visibles para la operación, y esta "tiene éxito" con **cero filas afectadas y ningún error** -- ese es el comportamiento real de PostgREST, no un bug de Supabase. Se confirmó armando exactamente esa consulta (`update products set name = name where id = ...`, con el JWT del admin real, `is_admin()` en `false`): la operación devolvió éxito sin ninguna fila afectada. Revisando `services/products.ts`, `services/categories.ts`, `services/promotions.ts`, `services/inventory.ts`, `services/settings.ts`, `services/orders.ts`, `services/logistics.ts` y `services/storage.ts`, **ningún `update()`/`delete()` de todo el panel verificaba cuántas filas había afectado** -- todos revisaban únicamente `{ error }`, nunca `{ data }`. El resultado exacto que describió el usuario: la UI no mostraba ningún error (porque no lo había), pero tampoco se guardaba nada.

  **El frontend (formularios, hooks, diálogos de borrado) ya estaba bien construido -- se confirmó explícitamente, no se asumió.** `ProductForm.tsx`/`CategoryForm.tsx`/`PromotionForm.tsx`/`SettingsForm.tsx`/`ShippingRateForm.tsx` ya envuelven su `handleSubmit` en `try/catch` con un `submitError` mostrado inline; `DeleteProductDialog.tsx`/`DeleteCategoryDialog.tsx`/`DeletePromotionDialog.tsx`/`DeleteShippingRateDialog.tsx` ya capturan el error de `onConfirm()` y lo muestran junto al botón; `useOrder.ts`/`useOrders.ts` ya implementan actualización optimista con reversión y mensaje de error si Supabase rechaza el cambio (patrón documentado desde la Fase 15/19). Ninguno de estos archivos se tocó: no hacía falta -- toda esa infraestructura de manejo de errores ya existía y funcionaba, simplemente nunca tenía nada que atrapar porque las funciones de `services/` nunca lanzaban un error cuando RLS bloqueaba silenciosamente la escritura.

  **La corrección real, en dos partes:**
  1. **Dato**: `insert into public.admins (id, full_name, role) values ('c92794ad-bcdd-4460-83e4-c7b62f73d827', 'Administrador RegiShop', 'owner')` -- el `id` de `regi@gmail.com` en `auth.users`. Mismo mecanismo que la Fase 26 (un `insert` de datos, no una migración de esquema ni un cambio de policy): `admins` no tiene, ni debe tener, policy de `insert` para `authenticated` -- permitir que un usuario se autoasigne como admin sería una escalación de privilegios, así que la única forma correcta de dar de alta un admin real sigue siendo un `insert` directo contra la base, ejecutado una vez.
  2. **Código**: `src/lib/supabase/assert-write.ts` (nuevo) exporta `assertRowAffected(data, message)`, que lanza un `Error` con un mensaje claro y accionable si `data` viene vacío tras un `update()`/`delete()`. Cada función de escritura en `services/products.ts` (`updateProduct`, `deleteProduct`, `deleteProductImageRows`), `services/categories.ts` (`updateCategory`, `deleteCategory`), `services/promotions.ts` (`updatePromotion`, `deletePromotion`), `services/inventory.ts` (`updateInventorySettings`), `services/settings.ts` (`updateBusinessSettings`, `deleteBrandingAsset`), `services/orders.ts` (`updateOrderStatus`), `services/logistics.ts` (`updateShippingRate`, `deleteShippingRate`) y `services/storage.ts` (`deleteProductImageFiles`) se cambió para encadenar `.select("id")` (o el equivalente de Storage) tras el `update()`/`delete()`, y pasar el resultado por `assertRowAffected()` con un mensaje específico del recurso (ej. *"No se pudo actualizar el producto: no tenés permisos de administrador o el producto ya no existe."*) -- exactamente el estilo de mensaje que pedía el sprint. Esto es defensa en profundidad real, no solo un parche puntual: aunque la causa de hoy fue la tabla `admins` vacía, esta clase de fallo silencioso se repetiría con cualquier otro motivo futuro por el que RLS bloquee legítimamente una fila (una carrera, un permiso de un rol nuevo, una fila borrada por otro admin un instante antes) -- ahora el panel siempre lo muestra en vez de fallar en silencio.

  **Una excepción deliberada, no un descuido**: el `delete` dentro de `replaceShippingRateCities()` (`services/logistics.ts`, borra todas las ciudades ya asignadas a una tarifa antes de reinsertar el set actual) **no** recibió la misma aserción -- afectar cero filas ahí es un caso legítimo (una tarifa que todavía no tenía ninguna ciudad asignada), no una señal de fallo. Agregar la aserción ahí habría producido falsos positivos.

  **Auditoría de RLS: confirmado que ninguna policy estaba mal escrita, mismo resultado que la Fase 27.** Se listaron las ~76 policies de `public`/`storage` (`pg_policies`) para las tablas del panel completo -- Productos, Categorías, Promociones, Inventario (`stock_movements`), Configuración (`business_settings`), Pedidos, Clientes, Logística (`shipping_rates`/`shipping_rate_cities`), y los tres buckets de Storage (`product-images`/`branding`/`product-content`) -- confirmando que todas existen y están completas. Se inspeccionó el `USING`/`WITH CHECK` de las policies de escritura de `products`/`categories`/`promotions`/Storage: todas gateadas correctamente por `is_admin()`, sin ninguna diferencia respecto a lo ya documentado. `is_admin()` en sí (`security definer`, `select exists (select 1 from admins where id = auth.uid())`) también se confirmó intacta. Cero migraciones de policies creadas o modificadas -- la Fase 27 ya había llegado a esta misma conclusión una vez, y este sprint la reconfirma: el problema nunca estuvo en cómo están escritas las policies.

  **Housekeeping encontrado por el camino, sin relación directa con el bug reportado**: `supabase migration list --linked` mostró la migración del bucket `product-content` (Fase 28, `20260717142856_create_product_content_bucket.sql`) como no aplicada remotamente, aunque el bucket y sus policies **sí** existían en el proyecto real (alguien la había corrido directo contra la base, sin pasar por `supabase db push`, en algún momento de la Fase 28). Sin impacto funcional -- pero dejaba el historial de migraciones desincronizado, con riesgo de que un futuro `db push` intentara recrear objetos ya existentes y fallara. Corregido con `supabase migration repair --status applied 20260717142856`, que solo actualiza el bookkeeping (no vuelve a ejecutar el SQL) -- confirmado que las 11 migraciones locales quedaron con su versión remota exacta.

  **Verificado en vivo contra el proyecto real, no solo en teoría**: (1) antes del fix, la misma simulación de JWT (`set_config('request.jwt.claims', ...)` + `set local role authenticated`, en una transacción con `rollback`) confirmó `is_admin() = false` y un `update` en `products` con 0 filas afectadas y sin error; (2) después del `insert` en `admins`, la misma simulación confirmó `is_admin() = true` y `update`s reales afectando 1 fila en `products`, `categories` y `promotions` -- los tres módulos que el usuario reportó rotos, probados explícitamente; (3) `pnpm exec tsc --noEmit`, `pnpm lint` y `pnpm build` (build de producción completo, 28 rutas) limpios tras los cambios de código; (4) barrido de smoke test con el servidor de desarrollo real: `/admin/login` responde 200, las rutas protegidas (`/admin/productos`, `/admin/categorias`, `/admin/promociones`) responden 307 sin sesión (comportamiento esperado), `/` responde 200, sin errores en el log del servidor. Sin browser/Playwright disponible en este entorno para iniciar sesión real y clickear "Guardar"/"Eliminar" en la UI -- la verificación de la escritura real se hizo al nivel que de verdad decide el resultado (Postgres + RLS, simulando exactamente el JWT que produce la sesión del admin), documentado explícitamente en vez de asumido.

  **Cómo verificar que quedó funcionando** (para el usuario, al loguearse con su cuenta real): entrar a `/admin/productos`, editar un producto existente (cambiar el nombre, Guardar cambios) y confirmar que el cambio persiste después de refrescar la página; eliminar un producto de prueba y confirmar que desaparece de la tabla; repetir edición + eliminación en `/admin/categorias` y `/admin/promociones`. Si alguna de estas operaciones fallara por cualquier otro motivo futuro, ahora debería mostrar un mensaje explícito en pantalla (nunca fallar en silencio) -- si eso llegara a pasar, el mensaje mismo indica la causa más probable ("no tenés permisos de administrador o el registro ya no existe").

---

### Fase 31 — Sprint 6.6 (segunda vez que se usa este número): Categorías con Imagen Premium
- **Sprint de funcionalidad nueva** (no un bugfix): agregar soporte completo para imágenes personalizadas por categoría -- panel, Storage y tienda pública -- sin tocar Productos, `ProductCard`, la página individual de producto, el checkout, el carrito, Pedidos, la descripción enriquecida ni el selector de vista del catálogo. Nota de numeración: "Sprint 6.6" ya se había usado para la Fase 28 (descripción enriquecida) -- se sigue el mismo criterio ya aplicado varias veces en este documento (cronología real de sprints ejecutados sobre este repositorio, no el número que trae cada pedido).

  **Hallazgo clave antes de escribir código, que redujo el alcance real de la migración pedida**: `categories.image_url` **ya existía** en la tabla desde el diseño original (Fase 8, `docs/database-architecture.md`) -- nullable, sin ningún consumidor de escritura. Más todavía: `services/storefront/categories.ts` (`getPublicCategories()`/`getPublicCategoryBySlug()`) **ya la leía y la mapeaba** a `Category.image` desde la Fase 11 -- la tienda pública ya tenía el dato disponible, simplemente `CategoryCard.tsx` nunca lo renderizaba (mismo estado exacto en el que estaban las fotos de producto antes de la Fase 12). Confirmado directamente contra el esquema real (`information_schema.columns`) antes de escribir ninguna migración -- **no se creó ninguna columna nueva ni migración de esquema para `image_url` en sí**, evitando una migración redundante que el sprint pedía "por si acaso" pero que ya estaba resuelta. Lo único que realmente faltaba: un bucket de Storage para las imágenes, la escritura desde el panel, y la lectura visual en `CategoryCard.tsx`.

  **Migración real, la única necesaria**: `supabase/migrations/20260717160000_create_category_images_bucket.sql` -- bucket `category-images` (público de lectura, 5 MB por archivo, `image/png`/`jpeg`/`webp` exactamente como pedía el sprint) + sus 4 policies de Storage (lectura pública, escritura solo `is_admin()`), mismo patrón exacto que `product-images`/`product-content`/`branding` -- un bucket propio, no reutilizado entre dominios (CLAUDE.md, sección 11). Aplicada al proyecto real con `supabase db push --linked` y verificada con una consulta directa a `storage.buckets`/`pg_policies`.

  **`services/storage.ts`** gana `uploadCategoryImage(categoryId, file)`/`deleteCategoryImageFile(url)` -- mismo patrón que `uploadProductImage`/`deleteProductImageFiles` (carpeta por registro, nombre con UUID para evitar colisiones/caché de CDN), pero de un único archivo por categoría (como `uploadBrandingAsset`, no una galería). `categoryId` es el id real en edición, o un UUID generado una sola vez en `CategoryForm.tsx` mientras la categoría todavía no existe en modo alta -- mismo criterio ya usado por `descriptionScopeId` en `ProductForm.tsx` (Fase 28).

  **`services/categories.ts`** extendido de forma aditiva: `AdminCategory`/`CategoryFormInput` ganan `imageUrl: string | null`; `CATEGORY_SELECT`/`mapCategoryRow`/`toRow` leen y escriben `image_url`. Ningún consumidor existente que destructuraba `AdminCategory` se rompió (cambio puramente aditivo). Se agregan `MAX_CATEGORY_IMAGE_SIZE_BYTES`/`_MB` (5 MB, igual al límite del bucket) y `ALLOWED_CATEGORY_IMAGE_TYPES` -- mismo criterio ya documentado para `MAX_BRANDING_FILE_SIZE_BYTES` (Fase 14): duplicar el límite del bucket acá es la única forma de validar en el cliente antes de subir, sin una consulta extra.

  **`CategoryImageField.tsx`** (nuevo, `components/admin/categories/`): mismo patrón que `BrandingAssetField.tsx` (upload/reemplazo/borrado diferido hasta guardar, preview local con `URL.createObjectURL`), pero con una preview rectangular más grande (`aspect-[4/3]`, no un cuadrado de 64px) para reflejar de verdad cómo se va a ver la imagen en la tarjeta de categoría de la tienda -- un logo pequeño y una foto de portada de categoría no comunican lo mismo con el mismo tamaño de preview.

  **`CategoryForm.tsx`** gana el campo de imagen con exactamente la misma secuencia de guardado que `SettingsForm.tsx` usa para Logo/Favicon: si hay un archivo pendiente, subirlo primero y recién después borrar el anterior (nunca al revés, para no quedar sin ninguna imagen si la subida fallara a mitad de camino); si se marcó "Quitar" sin reemplazo, borrar el archivo existente. Validación de tipo (`ALLOWED_CATEGORY_IMAGE_TYPES`) y tamaño (`MAX_CATEGORY_IMAGE_SIZE_BYTES`) ocurre en el cliente, antes de guardar el archivo como "pendiente" -- nunca se intenta subir un archivo inválido, mismo criterio que el bug 5 del Sprint 6.0.1 (favicon/logo).

  **`CategoryCard.tsx`, el corazón visual del sprint, rediseñado de punta a punta** (mismo componente compartido por Home → `FeaturedCategories.tsx` y `/categorias` → índice, principio de reutilización ya aplicado a `ProductCard` en el Sprint 6.7): la imagen real (cuando existe, vía `isRealImageUrl` -- copia local a propósito, mismo criterio que `ProductGallery.tsx`, un solo consumidor no cruza el umbral de "3 o más lugares") pasa a llenar toda la tarjeta (`aspect-[4/3]`, sin cambios de proporción respecto a la versión anterior -- menor riesgo de romper el ritmo de la grilla en ningún breakpoint), con un degradado (`from-black/70 via-black/10 to-transparent`) y el nombre + una flecha superpuestos en la parte inferior -- el "overlay ligero con el nombre" que pedía el sprint, reemplazando el bloque de texto separado (nombre + descripción + flecha) que tenía la tarjeta antes. La descripción se sacó de la tarjeta a propósito -- no estaba en el pedido explícito del overlay ("ligero" implica mínimo, solo el nombre), y sigue mostrándose igual de prominente en el header de `/categorias/[slug]` (sin tocar esa página). Sin imagen real todavía, el mismo placeholder decorativo de siempre (gradiente + textura + inicial) sirve de fondo debajo del mismo overlay -- para que una categoría con foto y una sin foto se vean igual de terminadas en la misma grilla, nunca a medio hacer. Hover: elevación sutil (`y: -4`, mismo valor que el `ProductCard` rediseñado en el Sprint 6.7, para consistencia entre ambos rediseños), zoom muy sutil (`scale-105`) sobre la imagen/placeholder, sombra (`hover:shadow-xl`) y borde (`hover:border-foreground/15`) modernizados, más `focus-visible:ring-2` en la tarjeta completa (mismo patrón de accesibilidad ya establecido para `ProductCard`).

  **Decisión deliberada: `/categorias/[slug]/page.tsx` no ganó ningún banner de imagen en su header.** Esta página renderiza `CatalogToolbar`/`ProductGrid`/`CatalogResults` -- los mismos componentes explícitamente protegidos este sprint ("selector de vista del catálogo", "no modificar el catálogo de productos") -- y aunque el bloque de header (breadcrumb + título + descripción) es local a este archivo y no se comparte con `/productos`, se optó por no tocar esta página en absoluto para eliminar cualquier ambigüedad sobre "tocar el catálogo". El sprint pedía explícitamente "Categorías destacadas del Home" y "Página de categorías" (el índice, `/categorias`) -- ambas cubiertas enteramente por el rediseño de `CategoryCard.tsx`, sin necesitar tocar ningún archivo de página. Mostrar la imagen de la categoría como banner en su propia página de detalle queda documentado como mejora futura posible (sección 13), no resuelta en este sprint.

  **`next.config.ts` no necesitó ningún cambio**: `images.remotePatterns` ya está definido de forma genérica (`pathname: "/storage/v1/object/public/**"`, derivado de `NEXT_PUBLIC_SUPABASE_URL`, Fase 12) -- cubre automáticamente cualquier bucket nuevo, incluido `category-images`, sin necesitar una entrada por bucket.

  **Verificado en vivo contra el proyecto real, con una técnica no destructiva** (sin credenciales de admin real disponibles en este entorno para hacer una subida multipart auténtica vía la UI): se identificó una foto real ya subida a `product-images` (URL absoluta de Supabase Storage) y se asignó temporalmente a `categories.image_url` de "Hogar" por SQL directo, confirmando en el HTML servido de `/categorias` que `next/image` la sirve correctamente a través de `/_next/image` (con `srcSet` responsivo, `loading="lazy"`, `sizes` aplicado) -- prueba de que `Category.image` → `CategoryCard` → `isRealImageUrl` → `next/image` → `next.config.ts` funcionan de punta a punta contra el proyecto real. Revertido inmediatamente después (`image_url` de "Hogar" restaurado a su valor original de seed, confirmado por SQL y por una segunda carga de `/categorias` sin ninguna URL de Storage en el HTML). Además: `pnpm exec tsc --noEmit`, `pnpm lint` y `pnpm build` (34 rutas) limpios; smoke test del servidor de desarrollo real confirmando 200 en `/`, `/categorias`, `/categorias/hogar` y 307 (sin sesión, comportamiento esperado) en `/admin/categorias/nueva`. Sin browser/Playwright disponible en este entorno para completar el flujo de subida real desde la UI del panel (seleccionar archivo → preview → guardar) -- documentado explícitamente como pendiente de verificación visual por el propio usuario, no asumido como probado.

  **Confirmado que las categorías existentes siguen funcionando sin imagen**: las 5 categorías activas del proyecto real siguen con sus rutas de seed heredadas (`/categories/hogar.jpg`, etc., que nunca existieron como archivo) y se siguen mostrando con el placeholder decorativo -- ninguna quedó rota ni con un ícono de imagen caída, exactamente el comportamiento que pedía el sprint ("las categorías antiguas deben seguir funcionando aunque no tengan imagen").

---

### Fase 32 — Sprint 6.3.2: Auditoría Completa del Sistema de Permisos del Panel Administrativo
- **Sprint de auditoría, disparado por un reporte del usuario que parecía repetir la Fase 30**: "no puedo eliminar el logo", "no puedo reemplazar imágenes de producto", "no puedo subir videos", "no puedo editar productos/categorías/promociones", con el mensaje "No se pudo eliminar el archivo: no tenés permisos de administrador." apareciendo en varios casos -- ese mensaje es literalmente el texto que la Fase 30 había escrito. Instrucción explícita del usuario: no asumir que es RLS de nuevo, auditar toda la cadena real (middleware, sesión, `is_admin()`, RLS, Storage, frontend) antes de tocar nada.

  **Metodología: verificar cada capa con evidencia directa, no reproducir el diagnóstico anterior de memoria.** Se repitió la simulación de JWT (`set_config('request.jwt.claims', ...)` + `set local role authenticated`, transacción con `rollback`) para el admin real (`regi@gmail.com`) -- resultado: `is_admin()` sigue devolviendo `true`, `admins` sigue con su fila, y un `update` real en `products`/`categories`/`promotions` sigue afectando 1 fila cada uno, sin cambios respecto a como quedó al cerrar la Fase 30. **Esto descartó de entrada la hipótesis "es RLS otra vez"** -- la capa de base de datos estaba, y sigue estando, correcta. Se leyeron además `lib/supabase/client.ts`/`server.ts`/`middleware.ts` y `src/middleware.ts` (la cadena real de autenticación de este proyecto -- sin `requireAdmin()`/`isAdmin()`/tabla `profiles`/claims custom, que el pedido del usuario asumía que podían existir pero nunca fueron parte de la arquitectura de este proyecto: acá la autorización es Supabase Auth + RLS + `is_admin()`, documentado desde la Fase 9) -- sin ningún bug encontrado ahí, coinciden exactamente con el patrón oficial de `@supabase/ssr`.

  **Causa real #1 (código, reproducida con evidencia directa, la explicación central del reporte)**: los tres helpers `getStoragePathFromUrl`/`getCategoryImagePathFromUrl`/`getBrandingPathFromUrl` (`services/storage.ts`/`services/settings.ts`) extraen la ruta de un objeto de Storage recortando la URL pública después del marcador `/object/public/<bucket>/` -- pero esa URL viene armada con `encodeURI()` (comportamiento interno de `getPublicUrl()` en `@supabase/storage-js`, confirmado leyendo el bundle instalado en `node_modules`), así que un archivo subido con un nombre que `encodeURI` escapa (espacios, por ejemplo -- extremadamente común: capturas de pantalla, fotos exportadas de herramientas de IA, cualquier nombre "humano") queda con esa parte todavía percent-encoded (`%20`) en la ruta extraída. Sin decodificarla, `.remove([path])` le pide a Storage que borre un objeto cuyo nombre tiene literalmente `%20`, que no existe -- el objeto real tiene un espacio de verdad. **Reproducido de tres formas independientes, no solo teorizado**: (1) un `DELETE` anónimo real contra la Storage REST API de una imagen real y existente devolvió `200` con `[]` (sin ningún error) -- confirma que Storage, igual que PostgREST, responde "éxito, cero coincidencias" en vez de un error explícito cuando la ruta no matchea; (2) una consulta directa a `storage.objects` confirmó que la ruta *decodificada* coincide exactamente con un objeto real, y la ruta *codificada* (la que el código de antes le pasaba a `.remove()`) no coincide con ninguno; (3) un script de Node aislado confirmó que `decodeURIComponent()` sobre la ruta extraída reproduce el nombre real del objeto carácter por carácter. Esta es la causa exacta del mensaje "No se pudo eliminar el archivo: no tenés permisos de administrador." -- el archivo nunca tuvo un problema de permisos, la ruta que se le pedía borrar a Storage simplemente no existía.

  **El bug en sí es anterior a la Fase 30, no introducido por ella -- pero la Fase 30 lo hizo visible por primera vez, con el diagnóstico equivocado.** `getStoragePathFromUrl`/`getBrandingPathFromUrl` existen desde las Fases 10/14 respectivamente; antes de que la Fase 30 agregara `assertRowAffected()` a estas funciones, un `.remove()` que no encontraba ningún objeto simplemente no hacía nada -- ni error, ni éxito visible, el archivo quedaba huérfano en el bucket para siempre, en silencio, sin que nadie lo notara. La Fase 30 (correctamente) empezó a detectar esa condición de "cero archivos afectados" -- pero al no considerar que la causa pudiera ser una ruta mal decodificada (solo consideró "RLS bloqueó la operación"), el mensaje de error nuevo terminó **diagnosticando mal un bug real y mucho más viejo**. Corregido en los tres helpers con `decodeURIComponent()` sobre la ruta extraída.

  **Corrección arquitectónica adicional, no solo el `decodeURIComponent()`**: `deleteProductImageFiles()`, `deleteCategoryImageFile()` y `deleteBrandingAsset()` dejaron de ser bloqueantes ante un resultado ambiguo (0 archivos afectados, sin error de Supabase). Son limpiezas de Storage que ocurren como *efecto secundario* de guardar un formulario (reemplazar/quitar una imagen de producto, categoría, logo o favicon) -- no son la acción principal que pidió el administrador. Antes de este sprint, si esa limpieza fallaba por cualquier motivo ambiguo, todo el guardado del formulario abortaba con un error de "permisos" que ni siquiera era sobre lo que el admin estaba tratando de hacer. Ahora: un error real de Supabase (RLS, red) se sigue propagando -- ahí sí hay algo genuinamente mal y el admin debe enterarse -- pero un resultado vacío/ambiguo ya no bloquea el guardado, solo deja un `console.warn` (pedido explícito del sprint: "agregar logs para identificar dónde falla") para poder diagnosticarlo sin interrumpir al admin. **Distinto de `deleteProduct()`/`deleteCategory()`/`deletePromotion()`/`deleteShippingRate()`** (el borrado completo de un registro, la acción principal que el admin pidió con el botón "Eliminar") -- esas cuatro siguen usando `assertRowAffected()` sin cambios: ahí si no se borra nada, el admin tiene que saberlo, porque es exactamente lo que pidió hacer.

  **Causa real #2 (infraestructura, hallazgo independiente, no relacionado con el código del panel)**: las variables de entorno `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY` del ambiente **Production** en Vercel estaban configuradas como **strings vacíos** (`""`) -- confirmado descargándolas con `vercel env pull --environment=production` y comparándolas contra `.env.local` y contra el ambiente Development de Vercel (ese sí las tenía correctas). Como Next.js incrusta las variables `NEXT_PUBLIC_*` en el bundle del cliente **en tiempo de build**, cualquier deploy de producción hecho mientras esas variables estuvieran vacías habría quedado con el cliente de Supabase apuntando a una URL vacía -- rompiendo *todo* lo que dependiera de Supabase en la tienda pública, no solo el panel. Verificado que el deploy de producción vigente en ese momento (`somapp-commerce-engine.vercel.app`) seguía funcionando correctamente (6 productos renderizando bien) -- evidencia de que ese deploy específico se había construido *antes* de que las variables quedaran vacías, así que el problema, aunque real y confirmado en la configuración, todavía no había afectado a un deploy en vivo. Se corrigió de todos modos (con autorización explícita del usuario, dado que implicaba tocar configuración de producción compartida): se reemplazaron las dos variables vacías por los valores correctos (los mismos ya usados en Development/localhost) vía `vercel env rm`/`vercel env add`, y se disparó un deploy nuevo (`vercel --prod`) para que el build siguiente ya no dependiera de que ese deploy viejo nunca se reconstruyera. Verificado el deploy nuevo en vivo: `somapp-commerce-engine.vercel.app` responde 200 con los 6 productos, `/categorias` 200, `/admin/login` 200.

  **Archivos modificados**: `services/storage.ts` (`decodeURIComponent()` en `getStoragePathFromUrl`/`getCategoryImagePathFromUrl`, `deleteProductImageFiles`/`deleteCategoryImageFile` no bloqueantes, ya no importa `assertRowAffected`), `services/settings.ts` (`decodeURIComponent()` en `getBrandingPathFromUrl`, `deleteBrandingAsset` no bloqueante -- `assertRowAffected` se mantiene para `updateBusinessSettings`, que sí es una escritura de fila primaria). Ninguna migración SQL, ninguna policy RLS modificada -- confirmado explícitamente en la auditoría que no hacía falta ninguna (mismo resultado que las Fases 27/30).

  **Verificado**: `pnpm exec tsc --noEmit`, `pnpm lint` y `pnpm build` (34 rutas) limpios. La corrección del `decodeURIComponent()` se probó con evidencia real contra el proyecto real (no solo lógica aislada) -- ver arriba, los tres métodos de reproducción. Vercel Production verificado en vivo tras el redeploy. Sin browser/Playwright disponible en este entorno para clickear el flujo completo (subir un archivo con espacios en el nombre → reemplazar → guardar) desde la UI real del panel -- la prueba de la ruta corregida se hizo directamente contra Storage/Postgres, no vía la interfaz.

  **Cómo verificar que quedó funcionando** (para el usuario): en `/admin/configuracion` → Apariencia → Identidad Visual, subir un logo o favicon con un nombre de archivo que tenga espacios (ej. "Logo Final v2.png"), guardar, y después reemplazarlo o quitarlo -- debería completarse sin ningún error. Repetir con una imagen de producto o de categoría con nombre similar. Si en algún momento la limpieza de Storage no encuentra el archivo (por cualquier motivo ambiguo), el guardado debe completarse igual -- revisar la consola del navegador (F12) para ver el `console.warn` informativo, que ya no bloquea nada.

---

### Fase 33 — Sprint 6.4.1: Optimización del Catálogo para Dispositivos Móviles
- **Sprint de presentación pura**: el catálogo mostraba 1 producto por fila en mobile -- demasiado scroll comparado con tiendas de referencia (Mercado Libre, Amazon, AliExpress, Falabella). Objetivo: 2 columnas por defecto en mobile, 2-3 en tablet, desktop sin cambios -- sin nuevas consultas a Supabase, sin tocar Checkout/Carrito/Panel/página de producto/descripción enriquecida, y sin duplicar `ProductCard` ni crear un componente nuevo.

  **La misma tensión arquitectónica que la Fase 29 (Sprint 6.7), pero esta vez el pedido central era exactamente lo que esa fase había evitado tocar.** `ProductGrid.tsx` es un único componente compartido por el catálogo (`/productos`, `/categorias/[slug]`) y `RelatedProducts.tsx` en la página individual de producto -- explícitamente protegida este sprint. La Fase 29 había resuelto una tensión parecida dejando `ProductGrid` intacto y solo achicando `ProductCard`; acá eso no alcanza, porque el pedido explícito es "modificar el grid" (cambiar cuántas columnas muestra), no la altura de cada tarjeta.

  **Resuelto con un prop opcional (`dense`), no con un segundo componente.** `ProductGrid` acepta ahora `dense?: boolean`: sin pasarlo, se comporta exactamente igual que siempre (`grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-4`) -- es el caso de `RelatedProducts.tsx`, que no cambió ni una línea y sigue pasando exactamente los mismos props que antes. Con `dense` (pasado únicamente por `CatalogResults.tsx` -- exclusivo de `/productos` y `/categorias/[slug]`, nunca usado por la página de producto -- y por los fallbacks de `Suspense` de esas dos páginas, para que no haya un salto visual de 1 a 2 columnas al hidratar), el grid pasa a `grid-cols-2 gap-3 sm:gap-4 md:grid-cols-3 md:gap-6 lg:grid-cols-4`: 2 columnas en mobile (el objetivo central), 3 en tablet (`md`, 768-1023px -- cumple "2 o 3 según el ancho" combinado con el paso intermedio en `sm`), 4 en desktop sin cambios. Gaps más chicos en las columnas angostas (`gap-3`/`gap-4` en vez de `gap-6`) para no desperdiciar el espacio ya escaso de 2 columnas -- pedido explícito de la sección ESPACIADO.

  **`ProductCard.tsx` recibe el mismo `dense` (reenviado por `ProductGrid`), no para un segundo archivo sino para ajustar sus propias clases de Tailwind condicionalmente** -- sigue siendo el mismo componente, una sola definición, sin duplicar nada (`ARQUITECTURA` del sprint lo pedía explícitamente). Sin `dense` (Related Products, y el carrusel de `FeaturedProducts.tsx` en la Home, que ni siquiera pasa por `ProductGrid`), la tarjeta es 100% idéntica pixel a pixel a como quedó en la Fase 29 -- confirmado explícitamente contra el HTML servido real (ver "Verificado" más abajo). Con `dense`: título `line-clamp-2` en vez de `line-clamp-1` (pedido explícito: "si el nombre es muy largo, limitar a 2 líneas"), padding del bloque de info y del botón más ajustado en el escalón mobile (`p-2`/`px-2 pb-2` en vez de `p-3`/`px-3 pb-3`), precio un escalón más chico (`text-sm sm:text-base` en vez de `text-base sm:text-lg` -- sigue siendo perfectamente legible, mismo criterio que ya usan tiendas de referencia para tarjetas de 2 columnas), badges reposicionados más cerca del borde (`top-1.5 left-1.5 gap-1` en vez de `top-2.5 left-2.5 gap-1.5`, coherente con una tarjeta más angosta), y un `sizes` de `next/image` propio (`25vw`/`33vw`/`50vw` en vez de `25vw`/`50vw`/`100vw`) que refleja con precisión la nueva grilla de 2/3/4 columnas -- evita pedirle a Next.js una imagen de calidad mayor a la que realmente se necesita en una tarjeta angosta, la razón por la que este prop existe también en `ProductCard` y no solo en `ProductGrid` (de lo contrario, `RelatedProducts` habría heredado un `sizes` pensado para una tarjeta más angosta de lo que en realidad ocupa, sirviendo una imagen más chica de lo necesario -- una regresión de calidad, aunque menor, en la página protegida).

  **Botón "Agregar al carrito"**: se mantiene `size="sm"` en ambos casos -- ya era compacto desde la Fase 29, y el tamaño de un `<Button>` es una prop de JS (no una clase de Tailwind), así que no puede variar por breakpoint sin JS adicional; con `size="sm"` ya cabe bien incluso en la columna angosta de 2 columnas, confirmado visualmente en el HTML servido (sin overflow ni texto cortado).

  **Verificado en vivo, comparando el HTML servido de las tres rutas relevantes, no solo el código**: `/productos` y `/categorias/hogar` muestran la clase `grid-cols-2 gap-3 sm:gap-4 md:grid-cols-3 md:gap-6 lg:grid-cols-4` y sus 12/6 tarjetas con `line-clamp-2` (0 aparición de `line-clamp-1` proveniente de `ProductCard` -- las 2 coincidencias de `line-clamp-1` en `/productos` son del `Select` de shadcn, sin relación); `/productos/iphone-14` (página de producto, con su sección "Productos relacionados") sigue mostrando exactamente `grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-4`, `line-clamp-1` y el padding `p-3 pb-2 sm:p-4 sm:pb-2` de siempre -- cero clases `dense` presentes, confirmando que la página protegida no cambió ni un carácter de su HTML. `pnpm exec tsc --noEmit`, `pnpm lint` y `pnpm build` (34 rutas) limpios. Sin nuevas consultas a Supabase ni cambios a `lib/search.ts`/`lib/sort.ts`/`services/storefront/*` -- confirmado por diff, este sprint es 100% presentación (`ProductGrid.tsx`, `ProductCard.tsx`, `CatalogResults.tsx`, y los fallbacks de `Suspense` en `/productos/page.tsx`/`/categorias/[slug]/page.tsx`).

  **Archivos modificados**: `components/storefront/ProductGrid.tsx` (prop `dense`), `components/storefront/ProductCard.tsx` (prop `dense`, clases condicionales), `components/storefront/CatalogResults.tsx` (pasa `dense`), `app/(storefront)/productos/page.tsx` y `app/(storefront)/categorias/[slug]/page.tsx` (pasan `dense` en el fallback de `Suspense`). Ninguna migración, ningún cambio en `services/`, `lib/search.ts`, `lib/sort.ts` ni en el panel administrativo.

---

## 10. Reglas para futuras conversaciones

Esta es la sección más importante del documento. Cualquier Claude que trabaje en este proyecto en una conversación nueva debe:

1. **Leer este documento completo antes de tocar código.** No asumir contexto del historial de chat — puede no existir.
2. **Nunca romper funcionalidades ya aprobadas** (ver sección 12). Si una tarea nueva requiere modificar algo aprobado, decirlo explícitamente y pedir confirmación antes de proceder, no hacerlo silenciosamente.
3. **Nunca reescribir componentes solo por preferencia personal.** Si un componente funciona y cumple los principios de la sección 6, no se refactoriza "porque se puede escribir mejor". Se justifica el cambio con una razón técnica concreta.
4. **Siempre priorizar reutilización** sobre crear algo nuevo desde cero.
5. **Siempre priorizar simplicidad.** Este proyecto explícitamente evita microservicios, event bus, Context API además de Zustand, librerías de formularios antes de que hagan falta, y cualquier abstracción que no resuelva un problema real y presente.
6. **Nunca agregar sobreingeniería.** No construir para casos hipotéticos futuros que nadie pidió. Tres líneas parecidas repetidas dos veces no ameritan una abstracción; código realmente duplicado en tres o más lugares, sí.
7. **No modificar la arquitectura sin justificación técnica real**, y documentarla acá si el cambio se hace.
8. **Mantener coherencia visual.** Toda sección nueva debe sentirse parte de la misma tienda: mismos radios de borde, misma paleta neutra (oklch, tokens de shadcn), mismas transiciones y timings (`lib/motion.ts`), misma tipografía y jerarquía.
9. **Orden de construcción: primero la tienda pública, después Supabase, después el panel administrativo.** Este orden no es arbitrario — ya se siguió así hasta acá y evitó atar decisiones de diseño a un schema de base de datos prematuro.
10. **Verificar en el navegador, no solo en el código.** Este proyecto tiene la costumbre de levantar el servidor de desarrollo y probar cada cambio visualmente (desktop/tablet/mobile) y funcionalmente antes de darlo por terminado. Mantenerla.
11. **Actualizar este documento** cuando se complete un sprint que cambie el estado del proyecto (sección 7), agregue una pieza nueva a la arquitectura (sección 5), o cierre parte del roadmap (sección 8).

---

## 11. Convenciones

- **Nombres de componentes**: `PascalCase`, un componente principal por archivo (`ProductCard.tsx` exporta `ProductCard`). Subcomponentes internos no exportados están permitidos y se prefieren sobre un archivo nuevo cuando no se reutilizan en ningún otro lugar (ejemplo: `CartButton` dentro de `Header.tsx`).
- **Nombres de archivo**: `PascalCase` para componentes (`ProductGallery.tsx`), `kebab-case` para el store (`cart-store.ts`), `camelCase` para lib/config (`utils.ts`, `products.ts`).
- **Rutas**: en español, siguiendo lo que ve el usuario (`/productos`, `/carrito`, `/checkout`), consistente con que toda la copy del sitio está en español (es-PY).
- **Imports**: siempre con el alias `@/`, nunca rutas relativas largas (`../../../`).
- **Tipado**: cada tipo de dominio se exporta junto a su fuente de datos, no en un archivo `types/` separado — `Product` vive en `config/products.ts`, `CartLineItem` en `store/cart-store.ts`, `CheckoutFormValues` en `lib/checkout.ts`.
- **Componentes cliente vs. servidor**: `"use client"` solo cuando hace falta (hooks de React, Framer Motion, Zustand, manejo de eventos). Las páginas son Server Components por defecto salvo que necesiten estado o hooks directamente (ejemplo: `/carrito` y `/checkout` son `"use client"` porque leen el store; `/productos/[slug]` es Server Component porque solo resuelve datos y delega la interactividad a sus hijos). `/productos` sigue siendo Server Component (conserva su `generateMetadata`) y delega a `CatalogResults` (`"use client"`) todo lo que necesita `useSearchParams`, envuelto en `Suspense` — patrón a repetir si otra página necesita leer query params sin perder su metadata ni forzar renderizado dinámico global.
- **Config**: siempre objetos `as const`, nunca funciones con lógica compleja. Si hace falta derivar un valor (buscar un producto por slug, filtrar relacionados), es una función exportada aparte (`getProductBySlug`, `getRelatedProducts`), no un getter escondido dentro del objeto de config.
- **Hooks**: la tienda pública fue sin custom hooks propios hasta la Fase 24 (Sprint 6.4) — antes, solo hooks de librerías (`useCartStore`, `useReducedMotion`, `useRouter`, `useState`). Esto no cambió con la Fase 11 (conexión a Supabase): ningún componente del storefront fetchea sus propios datos, todos siguen recibiendo props ya resueltas por su página. Lo que sí cambió en la Fase 24 es que `useProductShowcase` (`hooks/useProductShowcase.ts`) es el primer hook propio del storefront -- pero **no** rompe la regla de "sin fetching en el storefront": encapsula únicamente el estado de *presentación* del carrusel del Hero (índice actual, dirección, pausa), nunca una consulta a Supabase -- los productos le llegan ya resueltos por `ProductShowcase.tsx`, que a su vez los recibe por prop desde Home. El panel administrativo, desde la Fase 10, tiene sus propios hooks *de datos* en `src/hooks/` (`useProducts`, `useProduct`, `useCategories`) — ver la convención de "Capas del panel" más abajo; son un tipo de hook distinto (fetching) del que agregó la Fase 24 (presentación).
- **Store**: dos stores de Zustand hasta la fecha: `useCartStore` (carrito) y `useCheckoutStore` (formulario de checkout, desde el Sprint 3.6 — incluye `latitude`/`longitude` y la acción `setLocation` desde el Sprint 3.7). Sin middleware de persistencia en ninguno. Si se agrega un tercer store en el futuro, documentarlo en la sección 5.
- **Componentes que dependen de `window`/`document` al importarse** (como `leaflet`): no pueden importarse de forma estática desde ningún archivo que el servidor también evalúe. El patrón de este proyecto es separar en dos archivos — uno con la lógica real (`LeafletMap.tsx`) y otro público que lo carga con `next/dynamic(..., { ssr: false })` (`LocationPicker.tsx`) — en vez de forzar `ssr: false` en un componente que ya tiene lógica de negocio mezclada.
- **Helpers**: `lib/utils.ts` es para utilidades genéricas de toda la app (`cn`, `formatPrice`, `getWhatsAppUrl`); `lib/motion.ts` para variants de Framer Motion; `lib/products.ts`, `lib/checkout.ts` y `lib/whatsapp.ts` son helpers de dominio específico. Regla concreta desde el Sprint 3.8: armar un mensaje o texto de negocio (el pedido para WhatsApp) es responsabilidad de `lib/`, nunca del componente que lo dispara — `OrderConfirmationDetails` solo llama a `getOrderWhatsAppUrl()`.
- **Pipeline del catálogo** (desde el Sprint 4.4): tres responsabilidades, tres archivos — `lib/categories.ts` (categoría), `lib/search.ts` (búsqueda), `lib/sort.ts` (orden) — cada uno una función pura sin JSX ni hooks. Se aplican siempre en ese orden (categoría → búsqueda → orden) y nunca se mezclan entre sí. La categoría se resuelve en la página (Server Component, antes de llegar a `CatalogResults`); búsqueda y orden se resuelven dentro de `CatalogResults`. Si se agrega un cuarto filtro (precio/stock, ver roadmap), debería seguir el mismo patrón: su propio archivo en `lib/`, aplicado en el punto correspondiente de la cadena. Desde la Fase 11, `searchProducts()` recibe `categories` por parámetro en vez de importarlo de `config/categories.ts` — sigue siendo una función pura, ahora sin ninguna dependencia oculta a `config/`.
- **Nota de duplicación conocida y deliberada**: `ProductCard.tsx` mantiene su propia copia local de `getStockStatus` en vez de importarla de `lib/products.ts`. Esto fue una decisión explícita del Sprint 3.2 porque en ese momento `ProductCard` era un componente ya aprobado que no debía tocarse. Es deuda técnica documentada, no un descuido — unificarla es un cambio de una línea cuando se autorice tocar ese componente.
- **Base UI, no Radix**: para renderizar un `Button`/`Badge`/etc. como otro elemento (un `<a>` o `<Link>`), usar `render={<Link href="..." />}` + `nativeButton={false}`. El patrón `asChild` de Radix **no existe** en este proyecto y no debe usarse.
- **Dos root layouts, un solo `globals.css`** (desde la Fase 9): `app/(storefront)/layout.tsx` y `app/admin/layout.tsx` son las dos únicas raíces de la app — cualquier ruta nueva de la tienda pública va dentro de `(storefront)/`, cualquier ruta nueva del panel va dentro de `admin/`. Ninguna otra carpeta de primer nivel debajo de `app/` debería tener su propio `layout.tsx` con `<html>`/`<body>` salvo que se justifique un tercer "shell" completamente distinto — no es el patrón a repetir para separaciones menores (para eso alcanza un layout anidado normal, como `admin/(panel)/layout.tsx`).
- **`components/admin/` nunca importa de `components/storefront/`, y viceversa**: mismo criterio que ya separaba `storefront/` de `layout/` — cada uno tiene su propia identidad, y lo único que ambos comparten es `components/shared/` (genérico, sin conocimiento de comercio ni de panel) y `components/ui/` (vendored).
- **Verificación de sesión de admin**: siempre `supabase.auth.getUser()`, nunca `getSession()`, en cualquier código de servidor que decida si algo se muestra o no — `getUser()` revalida contra el servidor de Auth, `getSession()` solo lee la cookie sin garantía de que siga siendo válida. El middleware (`lib/supabase/middleware.ts`) es una redirección temprana para mejor UX, no la barrera real; la barrera real es el `getUser()` de `admin/(panel)/layout.tsx`.
- **Capas del panel administrativo** (desde la Fase 10, confirmado con un segundo caso real en la Fase 13): `components/admin/<módulo>/` → `hooks/` → `services/` → Supabase, siempre en ese sentido. Un componente nunca llama a `services/` directamente (siempre pasa por un hook), y un hook nunca arma una query de Supabase a mano (siempre llama a `services/`). Cada tabla (o grupo de tablas fuertemente relacionado, como `products`+`product_images`) tiene su propio archivo en `services/`, con tipos propios del panel (prefijo `Admin*`, ej. `AdminProduct`, `AdminCategory`). Productos (`useProducts.ts`) y Categorías (`useCategories.ts`) ya tienen, cada uno por su cuenta, el mismo patrón de búsqueda + orden en memoria — con dos casos reales ahora (no uno), si un tercer módulo (Pedidos, Clientes) lo vuelve a necesitar, ahí sí se justifica evaluar extraerlo a un hook compartido en vez de seguir duplicándolo.
- **Capas de la tienda pública para datos de Supabase** (desde la Fase 11): más simple que el panel, porque nada fetchea del lado del cliente — `app/(storefront)/**/page.tsx` (Server Component) → `services/storefront/*.ts` → Supabase, sin capa de hooks intermedia. `services/storefront/` reutiliza los tipos `Product`/`Category` que ya vivían en `config/products.ts`/`config/categories.ts` (a diferencia de `services/` del panel, que tiene sus propios tipos `Admin*`) — es la aplicación directa del principio 3 de la sección 6: los componentes no saben ni les importa si esos tipos vienen de un array en memoria o de una fila de Supabase.
- **Componentes nuevos de shadcn se siguen agregando con el CLI**: `alert-dialog` y `switch` (Fase 10) se sumaron con `pnpm dlx shadcn@latest add`, igual que todo lo que ya había en `components/ui/` — nunca escritos a mano, ni siquiera para el panel.
- **Fotos reales vs. placeholder decorativo** (desde la Fase 12): `ProductCard`/`ProductGallery` muestran la foto real de `product.images` con `next/image` cuando el valor es una URL absoluta (`isRealImageUrl`, `url?.startsWith("http")`); si no hay ninguna (o falla la carga, vía `onError`), cae al mismo placeholder de gradiente + inicial de siempre. Cualquier componente nuevo que muestre una foto de producto/categoría debería seguir el mismo criterio, en vez de asumir que `images`/`image` siempre tiene una URL válida. `next.config.ts` habilita el dominio de Supabase Storage para `next/image` derivándolo de `NEXT_PUBLIC_SUPABASE_URL` en vez de hardcodear el project ref — no hace falta tocarlo al lanzar un cliente nuevo.
- **Íconos guardados como identificador, nunca como componente** (desde la Fase 8, `benefits.icon_name`; extendido a `categories.icon_name` en la Fase 13): cualquier tabla que necesite asociar un ícono de lucide-react guarda un `text` (ej. `"smartphone"`), nunca un componente de React. La resolución a un componente real vive únicamente en la capa de presentación — `components/admin/categories/category-icons.ts` para categorías —, nunca en `services/` ni `hooks/`. Si un módulo nuevo necesita el mismo patrón, seguirlo: un mapa `{ [identificador]: LucideIcon }` con un fallback genérico, en su propio archivo dentro de `components/`.
- **Categorías resueltas por prop en toda la tienda pública** (desde la Fase 13): ningún componente de `storefront/` importa `categories` de `config/categories.ts` como valor — todos (`ProductCard`, `ProductGallery`, `ProductBuyBox`, `ProductGrid`, `FeaturedProducts`, `RelatedProducts`, `CatalogResults`) reciben `categories: Category[]` por prop, resuelta por la página vía `services/storefront/categories.ts`. Un componente nuevo que necesite el nombre/color de una categoría debe seguir el mismo patrón (recibirla por prop), no importar el array de config ni hacer su propia consulta a Supabase. El `import type { Category }` (sin valor) sigue siendo válido y esperado en cualquier archivo que necesite el tipo — se borra en tiempo de compilación, no es una dependencia activa.
- **Identidad del negocio resuelta por prop en la tienda pública, con excepciones documentadas** (desde la Fase 14): `Header`, `Footer`, `Hero` (solo WhatsApp) y `/contacto` reciben `settings: BusinessSettings` por prop en vez de importar `config/business.ts`/`config/footer.ts` — mismo patrón que categorías, resuelto vía `services/storefront/business.ts`. **Excepción permanente, no transitoria**: `formatPrice()` (moneda/locale), `lib/whatsapp.ts` (mensaje del pedido) y el centro del mapa de Checkout siguen leyendo `config/business.ts` estático porque migrarlos requiere tocar Carrito/Checkout/WhatsApp — protegidos explícitamente en el Sprint 5.5. Un componente nuevo de la tienda pública que muestre nombre/WhatsApp/contacto/redes/ubicación del negocio debe recibir `BusinessSettings` por prop (nunca importar `config/business.ts` directamente); si necesita mostrar un precio o mandar un mensaje de WhatsApp del pedido, sigue usando `formatPrice()`/`lib/whatsapp.ts` tal como están, sin intentar migrarlos por su cuenta.
- **Un solo bucket por tipo de archivo, nunca reutilizado entre dominios distintos** (desde la Fase 14): `product-images` es de productos, `branding` es de la identidad del negocio (logo/favicon) — mismo criterio que ya separa `services/products.ts` de `services/categories.ts`: cada dominio con datos y ciclo de vida propios tiene su propio bucket, con límites de tamaño y tipos MIME ajustados a lo que ese dominio realmente necesita (branding permite SVG/ICO, product-images no).
- **Escrituras públicas desde el checkout van por una función de Postgres (`security definer`), nunca por policies de `insert` directas para `anon`** (desde la Fase 15): si un sprint futuro necesita que la tienda pública escriba en una tabla nueva (no solo leer), el patrón a seguir es `create_<algo>` en `supabase/migrations/`, invocada vía `supabase.rpc()` desde `services/storefront/<algo>.ts` con el cliente de **browser** — no agregar una policy de `insert to anon` a mano. La función controla exactamente qué se puede crear (valores por default, validaciones mínimas) y es la única forma de garantizar atomicidad real entre varias tablas desde un cliente que no puede abrir una transacción SQL directa.
- **`services/storefront/*` usa el cliente de servidor solo cuando lee durante el render de una página; si escribe (o si lee en respuesta a una interacción ya en el cliente), usa el de browser** (desde la Fase 15, `services/storefront/orders.ts`): la regla que decide cuál cliente de Supabase usar no es "¿es código de storefront o de panel?", es "¿esto corre durante el render de una página (Server Component) o en respuesta a un evento ya en el navegador (Client Component)?".
- **Cambios de estado optimistas, con reversión si Supabase los rechaza** (desde la Fase 15, `useOrder.ts`/`useOrders.ts`): un cambio que el usuario espera ver reflejado "inmediatamente" (pedido explícito del sprint) se aplica primero al estado local de React, y recién después se confirma contra Supabase — si la escritura falla, se revierte el estado local y se muestra el error, nunca se deja la UI mostrando algo que no se guardó de verdad.
- **Integrar un módulo nuevo con uno protegido: un trigger de Postgres en la tabla que ya se actualiza, no un cambio en el servicio protegido** (desde la Fase 19, `apply_order_stock_movement` en `orders`): cuando un sprint pide que un cambio en una tabla (acá, `orders.status`) dispare un efecto en otra (`products.stock` + `stock_movements`) pero prohíbe tocar el módulo dueño de esa tabla, el patrón a seguir es un trigger `before/after update` en la tabla que ya se actualiza, no una función nueva que el servicio protegido tendría que empezar a llamar. El trigger se engancha al `update` que ya existe, sin que `services/orders.ts` (o el que corresponda) se entere ni cambie una línea. Ese trigger no necesita `security definer` si quien dispara el `update` original ya tiene, por RLS, los permisos que el trigger necesita en las tablas afectadas -- ver el punto siguiente para cuándo sí hace falta.
- **Una función de Postgres para una escritura administrativa no necesita `security definer` si el invocador ya es un admin autenticado** (desde la Fase 19, `register_stock_movement`, contrastada con `create_order` de la Fase 15): `security definer` bypasea RLS y es indispensable cuando el invocador es `anon` (el checkout público) y necesita escribir en tablas que `anon` no puede tocar directamente. Cuando el invocador siempre es un admin autenticado que ya tiene permiso de RLS sobre las tablas involucradas, una función `security invoker` (el default) alcanza -- la función sigue siendo el único punto de entrada que centraliza la regla de negocio (acá, "nunca stock negativo" + "el stock y su historial se mueven juntos"), pero no hace falta escalar privilegios para lograrlo.
- **Un refetch completo después de una mutación puede desmontar el propio formulario que la disparó** (bug real encontrado y corregido en la Fase 19, `useInventoryItem.ts`): si un hook expone un `isLoading` que también se pone en `true` durante un refetch posterior a una escritura, y el componente que lo consume reemplaza toda la vista mientras `isLoading` es `true` (patrón ya establecido: `if (isLoading) return <p>Cargando...</p>`), cualquier formulario con estado local propio dentro de esa vista se remonta -- y pierde ese estado -- en cada envío exitoso. La corrección, y el patrón a seguir de acá en más: tras una mutación exitosa, actualizar el estado local de forma optimista con el resultado que ya devuelve la escritura (mismo criterio que `useOrder.ts`/`useOrders.ts`, un punto más arriba en esta misma lista), en vez de volver a llamar a la función de fetch inicial.
- **Un archivo que importa `lib/supabase/server.ts` no puede compartirse con un Client Component, sin importar qué función se use** (lección real del Sprint 6.0.1, bug 3): `lib/supabase/server.ts` importa `next/headers` (una Dynamic API exclusiva de Server Components). Si un archivo de `services/storefront/` importa ese módulo para su versión "cliente de servidor" de una función, y **ese mismo archivo** también define una versión "cliente de browser" para que un Client Component la use, el build se rompe apenas ese Client Component lo importe -- Next.js analiza la cadena de imports de todo el archivo, no cuál función específica se usa. La solución es un archivo hermano nuevo (ej. `business-client.ts` junto a `business.ts`), sin ningún import de `lib/supabase/server.ts`, aunque eso implique duplicar un tipo/mapeo de fila chico -- `tsc`/`eslint` no detectan este error, solo aparece al compilar/ejecutar en el navegador.
- **Filtrar `is_active` (y vigencia de fechas) explícitamente en `services/storefront/*`, no confiar solo en RLS** (desde el Sprint 6.0.1, bug 4): `..._public_read_active` (RLS) filtra correctamente para el rol `anon`, pero convive con policies `..._admin_read_all` para `authenticated`, combinadas con OR -- si la tienda pública se navega con una sesión de admin activa en el mismo navegador (dos pestañas, un escenario común al probar el panel), la request corre como `authenticated` y ve filas inactivas/vencidas también. Todas las lecturas públicas de `products`/`categories`/`promotions` agregan `.eq("is_active", true)` (y, para promociones, el rango de fechas) como segunda capa explícita, independiente de RLS. Cualquier tabla nueva que la tienda pública lea con el mismo patrón "público ve activos, admin ve todo" debe seguir este mismo criterio desde el día uno, no confiar en que RLS alcanza.
- **`isRealImageUrl` vive en `lib/products.ts`, compartida** (desde el Sprint 6.0.1): dejó de estar duplicada en `ProductCard.tsx`/`ProductGallery.tsx` en cuanto apareció un tercer consumidor real (`services/storefront/promotions.ts`, bug 2) -- cruzando el umbral de "3 o más lugares" (principio 6). Cualquier componente nuevo que necesite distinguir una foto real de Storage de una ruta de placeholder debe importarla de `lib/products.ts`, no reescribir la función ni asumir que `images`/`image_url` siempre tiene una URL válida -- esto aplica tanto a la tienda pública como al panel (`ProductsTable.tsx`/`ProductImagesField.tsx` la adoptaron en el mismo sprint, tras encontrarse con miniaturas rotas).
- **Ningún componente calcula un descuento por su cuenta: siempre `getProductDisplayPrice()`/`calculatePromotion()` (`lib/promotions.ts`, Sprint 6.1)** -- cualquier componente nuevo que muestre el precio de un producto debe recibir `promotions: PublicPromotion[]` por prop (resuelta por la página vía `getActivePromotions()`, mismo patrón que `categories`/`settings`) y llamar a `getProductDisplayPrice(product, promotions)` para decidir precio/tachado/badge, nunca comparar `product.price`/`oldPrice` a mano. Si algún día hace falta el resultado "crudo" del motor (sin el fallback legacy), `calculatePromotion()` es la función de más bajo nivel.
- **`lib/` puede importar tipos de `config/`, pero nunca de `services/` -- así se evita un ciclo `lib` ↔ `services`** (Sprint 6.1): `services/storefront/promotions.ts` ya importaba `DiscountType`/`formatDiscountLabel` de `lib/promotions.ts` desde la Fase 18; si una función nueva de `lib/promotions.ts` necesitara el tipo `PublicPromotion` de ese mismo archivo de `services/`, crearía un ciclo de imports. La solución, generalizable a cualquier situación parecida: definir en `lib/` un tipo estructural mínimo con solo los campos que esa función necesita (acá, `PricingPromotion`) -- cualquier tipo más rico de `services/` (como `PublicPromotion`) lo satisface automáticamente por structural typing de TypeScript, sin necesitar un `import` ni un cast.
- **Un archivo compartido entre "una sola promoción" y "un carrusel de promociones" no duplica el JSX de una promoción individual** (Sprint 6.1, `PromotionalBanner.tsx`): cuando un componente necesita renderizar lo mismo en dos contextos con animaciones distintas (una entrada con stagger vs. una transición de slide), se extrae la pieza de contenido puro a una función/componente interno propio (acá, `PromotionContent`) que decide -- vía un prop simple, no una condicional compleja -- si aplica su propio stagger de entrada o no, y cada contexto (el caso simple, el carrusel) la reutiliza tal cual.
- **Una tabla de referencia geográfica/de catálogo sin ningún consumidor que escriba desde la aplicación no lleva policies de escritura, ni para admins** (Sprint 6.2, `departments`/`cities`): cuando un sprint pide explícitamente "esto nunca se edita desde el Panel", la forma correcta de garantizarlo no es "no construir un formulario para editarlo" -- es no darle a nadie, ni siquiera a `is_admin()`, permiso de RLS para escribir. La única vía de modificación queda siendo un archivo de seed/migración de datos corrido a mano, exactamente igual al criterio que ya regía `supabase/seed.sql` desde la Fase 8.
- **Un `<select>` con opciones que dependen de otra selección (departamento → ciudades) va con el fetch de la lista "padre" (departamentos) arrancando lo antes posible, no adentro del componente que recién se monta cuando ya se conoce un valor precargado** (Sprint 6.2, lección real -- ver sección 9, Fase 22, para el bug completo): si un formulario de edición necesita resolver un valor preexistente antes de poder montar el picker (acá, a qué departamento pertenecen las ciudades ya asignadas a una tarifa), y el picker es quien pide la lista de opciones (`useDepartments()`), esa lista arranca su fetch recién en ese mismo instante -- tarde para alcanzar a resolver la etiqueta del valor que el picker ya trae. La lista "padre" debe pedirse en el componente contenedor, con su propio fetch arrancando en el primer render (en paralelo con cualquier otro dato que el formulario necesite), y pasarse por prop al picker.
- **`services/storefront/logistics.ts`, cliente de browser en las tres funciones (lecturas y no solo la escritura)** (Sprint 6.2): la regla general sigue siendo "cliente de servidor si se lee durante el render de una página, de browser si se lee/escribe en respuesta a una interacción ya en el cliente" (sección 11, regla de la Fase 15) -- acá ninguna de las tres funciones tiene nunca un consumidor de servidor posible, porque su único consumidor (`ShippingCitySelect.tsx`, dentro de `/checkout`) es "use client" de punta a punta. Mismo criterio ya aplicado a `services/storefront/orders.ts`.
- **Sanitizar contenido HTML generado por el usuario en el límite donde el dato sale de la base, no solo en el componente que lo renderiza** (lección real del Sprint 6.6, `services/storefront/products.ts`): sanitizar únicamente en el componente de presentación (`ProductRichDescription.tsx`) evita que el HTML peligroso se ejecute o se inserte en el DOM, pero no evita que la versión sin sanitizar todavía viaje en el payload de RSC si el mismo objeto (`Product`, con el campo crudo) se pasa como prop a algún Client Component de la misma página -- React serializa cada prop para la hidratación, sin importar si algún componente la muestra visualmente. La sanitización real tiene que vivir en el `services/storefront/*` que lee el dato de Supabase, antes de que ese dato llegue a cualquier página o componente -- el sanitizado en el componente de presentación queda como defensa en profundidad adicional, no como la única barrera.
- **Antes de reintroducir una librería de un tipo ya usado en el proyecto para una necesidad nueva, verificar que compile con `pnpm build` (Turbopack), no solo con `pnpm dev`** (lección real del Sprint 6.6): `isomorphic-dompurify` pasaba `tsc`/`eslint`/`pnpm dev` sin ningún error, y solo falló al hacer `pnpm build` -- Turbopack no lograba resolver los archivos internos de `jsdom` (del que depende esa librería para correr en Node) al "Collecting page data". Cualquier dependencia nueva que dependa de una emulación de DOM en el servidor (`jsdom`, `happy-dom`, etc.) es candidata a este mismo problema -- confirmar con un build de producción real antes de darla por buena, no solo con el servidor de desarrollo.
- **Todo `update()`/`delete()` en `services/*.ts` del panel debe encadenar `.select("id")` (o el campo que corresponda) y pasar el resultado por `assertRowAffected()` (`lib/supabase/assert-write.ts`, Sprint 6.3.1)** -- lección real, no preventiva: un `update`/`delete` bloqueado por RLS (`USING`) no devuelve ningún error de Postgres, a diferencia de un `insert` bloqueado por `WITH CHECK` (que sí lanza un `42501` explícito) -- la operación "tiene éxito" con cero filas afectadas, y sin este chequeo el panel entero reportaba una edición o un borrado como exitosos aunque no hubieran cambiado nada. La única excepción legítima es un `delete` cuyo "cero filas" es un resultado esperado y no un fallo (ej. `replaceShippingRateCities()` borrando ciudades de una tarifa que todavía no tenía ninguna asignada) -- ahí no corresponde la aserción. Cualquier `update()`/`delete()` nuevo que se agregue a un `services/*.ts` del panel debe seguir este mismo patrón desde el día uno, no confiar en que la ausencia de `error` significa que la escritura ocurrió de verdad.
- **Decodificar (`decodeURIComponent()`) cualquier ruta de Storage extraída de una URL pública, y distinguir "acción principal" de "limpieza secundaria" al decidir si una escritura de Storage debe bloquear el guardado** (lección real, Sprint 6.3.2): `getPublicUrl()` (`@supabase/storage-js`) arma la URL con `encodeURI()` -- un archivo subido con un nombre que tenga espacios u otros caracteres especiales (`file.name` se usa tal cual, sin sanitizar, en `uploadProductImage`/`uploadCategoryImage`/`uploadBrandingAsset`/`uploadDescriptionAsset`) queda con esa parte percent-encoded en la URL guardada. Cualquier función que reconstruya la ruta del objeto recortando esa URL (`getStoragePathFromUrl`/`getCategoryImagePathFromUrl`/`getBrandingPathFromUrl`) tiene que decodificarla antes de pasarla a `.remove()` -- si no, Storage busca un objeto con `%20` literal en el nombre, no lo encuentra, y devuelve `200` con un array vacío (mismo comportamiento "éxito silencioso" que un `update`/`delete` de tabla bloqueado por RLS, confirmado en vivo con un DELETE anónimo real contra la Storage REST API). Separado de esto: `deleteProductImageFiles`/`deleteCategoryImageFile`/`deleteBrandingAsset` son *limpiezas* que ocurren como efecto secundario de guardar un formulario (reemplazar/quitar una imagen), no la acción que el admin pidió explícitamente -- por eso NO llevan `assertRowAffected()` (un resultado ambiguo se registra con `console.warn`, sin bloquear el guardado). `deleteProduct()`/`deleteCategory()`/`deletePromotion()`/`deleteShippingRate()` sí siguen bloqueando: ahí borrar el registro **es** la acción principal que el admin pidió con el botón "Eliminar". Cualquier función nueva de limpieza de Storage debe seguir el mismo criterio: decodificar la ruta, y no bloquear el flujo principal por un resultado ambiguo en un efecto secundario.
- **Las variables `NEXT_PUBLIC_*` de Vercel se incrustan en el bundle en tiempo de build, no en tiempo de ejecución** (Sprint 6.3.2): un deploy de producción construido mientras `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY` estuvieran vacías en el dashboard de Vercel quedaría con el cliente de Supabase roto de forma permanente en ese build específico, sin importar qué se corrija después en el dashboard -- hace falta un deploy nuevo (`vercel --prod`) para que el build siguiente tome los valores corregidos. Antes de asumir que un problema reportado por el usuario es de código, vale la pena confirmar que las variables de entorno de Production en Vercel coinciden con las de Development/`.env.local` (`vercel env pull --environment=production` a un archivo temporal, comparar, borrar el archivo) -- es una causa real, independiente de cualquier bug de RLS o de la aplicación, y se encontró exactamente así en este sprint (aunque el deploy vigente en ese momento no llegó a verse afectado, por casualidad de timing).

---

## 12. Estado aprobado

Lo siguiente está aprobado y **no debe modificarse salvo necesidad técnica real, justificada explícitamente**:

- La estructura de carpetas completa (sección 5).
- `Header` y `Footer` (`components/layout/`). Excepciones autorizadas: el Sprint 4.2 conectó el input de búsqueda (antes solo visual) a `/productos?q=...`; el Sprint 4.4 generalizó ese mismo buscador para que también funcione dentro de `/categorias/[slug]`. Mismo JSX, mismos estilos en ambos casos. El Sprint 4.5 no tocó ninguno de los dos: el link "Contacto" de `mainNav` ya existía y apuntaba a `/contacto`, solo faltaba la ruta. La Fase 14 les agregó a ambos un prop `settings: BusinessSettings` (ya no importan `config/business.ts`/`config/footer.ts`) y le sumó a `Header` el logo real (`next/image`) con fallback al nombre en texto — mismo JSX/estilos para el resto, ver sección 9.
- Toda la Home: Hero, Categorías Destacadas (`FeaturedCategories` reutiliza `CategoryCard` desde el Sprint 4.4, con el link corregido), Productos Destacados, Banner Promocional, Beneficios, Testimonios. Excepciones autorizadas: la Fase 13 le agregó a `FeaturedProducts` un prop `categories` (ya no importa `config/categories.ts`); la Fase 14 le agregó a `Hero` un prop `settings` (solo para el link de WhatsApp — el resto de su copy sigue viniendo de `config/site.ts`, sin cambios); la Fase 24 le agregó a `Hero` un prop `showcaseProducts` y reemplazó el panel derecho estático por `ProductShowcase.tsx` -- textos, botones, columna izquierda y el stagger de entrada de Framer Motion no cambiaron. Mismo JSX y estilos en el resto de los casos, ver sección 9.
- **El Showcase de productos del Hero** (`ProductShowcase.tsx` + `useProductShowcase.ts`, Fase 24): el carrusel automático (1.5s), su pausa en hover, su navegación manual (flechas + dots con loop infinito) y `getPublicHeroProducts()` (destacados primero, más recientes si no hay ninguno, nunca vacío) quedan aprobados como el reemplazo oficial del marcador decorativo estático que tenía el Hero desde el Sprint 2.1. Cambiar el intervalo de auto-avance, el criterio de selección de productos, o volver a un panel derecho estático requiere la misma justificación explícita que cualquier otro estado aprobado.
- El catálogo (`/productos`) y sus componentes (`ProductGrid`, `CatalogToolbar` — controlado desde el Sprint 4.3 —, `CatalogResults` desde el Sprint 4.2, generalizada en el Sprint 4.4 para funcionar también en `/categorias/[slug]`). Excepciones autorizadas: la Fase 13 le agregó a `ProductGrid` un prop `categories`, reenviado a cada `ProductCard`; el Sprint 6.1 le agregó a `ProductGrid`/`CatalogResults` un prop `promotions`, reenviado de la misma forma (ver más abajo y sección 9); el **Sprint 6.4.1 (Fase 33) le agregó a `ProductGrid`/`ProductCard` un prop `dense`** -- sin pasarlo (`RelatedProducts.tsx`, página de producto, protegida), el grid y la tarjeta se comportan exactamente igual que siempre; con él (`CatalogResults.tsx`, exclusivo del catálogo), el grid pasa a 2 columnas en mobile / 3 en tablet / 4 en desktop (antes 1/2/4), con gaps y padding de tarjeta más ajustados. Ver sección 9, Fase 33.
- La navegación por categorías (`/categorias`, `/categorias/[slug]`) y `CategoryCard` (Sprint 4.4).
- La página de producto (`/productos/[slug]`) y sus componentes (`ProductGallery`, `ProductBuyBox`, `RelatedProducts`). Excepciones autorizadas: la Fase 12 modificó `ProductGallery` para mostrar la foto real cuando existe; la Fase 13 les agregó a los tres (`ProductGallery`, `ProductBuyBox`, `RelatedProducts`) un prop `categories`, ya no importan `config/categories.ts` (ver más abajo y sección 9); la Fase 19 deshabilitó el botón "Comprar por WhatsApp" de `ProductBuyBox` cuando el producto no tiene stock, instrucción explícita de ese sprint (ver sección 9); el Sprint 6.1 agregó a `ProductBuyBox`/`RelatedProducts` un prop `promotions`, para mostrar el precio promocional vía `getProductDisplayPrice()` (ver más abajo y sección 9) -- `ProductGallery` **no** se tocó en este último sprint, nunca mostró un precio.
- El carrito (`/carrito`) y el store global (`store/cart-store.ts`).
- El checkout (`/checkout`), sus componentes de formulario y el store `store/checkout-store.ts`. Excepción autorizada: el Sprint 6.2 modificó `ShippingInformation.tsx` (los `<Input>` de departamento/ciudad pasaron a ser `<Select>` conectados a Supabase, vía el componente nuevo `ShippingCitySelect.tsx`), `CheckoutSummary.tsx` (el placeholder de envío pasó a mostrar el costo real y sumarlo al Total) y `store/checkout-store.ts` (campos nuevos `shippingChecked`/`shippingRateId`/`shippingRateName`/`shippingCost`/`shippingEstimatedDays` + acciones `setShippingResult`/`resetShipping`) -- instrucción explícita del sprint ("modificar únicamente la parte correspondiente al cálculo del envío"), sin tocar el resto del flujo. Ningún otro campo, paso ni componente del checkout se tocó (ver sección 9, Fase 22).
- La confirmación de pedido (`/checkout/confirmacion`), `OrderConfirmationDetails` y el envío por WhatsApp (Sprint 3.6 + 3.8), incluyendo `lib/whatsapp.ts`. Excepciones autorizadas: la Fase 15 modificó `OrderConfirmationDetails.tsx` (y únicamente ese archivo, ningún otro paso del checkout) para llamar a `createOrder()` antes de abrir WhatsApp — el formato del mensaje no cambió una línea; el Sprint 6.0.1 (bug 3) modificó `lib/whatsapp.ts` (`getOrderWhatsAppUrl()` recibe `whatsappNumber` por parámetro en vez de importar `config/business.ts`) y `OrderConfirmationDetails.tsx` (resuelve ese número desde `business_settings` con el cliente de browser) — el formato del texto del mensaje en sí tampoco cambió (ver sección 9).
- `LocationPicker`/`LeafletMap` y la selección de ubicación en `ShippingInformation` (Sprint 3.7).
- El sistema de diseño visual: paleta neutra basada en tokens `oklch` de shadcn, radios de borde, sombras, tipografía (Geist) y timings de animación ya establecidos en `lib/motion.ts`.
- `ProductCard`, específicamente — declarado "no tocar" en más de un sprint consecutivo. Excepciones autorizadas hasta la fecha: el Sprint 4.1 (agregó el `onClick` de "Agregar al carrito" que le faltaba), la Fase 12 (mostrar la foto real cuando existe, con fallback al placeholder), la Fase 13 (recibe `categories` por prop en vez de importar `config/categories.ts`, instrucción explícita del sprint), el Sprint 6.0.1 (agregó el `<Link>` a `/productos/[slug]` que nunca tuvo -- bug 1, ver sección 9), el Sprint 6.1 (recibe `promotions` por prop, para mostrar el precio promocional vía `getProductDisplayPrice()` en vez del precio/badge legacy calculado a mano -- instrucción explícita del sprint, ver sección 9), el **Sprint 6.7 (Fase 29), rediseño visual completo autorizado explícitamente**: menos altura (sin descripción corta ni fila de stock separada), imagen más protagonista, badges modernizados (incluye "Últimas unidades" migrado de fila de texto a badge), botón más compacto (`size="sm"`), hover/focus/sombra rediseñados con `focus-within:ring-2` en toda la tarjeta, y unificación de `isRealImageUrl()`/`getStockStatus()` con `lib/products.ts` (deuda técnica ya documentada, resuelta al tocar el archivo) -- sin agregar ningún botón ni funcionalidad nueva, sin cambiar la cantidad de columnas de `ProductGrid` (protegido, usado también por Productos Relacionados en la página de producto), y el **Sprint 6.4.1 (Fase 33), prop `dense` agregado explícitamente para el catálogo de 2 columnas en mobile**: sin ese prop (Productos Relacionados, Home) la tarjeta es pixel-idéntica a la Fase 29 -- confirmado contra el HTML servido real; con él, padding/gaps/precio más chicos, título `line-clamp-2` (en vez de `-1`) y un `sizes` de `next/image` propio para la grilla más angosta. Ver sección 9, Fase 33, para el detalle completo. Sigue "no tocar" para cualquier otro cambio sin la misma justificación explícita.
- **El módulo Categorías del Panel Administrativo, extendido con imagen** (Fase 31, Sprint 6.6): `CategoryForm.tsx`/`CategoryImageField.tsx`/`services/categories.ts`/`services/storage.ts` -- la subida/reemplazo/borrado diferido de `image_url` sigue exactamente el mismo patrón ya aprobado para Logo/Favicon (`SettingsForm.tsx`/`BrandingAssetField.tsx`, Fase 14) y para las imágenes de producto (`ProductForm.tsx`, Fase 10). El bucket `category-images` (5 MB, `png`/`jpeg`/`webp`) y sus policies quedan aprobados como la implementación oficial. `CategoryCard.tsx` queda rediseñado de punta a punta: imagen a pantalla completa con degradado + nombre superpuesto (reemplazando el bloque de texto separado que tenía antes), mismo tratamiento de hover/foco que `ProductCard` (Sprint 6.7) para mantener coherencia visual entre ambos rediseños. Cambiar este patrón de guardado, el bucket, o volver al bloque de texto separado en `CategoryCard.tsx` requiere la misma justificación explícita que cualquier otro estado aprobado. Ver sección 9, Fase 31.
- El diseño de base de datos (`docs/database-architecture.md`, Sprint 4.0): las 12 tablas, sus columnas y sus relaciones están aprobadas como base oficial. Cambiarlas (agregar/quitar una tabla o columna) requiere la misma justificación explícita que romper cualquier otro estado aprobado — no es un borrador descartable.
- La página de contacto (`/contacto`, Sprint 4.5) y el helper `getGoogleMapsUrl()` (`lib/utils.ts`). Excepción autorizada: la Fase 14 la hizo `async` para leer `BusinessSettings` en vez de `config/business.ts`/`config/footer.ts`, y le sumó a `getGoogleMapsUrl()` un segundo parámetro opcional de coordenadas (cambio aditivo, no rompe el uso anterior con solo `address`).
- Las páginas institucionales (`/politicas/[slug]`, `/preguntas-frecuentes`, Sprint 4.6) y sus fuentes de contenido (`config/policies.ts`, `config/faq.ts`).
- La infraestructura de Supabase (`supabase/migrations/`, `supabase/seed.sql`, Fase 8, ya aplicada al proyecto real): las 12 tablas, sus índices, el bucket `product-images` y sus policies, y las policies de RLS quedan aprobadas como la implementación oficial de `docs/database-architecture.md`. Cambiarlas — agregar/quitar una columna, una policy o un índice — requiere la misma justificación explícita que romper cualquier otro estado aprobado; no son borradores descartables.
- La estructura del Panel Administrativo (Fase 9): el route group `app/(storefront)/` y su separación de `app/admin/` (dos root layouts), `middleware.ts` + `lib/supabase/middleware.ts`, `admin/(panel)/layout.tsx` (verificación de sesión), y los componentes de `components/admin/` (`AdminShell`, `AdminSidebar`, `AdminHeader`, `LogoutButton`). Cambiar el mecanismo de autenticación, la separación de layouts o el patrón de protección de rutas requiere la misma justificación explícita que cualquier otro estado aprobado. (`AdminPlaceholder` se eliminó en el Sprint 6.0.1 por quedar sin consumidores, no es parte de este estado aprobado.)
- El módulo Productos del Panel Administrativo (Fase 10): `/admin/productos`, `/admin/productos/nuevo`, `/admin/productos/[id]/editar`; `components/admin/products/` (`ProductsTable`, `ProductForm`, `ProductImagesField`, `DeleteProductDialog`); la capa `services/products.ts`/`services/categories.ts`/`services/storage.ts` y los hooks `useProducts`/`useProduct`/`useCategories`; y la migración `products_admin_fields.sql` (columnas `is_active`/`description` en `products`). Cambiar el esquema, las policies o el patrón de capas (`components → hooks → services → Supabase`) de este módulo requiere la misma justificación explícita que cualquier otro estado aprobado — y sienta el patrón a seguir para los módulos de Categorías/Pedidos/Clientes/Promociones/Configuración que siguen. **Excepción autorizada (Fase 28, Sprint 6.6)**: el `<textarea>` de "Descripción completa" se reemplazó por `RichTextEditor.tsx` (editor Tiptap, `components/admin/products/` + `rich-text/`) -- el resto del formulario (nombre, slug, descripción corta, categoría, precio, stock, imágenes de galería) no cambió. Ver sección 9, Fase 28.
- **El editor enriquecido de descripción de producto** (`RichTextEditor.tsx`, `EditorToolbar.tsx`, `ResizableImage`/`Video` en `rich-text/extensions.ts`, `ProductRichDescription.tsx`, `lib/sanitize-html.ts`, el bucket `product-content`, Fase 28): Tiptap como editor headless, sanitización con `sanitize-html` (no `dompurify`), la descripción mostrándose únicamente en `/productos/[slug]` y nunca en `getPublicProducts()`/`getPublicHeroProducts()` (Catálogo, Home, Destacados, Showcase) quedan aprobados como la implementación oficial. Cambiar la librería del editor, el bucket de Storage, el criterio de sanitización, o hacer que la descripción enriquecida llegue a algún componente fuera de la página individual del producto (`ProductCard`, `FeaturedProducts`, `ProductShowcase`, el Catálogo) requiere la misma justificación explícita que cualquier otro estado aprobado.
- La conexión de la tienda pública a Supabase (Fase 11): `services/storefront/products.ts`/`categories.ts` como única vía de lectura pública, el `cliente de servidor` (no el de browser) como su base, la ausencia deliberada de `generateStaticParams` en `/productos/[slug]` y `/categorias/[slug]`, y el hecho de que RLS (no un filtro de aplicación) es quien decide qué producto/categoría es visible públicamente. Cambiar esto — por ejemplo, volver a leer de `config/*.ts`, o mover el fetching al cliente — requiere la misma justificación explícita que cualquier otro estado aprobado. `config/products.ts` y `config/categories.ts` en sí (los archivos, sus tipos, sus datos placeholder) siguen aprobados como fallback/documentación tal como quedaron.
- El renderizado de fotos reales de producto (Fase 12): `next.config.ts` (`images.remotePatterns` derivado de `NEXT_PUBLIC_SUPABASE_URL`), y la lógica de `ProductCard`/`ProductGallery` que muestra la foto real cuando `isRealImageUrl` la detecta, con fallback al placeholder. Cambiar el criterio de detección, el dominio permitido, o volver a un placeholder incondicional requiere la misma justificación explícita que cualquier otro estado aprobado.
- El módulo Categorías del Panel Administrativo (Fase 13): `/admin/categorias`, `/admin/categorias/nueva`, `/admin/categorias/[id]/editar`; `components/admin/categories/` (`CategoriesTable`, `CategoryForm`, `DeleteCategoryDialog`, `category-icons.ts`); la capa `services/categories.ts` (extendida a CRUD completo) y los hooks `useCategories`/`useCategory`; y la migración `categories_admin_fields.sql` (columna `icon_name`). El borrado seguro (bloqueo en la UI + `on delete restrict` a nivel de base) es parte de este estado aprobado — no debe removerse ni debilitarse sin la misma justificación explícita que cualquier otro cambio. Cambiar el esquema, las policies o el patrón de capas de este módulo requiere esa misma justificación.
- La eliminación de la dependencia activa de `config/categories.ts` en la tienda pública (Fase 13): `ProductCard`, `ProductGallery`, `ProductBuyBox`, `ProductGrid`, `FeaturedProducts` y `RelatedProducts` reciben `categories: Category[]` por prop en vez de importar el array de config. Volver a importar `categories` como valor en cualquiera de estos componentes requiere la misma justificación explícita que cualquier otro estado aprobado. Excepción ya documentada y aceptada: `CartItem.tsx`/`CheckoutSummary.tsx` (Carrito/Checkout) siguen importando `config/categories.ts` — fuera de alcance por instrucción explícita del Sprint 5.4, no un descuido.
- El módulo Configuración General del Panel Administrativo (Fase 14): `/admin/configuracion`; `components/admin/settings/` (`SettingsForm`, `BrandingAssetField`); la capa `services/settings.ts` (lectura/actualización de `business_settings` + subida/borrado de branding) y el hook `useSettings`; la migración `business_settings_admin_fields.sql` (columnas `tagline`/`logo_url`/`favicon_url`/`instagram_url`/`facebook_url`/`tiktok_url`/`map_country`/`map_city`); y el bucket de Storage `branding` con sus policies. Cambiar el esquema, las policies, el bucket o el patrón de capas de este módulo requiere la misma justificación explícita que cualquier otro estado aprobado. **Excepción autorizada (Fase 23, Sprint 6.3)**: la tarjeta "Branding" (Logo + Favicon) se ocultó de `SettingsForm.tsx` -- pedido explícito de "ocultar, no eliminar" -- sin tocar `services/settings.ts`, el bucket ni `BrandingAssetField.tsx` (que sigue existiendo intacto, sin usarse). Ver sección 9, Fase 23. **Reemplazada por la Fase 25 (Sprint 6.5)**: la tarjeta "Branding" oculta se reemplaza definitivamente por la pestaña "Apariencia" (`Tabs`/`TabsList`/`TabsTrigger`/`TabsContent` de shadcn, agregado con el CLI), que reconecta Logo/Favicon bajo el nombre "Identidad Visual" con la lógica exacta de siempre (`BrandingAssetField.tsx`/`services/settings.ts` sin ningún cambio) y agrega tres secciones puramente visuales sin funcionalidad -- Hero, Colores del tema, Tipografía -- quedan aprobadas como la estructura oficial preparada para esas tres personalizaciones futuras. Sigue siendo un único `<form>` con un único submit: las pestañas son solo navegación de UI. Cambiar esta estructura de pestañas, o implementar la lógica real de Hero/Colores del tema/Tipografía sin pasar antes por la interfaz ya preparada acá, requiere la misma justificación explícita que cualquier otro estado aprobado. Ver sección 9, Fase 25.
- La migración parcial de la tienda pública fuera de `config/business.ts`/`config/footer.ts` (Fase 14): `Header`, `Footer`, `Hero` (solo WhatsApp) y `/contacto` reciben `BusinessSettings` por prop; `services/storefront/business.ts` es la única vía de lectura pública **durante el render de una página** (cliente de servidor); `services/storefront/business-client.ts` (Sprint 6.0.1) es la vía equivalente para el único caso que no puede resolverse en el servidor (`/checkout/confirmacion`, "use client" de punta a punta) -- ambos archivos existen a propósito por separado, ver sección 6/11 para por qué no pueden fusionarse. `(storefront)/layout.tsx` usa `generateMetadata` dinámico para título/descripción/favicon. **Dos de las tres exclusiones de la Fase 14 siguen vigentes, la tercera se resolvió en el Sprint 6.0.1**: `formatPrice()` (moneda/locale) y el centro del mapa de Checkout siguen leyendo `config/business.ts` estático — cambiar cualquiera de las dos requiere la misma autorización explícita que cualquier otro estado aprobado. `lib/whatsapp.ts` (el número de WhatsApp del mensaje del pedido) **ya no** es una excepción: desde el Sprint 6.0.1 (bug 3) lee `business_settings` vía `business-client.ts`, no `config/business.ts`.
- El módulo Pedidos del Panel Administrativo (Fase 15): `/admin/pedidos`, `/admin/pedidos/[id]`; `components/admin/orders/` (`OrdersTable`, `OrderDetailView`, `OrderStatusSelect`, `order-status.ts`); la capa `services/orders.ts` (lectura + cambio de estado) y los hooks `useOrders`/`useOrder`. Cambiar el esquema, las policies o el patrón de capas de este módulo requiere la misma justificación explícita que cualquier otro estado aprobado.
- La función de Postgres `create_order` (Fase 15, `supabase/migrations/..._create_order_function.sql`) y su condición de único punto de escritura pública hacia `customers`/`orders`/`order_items`: RLS sigue 100% cerrada a `is_admin()` en las tres tablas, sin ninguna policy de `insert` para `anon`. Cambiar esto — agregar policies de escritura directas, modificar qué campos acepta la función, o quitar la garantía de atomicidad — requiere la misma justificación explícita que cualquier otro estado aprobado.
- La integración de Checkout con Supabase (Fase 15): `OrderConfirmationDetails.tsx` llama a `createOrder()` (`services/storefront/orders.ts`) antes de abrir WhatsApp, con el patrón de pestaña pre-abierta + redirección tras el guardado. El resto de los componentes de Checkout (`CheckoutForm`, `ShippingInformation`, `PaymentMethod`, `OrderNotes`, `CheckoutSummary`, `store/checkout-store.ts`) no se tocaron y siguen protegidos como antes.
- El módulo Clientes del Panel Administrativo (Fase 16): `/admin/clientes`, `/admin/clientes/[id]`; `components/admin/customers/` (`CustomersTable`, `CustomerDetailView`); la capa `services/customers.ts` (100% solo lectura) y los hooks `useCustomers`/`useCustomer`. Su condición de módulo de solo consulta (sin alta/edición/baja de clientes) es parte de este estado aprobado — agregar un formulario de creación/edición manual de clientes requiere la misma justificación explícita que cualquier otro cambio de arquitectura, porque contradice el diseño deliberado de "los clientes se crean solos" (Fase 15/16).
- El cálculo de estadísticas de clientes en memoria, no en una vista de Postgres (Fase 16): mientras el volumen de pedidos no lo justifique, `services/customers.ts` sigue calculando total gastado/cantidad de compras/ticket promedio/primera y última compra a partir de los pedidos ya traídos. Introducir una vista o función agregada de Postgres para esto es una optimización válida a futuro (sección 13), no algo a hacer sin señalarlo explícitamente primero.
- El Dashboard del Panel Administrativo (Fase 17, rediseño visual en el Sprint 6.0.1): `/admin`; `components/admin/dashboard/` (`SummaryCard` -- con `tone` desde el Sprint 6.0.1 --, `RecentOrdersTable`, `TopProductsList`, `SalesChart` (nuevo, Sprint 6.0.1), `OrderStatusBreakdown` (Recharts desde el Sprint 6.0.1, antes CSS puro), `Dashboard`); la capa `services/dashboard.ts` (100% solo lectura, agrega 5 tablas) y el hook `useDashboard`. Que todas las cifras se calculen en cada carga (nunca se guarden, nunca se hardcodeen) es parte de este estado aprobado — cachear o persistir alguna de estas métricas requiere la misma justificación explícita que cualquier otro cambio de arquitectura. Recharts es la única librería de gráficos aprobada para el panel; el rediseño visual del Sprint 6.0.1 no agregó ni cambió ninguna consulta de `services/dashboard.ts`.
- El criterio de "ventas totales incluye todos los pedidos, sin importar su estado" (establecido en la Fase 16 para clientes, confirmado y reaplicado en la Fase 17 para el Dashboard): cambiarlo para excluir pedidos cancelados de las cifras de venta es una decisión de negocio legítima, pero requiere aplicarse de forma consistente en ambos lugares a la vez, con la misma justificación explícita que cualquier otro estado aprobado — no decidirlo distinto en un módulo nuevo sin revisar los ya existentes.
- El módulo Promociones del Panel Administrativo (Fase 18): `/admin/promociones`, `/nueva`, `/[id]/editar`; `components/admin/promotions/` (`PromotionsTable`, `PromotionForm`, `DeletePromotionDialog`, `promotion-status.ts`); la capa `services/promotions.ts` (CRUD completo, sin restricción de borrado) y los hooks `usePromotions`/`usePromotion`/`usePromotionTargets`; `lib/promotions.ts` (`formatDiscountLabel`, `getPromotionStatus`, y desde el Sprint 6.1 también `calculatePromotion`/`getProductDisplayPrice`, ver más abajo). Cambiar el esquema, las policies o el patrón de capas de este módulo requiere la misma justificación explícita que cualquier otro estado aprobado.
- **(Revertido en el Sprint 6.1 -- ya no vigente, se deja documentado por trazabilidad)** Hasta la Fase 18, una promoción no recalculaba el precio/badge de descuento mostrado de un producto o categoría puntual (decisión heredada del diseño original de la Fase 8). El Sprint 6.1 autorizó y construyó exactamente esa conexión: `getProductDisplayPrice()`/`calculatePromotion()` (`lib/promotions.ts`) son ahora la única fuente aprobada para decidir el precio/tachado/badge mostrado de un producto, consumidas por `ProductCard`/`ProductBuyBox` (ver más abajo). Revertir esta conexión (volver a un precio de catálogo que ignore las promociones vigentes) requiere la misma justificación explícita que cualquier otro cambio de arquitectura.
- **El motor de precios promocionales** (Fase 21, Sprint 6.1): `calculatePromotion(product, promotions)` y `getProductDisplayPrice(product, promotions)` (`lib/promotions.ts`) son la única fuente aprobada para decidir precio final/tachado/badge de un producto -- ningún componente debe volver a comparar `product.price`/`oldPrice` a mano. Prioridad fija (producto puntual > categoría, nunca acumular) y el tipo mínimo `PricingPromotion` (para evitar un ciclo de imports con `services/storefront/promotions.ts`) son parte de este estado aprobado. `services/storefront/promotions.ts` expone `getActivePromotions()` (plural, reemplaza a la antigua `getActivePromotion()`) como única función que trae las promociones vigentes, con `PublicPromotion` extendido de forma aditiva (`discountType`/`discountValue`/`productId`/`categorySlug`). Cambiar el algoritmo de prioridad, dejar de filtrar `is_active`/vigencia explícitamente, o permitir que se acumulen dos descuentos requiere la misma justificación explícita que cualquier otro estado aprobado.
- El banner promocional dinámico (`PromotionalBanner.tsx`, Fase 18, convertido en carrusel en el Sprint 6.1): recibe `promotions: PublicPromotion[]` por prop (ya no un `promotion` singular) en vez de importar `config/promotion.ts`; sin ninguna promoción vigente, no se renderiza; con exactamente una, se comporta idéntico a como lo hacía desde la Fase 18 (mismo JSX vía `PromotionContent`, mismo stagger de entrada, sin controles de navegación); con dos o más, se convierte en un carrusel automático (~2.5s por slide, transición horizontal de Framer Motion, loop infinito, flechas + dots que reinician el temporizador, pausa en hover) -- este comportamiento condicional por cantidad de promociones es parte de este estado aprobado, no una decisión de implementación libre. El estilo visual único (antes "dark"/"light" configurable) y el texto del botón ("Ver oferta") siguen fijos en el componente a propósito -- no son datos de negocio, son decisiones de diseño/copy genérico, y no deben moverse a `business_settings` ni a ninguna tabla sin la misma justificación explícita que cualquier otro cambio de arquitectura. Desde el Sprint 6.0.1 (bug 2), `PublicPromotion` incluye `imageUrl` (la imagen principal de `product_images` cuando la promoción es de un producto, vía `services/storefront/promotions.ts`) y el banner la muestra con `next/image` en vez del placeholder decorativo cuando existe -- sin promoción de producto, o sin foto real todavía, sigue el mismo placeholder de siempre.
- `app/favicon.ico` movido a `public/favicon-default.ico` (Fase 14): el archivo especial de Next.js se retiró a propósito para que el favicon dinámico (`business_settings.favicon_url`) pueda ser la única fuente del ícono sin competir con la convención de archivo. Revertir este movimiento (recrear `app/favicon.ico`) reintroduciría el bug de los dos `<link rel="icon">` — requiere la misma justificación explícita que cualquier otro cambio de arquitectura.
- El módulo Inventario del Panel Administrativo (Fase 19): `/admin/inventario`, `/admin/inventario/[id]`; `components/admin/inventory/` (`InventoryTable`, `InventoryDetailView`, `StockMovementForm`, `inventory-status.ts`); la capa `services/inventory.ts` y los hooks `useInventory`/`useInventoryItem`; la tabla `stock_movements` (historial inmutable, sin policy de `update`/`delete`) y las columnas `products.sku`/`products.low_stock_threshold`/`orders.stock_adjusted`. Cambiar el esquema, las policies o el patrón de capas de este módulo requiere la misma justificación explícita que cualquier otro estado aprobado.
- La función `register_stock_movement` y el trigger `apply_order_stock_movement` (Fase 19, `supabase/migrations/..._create_inventory.sql`), y su condición de únicos puntos de escritura de `products.stock`: ningún componente ni servicio hace un `update products set stock = ...` directo -- ni siquiera `services/inventory.ts`. El trigger se engancha al `update` de `orders.status` que `services/orders.ts` (Pedidos) ya hacía desde la Fase 15, sin que ese archivo cambie una línea; la bandera `orders.stock_adjusted` es lo que evita descontar o restituir dos veces. Cambiar esto -- agregar una escritura directa a `stock`, modificar cuándo se descuenta/restituye, o quitar la garantía de "nunca negativo" -- requiere la misma justificación explícita que cualquier otro estado aprobado.
- El comportamiento de "Sin stock" en la tienda pública (`ProductCard.tsx`, `ProductBuyBox.tsx`): con `stock <= 0`, ambos botones ("Agregar al carrito" y, desde la Fase 19, "Comprar por WhatsApp") se muestran deshabilitados y el producto sigue visible, nunca oculto. La excepción autorizada de la Fase 19 en `ProductBuyBox.tsx` (deshabilitar también el botón de WhatsApp) es parte de este estado aprobado -- revertirla, u ocultar un producto sin stock en vez de deshabilitar sus botones, requiere la misma justificación explícita que cualquier otro cambio.
- El filtro explícito de `is_active` (y vigencia de fechas para promociones) en `services/storefront/products.ts`/`categories.ts`/`promotions.ts` (Sprint 6.0.1, bug 4), independiente de RLS: quitarlo -- volviendo a confiar únicamente en RLS -- reintroduciría el bug de categorías/productos/promociones inactivos visibles para quien navega la tienda con una sesión de admin activa en el mismo navegador. Requiere la misma justificación explícita que cualquier otro estado aprobado.
- La validación de tamaño de logo/favicon en `SettingsForm.tsx`/`BrandingAssetField.tsx` (`MAX_BRANDING_FILE_SIZE_BYTES`/`_MB`, `services/settings.ts`, Sprint 6.0.1, bug 5): el archivo se valida contra el límite del bucket `branding` (2 MB) antes de guardarlo como pendiente, nunca después. Quitar esta validación reintroduciría el error de Supabase Storage apareciendo recién al guardar. Si el límite del bucket cambia alguna vez, esta constante debe actualizarse junto con la migración correspondiente.
- La guarda `isRealImageUrl` en `ProductsTable.tsx`/`ProductImagesField.tsx` (panel, Sprint 6.0.1), en paralelo a la que ya tenía `ProductCard.tsx`/`ProductGallery.tsx` (tienda pública, Fase 12): ambas capas del proyecto (panel y storefront) deben mostrar el placeholder cuando `product_images.url` no es una URL real de Storage, nunca intentar cargarla directamente. Cualquier componente nuevo (panel o storefront) que muestre una foto de producto debe seguir este mismo criterio desde el día uno.
- El módulo Logística del Panel Administrativo (Fase 22): `/admin/logistica`, `/admin/logistica/nueva`, `/admin/logistica/[id]/editar`; `components/admin/logistics/` (`ShippingRatesTable`, `ShippingRateForm`, `DepartmentCityPicker`, `DeleteShippingRateDialog`); la capa `services/logistics.ts` y los hooks `useShippingRates`/`useShippingRate`/`useDepartments`/`useCitiesByDepartment` (`hooks/useDepartmentsAndCities.ts`); las tablas `departments`/`cities`/`shipping_rates`/`shipping_rate_cities` y las columnas `orders.shipping_rate_id`/`shipping_rate_name`. Cambiar el esquema, las policies o el patrón de capas de este módulo requiere la misma justificación explícita que cualquier otro estado aprobado.
- Que `departments`/`cities` no tengan ninguna policy de escritura -- ni para `is_admin()` -- es parte de este estado aprobado, no una limitación a resolver en silencio: la única forma soportada de modificar estas dos tablas es `supabase/seed_logistics.sql` (o una migración de datos futura). Agregar un formulario de alta/edición de departamentos o ciudades desde el Panel requiere la misma justificación explícita que cualquier otro cambio de arquitectura, porque contradice el diseño deliberado del Sprint 6.2.
- El cálculo de envío del Checkout (Fase 22): `ShippingCitySelect.tsx` es la única pieza autorizada a calcular un costo de envío -- busca la tarifa que cubre la ciudad elegida y escribe el resultado en `store/checkout-store.ts`; ningún otro componente debe volver a calcularlo por su cuenta. Que una ciudad sin tarifa configurada muestre "A confirmar" (nunca un costo inventado, nunca bloquea la compra) y que el mensaje de WhatsApp (`lib/whatsapp.ts`) siga sin sumar el envío al total (excepción documentada, ver sección 13) son ambos parte de este estado aprobado.
- La fila de envío en `/admin/pedidos/[id]` (`OrderDetailView.tsx`, Fase 22): con Delivery, siempre se muestra -- el monto real si `shippingCost` no es null, "A confirmar" en ámbar si lo es -- nunca se oculta como antes de este sprint. Con Retiro en tienda, sigue sin mostrarse. Revertir a "ocultar la fila cuando `shippingCost` es null" reintroduciría exactamente el problema que este sprint pidió resolver ("que el administrador vea inmediatamente que debe informar el costo manualmente").

Cualquier tarea que requiera tocar algo de esta lista debe decirlo de forma explícita antes de hacerlo, no como efecto colateral de otra tarea.

---

## 13. Pendientes (próximos sprints)

En orden esperado de ejecución (ver también sección 8):

1. ~~Crear el primer usuario administrador real (de producción) en Supabase Auth + su fila en `admins`.~~ **Hecho (Fase 26, Sprint 6.2.1.1)** — ver sección 9 para el detalle: la cuenta real (`felixmatiasarandarromero@gmail.com`) ya existía en `auth.users`, pero le faltaba su fila en `admins`, lo que rompía silenciosamente `is_admin()` para toda escritura del panel.
2. Persistencia del carrito y del checkout.
3. **Reevaluar la limpieza del carrito tras un pedido confirmado** — deliberadamente no implementada desde el Sprint 3.8 porque "sin backend que confirme que el pedido se guardó de verdad, vaciarlo en el click podría perder el pedido". Desde la Fase 15 esa condición ya no aplica (`createOrder()` sí confirma el guardado antes de abrir WhatsApp) — sigue sin implementarse porque el Sprint 5.6 no lo pidió explícitamente, no porque el bloqueo técnico original siga vigente. Buen candidato para el próximo sprint que toque Carrito/Checkout.
4. Filtros de catálogo por precio/stock (búsqueda, ordenamiento y navegación por categoría ya resueltos, Sprints 4.2, 4.3 y 4.4, conectados a Supabase desde la Fase 11).
5. Paginación real en `/productos`, `/categorias/[slug]` (storefront), `/admin/categorias`, `/admin/pedidos`, `/admin/inventario` y `/admin/logistica` (panel) — la de `/admin/productos` ya es real desde la Fase 10; las demás quedaron preparadas pero deliberadamente no implementadas (alcance explícito de sus sprints).
6. Página institucional "Nosotros" — única página institucional pendiente; Contacto (Sprint 4.5) y Políticas/FAQ (Sprint 4.6) ya están resueltas.
7. Optimizar la latencia de búsqueda/orden en `/productos` y `/categorias/[slug]` (hoy cada cambio de `?q=`/`?sort=` dispara un round-trip real al ser rutas dinámicas desde la Fase 11) con caché/ISR, si la experiencia lo empieza a justificar.
8. Gestión de imágenes de categoría (`image_url`) desde el panel — el módulo de Categorías (Fase 13) no la incluyó (no se pidió); hoy sigue sin ningún flujo de subida, a diferencia de `product-images` que ya tiene el suyo desde la Fase 10.
9. `CartItem.tsx`/`CheckoutSummary.tsx` (Carrito/Checkout) siguen importando `categories` de `config/categories.ts` directamente — únicas dos referencias activas que quedan en el proyecto. Dejadas así originalmente por instrucción explícita del Sprint 5.4 ("NO modificar Carrito/Checkout"); reevaluadas y dejadas así a propósito también en el Sprint 6.0.1 (Checkout no estaba blanket-protegido esta vez, pero migrarlas hubiera requerido agregar un tercer fetch de browser-client a la tienda pública sin que ningún bug lo pidiera). Migrarlas al mismo patrón de prop (`categories` resuelta por la página, con el mismo criterio ya usado para `business_settings` en `OrderConfirmationDetails.tsx`) cuando un sprint futuro lo priorice explícitamente.
10. **Migrar `formatPrice()` (moneda/locale) y el centro del mapa de Delivery (`ShippingInformation`/`LocationPicker`) a `business_settings`** — las dos siguen leyendo `config/business.ts` estático. El número de WhatsApp del mensaje del pedido (`lib/whatsapp.ts`), la tercera exclusión original de la Fase 14, se resolvió en el Sprint 6.0.1 (bug 3) -- ya lee `business_settings` vía `services/storefront/business-client.ts`. Cambiar el WhatsApp desde `/admin/configuracion` ahora sí afecta el mensaje de confirmación de pedido; cambiar la moneda o la ubicación del mapa, todavía no. Requiere tocar Checkout/Carrito, así que necesita autorización explícita antes de encararse.
11. `footerConfig.legalLinks`/`footerConfig.developedBy` siguen viniendo de `config/footer.ts` — no forman parte de ninguna de las 5 secciones del Sprint 5.5 (Configuración General no gestiona links legales ni el crédito de "Desarrollado por"). Posible alcance de un sprint futuro si se pide gestionar contenido legal desde el panel.
12. Panel administrativo: su propio Header/Sidebar (`AdminHeader.tsx`) sigue mostrando `siteConfig.name` estático, no `business_settings.store_name` — no se pidió en el Sprint 5.5 (el alcance era la tienda pública), pero sería una inconsistencia visible si un cliente cambia el nombre de su negocio y el panel sigue mostrando el nombre viejo.
13. `<title>` de páginas individuales del storefront (`/productos/[slug]`, `/categorias/[slug]`, `/productos`, `/categorias`, políticas, FAQ) siguen usando `siteConfig.name` estático como sufijo — solo el `<title>` global (root layout) y el de `/contacto` se volvieron dinámicos en la Fase 14. Migrarlos es mecánico (mismo patrón, un `getPublicBusinessSettings()` más por página) pero no estaba en el alcance explícito del sprint.
14. `shipping_cost` (`orders`) sigue sin ningún cálculo real — `create_order` (Fase 15) siempre lo deja `null` y `total = subtotal`, exactamente como ya documentaba el diseño de la Fase 8 ("hoy no existe cálculo real, columna lista para cuando exista"). El día que haya una lógica de envío real, `services/storefront/orders.ts` es el único lugar que necesita cambiar.
15. Borrado de pedidos — no se pidió en el Sprint 5.6 (solo lectura + cambio de estado) y no hay ninguna policy ni función que lo permita hoy; "Cancelado" es el estado que cumple ese rol sin borrar el registro.
16. **Navegación Pedido → Cliente** (dirección inversa a la ya implementada Cliente → Pedido): `OrderDetailView.tsx`/`services/orders.ts` no exponen ningún link de vuelta al cliente — hubiera requerido tocar el módulo de Pedidos, explícitamente prohibido en el Sprint 5.7 ("NO modificar Pedidos"). Cuando se autorice, es un cambio chico: agregar `customer_id` a `AdminOrder` y un `Link` en `OrderDetailView.tsx` hacia `/admin/clientes/[customerId]`.
17. Estadísticas de clientes calculadas en memoria (`services/customers.ts`), no en una vista de Postgres — válido para el volumen actual de una tienda chica; si el volumen de pedidos crece lo suficiente, una vista o función agregada (`customer_stats`, con `security_invoker`) es el paso natural siguiente (ver justificación completa en sección 6, Fase 16).
18. **"Variación futura" de las tarjetas del Dashboard** (`SummaryCard.tsx`, prop `trend`): la estructura ya existe (Fase 17) pero ninguna tarjeta calcula todavía una variación real (por ejemplo, "+12% vs. el mes pasado") — requeriría decidir un período de comparación y traer datos históricos, fuera del alcance explícito del Sprint 5.8.
19. Gráficos interactivos para "Estados de pedidos" (hoy barras de CSS puro, deliberadamente sin librería de gráficos — Sprint 5.8) y para cualquier otra serie del Dashboard — explícitamente fuera de alcance ("No implementar gráficos todavía").
20. Navegación desde "Productos más vendidos" (Dashboard) hacia el producto en `/admin/productos/[id]/editar` — no se pidió en el Sprint 5.8 (a diferencia de "Últimos pedidos", que sí pide navegación explícita), fácil de agregar sin tocar el módulo de Productos (solo un `Link` nuevo en `TopProductsList.tsx`) si se prioriza en un sprint futuro.
21. Agregar una vista o función agregada de Postgres para las consultas de agregación (`services/customers.ts`, Fase 16; `services/dashboard.ts`, Fase 17) si el volumen de pedidos de un cliente futuro deja de ser trivial para calcular en memoria — evaluado y descartado dos veces por ahora, documentado en ambos casos (sección 6).
22. ~~Conectar una promoción activa con el precio/badge mostrado de su producto o categoría.~~ **Hecho (Fase 21, Sprint 6.1)** — `getProductDisplayPrice()`/`calculatePromotion()` (`lib/promotions.ts`) calculan el precio final/tachado/badge de cualquier producto a partir de las promociones vigentes, con prioridad producto > categoría y sin acumular, consumidos por `ProductCard`/`ProductBuyBox` en Home/Destacados/Catálogo/Producto/Relacionados/Categorías.
23. Columna de imagen para `promotions` — el banner promocional sigue mostrando el mismo placeholder decorativo de siempre (Sparkles + blobs) porque `promotions` no tiene ninguna columna `image_url` todavía; agregarla es una migración chica si se prioriza en un sprint futuro.
24. **Reactivar un pedido de `cancelled` de vuelta a un estado activo no vuelve a descontar stock** — `apply_order_stock_movement` (Fase 19) solo actúa sobre dos transiciones: entrar en un estado activo sin haber descontado (`stock_adjusted = false`) y pasar a `cancelled` habiendo descontado (`stock_adjusted = true`). Un pedido cancelado que se reactiva ya tiene `stock_adjusted = false`, así que al volver a "Confirmado" el trigger sí descuenta de nuevo correctamente -- este caso **sí** está cubierto. Lo que no está cubierto, porque no se pidió, es revertir manualmente un pedido de un estado activo de vuelta a "Pendiente": el trigger no tiene ninguna regla para esa transición (no es de las dos que definió el sprint), así que el stock quedaría descontado sin restituir hasta que el pedido se cancele o vuelva a confirmarse. No es un bug de la implementación pedida -- es una transición que "Pendiente" nunca vuelve a ocurrir en el flujo normal de Pedidos (Confirmado → Pendiente no es un paso que la UI fomente), documentada acá por transparencia.
25. Sin registro de "quién" hizo un movimiento manual de stock -- `stock_movements` no tiene una columna `admin_id`/`created_by`; no se pidió este sprint (a diferencia de, por ejemplo, `admins.id` que si existe desde la Fase 8 para otro propósito). Agregarla es una migración chica (`admin_id uuid references admins(id)`) si un sprint futuro necesita trazabilidad de quién ajustó qué.
26. Gestión de imágenes de categoría, de promoción, o filtros de precio/stock avanzados sobre `/admin/inventario` (ej. "ver solo productos con stock bajo") -- no se pidieron en el Sprint 6.0; hoy el buscador de Inventario solo filtra por nombre/SKU, no por estado calculado.
27. Un archivo huérfano en el bucket `branding` (`favicon/...tiny-favicon.png`, del propio proceso de verificación del Sprint 6.0.1): `business_settings.favicon_url` ya se restauró a `null` correctamente, pero Supabase no permite borrar `storage.objects` por SQL directo ("Direct deletion from storage tables is not allowed") y el archivo en sí no se removió por la Storage API antes de cerrar el sprint. Sin impacto funcional (nada lo referencia), pero queda como housekeeping menor para un sprint futuro que toque `/admin/configuracion`.
28. **`useReducedMotion()` sin `mounted`-gate en `Hero.tsx`/`FeaturedCategories.tsx`/`FeaturedProducts.tsx`/`FeaturedBenefits.tsx`/`TestimonialsSection.tsx`** — el mismo riesgo de mismatch de hidratación ya diagnosticado y corregido en `Header.tsx` (una consulta anterior al Sprint 6.1) y, de paso, en `PromotionalBanner.tsx` (Sprint 6.1, por tocar ya esas líneas para el carrusel) sigue latente, sin reportarse nunca como bug real, en estos cinco archivos. Corregirlos es mecánico (mismo patrón de `mounted` ya establecido) pero no estaba en el alcance explícito de ningún sprint hasta la fecha.
29. `CartItem.tsx`/`CheckoutSummary.tsx` (Carrito/Checkout) no muestran el precio promocional calculado por `getProductDisplayPrice()` — el Sprint 6.1 explícitamente no tocó Carrito ni Checkout, así que un producto agregado al carrito sigue mostrando el precio con el que se agregó, sin recalcular si una promoción se activa o desactiva después. Mismo criterio que el pendiente #9 (esos dos archivos siguen además importando `config/categories.ts` directamente): migrarlos a recibir datos resueltos por prop, ambos a la vez, cuando un sprint futuro autorice explícitamente tocar Carrito/Checkout.
30. Inicialización de git (todavía no se hizo).
31. **`lib/whatsapp.ts` no suma el costo de envío al mensaje ni al total mostrado** (Fase 22) — excepción documentada, no un descuido: "NO modificar WhatsApp" fue instrucción explícita del Sprint 6.2. El texto sigue mostrando `summary.shippingPlaceholder` y `Total: {subtotal}`, aunque `CheckoutSummary`/`orders.total` en Supabase ya reflejen el costo real. Requiere autorización explícita para tocar Checkout/WhatsApp, mismo criterio que el pendiente #10.
32. **Seed de ciudades de Paraguay, extenso pero no verificado contra el nomenclador oficial completo de la DGEEC** (`supabase/seed_logistics.sql`, Fase 22) — 228 ciudades reales y reconocibles, priorizando exactitud sobre exhaustividad. Ampliarlo (agregar distritos faltantes, corregir algún nombre) es una migración de datos chica, sin tocar el esquema, si un cliente puntual lo necesita más completo.
33. **Funciones de logística explícitamente no implementadas, dejadas para versiones futuras sin necesitar rediseñar el esquema** (Fase 22, alcance explícito del Sprint 6.2): envío gratis, transportadoras, cálculo por peso/volumen/distancia, múltiples sucursales, Retiro en tienda vinculado a una tarifa/costo, Delivery Express. `shipping_rates` sin columna de departamento (el alcance geográfico vive en `shipping_rate_cities`) es justamente lo que deja la puerta abierta a la mayoría de estas sin migración -- agregarlas de verdad sigue siendo trabajo de un sprint futuro, no algo ya resuelto en silencio.
34. **Navegación Pedido → Tarifa de envío usada** (`OrderDetailView.tsx` muestra `shippingRateName` como texto, sin link a `/admin/logistica/[id]/editar`) — no se pidió en el Sprint 6.2 y la tarifa puede haber sido borrada desde entonces (`shipping_rate_id` es `on delete set null`); agregar el link solo tiene sentido si además se maneja el caso "la tarifa ya no existe", fuera de alcance de este sprint.
35. **Configuración → Apariencia → Hero, seleccionable pero sin funcionalidad todavía** (Fase 24, interfaz preparada en la Fase 25) — la pestaña "Apariencia" de `/admin/configuracion` ya muestra las 3 opciones (Imagen/Video/Productos destacados) como tarjetas puramente visuales, sin `onClick` ni estado de selección, y el Hero de la Home siempre muestra el Showcase de productos, sin leer ninguna preferencia. Implementar el selector real -- una columna nueva en `business_settings`, conectar las 3 tarjetas a un estado controlado en `SettingsForm.tsx`, y la lógica condicional en `Hero.tsx` para renderizar uno u otro modo -- queda como un sprint futuro.
36. **Hero por imagen personalizada o por video** (Fase 24) — mismo motivo que el punto anterior: el sprint pidió explícitamente no implementar ninguno de los dos todavía. El día que se prioricen, tendrían que convivir con el Showcase de productos ya construido como una tercera/cuarta opción del mismo selector de "Apariencia" del punto 35, no reemplazarlo.
37. **Configuración → Apariencia → Colores del tema, sin funcionalidad todavía** (Fase 25) — la pestaña ya muestra la estructura visual (Color principal/secundario/de botones, con swatches de ejemplo tomados de los tokens `oklch` actuales), pero no hay ningún campo editable, ninguna columna en `business_settings`, ni ninguna forma de que un color elegido acá se refleje en `globals.css`/el tema real de la tienda. Implementarlo de verdad requeriría decidir cómo una paleta por cliente se traduce a los tokens CSS existentes (¿nuevas columnas + inyección de CSS variables en runtime? ¿un archivo de tema por cliente?) -- una decisión de arquitectura no resuelta por este sprint, deliberadamente.
38. **Configuración → Apariencia → Tipografía, sin funcionalidad todavía** (Fase 25) — hoy es únicamente el mensaje "Disponible próximamente.", tal como pidió el sprint. Ni siquiera tiene una estructura visual preparada (a diferencia de Hero/Colores del tema) porque el sprint no la pidió -- el día que se priorice, empezaría desde cero, no desde un preview ya construido.
39. **Redimensionar imágenes/video con arrastre libre en el editor enriquecido** (Fase 28) — hoy es por presets de ancho (25/50/75/100%), no un handle de arrastre continuo. Cubre el requisito del sprint ("redimensionarse") sin la complejidad de un NodeView custom con eventos de puntero -- si se prioriza el arrastre libre más adelante, es un cambio acotado a `ResizableImage`/`Video` (`rich-text/extensions.ts`) y su NodeView, sin tocar el resto del editor.
40. **Tablas dentro de la descripción enriquecida** (Fase 28) — el sprint mencionó "tabla si existe" solo como ejemplo de qué podría contener una descripción, no como requisito explícito de la lista de funcionalidades pedidas; no se registró ninguna extensión de tabla en Tiptap (Tiptap no trae una oficial en `starter-kit`, requeriría `@tiptap/extension-table` aparte). Un admin no tiene hoy ninguna forma de insertar una tabla desde el editor.
41. **Miniatura/thumbnail personalizado para los videos subidos como archivo** (Fase 28) — el `<video controls>` usa el thumbnail que el navegador genere por defecto (primer frame o un ícono genérico según el navegador). No se implementó ninguna forma de elegir/subir una miniatura propia, no pedida por el sprint.
42. **Imagen de categoría como banner en `/categorias/[slug]`** (Fase 31) — la imagen ya se muestra en Home y en el índice `/categorias` (vía `CategoryCard`), pero la página de detalle de una categoría sigue con un header de solo texto (breadcrumb + título + descripción), sin ningún banner visual. Decisión deliberada de ese sprint para no tocar ningún archivo cerca del pipeline del catálogo (`ProductGrid`/`CatalogToolbar`/`CatalogResults`, explícitamente protegidos) -- agregar el banner es un cambio acotado al bloque de header local de esa página si un sprint futuro lo prioriza.
43. **Verificación visual real (subir/reemplazar/quitar una imagen desde la UI del panel) de las Categorías con Imagen** (Fase 31) — verificado en este entorno al nivel de datos/render contra el proyecto real (ver sección 9), pero sin browser disponible para completar el flujo de principio a fin desde `/admin/categorias/nueva`/`/editar` como lo haría un admin real. Pendiente de confirmación visual por el propio usuario.

Nota: el flujo completo de compra de la V1 (Home → catálogo → producto → carrito → checkout con mapa → confirmación → WhatsApp) quedó cerrado en el Sprint 3.8. El buscador del catálogo quedó resuelto en el Sprint 4.2, el ordenamiento en el Sprint 4.3, la navegación por categorías en el Sprint 4.4, la página de contacto en el Sprint 4.5, las páginas institucionales de políticas/FAQ en el Sprint 4.6, y la localización a Paraguay en el Sprint 4.7. La infraestructura de Supabase (esquema, índices, Storage, RLS, seed) quedó aplicada al proyecto real en la Fase 8, la estructura + autenticación del Panel Administrativo quedó lista en la Fase 9, el primer módulo funcional (Productos) quedó operativo en la Fase 10, la tienda pública completa (Home/Destacados/Catálogo/Producto/Relacionados/Categorías) quedó conectada de verdad a Supabase en la Fase 11, la corrección de las fotos de producto en la Fase 12, el segundo módulo funcional del panel (Categorías) quedó operativo en la Fase 13, el tercero (Configuración General) en la Fase 14, el cuarto (Pedidos) en la Fase 15, el quinto (Clientes, de solo lectura) en la Fase 16, el Dashboard con datos reales en la Fase 17, el sexto (Promociones) en la Fase 18, y el séptimo y último módulo planeado originalmente (Inventario) en la Fase 19 — con este último, los 7 módulos planeados del panel administrativo quedan completos: el stock se controla automáticamente (descuento al confirmar/preparar un pedido, restitución al cancelarlo), con historial completo de movimientos y nunca queda negativo. `config/products.ts`/`config/categories.ts`/`config/promotion.ts` quedan como fallback/documentación, sin usarse por el resto de la tienda. **Con la Fase 19, la Versión 1.0 de Somapp Commerce Engine queda completa**, y **con la Fase 20 (Sprint 6.0.1 — Auditoría Final y Corrección de Bugs) queda estabilizada como Release Candidate**: 6 bugs reales corregidos (navegación de tarjetas de producto, imagen del banner de promoción, WhatsApp del checkout, categorías inactivas con sesión de admin, validación de tamaño de favicon/logo, Dashboard con gráficos de Recharts) más una auditoría de código (miniaturas rotas en el panel, un nombre de categoría corrompido en la base real, código muerto eliminado). **Y con la Fase 21 (Sprint 6.1), el sistema de Promociones queda definitivamente terminado**: el banner de la Home se convierte en un carrusel automático cuando hay dos o más promociones vigentes a la vez (con una sola, se comporta exactamente igual que antes), y un motor de precios único (`calculatePromotion()`/`getProductDisplayPrice()`, `lib/promotions.ts`) conecta por primera vez una promoción activa con el precio realmente mostrado de un producto — con prioridad fija (producto puntual antes que categoría) y sin acumular nunca dos descuentos —, cerrando la última pieza que el diseño original (Fase 8) había dejado deliberadamente pendiente. **Y con la Fase 22 (Sprint 6.2), el panel gana un octavo módulo no planeado originalmente, Logística**: tarifas de envío por ciudad completamente administrables, con cobertura geográfica real de Paraguay (`departments`/`cities`, seed propio) y cálculo automático del costo de envío en el checkout público -- con "A confirmar" (nunca un costo inventado) cuando la ciudad elegida no tiene ninguna tarifa configurada. Es el primer sprint que deja de tener ningún costo de envío hardcodeado en el código. Mostrar la ubicación elegida dentro de la UI de `/checkout/confirmacion` (hoy solo va en el mensaje de WhatsApp, aunque desde la Fase 15 también queda guardada en `orders.latitude`/`longitude` y visible desde `/admin/pedidos/[id]`) queda como posible mejora futura, no priorizada todavía (ver sección 7). **Y con la Fase 24 (Sprint 6.4), el panel derecho del Hero deja de ser un marcador decorativo estático**: un Showcase dinámico rota automáticamente entre productos reales de Supabase (destacados primero, más recientes si no hay ninguno, nunca vacío), con imagen/nombre/precio/botón "Ver producto", pausa en hover y navegación manual por flechas o indicadores -- el resto del Hero (textos, botones, layout) no cambió. Es el primer custom hook propio de la tienda pública (`useProductShowcase`), acotado a estado de presentación sin fetchear nada por su cuenta. La configuración de qué tipo de Hero mostrar (imagen/video/productos) desde el Panel queda explícitamente pendiente (ver sección 13). **Y con la Fase 25 (Sprint 6.5), Configuración se reorganiza en dos pestañas (General/Apariencia)**: la tarjeta "Branding" oculta desde la Fase 23 se reemplaza definitivamente por "Identidad Visual" dentro de "Apariencia", con exactamente la misma lógica de Logo/Favicon de siempre, y se suman tres secciones puramente visuales -- Hero, Colores del tema, Tipografía -- preparadas para futuros sprints, sin ninguna funcionalidad ni columna nueva detrás todavía. **Y con la Fase 28 (Sprint 6.6), la descripción completa de producto gana un editor enriquecido profesional (Tiptap)** -- negrita, cursiva, subrayado, tachado, títulos, listas, citas, separadores, enlaces, color, imágenes (con resize/alineación), GIFs y videos (archivo o YouTube), mostrado únicamente en `/productos/[slug]` (nunca en el Catálogo, la Home, `ProductCard` ni el Showcase del Hero, que ni siquiera reciben ese campo desde Supabase) y sanitizado dos veces contra XSS.

---

## 14. Cómo debe trabajar Claude en este proyecto

Claude debe comportarse como un **Senior Software Engineer** trabajando en un producto real con clientes reales detrás, no como un generador de código que maximiza velocidad a costa de calidad. En concreto:

- **Justificar decisiones importantes.** Si hay más de una forma razonable de resolver algo (dónde poner un tipo, si extraer un componente o no, qué prop pasar), decir por qué se eligió una sobre la otra — no simplemente aplicarla en silencio.
- **Evitar sobreingeniería activamente.** Antes de crear una abstracción, una carpeta nueva, o una librería nueva, preguntarse si el problema ya se presentó de verdad o si se está resolviendo algo hipotético.
- **Priorizar reutilización por sobre creación.** Ver si `ProductCard`, `ProductGrid`, `Breadcrumb`, `FormField`, `EmptyState`, los helpers de `lib/`, o los patrones de `config/` ya resuelven el problema antes de escribir algo nuevo.
- **Mantener consistencia con el resto del proyecto** en nomenclatura, estructura, paleta visual y timing de animaciones — nunca introducir un estilo o patrón nuevo "porque es mejor" sin que haya una razón concreta y sin actualizar este documento para reflejarlo.
- **Verificar, no asumir.** Cuando el cambio afecta una página o componente visual, levantar el servidor de desarrollo y confirmar el resultado real (build, lint, y visualmente en el navegador en desktop/tablet/mobile) antes de reportar la tarea como terminada.
- **Respetar el alcance de cada tarea.** Si se pide un componente, no aprovechar para "mejorar" otros tres que no fueron pedidos. Si se detecta algo que vale la pena mejorar fuera de alcance, mencionarlo como observación al final, no actuarlo por cuenta propia.
